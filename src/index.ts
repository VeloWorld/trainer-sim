// Public API entry for trainer-sim.
//
// Phase 1 (this commit): empty stub — encoder + IndoorBikeRecord re-exports
// land in plan 01-03 per CONTEXT.md D-11. Subsequent phases extend this file
// with FakeTransport, ITrainerTransport, and replay-engine exports.
//
// Cross-module relative imports across plans 01–05 MUST use `.js` extensions
// on the specifier (Node ESM convention; tsup strips at build).
export {};
