---
phase: 02-fit-loader-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/types.ts
  - src/fit/errors.ts
autonomous: true
requirements:
  - FIT-01
  - FIT-02
  - FIT-03
  - FIT-04
  - FIT-05
tags:
  - fit
  - foundation
  - types
  - errors
  - parser-dep

must_haves:
  truths:
    - "fit-file-parser@~3.0.0 is a runtime dependency in package.json (D-FIT-08)"
    - "src/types.ts exports a `RideRecord` interface with shape `{ timestamp: number; power?: number; cadence?: number }` (D-FIT-01)"
    - "src/fit/errors.ts exports an abstract base class `FitLoadError` extending Error (D-FIT-06)"
    - "src/fit/errors.ts exports four concrete subclasses: `InvalidFitHeaderError`, `FitCrcError`, `FitTruncatedError`, `NoRecordMessagesError` (D-FIT-06)"
    - "src/fit/errors.ts does NOT export a `DeveloperFieldShadowError` (D-FIT-10 — shadow conflicts are non-fatal, debuglog only, NOT a typed error)"
    - "Each error subclass sets `this.name = this.constructor.name` so stack traces identify the concrete class"
    - "All cross-module relative imports use `.js` extensions on the specifier (Phase 1 convention)"
  artifacts:
    - path: "src/types.ts"
      provides: "Public `RideRecord` type — the contract Phase 3 (replay) and Phase 4 (FakeTransport) iterate over"
      exports: ["RideRecord"]
      min_lines: 12
    - path: "src/fit/errors.ts"
      provides: "First typed-error hierarchy in trainer-sim — FitLoadError + 4 concrete subclasses (D-FIT-06)"
      exports: ["FitLoadError", "InvalidFitHeaderError", "FitCrcError", "FitTruncatedError", "NoRecordMessagesError"]
      min_lines: 20
    - path: "package.json"
      provides: "fit-file-parser declared as runtime dep at ~3.0.0 (D-FIT-08)"
      contains: '"fit-file-parser"'
  key_links:
    - from: "src/fit/errors.ts"
      to: "Error"
      via: "abstract class FitLoadError extends Error"
      pattern: "abstract\\s+class\\s+FitLoadError\\s+extends\\s+Error"
    - from: "src/types.ts"
      to: "Phase 3 / Phase 4 consumers (future)"
      via: "named export of RideRecord interface"
      pattern: "export\\s+interface\\s+RideRecord"
---

<objective>
Lay the Phase 2 foundation: install the locked FIT parser dependency, author the
shared `RideRecord` type that downstream phases will consume, and ship the first
typed-error hierarchy in trainer-sim.

Implements (sets up the type and error surface that satisfy):
- FIT-01 (loader entry-points return `RideRecord[]`) — type defined here
- FIT-02 (record stream type) — type defined here
- FIT-03 (epoch ms timestamp) — type's `timestamp: number` JSDoc'd as Unix ms
- FIT-04 (load-without-throwing) — `FitLoadError` subclasses are reserved for *corrupt* input only; valid-but-weird input does NOT throw
- FIT-05 (dev-field shadow) — note the *absence* of `DeveloperFieldShadowError` is intentional per D-FIT-10 (locked 2026-05-16); shadow handling is debuglog-only

Honors locked decisions:
- D-FIT-01 (RideRecord shape, optional fields, undefined-vs-zero semantics)
- D-FIT-06 (typed error hierarchy, abstract base + 4 concrete subclasses)
- D-FIT-08 (parser is `fit-file-parser@~3.0.0`)
- D-FIT-10 (no DeveloperFieldShadowError — shadow conflicts non-fatal)
- Phase 1 convention: `.js` extension on relative-import specifiers

Purpose: Wave 1 ships the contract everyone signs. Wave 2 (fixtures) and Wave 3
(loader + normalize) depend on these types and the parser binary being installed.
This plan stays small and parallel-safe: three tasks, no checkpoint, ~10-15%
context each.

Output: `package.json` updated with one new dependency; `src/types.ts` (~12 lines);
`src/fit/errors.ts` (~25 lines).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-fit-loader-normalization/02-CONTEXT.md
@.planning/phases/02-fit-loader-normalization/02-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install fit-file-parser@~3.0.0 as a runtime dependency</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (existing — Phase 1 dependency layout to extend, NOT restructure)
    - .planning/research/STACK.md (D-FIT-08 lock rationale: MIT, dual ESM+CJS, ships TS types, last published 2026-05-09)
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Standard Stack → Phase 2 additions; §Open Questions #3 — pin tilde not caret because we exploit `parse(buf, callback)` synchronous-callback property in D-FIT-07)
  </read_first>
  <action>
    Run `npm install --save fit-file-parser@~3.0.0` from the repo root. The tilde
    range pins the minor version (3.0.x): a future minor bump may flip the parser's
    sync-callback property exploited by `loadFitFromBuffer` (D-FIT-07) or fix the
    CRC TODO (RESEARCH §Critical Finding), so a minor bump must be an intentional
    decision, not a passive `npm update`.

    Do NOT install `@garmin/fitsdk` — D-FIT-05 chose the scrubber path over the
    Garmin-encoder fixture-generator path; `@garmin/fitsdk` is NOT a dev-dep here.
    Do NOT install `@types/fit-file-parser` — `fit-file-parser@3.0.0` ships its own
    `.d.ts` (verified in STACK.md). Adding `@types/*` would shadow the bundled types.

    After install, verify package.json has `"fit-file-parser": "~3.0.0"` under
    `dependencies` (NOT `devDependencies`; this is a runtime dep). Verify
    `node_modules/fit-file-parser/package.json` exists and reports version 3.0.0.

    Commit per Phase 1's atomic-commit pattern with message:
    `chore(02-01): install fit-file-parser@~3.0.0 (D-FIT-08)`
  </action>
  <acceptance_criteria>
    - package.json declares `fit-file-parser` under `dependencies`: `node -e "const p=require('./package.json'); if(!p.dependencies||!p.dependencies['fit-file-parser']) process.exit(1)"` exits 0.
    - Version range is `~3.0.0` (tilde, not caret): `node -e "const p=require('./package.json'); if(p.dependencies['fit-file-parser']!=='~3.0.0') process.exit(1)"` exits 0.
    - `fit-file-parser` is NOT under `devDependencies`: `node -e "const p=require('./package.json'); if(p.devDependencies&&p.devDependencies['fit-file-parser']) process.exit(1)"` exits 0.
    - `@garmin/fitsdk` is NOT installed (we deliberately rejected this path per D-FIT-05): `node -e "const p=require('./package.json'); const all={...(p.dependencies||{}),...(p.devDependencies||{})}; if(all['@garmin/fitsdk']) process.exit(1)"` exits 0.
    - `@types/fit-file-parser` is NOT installed: `node -e "const p=require('./package.json'); const all={...(p.dependencies||{}),...(p.devDependencies||{})}; if(all['@types/fit-file-parser']) process.exit(1)"` exits 0.
    - Installed package version on disk is 3.0.x: `node -e "const v=require('fit-file-parser/package.json').version; if(!/^3\.0\./.test(v)) process.exit(1)"` exits 0.
    - package-lock.json is updated and committed (`git status` shows no untracked package-lock.json after the commit).
    - `npm run build` still exits 0 (Phase 1 build remains green; new dep doesn't break anything).
  </acceptance_criteria>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies||p.dependencies['fit-file-parser']!=='~3.0.0') process.exit(1)" && node -e "const v=require('fit-file-parser/package.json').version; if(!/^3\.0\./.test(v)) process.exit(1)" && node -e "const p=require('./package.json'); const all={...(p.dependencies||{}),...(p.devDependencies||{})}; if(all['@garmin/fitsdk']||all['@types/fit-file-parser']) process.exit(1)" && npm run build</automated>
  </verify>
  <done>
    `fit-file-parser@~3.0.0` installed as a production dependency. No `@garmin/fitsdk`, no `@types/fit-file-parser`. `npm run build` still passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author src/types.ts with the RideRecord interface (D-FIT-01)</name>
  <files>src/types.ts</files>
  <read_first>
    - .planning/phases/02-fit-loader-normalization/02-CONTEXT.md (D-FIT-01 — exact shape `{ timestamp: number; power?: number; cadence?: number }`; absent-vs-zero JSDoc semantics)
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Code Examples Example 4 — full `src/types.ts` skeleton with JSDoc; §Pitfall 7 — timestamp is `number` not `string`; §Pitfall 6 — absent-vs-zero distinction)
    - .planning/research/ARCHITECTURE.md (§Recommended Project Structure — types layer location)
    - src/ftms/indoor-bike-data.ts (Phase 1 — confirms encoder's `IndoorBikeRecord` already gates flag bits on `value === undefined`; D-FIT-01 lines up with this convention)
  </read_first>
  <action>
    Create `src/types.ts` (NEW file — does not exist yet). Author the `RideRecord`
    interface verbatim per D-FIT-01 with JSDoc that documents the absent-vs-zero
    distinction and the Unix-epoch-ms timestamp convention:

    1. File header JSDoc block: name the file's role ("Phase 2 introduces shared
       types; Phase 3 will iterate over `RideRecord`, Phase 4 will add
       `ITrainerTransport` and `Config` here"); cite D-FIT-01.
    2. `export interface RideRecord` with three members:
       - `timestamp: number` — JSDoc: "Unix epoch milliseconds (NOT FIT epoch — the 1989-12-31 UTC offset has been applied). FIT-03."
       - `power?: number` — JSDoc: "Watts. `undefined` = no power signal (sensor disconnected, file lacks the field, FIT invalid sentinel). `0` = rider coasting. Do NOT collapse `undefined` to `0` — Phase 1's encoder gates the FTMS flag bit on `value === undefined`."
       - `cadence?: number` — JSDoc: "RPM. Same absent-vs-zero semantics as `power`."

    Per Phase 1 convention, use the `~80 line per task` style budget — this file
    will be ~12-18 lines, all JSDoc + interface. Do NOT export anything else from
    `src/types.ts` in this task; Phase 4 adds `ITrainerTransport` later. Do NOT
    re-export `RideRecord` from `src/index.ts` in this task — the public re-export
    happens in plan 02-03 alongside the loader entry points.

    `verbatimModuleSyntax: true` (in `tsconfig.json`) means: when a future consumer
    re-exports `RideRecord` they must use `export type { RideRecord } from
    './types.js'` (the `.js` extension preserves Phase 1 ESM convention). This
    task does not write that re-export — flagged here for plan 02-03.

    Commit per Phase 1's atomic-commit pattern:
    `feat(02-01): add RideRecord type per D-FIT-01`
  </action>
  <acceptance_criteria>
    - `src/types.ts` exists: `test -f src/types.ts`.
    - Exports `RideRecord` as an interface (not a type alias): `grep -cE '^export\s+interface\s+RideRecord' src/types.ts` reports 1.
    - Has all three properties — `timestamp` required, `power` and `cadence` optional: `grep -E '^\s*timestamp:\s*number' src/types.ts` exits 0 AND `grep -E '^\s*power\?:\s*number' src/types.ts` exits 0 AND `grep -E '^\s*cadence\?:\s*number' src/types.ts` exits 0.
    - JSDoc documents undefined-vs-zero distinction (D-FIT-01 wire-honesty): `grep -F 'undefined' src/types.ts | grep -F '0'` exits 0 (some line mentions both).
    - JSDoc cites FIT-03 epoch ms convention: `grep -F 'FIT-03' src/types.ts` exits 0 OR `grep -iF 'unix epoch' src/types.ts` exits 0.
    - Does NOT export anything besides `RideRecord` (Phase 4 adds more later): `grep -cE '^export\s+' src/types.ts` reports 1.
    - Does NOT re-declare `IndoorBikeRecord` (that lives in `src/ftms/indoor-bike-data.ts`): `grep -cE 'IndoorBikeRecord' src/types.ts` reports 0.
    - tsup build still exits 0: `npm run build`. Note: nothing imports `src/types.ts` yet, but tsc-via-tsup type-checks all of `src/`, so the file must be valid TS.
    - Test-mode typecheck still exits 0: `npm run typecheck:test`.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/types.ts && grep -cE '^export\s+interface\s+RideRecord' src/types.ts && grep -E '^\s*timestamp:\s*number' src/types.ts && grep -E '^\s*power\?:\s*number' src/types.ts && grep -E '^\s*cadence\?:\s*number' src/types.ts && [ "$(grep -cE '^export\s+' src/types.ts)" = "1" ] && npm run build && npm run typecheck:test</automated>
  </verify>
  <done>
    `src/types.ts` exists with `RideRecord` interface authored per D-FIT-01 (timestamp number, power/cadence optional). JSDoc documents undefined-vs-zero semantics and Unix-epoch-ms timestamp. Build and test-typecheck still green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Author src/fit/errors.ts with the FitLoadError hierarchy (D-FIT-06, D-FIT-10)</name>
  <files>src/fit/errors.ts</files>
  <read_first>
    - .planning/phases/02-fit-loader-normalization/02-CONTEXT.md (D-FIT-06 — abstract base + 4 concrete subclasses; D-FIT-10 — NO DeveloperFieldShadowError, shadow non-fatal)
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Code Examples Example 3 — full `errors.ts` skeleton; CRITICAL: the RESEARCH example INCLUDES `DeveloperFieldShadowError` because it predates D-FIT-10's amendment — DO NOT include that class here)
    - .planning/REQUIREMENTS.md (FIT-05 amended 2026-05-16 — shadow conflicts emit util.debuglog warning, do NOT throw)
    - tsconfig.json (strict mode + noUncheckedIndexedAccess affects what compiles)
  </read_first>
  <action>
    Create `src/fit/errors.ts` (NEW file; the `src/fit/` directory does not yet
    exist — `mkdir -p src/fit` first or write the file via the editor which will
    create the directory). Author the typed error hierarchy per D-FIT-06.

    Required structure (~25 lines):

    1. File header JSDoc: cite "First typed-error hierarchy in trainer-sim. Future phases (replay timeouts, transport failures) follow the same `extends FitLoadError`-style pattern. Per D-FIT-06: fail-fast on corrupt input; valid-but-weird input (autopause gaps, sparse cadence, null power) does NOT throw — that's the happy path." Cross-reference: D-FIT-10 explicitly excludes `DeveloperFieldShadowError` — shadow conflicts are non-fatal and emit `util.debuglog('trainer-sim:fit')` only, handled in `loader.ts` (plan 02-03), NOT thrown.
    2. Export `abstract class FitLoadError extends Error`. Constructor takes `message: string`, calls `super(message)`, and sets `this.name = this.constructor.name` so stack traces identify the concrete subclass (per RESEARCH.md Example 3). Mark `abstract` so `new FitLoadError(...)` is a compile-time error — consumers always throw a concrete subclass.
    3. Export FOUR concrete subclasses, each as `export class X extends FitLoadError {}` with no body (the base class handles `name` propagation):
       - `InvalidFitHeaderError` — bad magic / wrong header bytes / header length not 12 or 14.
       - `FitCrcError` — CRC-16/ARC mismatch (we compute this ourselves because `fit-file-parser` 3.0 has CRC TODO-commented-out; per RESEARCH §Pattern 2).
       - `FitTruncatedError` — file ends mid-header / mid-data / mid-CRC-trailer.
       - `NoRecordMessagesError` — valid FIT (header + CRC + parses) but contains zero `record` messages (workout-only file, GPX export mislabeled).

    EXPLICITLY DO NOT EXPORT `DeveloperFieldShadowError`. RESEARCH.md Example 3
    INCLUDES it; that example is stale because D-FIT-10 was amended after the
    research was written. The locked decision (CONTEXT.md D-FIT-10, REQUIREMENTS.md
    FIT-05 amendment 2026-05-16) makes shadow conflicts non-fatal: the loader emits
    `util.debuglog('trainer-sim:fit')` and returns whatever the parser produced
    (which on shadow is the dev-field value). Including a `DeveloperFieldShadowError`
    class here would invite future code to throw it, breaking the locked behavior.

    Do NOT re-export from `src/index.ts` in this task — the public re-export
    happens in plan 02-03 alongside the loader entry points.

    Commit per Phase 1's atomic-commit pattern:
    `feat(02-01): add FitLoadError hierarchy per D-FIT-06`
  </action>
  <acceptance_criteria>
    - `src/fit/errors.ts` exists: `test -f src/fit/errors.ts`.
    - Exports an abstract base class `FitLoadError extends Error`: `grep -E '^export\s+abstract\s+class\s+FitLoadError\s+extends\s+Error' src/fit/errors.ts` exits 0.
    - Constructor sets `this.name = this.constructor.name` (concrete-class identity in stacks): `grep -F 'this.name = this.constructor.name' src/fit/errors.ts` exits 0.
    - Exports four concrete subclasses extending `FitLoadError`: `grep -cE '^export\s+class\s+(InvalidFitHeaderError|FitCrcError|FitTruncatedError|NoRecordMessagesError)\s+extends\s+FitLoadError' src/fit/errors.ts` reports 4.
    - Does NOT export `DeveloperFieldShadowError` (D-FIT-10 lock — shadow is debuglog-only, NOT a typed error): `grep -cE 'DeveloperFieldShadowError' src/fit/errors.ts` reports 0.
    - Does NOT export any other class (only the abstract base + four concrete): `grep -cE '^export\s+(abstract\s+)?class' src/fit/errors.ts` reports 5.
    - File cites D-FIT-06 in a comment: `grep -F 'D-FIT-06' src/fit/errors.ts` exits 0.
    - File mentions D-FIT-10 / debuglog rationale for the absence of shadow error: `grep -F 'D-FIT-10' src/fit/errors.ts` exits 0 OR `grep -i 'debuglog' src/fit/errors.ts` exits 0.
    - tsup build exits 0: `npm run build`.
    - Test-mode typecheck exits 0: `npm run typecheck:test`.
    - Smoke check via Node: `node --input-type=module -e "import('./dist/index.js').then(m=>{ /* errors.ts not yet re-exported in plan 02-01 — this just confirms build is clean */ })"` exits 0. (Plan 02-03 re-exports the error classes; this task only confirms the file compiles.)
  </acceptance_criteria>
  <verify>
    <automated>test -f src/fit/errors.ts && grep -E '^export\s+abstract\s+class\s+FitLoadError\s+extends\s+Error' src/fit/errors.ts && grep -F 'this.name = this.constructor.name' src/fit/errors.ts && [ "$(grep -cE '^export\s+class\s+(InvalidFitHeaderError|FitCrcError|FitTruncatedError|NoRecordMessagesError)\s+extends\s+FitLoadError' src/fit/errors.ts)" = "4" ] && [ "$(grep -cE 'DeveloperFieldShadowError' src/fit/errors.ts)" = "0" ] && [ "$(grep -cE '^export\s+(abstract\s+)?class' src/fit/errors.ts)" = "5" ] && npm run build && npm run typecheck:test</automated>
  </verify>
  <done>
    `src/fit/errors.ts` exports `FitLoadError` (abstract) plus four concrete subclasses (`InvalidFitHeaderError`, `FitCrcError`, `FitTruncatedError`, `NoRecordMessagesError`). NO `DeveloperFieldShadowError` per D-FIT-10. Build and test-typecheck pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `npm install` ↔ npm registry | Tarball integrity verified by npm via package-lock.json hash; pin to `~3.0.0` so a malicious 3.1.x cannot be auto-pulled |
| Future caller ↔ `RideRecord` consumer | Type is the contract; runtime values cross this boundary in plans 02-03/04/05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Supply-chain attack on `fit-file-parser` (typo-squat or compromised version) | mitigate | Pin `~3.0.0` (tilde, no minor bumps); package-lock.json committed and reviewed; npm audit run before merge. The package was last published 2026-05-09 by `jimmykane` with consistent maintainer history (verified in RESEARCH.md). |
| T-02-02 | Tampering | `RideRecord` shape drift (someone adds `speed` and breaks Phase 1 encoder's flag-bit gating) | accept | TypeScript interface is the contract; widening it is a deliberate code change reviewed against D-FIT-01 lock. v2 widens via FTMS-06/07 (deferred per CONTEXT.md). |
| T-02-03 | Information Disclosure | Error messages leak internal implementation details (file paths, parser internals) | accept | Phase 2 errors are surfaced to a developer running tests / debugging, not to an end user. CRC mismatch / truncation messages SHOULD include byte offsets — that's the point. No PII in the input bytes (FIT files are sport telemetry). |
| T-02-04 | Repudiation | A `DeveloperFieldShadowError` slipping into the hierarchy would silently break the D-FIT-10 contract | mitigate | Acceptance criteria grep-bans `DeveloperFieldShadowError` in `src/fit/errors.ts`. The discrepancy with RESEARCH.md Example 3 is documented in the task action so executors don't copy-paste the stale example. |
</threat_model>

<verification>
- `package.json` declares `fit-file-parser` as a runtime dep at `~3.0.0`; no `@garmin/fitsdk`, no `@types/fit-file-parser`.
- `node_modules/fit-file-parser/package.json` reports version 3.0.x.
- `src/types.ts` exports a single `RideRecord` interface with `timestamp: number`, `power?: number`, `cadence?: number`. JSDoc documents undefined-vs-zero (D-FIT-01) and Unix-epoch-ms (FIT-03).
- `src/fit/errors.ts` exports an abstract `FitLoadError extends Error` and exactly four concrete subclasses (`InvalidFitHeaderError`, `FitCrcError`, `FitTruncatedError`, `NoRecordMessagesError`). NO `DeveloperFieldShadowError` (D-FIT-10).
- `npm run build` and `npm run typecheck:test` both exit 0.
- No file from this plan is yet re-exported from `src/index.ts` — that wiring lands in plan 02-03 alongside the loader.
</verification>

<success_criteria>
- The Phase 2 contract surface (RideRecord type + FitLoadError hierarchy) is in place for plans 02-02, 02-03, 02-04, 02-05 to consume.
- Locked decisions D-FIT-01, D-FIT-06, D-FIT-08, D-FIT-10 are reflected in source.
- Phase 1 inheritance (`.js`-extension import convention, dual-publish exports map, Node 24 engines) is preserved.
- Three atomic conventional-prefix commits land cleanly: `chore(02-01)`, `feat(02-01)` × 2.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fit-loader-normalization/02-01-SUMMARY.md` documenting:
- The exact installed version of `fit-file-parser` (from `node_modules/fit-file-parser/package.json`).
- Line counts of `src/types.ts` and `src/fit/errors.ts`.
- Confirmation that `DeveloperFieldShadowError` is NOT in `errors.ts` (D-FIT-10 reasoning).
- Confirmation that `npm run build` and `npm run typecheck:test` both pass post-commit.
- Note that re-exports from `src/index.ts` are deferred to plan 02-03.
</output>
</content>
