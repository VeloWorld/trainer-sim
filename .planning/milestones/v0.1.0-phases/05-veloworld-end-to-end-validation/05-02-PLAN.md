---
phase: 05-veloworld-end-to-end-validation
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/package.json
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/package.json
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/pnpm-lock.yaml
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fit-loader.ts
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fit-loader.test.ts
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/replay-scheduler.test.ts
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts
  - /Users/agniveshpatel/dev/agni21/veloworld-ride/.github/workflows/ci.yml
autonomous: false
requirements: [VW-01, VW-02, VW-03]
must_haves:
  truths:
    - "VW's ROOT `package.json` `pnpm.onlyBuiltDependencies` array contains `\"trainer-sim\"` alongside the existing `\"electron\"` and `\"esbuild\"`. Without this, pnpm 10.33.0's strict mode blocks trainer-sim's Wave 0 `prepare` hook with `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED` and `dist/` is never materialized on the consumer side — Plan 05-01's prepare-hook contract silently fails."
    - "VW's `apps/desktop/package.json` has a single new entry: `\"trainer-sim\": \"github:VeloWorld/trainer-sim#<wave-0-sha>\"`. The sha is the value verbatim from `.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt` written by Plan 05-01."
    - "VW's `pnpm-lock.yaml` records trainer-sim under the `resolution: { type: 'git', repo: 'https://github.com/VeloWorld/trainer-sim.git', commit: '<wave-0-sha>' }` shape."
    - "VW's `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is rewritten to compose trainer-sim's `createFakeTransport` (with the `{ buffer: Uint8Array }` source variant), implementing VW's 9-method `ITrainerTransport` from `@veloworld/ble`. The class delegates `connect`, `disconnect`, `onTelemetry` (via `inner.onData`) to trainer-sim; the 5 BLE-shaped methods (`scan`, `probeControlPointSupport`, `requestControl`, `setGradeSimulation`, `releaseControl`) stay as VW-owned synthetic implementations or no-ops; `reconnect` re-runs `connect`. Per user override #2, pause/resume on `useRideStore` is `inner.disconnect()` / `inner.connect()` (Path A, ~5ms FIT re-parse on resume). The 8-byte `encodeIndoorBikeFrame` function and the `loadFitFromBytes` / `ReplayScheduler` imports are GONE from this file — trainer-sim handles all three concerns."
    - "VW's adapter `fake-trainer-transport.ts` registers a `'complete'` listener on the inner trainer-sim transport that calls `useBleStore.getState().setSensorState('trainer', 'disconnected')` exactly once when trainer-sim's scheduler reaches end-of-records. The listener is unregistered in `teardown()` via `this.inner.off('complete', this.completeListener)`. This is the wire-up that makes `dev-mode-save-flow.test.ts` line 254 (`expect(disconnectedCalls).toHaveLength(1)`) pass post-rewrite — without it, CR-04 regression fires."
    - "VW's `apps/desktop/src/renderer/src/lib/dev/fit-loader.ts` is DELETED (per user override #3 — explicit deletion, not dead-code-with-tree-shake)."
    - "VW's `apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts` is DELETED (per user override #3)."
    - "VW's `apps/desktop/src/renderer/src/lib/__tests__/dev/fit-loader.test.ts` is DELETED (per user override #3)."
    - "VW's `apps/desktop/src/renderer/src/lib/__tests__/dev/replay-scheduler.test.ts` is DELETED (per user override #3)."
    - "VW's `apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` is rewritten so the hoisted `vi.mock('../../dev/fit-loader', ...)` block is replaced with `vi.mock('trainer-sim', ...)` mocking `createFakeTransport`. Test #5 (`onTelemetry handler receives DataView frames built from FIT records`) asserts `parsed.instantaneousSpeed === undefined` (not a number) per the 6-byte trainer-sim frame format and RESEARCH Open Question 4."
    - "VW's `apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` (the Phase 10 CR-01..CR-04 regression integration test) (a) imports `RideRecord` from `'trainer-sim'` (NOT `'../../dev/replay-scheduler'`); (b) replaces `vi.mock('../../dev/fit-loader', ...)` with `vi.mock('trainer-sim', ...)` targeting `createFakeTransport`; (c) drives natural exhaustion via a captured `'complete'` listener fired manually (NOT `vi.advanceTimersByTime` against the deleted `ReplayScheduler`); (d) all CR-01..CR-04 assertions still pass — including `expect(disconnectedCalls).toHaveLength(1)` (CR-04) which now traces to the adapter's `'complete'` → `setSensorState('trainer', 'disconnected')` wiring."
    - "VW's `.github/workflows/ci.yml` `build-and-prod-bundle-grep` job has `strategy.matrix.os = [ubuntu-latest, macos-latest]` with `fail-fast: false` and `runs-on: ${{ matrix.os }}`. All other steps are byte-identical to the pre-edit file."
    - "VW's existing dev-mode tree-shake gate still strips trainer-sim from the production renderer bundle (`grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js` exits 1 — no matches — after running `pnpm --filter @veloworld/desktop build`)."
    - "Locally on the developer's macOS machine (Node 24, pnpm 10.x), running `pnpm install --frozen-lockfile` (after lockfile regen), `pnpm typecheck`, `pnpm test`, and `pnpm --filter @veloworld/desktop build` from the VW repo root all exit 0."
    - "Post-replan grep audit: `grep -rln 'dev/fit-loader\\|dev/replay-scheduler' apps/desktop/src/ packages/ 2>/dev/null | grep -v node_modules` outputs ZERO files. This is the exact grep that would have caught the 2026-05-18 deviation pre-execution; it is now a wave-1 acceptance criterion."
  artifacts:
    - path: "/Users/agniveshpatel/dev/agni21/veloworld-ride/package.json"
      provides: "pnpm 10 strict-mode allowlist for trainer-sim's Wave 0 prepare hook"
      contains: '"trainer-sim"'
    - path: "/Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/package.json"
      provides: "Git-ref devDependency on trainer-sim Wave 0 sha"
      contains: 'github:VeloWorld/trainer-sim#'
    - path: "/Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts"
      provides: "Adapter wrapping trainer-sim's `createFakeTransport` with VW's 9-method ITrainerTransport surface, including `'complete'` -> `setSensorState('trainer', 'disconnected')` wiring"
      contains: "createFakeTransport"
      min_lines: 60
    - path: "/Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts"
      provides: "VW Phase 10 CR-01..CR-04 regression test repointed at trainer-sim's surface"
      contains: "from 'trainer-sim'"
    - path: "/Users/agniveshpatel/dev/agni21/veloworld-ride/.github/workflows/ci.yml"
      provides: "macOS + Linux Node 24 matrix on the build-and-prod-bundle-grep job"
      contains: "macos-latest"
  key_links:
    - from: "VW root `package.json` `pnpm.onlyBuiltDependencies`"
      to: "trainer-sim's `prepare` hook in trainer-sim's own `package.json`"
      via: "explicit `\"trainer-sim\"` allowlist entry consumed by pnpm 10 strict-mode at install time"
      pattern: '"trainer-sim"'
    - from: "VW `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts`"
      to: "trainer-sim's `createFakeTransport`"
      via: "`import { createFakeTransport, type FakeTransport } from 'trainer-sim'` resolved through VW's git-ref devDependency"
      pattern: "from\\s+'trainer-sim'"
    - from: "VW adapter `connect(deviceId)` body"
      to: "trainer-sim's `{ buffer: Uint8Array }` source variant"
      via: "`createFakeTransport({ source: { buffer: bytes } })` where bytes come from `await window.veloworld.dev.readFitFile()`"
      pattern: "source:\\s*\\{\\s*buffer:"
    - from: "VW adapter `'complete'` listener"
      to: "VW store's `setSensorState('trainer', 'disconnected')`"
      via: "`this.inner.on('complete', () => useBleStore.getState().setSensorState('trainer', 'disconnected'))` registered after `inner.connect()`, unregistered in `teardown()`"
      pattern: "'complete'"
    - from: "VW adapter `useRideStore.subscribe` callback"
      to: "trainer-sim's `disconnect()` / `connect()` (Path A pause/resume)"
      via: "`void this.inner?.disconnect()` on paused, `void this.inner?.connect()` on resumed"
      pattern: "rideState"
    - from: "VW `pnpm-lock.yaml`"
      to: "trainer-sim's Wave 0 commit on `VeloWorld/trainer-sim`"
      via: "`resolution: { type: 'git', repo: 'https://github.com/VeloWorld/trainer-sim.git', commit: '<sha>' }`"
      pattern: "VeloWorld/trainer-sim"
    - from: "VW `.github/workflows/ci.yml` `build-and-prod-bundle-grep` job"
      to: "GitHub Actions macos-latest runner"
      via: "`strategy.matrix.os` containing `macos-latest`"
      pattern: "macos-latest"
---

<objective>
Land all local-VW edits required for the Phase 5 cross-repo PR, gated by `pnpm typecheck && pnpm test && pnpm --filter @veloworld/desktop build` exiting 0 on the developer's macOS workstation. This plan does NOT push the branch or open the PR — that is Plan 05-03's atomic unit. Splitting at this seam means: if Plan 05-02 leaves VW's local checkout in a broken state, Plan 05-03 cannot proceed and the developer can rerun 05-02 freely; if Plan 05-02 ends with everything green locally, the cross-repo PR plan starts from a known-good state.

This plan was first attempted on 2026-05-18 and aborted partway through Task 1 because two material gaps surfaced (see `05-02-DEVIATIONS.md`). Both gaps are folded into this revised plan: (1) pnpm 10.33.0 strict-mode requires opt-in via `pnpm.onlyBuiltDependencies` in VW's ROOT `package.json` for trainer-sim's git-ref `prepare` hook to run, and (2) `dev-mode-save-flow.test.ts` is the 4th consumer of the deleted vendored modules and CR-04 requires explicit `'complete'` -> `disconnected` wiring in the adapter rewrite.

The work decomposes into four vertical slices (one per task):

1. **Allowlist + dependency wiring + deletions:** Extend VW root `package.json` `pnpm.onlyBuiltDependencies` to include `"trainer-sim"`, pin trainer-sim's Wave 0 sha in `apps/desktop/package.json`, regenerate `pnpm-lock.yaml`, delete VW's now-redundant vendored modules (`dev/fit-loader.ts`, `dev/replay-scheduler.ts`) and their tests, and expand the CI matrix. This is the "infrastructure" pass — once it lands, VW's typecheck breaks (because `fake-trainer-transport.ts` still imports the deleted modules) but the dependency graph is now correct AND pnpm 10 strict-mode no longer blocks trainer-sim's prepare hook.
2. **Adapter rewrite (with `'complete'` -> `'disconnected'` wiring):** Rewrite `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` to compose trainer-sim's `createFakeTransport` instead of vendoring FIT/replay/encode internals. Per user override #1, the rewrite is an adapter wrapping trainer-sim — VW's `ITrainerTransport` 9-method surface is NOT replaced with `import type { ITrainerTransport } from 'trainer-sim'`; the trainer-sim type is used internally for the inner FakeTransport, and VW's own 9-method interface stays VW-owned per Anti-Pattern 6. Per user override #2, pause/resume is Path A (disconnect/reconnect on rideState transitions). New for the replan: the adapter registers a `'complete'` listener on the inner FakeTransport that calls `useBleStore.getState().setSensorState('trainer', 'disconnected')` and unregisters it in `teardown()` — this is what makes CR-04 (`disconnectedCalls toHaveLength(1)`) round-trip through the integration test in Task 4. Once this task lands, VW typecheck passes again.
3. **Unit-test rewrite + local-green gate (typecheck + test + build + grep):** Rewrite `__tests__/dev/fake-trainer-transport.test.ts` so the hoisted mocks point at `'trainer-sim'` instead of `'../../dev/fit-loader'`, the speed-channel assertion shifts to `expect(parsed.instantaneousSpeed).toBeUndefined()` per the 6-byte trainer-sim frame format, the inner-mock shape includes `on/off/once` so the captured `'complete'` listener is testable, and all 8-9 surviving test groups pass under `vi.useFakeTimers()`. Concludes with the local-green gate: `pnpm typecheck && pnpm test && pnpm --filter @veloworld/desktop build` all exit 0 from the VW repo root, then commit.
4. **Integration-test rewrite (`dev-mode-save-flow.test.ts`):** Rewrite VW's Phase 10 CR-01..CR-04 regression integration test to import `RideRecord` from `'trainer-sim'`, mock `'trainer-sim'`'s `createFakeTransport`, and trigger natural exhaustion via the captured `'complete'` listener (option a — cleanest test boundary). Re-run `pnpm test -- dev-mode-save-flow.test.ts` and the full `pnpm test` workspace to confirm the integration rewrite did not break the unit tests, then amend the Task 3 commit OR commit on top — the executor decides based on what produces the cleanest single Wave 1a commit. This task is sequential after Task 3 because the adapter+unit-test must be green first; the integration test exercises the adapter end-to-end.

This plan is `autonomous: false` because the executor must work in another git repo (`/Users/agniveshpatel/dev/agni21/veloworld-ride`) on a feature branch, may iterate on test failures (e.g., a hoisted-mock subtlety surfacing during the rewrite, or `handlerSpy.toHaveBeenCalledTimes(N)` assertions shifting from `2` to a different value because the emitter-based completion path replaces `vi.advanceTimersByTime`-driven scheduler emission), and Path A's "FIT re-parses on resume" is a design call the human ratified — but if the re-parse cost surfaces a perceptible UX issue during a manual ride-in-dev-mode smoke check, the human escalates per D-VW-08 (fix in trainer-sim first). The executor should NOT attempt to widen trainer-sim's API mid-plan.

Purpose: Make the VW PR mechanically ready to push. Output: VW's local checkout has all source/test/CI/dep edits, lockfile regenerated, all checks green, but no commit yet pushed and no PR yet opened.

Output:
- VW root `package.json` modified (`onlyBuiltDependencies` extended with `"trainer-sim"`).
- `apps/desktop/package.json` modified (one new dep entry).
- `pnpm-lock.yaml` regenerated and committed.
- `apps/desktop/src/renderer/src/lib/dev/{fit-loader.ts, replay-scheduler.ts}` DELETED.
- `apps/desktop/src/renderer/src/lib/__tests__/dev/{fit-loader.test.ts, replay-scheduler.test.ts}` DELETED.
- `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` rewritten (~80-100 LOC, replaces 273 LOC; includes `'complete'` -> `'disconnected'` wiring).
- `apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` rewritten (mocks point at `'trainer-sim'`; speed assertion is undefined-check; inner mock shape includes `on/off/once`).
- `apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` rewritten (mocks `'trainer-sim'`; imports `RideRecord` from `'trainer-sim'`; drives natural exhaustion via captured `'complete'` listener; CR-01..CR-04 still pass).
- `.github/workflows/ci.yml` modified (macOS + Linux matrix on the existing job).
- All edits committed to a feature branch `feat/phase-5-trainer-sim-canonical` (or whatever branch name the executor chooses; recorded in the local-green gate output).
- Local `pnpm typecheck`, `pnpm test`, `pnpm --filter @veloworld/desktop build` all exit 0.
- The production-bundle grep gate (`grep -El 'fake-transport|FakeTransport' apps/desktop/out/renderer/assets/*.js`) still exits 1 — no matches — proving the dev-mode tree-shake survives the trainer-sim addition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-veloworld-end-to-end-validation/05-CONTEXT.md
@.planning/phases/05-veloworld-end-to-end-validation/05-RESEARCH.md
@.planning/phases/05-veloworld-end-to-end-validation/05-PATTERNS.md
@.planning/phases/05-veloworld-end-to-end-validation/05-01-SUMMARY.md
@.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt
@.planning/phases/05-veloworld-end-to-end-validation/05-02-DEVIATIONS.md
@.planning/phases/04-faketransport-public-api/04-CONTEXT.md
@src/types.ts
@src/transport/fake-transport.ts

<interfaces>
<!-- The trainer-sim post-Wave-0 contract VW's adapter consumes. -->
<!-- Do NOT widen these — Anti-Pattern 6 is non-negotiable. -->

From trainer-sim `src/types.ts` (canonical via D-API-01):
```typescript
export interface ITrainerTransport {
  connect(): Promise<void>;
  disconnect(): Promise<void>;
  onData(handler: (data: DataView) => void): () => void;
  sendResistance(grade: number): Promise<void>;
}

export type FakeTransportSource =
  | { path: string }
  | { buffer: Buffer | Uint8Array }
  | { records: ReadonlyArray<RideRecord> };

export interface FakeTransportConfig {
  source: FakeTransportSource;
  speed?: number;       // default 1
  loop?: boolean;       // default false
  maxEmissionHz?: number; // default 1000
}

export interface FakeTransport extends ITrainerTransport {
  readonly received: { resistance: ReadonlyArray<number> };
  reset(): Promise<void>;
  on(event: 'complete', listener: () => void): void;
  off(event: 'complete', listener: () => void): void;
  once(event: 'complete', listener: () => void): void;
}
```

From trainer-sim `src/index.ts` (the runtime + type exports VW resolves):
```typescript
export { createFakeTransport } from './transport/fake-transport.js';
export type { ITrainerTransport, FakeTransport, FakeTransportConfig, FakeTransportSource, RideRecord } from './types.js';
```

NOTE: `RideRecord` IS exported at the trainer-sim package root per Phase 4's `src/index.ts`. This is what lets `dev-mode-save-flow.test.ts` rewrite `import type { RideRecord } from '../../dev/replay-scheduler'` -> `import type { RideRecord } from 'trainer-sim'`.

trainer-sim's `'complete'` event surface (FakeTransport):
```typescript
on(event: 'complete', listener: () => void): void;
off(event: 'complete', listener: () => void): void;
once(event: 'complete', listener: () => void): void;
```
- The adapter's `connect()` body wires `this.inner.on('complete', this.completeListener)` where `this.completeListener = () => useBleStore.getState().setSensorState('trainer', 'disconnected')`.
- `teardown()` unregisters via `this.inner.off('complete', this.completeListener)` BEFORE setting `this.inner = null`.
- In test mocks, `mockReturnValue.on` should be `vi.fn().mockImplementation((event, cb) => { if (event === 'complete') completeListener = cb })` so the test can fire the captured `'complete'` listener manually via `completeListener?.()` — replaces the previous `vi.advanceTimersByTime(5000)` driven by VW's deleted `ReplayScheduler`.

Renderer-process constraint (RESEARCH Pitfall 6):
- VW's adapter MUST use `{ source: { buffer: Uint8Array } }`. The `{ path }` variant calls `node:fs` which is not available in Electron renderer.
- VW's existing `window.veloworld.dev.readFitFile()` IPC returns the FIT bytes as `Uint8Array`. trainer-sim's `loadFitFromBuffer` is sync (D-FIT-07) and runs entirely in renderer memory.

VW's owned interface (NOT to be replaced — per user override #1):
- File: `/Users/agniveshpatel/dev/agni21/veloworld-ride/packages/ble/src/transport.ts`
- Shape: 9 methods (`scan`, `connect(deviceId: string)`, `onTelemetry`, `probeControlPointSupport`, `requestControl`, `setGradeSimulation`, `releaseControl`, `disconnect`, `reconnect`).
- Stays VW-owned. The adapter implements this interface and composes `FakeTransport` from trainer-sim INTERNALLY.

Wire-format expectation (RESEARCH "Wire-Format Compatibility"):
- trainer-sim emits 6-byte FTMS IndoorBikeData frames (no speed; bit-0 set).
- VW's `parseIndoorBikeData` reads bit-0 and returns `instantaneousSpeed: undefined` for these frames.
- VW's `BleManager.init` already coerces `parsed.instantaneousSpeed ?? null` (line 99-102 per RESEARCH).
- Result: dev-mode HUD shows speed=null, which is acceptable (FTMS-06 is v2-deferred).
- Tests MUST assert `expect(parsed.instantaneousSpeed).toBeUndefined()` (or `?? null`) — NOT compute speed from physics.

Pause/resume contract (per user override #2 — Path A):
- Adapter subscribes to `useRideStore` for rideState transitions.
- On `rideState === 'paused'`: `void this.inner?.disconnect()`.
- On `rideState === 'riding' && prev === 'paused'`: `void this.inner?.connect()`.
- The ~5ms FIT re-parse on resume is accepted. NOT Path B (which would preserve VW's `ReplayScheduler.pause/resume`).
- This is what makes the ~80-100 LOC adapter target achievable.

pnpm 10 strict-mode constraint (NEW for the replan — DEVIATIONS Gap 1):
- VW root `package.json` `packageManager` field is `pnpm@10.33.0`.
- pnpm 10's strict mode blocks `prepare` / `postinstall` lifecycle scripts for any dep not explicitly listed in `pnpm.onlyBuiltDependencies` (root `package.json`).
- Without opt-in, `pnpm install` errors with `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED` and trainer-sim's `dist/` is never materialized on the consumer side — Plan 05-01's whole prepare-hook contract silently fails.
- Fix: VW root `package.json` `pnpm.onlyBuiltDependencies` extends from `["electron", "esbuild"]` to `["electron", "esbuild", "trainer-sim"]`.
- This MUST happen before `pnpm install` runs in Task 1 sub-step B, or the install errors and rolls back.
</interfaces>

<vw_repo_paths>
<!-- All Wave 1 file edits are in /Users/agniveshpatel/dev/agni21/veloworld-ride; 11 files total -->
<!-- (9 originally + 2 added during the 2026-05-18 deviation replan: VW root package.json + dev-mode-save-flow.test.ts). -->
<!-- The executor MUST `cd /Users/agniveshpatel/dev/agni21/veloworld-ride` before running pnpm/git commands. -->

VW repo root: /Users/agniveshpatel/dev/agni21/veloworld-ride
VW current branch: main (per DEVIATIONS rollback section — feat/phase-5-trainer-sim-canonical was deleted on 2026-05-18 19:48 IST after the aborted attempt; the executor recreates it).

Files to modify (in VW) — 7 files:
- package.json (ROOT) — extend pnpm.onlyBuiltDependencies with "trainer-sim" (Gap 1 fix; pnpm 10 strict-mode opt-in)
- apps/desktop/package.json — add 1 line (trainer-sim git-ref dep)
- pnpm-lock.yaml (root level) — regenerate via `pnpm install`
- apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts — rewrite 273 LOC -> ~80-100 LOC; includes `'complete'` -> `'disconnected'` wiring
- apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts — modify mocks + speed assertion + inner mock `on/off/once`
- apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts — Phase 10 CR-01..CR-04 regression integration test; the 4th consumer of the deleted vendored modules; mock target + onComplete trigger must shift to trainer-sim's surface (Gap 2 fix)
- .github/workflows/ci.yml — add macOS matrix leg

Files to delete (in VW) — 4 files:
- apps/desktop/src/renderer/src/lib/dev/fit-loader.ts
- apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts
- apps/desktop/src/renderer/src/lib/__tests__/dev/fit-loader.test.ts
- apps/desktop/src/renderer/src/lib/__tests__/dev/replay-scheduler.test.ts

Files NOT to touch (in VW):
- packages/ble/src/transport.ts — the 9-method ITrainerTransport stays VW-owned (user override #1 / Anti-Pattern 6)
- packages/ble/src/ftms-codec.ts — VW's parseIndoorBikeData stays unchanged
- apps/desktop/src/renderer/src/lib/ble-manager.ts — VW-01 invariant (no changes to ride scene / physics / consumer code)
- apps/desktop/src/renderer/src/lib/dev-mode.ts — the dev-mode gate stays as-is
- apps/desktop/src/renderer/src/lib/__tests__/dev/fixtures/sample.fit — the existing 594-byte real-Garmin fixture is the D-VW-03 fixture (RESEARCH "Don't Hand-Roll" + Pitfall 7)
- apps/desktop/src/renderer/src/lib/stores/{ble-store,ride-store}.ts — store API surface stays as-is; the rewrite uses existing setSensorState/setSensorDevice/setDiscoveredDevices/clearTelemetryForSensor functions verbatim
</vw_repo_paths>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Allowlist trainer-sim + pin Wave 0 sha + delete vendored modules + add CI matrix</name>
  <files>/Users/agniveshpatel/dev/agni21/veloworld-ride/package.json, /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/package.json, /Users/agniveshpatel/dev/agni21/veloworld-ride/pnpm-lock.yaml, /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fit-loader.ts, /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts, /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fit-loader.test.ts, /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/replay-scheduler.test.ts, /Users/agniveshpatel/dev/agni21/veloworld-ride/.github/workflows/ci.yml</files>
  <read_first>
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt — read the exact 40-character sha to pin. This is the value Plan 05-01 captured.
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/package.json (ROOT) — current state of the file being modified in SUB-STEP B0. Inspect the existing `pnpm.onlyBuiltDependencies` array shape (currently `["electron", "esbuild"]`) and the `packageManager` field (must be `pnpm@10.33.0` or higher; if older, surface to human — the gap-1 fix only matters for pnpm 10+).
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/package.json — current state. Inspect the existing `dependencies` block to choose the insertion point.
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/.github/workflows/ci.yml — current state. The `build-and-prod-bundle-grep` job at lines 16-19 is the modification target.
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fit-loader.ts — read once before deleting (to confirm the deletion is safe; the post-deletion grep audit in this task verifies the only remaining stale reference is the to-be-rewritten adapter file).
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts — same: read once before deleting.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-PATTERNS.md §"`apps/desktop/package.json`" lines 124-150 and §".github/workflows/ci.yml" lines 286-317 — the exact insertion patterns and the matrix YAML diff.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-RESEARCH.md §"Pitfall 4" lines 430-444 (lockfile regen) and §"Pitfall 5" lines 446-465 (CI matrix).
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-02-DEVIATIONS.md §"Gap 1" — full root-cause + verbatim error message + fix for pnpm 10 strict-mode allowlist.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.github/workflows/ci.yml — the ALREADY-WORKING matrix syntax in trainer-sim is the proven template VW adopts. Read once for the exact `strategy.matrix.os` shape.
  </read_first>
  <action>
    Six sequential sub-steps, all inside `/Users/agniveshpatel/dev/agni21/veloworld-ride`. Run `cd /Users/agniveshpatel/dev/agni21/veloworld-ride` before each.

    SUB-STEP A — Create or check out a feature branch:
    1. `cd /Users/agniveshpatel/dev/agni21/veloworld-ride`
    2. `git status` — confirm a clean working tree on `main` per the DEVIATIONS rollback record. If unexpected uncommitted state surfaces, surface to human before proceeding.
    3. `git checkout -b feat/phase-5-trainer-sim-canonical` (the original branch was deleted on rollback; recreate it).

    SUB-STEP B0 (NEW for the replan; DEVIATIONS Gap 1 fix) — Allowlist trainer-sim in pnpm strict-mode:

    1. Read VW ROOT `package.json` to confirm the current `pnpm.onlyBuiltDependencies` shape. Expected current value: `["electron", "esbuild"]`.
    2. Use the Edit tool on `/Users/agniveshpatel/dev/agni21/veloworld-ride/package.json` to extend the array from `["electron", "esbuild"]` to `["electron", "esbuild", "trainer-sim"]` (alphabetic order is fine; either positioning is acceptable — the canonical form sorted is `["electron", "esbuild", "trainer-sim"]`).
    3. This MUST happen BEFORE SUB-STEP B's `pnpm install`. Without it, the install errors with `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED Failed to prepare git-hosted package fetched from "git@github.com:VeloWorld/trainer-sim.git": The git-hosted package "trainer-sim@0.0.1" needs to execute build scripts but is not in the "onlyBuiltDependencies" allowlist.` (verbatim error from the 2026-05-18 aborted attempt).

    SUB-STEP B — Pin the trainer-sim Wave 0 sha + regenerate lockfile (per RESEARCH §"Pitfall 4" + §"Code Examples 3" + PATTERNS §"`apps/desktop/package.json`"):

    1. Read the sha: `SHA=$(cat /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt | tr -d '[:space:]')`. Confirm `${#SHA}` is exactly 40 characters.
    2. Use the Edit tool on `apps/desktop/package.json` to add a single new key-value entry inside the existing `dependencies` object (NOT `devDependencies` — RESEARCH §"Code Examples 3" places it in `dependencies` because the dev-mode tree-shake gate is what isolates it from production, NOT the dep type). The key is `"trainer-sim"` and the value is `"github:VeloWorld/trainer-sim#${SHA}"` with `${SHA}` interpolated to the 40-char hex string read above. Position alphabetically (after `tailwind-merge`, before `three`, OR grouped near `fit-encoder` / `fit-file-parser` per PATTERNS recommendation; either is acceptable).
    3. Run `pnpm install` from VW repo root. This regenerates `pnpm-lock.yaml`. Expected duration: 30-90 seconds. If it errors with `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED`, SUB-STEP B0 was skipped or the edit didn't land — re-read VW root `package.json` and confirm `"trainer-sim"` is in the allowlist.
    4. Confirm the lockfile change is consistent: `git diff pnpm-lock.yaml | grep -E '(VeloWorld/trainer-sim|trainer-sim)' | head -20` should show new lockfile entries referencing `VeloWorld/trainer-sim` and the captured sha. If the diff is empty or references the wrong sha, surface the failure (do not proceed).
    5. Confirm `pnpm install` actually fetched a built `dist/` from the trainer-sim git-ref: `test -f node_modules/trainer-sim/dist/index.js && test -f node_modules/trainer-sim/dist/index.d.ts && echo "OK: prepare hook ran on consumer install"`. If either file is missing, the trainer-sim Wave 0 `prepare` hook didn't fire — escalate. Per DEVIATIONS, the typical cause now is SUB-STEP B0 (allowlist) being skipped or wrong-shaped; the secondary cause is a Plan 05-01 prepare-hook regression.

    SUB-STEP C — Delete VW's vendored modules and their tests (per user override #3):

    Use `git rm` so the deletion is staged immediately:
    1. `git rm apps/desktop/src/renderer/src/lib/dev/fit-loader.ts`
    2. `git rm apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts`
    3. `git rm apps/desktop/src/renderer/src/lib/__tests__/dev/fit-loader.test.ts`
    4. `git rm apps/desktop/src/renderer/src/lib/__tests__/dev/replay-scheduler.test.ts`

    After deletion, `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` AND `apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` will have BROKEN imports referencing `./fit-loader.js` / `./replay-scheduler.js` (adapter) and `'../../dev/replay-scheduler'` / `'../../dev/fit-loader'` (integration test). This is expected; Tasks 2 + 4 fix them. Do NOT touch the adapter or integration test files in this task.

    Confirm the consumer set is exactly what the plan expects: `grep -rln 'dev/fit-loader\|dev/replay-scheduler' apps/desktop/src/ packages/ 2>/dev/null | grep -v node_modules` should output EXACTLY two paths:
    - `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts`
    - `apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts`

    If anything else surfaces, that's an unexpected 5th-or-later consumer — surface to human (do NOT proceed).

    SUB-STEP D — Add macOS matrix to VW CI (per RESEARCH §"Pitfall 5" + §"Code Examples 4" + PATTERNS §".github/workflows/ci.yml"):

    Use the Edit tool on `.github/workflows/ci.yml`. The current job shape is:
    ```yaml
    jobs:
      build-and-prod-bundle-grep:
        runs-on: ubuntu-latest
        steps:
          - name: Checkout
          ...
    ```

    Replace those four lines (`build-and-prod-bundle-grep:` through `runs-on: ubuntu-latest`) with the matrix form (per PATTERNS line 300-313):
    ```yaml
    jobs:
      build-and-prod-bundle-grep:
        strategy:
          fail-fast: false
          matrix:
            os: [ubuntu-latest, macos-latest]
        runs-on: ${{ matrix.os }}
        name: ci (${{ matrix.os }})
        steps:
          - name: Checkout
          ...
    ```

    All `steps:` entries below are preserved verbatim (pnpm setup, Node setup, install, typecheck, test, build, prod-bundle grep). The `apps/desktop/out/renderer/assets/*.js` glob in the grep step is platform-agnostic per RESEARCH §"Pitfall 5".

    SUB-STEP E — Local checkpoint:

    Run from VW repo root:
    1. `git status` — expected: 4 files deleted (the vendored modules + tests), 4 files modified (`package.json` (ROOT, new for the replan), `apps/desktop/package.json`, `pnpm-lock.yaml`, `.github/workflows/ci.yml`). NOTE: this count of "4 modified" excludes the adapter file and the two test files — those land in Tasks 2-4.
    2. `git diff --stat HEAD` — confirm only the 8 expected files appear (4 deletions + 4 modifications).
    3. DO NOT run typecheck or test yet — the adapter still imports the deleted modules AND `dev-mode-save-flow.test.ts` still references them, so typecheck WILL fail until Tasks 2 + 4 land.
    4. Stage all changes: `git add package.json apps/desktop/package.json pnpm-lock.yaml .github/workflows/ci.yml` (the deletions were already staged via `git rm`).

    Do NOT commit yet — Task 3's local-green gate is the commit point.
  </action>
  <verify>
    <automated>cd /Users/agniveshpatel/dev/agni21/veloworld-ride &amp;&amp; { SHA=$(cat /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt | tr -d '[:space:]'); if [ ${#SHA} -ne 40 ]; then echo "FAIL: Wave 0 sha not 40 chars: '$SHA'"; exit 1; fi; ROOT_ALLOWLIST=$(grep -c '"trainer-sim"' package.json); if [ "$ROOT_ALLOWLIST" -lt "1" ]; then echo "FAIL: VW root package.json onlyBuiltDependencies missing trainer-sim — Gap 1 fix not applied"; exit 1; fi; PJSON_HIT=$(grep -c "github:VeloWorld/trainer-sim#$SHA" apps/desktop/package.json); if [ "$PJSON_HIT" != "1" ]; then echo "FAIL: trainer-sim sha pin missing or wrong in apps/desktop/package.json"; exit 1; fi; LOCK_HIT=$(grep -c "VeloWorld/trainer-sim" pnpm-lock.yaml); if [ "$LOCK_HIT" -lt "1" ]; then echo "FAIL: pnpm-lock.yaml has no VeloWorld/trainer-sim entry"; exit 1; fi; for f in apps/desktop/src/renderer/src/lib/dev/fit-loader.ts apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts apps/desktop/src/renderer/src/lib/__tests__/dev/fit-loader.test.ts apps/desktop/src/renderer/src/lib/__tests__/dev/replay-scheduler.test.ts; do if [ -f "$f" ]; then echo "FAIL: $f still exists, should be deleted"; exit 1; fi; done; CI_MATRIX_HIT=$(grep -c "macos-latest" .github/workflows/ci.yml); if [ "$CI_MATRIX_HIT" -lt "1" ]; then echo "FAIL: .github/workflows/ci.yml has no macos-latest"; exit 1; fi; CI_FAILFAST_HIT=$(grep -c "fail-fast: false" .github/workflows/ci.yml); if [ "$CI_FAILFAST_HIT" -lt "1" ]; then echo "FAIL: .github/workflows/ci.yml has no fail-fast: false"; exit 1; fi; test -f node_modules/trainer-sim/dist/index.js || { echo "FAIL: node_modules/trainer-sim/dist/index.js missing — prepare hook didn't run; check Gap 1 allowlist"; exit 1; }; STALE_REFS=$(grep -rln 'dev/fit-loader\|dev/replay-scheduler' apps/desktop/src/ packages/ 2>/dev/null | grep -v node_modules | sort); EXPECTED="apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts
apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts"; if [ "$STALE_REFS" != "$EXPECTED" ]; then echo "FAIL: unexpected stale refs to deleted modules:"; echo "$STALE_REFS"; exit 1; fi; echo "OK: Task 1 allowlist + dep pin + deletions + CI matrix landed correctly"; }</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c '"trainer-sim"' /Users/agniveshpatel/dev/agni21/veloworld-ride/package.json` is at least `1` (Gap 1 fix; pnpm 10 strict-mode allowlist opt-in landed).
    - Source assertion: `grep -c "github:VeloWorld/trainer-sim#$(cat /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt | tr -d '[:space:]')" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/package.json` outputs exactly `1`.
    - Source assertion: `grep -c "VeloWorld/trainer-sim" /Users/agniveshpatel/dev/agni21/veloworld-ride/pnpm-lock.yaml` is at least `1`.
    - Source assertion: `[ ! -f /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fit-loader.ts ]` exits 0 (file does NOT exist).
    - Source assertion: `[ ! -f /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts ]` exits 0.
    - Source assertion: `[ ! -f /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fit-loader.test.ts ]` exits 0.
    - Source assertion: `[ ! -f /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/replay-scheduler.test.ts ]` exits 0.
    - Source assertion: `grep -c "macos-latest" /Users/agniveshpatel/dev/agni21/veloworld-ride/.github/workflows/ci.yml` is at least `1`.
    - Source assertion: `grep -c "fail-fast: false" /Users/agniveshpatel/dev/agni21/veloworld-ride/.github/workflows/ci.yml` is at least `1`.
    - Source assertion: `grep -c "matrix.os" /Users/agniveshpatel/dev/agni21/veloworld-ride/.github/workflows/ci.yml` is at least `1` (the `runs-on: ${{ matrix.os }}` line).
    - CLI: `test -f /Users/agniveshpatel/dev/agni21/veloworld-ride/node_modules/trainer-sim/dist/index.js` exits 0 (proves the trainer-sim git-ref resolved AND the `prepare` hook ran on the consumer side — Plan 05-01's purpose end-to-end-confirmed AND Gap 1's pnpm allowlist did its job).
    - Source assertion: `grep -rln 'dev/fit-loader\|dev/replay-scheduler' /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/ /Users/agniveshpatel/dev/agni21/veloworld-ride/packages/ 2>/dev/null | grep -v node_modules` outputs EXACTLY two files: `apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` and `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` (the two to-be-rewritten files — Tasks 2 + 4 fix them).
  </acceptance_criteria>
  <done>
    VW root `package.json` `pnpm.onlyBuiltDependencies` extends to include `"trainer-sim"`. The trainer-sim Wave 0 sha is pinned in `apps/desktop/package.json`. `pnpm-lock.yaml` is regenerated and shows the resolution. The four vendored modules + tests are deleted via `git rm`. CI matrix has `[ubuntu-latest, macos-latest]` with `fail-fast: false`. `node_modules/trainer-sim/dist/index.js` exists locally (proves end-to-end git-ref + `prepare` works under pnpm 10 strict-mode). Adapter file AND integration test still have stale imports — Tasks 2 + 4 fix them.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite VW's `fake-trainer-transport.ts` to compose trainer-sim's createFakeTransport (with `'complete'` -> `'disconnected'` wiring)</name>
  <files>/Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts</files>
  <read_first>
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts — current 273 LOC state. Read the entire file once; the rewrite preserves the 9-method outer surface and the JSDoc header (the dev-mode tree-shake reasoning), and replaces the body with a delegation to trainer-sim plus the new `'complete'` -> `'disconnected'` listener.
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/packages/ble/src/transport.ts — VW's owned 9-method `ITrainerTransport` interface. The rewritten class implements this interface. DO NOT modify it.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/src/types.ts — trainer-sim's `FakeTransport`, `FakeTransportConfig`, `FakeTransportSource` definitions, including the `'complete'` event surface (`on/off/once`). Read for exact method names, parameter shapes, and JSDoc semantics.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-RESEARCH.md §"Pattern 1" lines 211-307 (the full ~80 LOC reference adapter), §"Pitfall 2" lines 407-415 (do NOT delete VW's interface), §"Pitfall 3" lines 417-428 (use `import type` for type symbols), §"Pitfall 6" lines 466-479 (use `{ buffer }` source variant only).
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-PATTERNS.md §"`apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts`" lines 154-198 — the structural rewrite map (which methods to preserve verbatim, which to rewrite).
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-02-DEVIATIONS.md §"Gap 2" — the CR-04 wiring requirement (`'complete'` event must drive `setSensorState('trainer', 'disconnected')` exactly once).
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/stores/ble-store.ts (and `ride-store.ts`) — read the exported store API surface so the rewrite uses the existing `setSensorState`, `setDiscoveredDevices`, `setSensorDevice`, `clearTelemetryForSensor` functions verbatim. Do NOT introduce new store methods.
  </read_first>
  <action>
    Replace the entire file `/Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` with a rewritten version following the structural map in PATTERNS §"fake-trainer-transport.ts" and the reference adapter in RESEARCH §"Pattern 1", PLUS the new `'complete'` -> `'disconnected'` wiring required by DEVIATIONS Gap 2 / CR-04. Use the Write tool (this file is being fully rewritten, not edited surgically).

    The rewritten file MUST:

    Structure:
    1. Preserve the JSDoc header from the current file's lines 1-27 (the dev-mode tree-shake reasoning + production-bundle hygiene contract). Update the implementation comments to reference trainer-sim composition rather than vendored modules.
    2. Imports: `import type { ITrainerTransport } from '@veloworld/ble'` (VW's owned interface — DO NOT replace), `import { createFakeTransport, type FakeTransport } from 'trainer-sim'` (per RESEARCH Pitfall 3 — use `import type` for the type symbol; trainer-sim has `verbatimModuleSyntax: true`), `import { useBleStore } from '../../stores/ble-store.js'`, `import { useRideStore } from '../../stores/ride-store.js'`. The `import { ReplayScheduler, type RideRecord } from './replay-scheduler.js'` and `import { loadFitFromBytes } from './fit-loader.js'` lines from the current file MUST be removed (their target files are deleted).
    3. The `encodeIndoorBikeFrame` function (current lines 60-89, 30 LOC, 8-byte frame with speed) is DELETED. trainer-sim's encoder produces 6-byte frames; the speed channel becomes `undefined` at the parser per the wire-format diff. Per the `<interfaces>` block above + RESEARCH "Wire-Format Compatibility", this is intentional and acceptable.
    4. Constants: `FAKE_DEVICE_ID = 'fake-trainer-fit'` and `FAKE_DEVICE_NAME = 'Trainer Sim (FIT replay)'` preserved verbatim from the current file.

    Class `FakeTrainerTransport implements ITrainerTransport` (where `ITrainerTransport` is VW's 9-method interface from `@veloworld/ble`):

    - Private fields: `inner: FakeTransport | null = null`, `telemetryHandler: ((data: DataView) => void) | null = null`, `innerDispose: (() => void) | null = null`, `rideStateUnsub: (() => void) | null = null`, AND `completeListener: (() => void) | null = null` (NEW for the replan — required for the `'complete'` -> `'disconnected'` wiring per DEVIATIONS Gap 2). NO `scheduler`, NO `records`, NO `frameBuffer` — those are dead concepts after the rewrite.

    - `async scan(): Promise<void>`: Preserve verbatim from current lines 103-113. Synthesizes a single device row with `FAKE_DEVICE_ID` / `FAKE_DEVICE_NAME` via `useBleStore.getState().setDiscoveredDevices(...)` and calls `setSensorState('trainer', 'scanning')`.

    - `async connect(deviceId: string): Promise<void>`: Per user override #1 + RESEARCH §"Pattern 1" + DEVIATIONS Gap 2 (CR-04 wiring):
      1. If `deviceId !== FAKE_DEVICE_ID`, throw `new Error(\`FakeTrainerTransport: unknown deviceId ${deviceId}\`)` (preserves current line 144-147 behavior).
      2. Call `this.teardown()` (private method below) to clean any prior inner.
      3. `useBleStore.getState().setSensorState('trainer', 'connecting')`.
      4. `const bytes = await window.veloworld.dev.readFitFile()` — preserves the existing IPC seam. Type is `Uint8Array` (per VW's existing main-side IPC contract; do NOT change `dev/preload-bridge.ts` or main-side `dev-mode.ts`).
      5. `this.inner = createFakeTransport({ source: { buffer: bytes } })` — RESEARCH Pitfall 6: use the `{ buffer }` variant ONLY; the `{ path }` variant calls `node:fs` which is unavailable in renderer.
      6. `this.innerDispose = this.inner.onData((dv) => this.telemetryHandler?.(dv))` — fan inner's `onData` out to VW's `onTelemetry` handler.
      7. NEW for the replan — register the `'complete'` listener BEFORE connect: `this.completeListener = () => useBleStore.getState().setSensorState('trainer', 'disconnected'); this.inner.on('complete', this.completeListener)`. This is the wiring that makes CR-04 (`expect(disconnectedCalls).toHaveLength(1)` in `dev-mode-save-flow.test.ts` line 254) pass; without it, the integration test fires a regression and Plan 05-04's verification cannot complete.
      8. `try { await this.inner.connect() } catch (err) { useBleStore.getState().setSensorState('trainer', 'idle'); throw err }` — `FitLoadError` family bubbles unchanged per Phase 4 D-API-04. (If connect throws, the listener registered in step 7 is left dangling on the inner; that's fine because `teardown()` would clean it, but since we already called teardown() at the top, the simplest safe thing is to also call teardown() in the catch branch to remove the dangling listener. The executor decides; either pattern is acceptable as long as the listener is unwired on failure.)
      9. `useBleStore.getState().setSensorState('trainer', 'connected')` and `setSensorDevice('trainer', FAKE_DEVICE_ID, FAKE_DEVICE_NAME)`.
      10. Pause/resume subscription (per user override #2 — Path A): track `prev = useRideStore.getState().rideState` outside the subscribe callback (closure). Subscribe with `this.rideStateUnsub = useRideStore.subscribe((s) => { if (s.rideState === prev) return; if (s.rideState === 'paused') void this.inner?.disconnect(); if (s.rideState === 'riding' && prev === 'paused') void this.inner?.connect(); prev = s.rideState })`. The closure-tracked `prev` matches Zustand v5's single-arg subscribe signature (the current file uses the same pattern at lines 161-177; preserve it).

    - `onTelemetry(handler: (data: DataView) => void): () => void`: Preserve current lines 204-209 verbatim. Stores the handler; returns a disposer that nulls it out.

    - `async probeControlPointSupport(): Promise<boolean> { return true }`: Preserve current line 217.
    - `async requestControl(): Promise<boolean> { return true }`: Preserve current line 220.
    - `async setGradeSimulation(_g: number): Promise<void> { /* no-op */ }`: Preserve current lines 222-225 (no-op for FIT replay; resistance is echo-only per trainer-sim's contract).
    - `releaseControl(): void { /* no-op */ }`: Preserve current line 234.

    - `async disconnect(): Promise<void>`:
      1. `this.teardown()` (private method below).
      2. `useBleStore.getState().setDiscoveredDevices([])`.
      3. `useBleStore.getState().setSensorState('trainer', 'idle')`.
      4. `useBleStore.getState().clearTelemetryForSensor('trainer')`.
      Mirrors current lines 260-266 except `teardown()` body now talks to `this.inner` instead of `this.scheduler`.

    - `async reconnect(): Promise<void> { return this.connect(FAKE_DEVICE_ID) }`: Preserve current line 269-271.

    - `private teardown(): void`:
      1. `this.innerDispose?.()` — disposes the `onData` subscription on the inner trainer-sim transport.
      2. `this.innerDispose = null`.
      3. NEW for the replan (DEVIATIONS Gap 2): unregister the `'complete'` listener BEFORE setting `this.inner = null`: `if (this.completeListener && this.inner) { this.inner.off('complete', this.completeListener) } this.completeListener = null`. The order matters — `off` must fire while `this.inner` still references the same FakeTransport instance the listener was registered against.
      4. `void this.inner?.disconnect()` — fire-and-forget; trainer-sim's `disconnect()` is idempotent (Phase 3 CR-01 invariant).
      5. `this.inner = null`.
      6. `this.rideStateUnsub?.()` — unsubscribes from `useRideStore`.
      7. `this.rideStateUnsub = null`.
      Replaces current `teardownReplay()` at lines 247-253 (renamed since it tears down trainer-sim's inner, not a vendored scheduler). Note: the natural-exhaustion path triggers `'complete'` -> the listener fires `setSensorState('trainer', 'disconnected')` -> the listener does NOT itself call `teardown()`. A subsequent explicit `disconnect()` call (e.g. in `dev-mode-save-flow.test.ts` line 260) tears down the listener via `off`. This matches CR-03's idempotent-teardown contract: a second teardown sees `this.completeListener === null` and short-circuits.

    Total expected file length: ~80-110 LOC including JSDoc header. The class IS smaller than the original 273 LOC because trainer-sim absorbs the FIT load + scheduler + encoder concerns; the new `completeListener` field + listener wiring adds ~6-8 LOC over the previous ~80 target.

    DO NOT:
    - Add a `pause()` or `resume()` method to the class — VW's 9-method `ITrainerTransport` doesn't have those (per RESEARCH §"Architectural Responsibility Map" — pause/resume is implemented INSIDE the rideState subscription, not as a public method).
    - Compute speed from physics inside the adapter (per `<interfaces>` constraint — speed=null is acceptable).
    - Touch any other VW source file (the entire rewrite is local to this one file).
    - Add `received` / `reset` surface to the adapter — those are FakeTransport-specific affordances trainer-sim provides; the adapter's CALLER (a test) can narrow `this.inner` to `FakeTransport` if needed (RESEARCH §"Code Insights" notes this in CONTEXT.md), but the public adapter surface stays the 9-method ITrainerTransport.
    - Subscribe to `'complete'` on every reconnect WITHOUT first unwiring the prior listener — the sequence `connect -> connect` (without an intermediate disconnect) MUST go through `teardown()` first to avoid leaking listeners on the prior inner. The spec's step 2 (call `this.teardown()` at the top of `connect()`) handles this.

    After the rewrite, run `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm typecheck` to confirm types resolve. If errors surface, fix them iteratively. Common gotchas:
    - "Cannot find module 'trainer-sim'" → trainer-sim's git-ref didn't resolve; rerun Task 1's `pnpm install`.
    - "Property 'X' does not exist on type 'FakeTransport'" → trainer-sim's API doesn't have what the adapter assumed; check trainer-sim's `src/types.ts` for the actual surface.
    - "ITrainerTransport from '@veloworld/ble' is missing methods" → the class body misses one of the 9 methods; cross-check against `packages/ble/src/transport.ts`.
    - "Type 'null' is not assignable to type 'FakeTransport'" or similar on `this.inner.off(...)` — guard with `if (this.completeListener && this.inner)` per the teardown spec above.
  </action>
  <verify>
    <automated>cd /Users/agniveshpatel/dev/agni21/veloworld-ride &amp;&amp; { F=apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts; if [ ! -f "$F" ]; then echo "FAIL: $F missing"; exit 1; fi; if grep -q "from '\\./fit-loader\\|from '\\./replay-scheduler\\|encodeIndoorBikeFrame\\|loadFitFromBytes\\|ReplayScheduler" "$F"; then echo "FAIL: $F still references deleted/banned symbols"; grep -n "from '\\./fit-loader\\|from '\\./replay-scheduler\\|encodeIndoorBikeFrame\\|loadFitFromBytes\\|ReplayScheduler" "$F"; exit 1; fi; if ! grep -q "from 'trainer-sim'" "$F"; then echo "FAIL: $F missing trainer-sim import"; exit 1; fi; if ! grep -q "createFakeTransport" "$F"; then echo "FAIL: $F missing createFakeTransport call"; exit 1; fi; if ! grep -q "source: { buffer:" "$F"; then echo "FAIL: $F missing { source: { buffer: ... } } shape — Pitfall 6"; exit 1; fi; if grep -q "source: { path:" "$F"; then echo "FAIL: $F uses { path } variant — forbidden in renderer (Pitfall 6)"; exit 1; fi; if ! grep -q "FAKE_DEVICE_ID\\s*=\\s*'fake-trainer-fit'" "$F"; then echo "FAIL: $F missing FAKE_DEVICE_ID constant"; exit 1; fi; if ! grep -q "on('complete'" "$F"; then echo "FAIL: $F missing on('complete' wiring — DEVIATIONS Gap 2 / CR-04"; exit 1; fi; if ! grep -q "off('complete'" "$F"; then echo "FAIL: $F missing off('complete' teardown — DEVIATIONS Gap 2 / CR-04"; exit 1; fi; if ! grep -q "setSensorState('trainer', 'disconnected')" "$F"; then echo "FAIL: $F missing setSensorState('trainer', 'disconnected') — CR-04 wiring"; exit 1; fi; LOC=$(wc -l &lt; "$F"); if [ "$LOC" -lt "60" ] || [ "$LOC" -gt "200" ]; then echo "WARN: $F is $LOC lines, expected ~80-110. Surface to human if very off-target."; fi; pnpm typecheck 2>&amp;1 | tail -20; echo "Typecheck exit: $?"; }</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "from 'trainer-sim'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is exactly `1`.
    - Source assertion: `grep -c "createFakeTransport" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is at least `1`.
    - Source assertion: `grep -c "source: { buffer:" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is exactly `1` (the buffer-variant call site).
    - Source assertion: `grep -c "source: { path:" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is exactly `0` (renderer cannot use `node:fs` per Pitfall 6).
    - Source assertion: `grep -c "from './fit-loader\|from './replay-scheduler\|encodeIndoorBikeFrame\|loadFitFromBytes\|ReplayScheduler" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is exactly `0` (the rewrite removed all references to deleted/internal symbols).
    - Source assertion: All 9 ITrainerTransport methods are defined in the class — `grep -cE '(scan|connect|onTelemetry|probeControlPointSupport|requestControl|setGradeSimulation|releaseControl|disconnect|reconnect)\s*\(' /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is at least `9`.
    - Source assertion: `grep -c "import type { ITrainerTransport } from '@veloworld/ble'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is exactly `1` (VW's owned interface preserved per Pitfall 2 / user override #1).
    - Source assertion: `grep -c "rideState" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is at least `2` (the pause/resume subscription).
    - Source assertion: `grep -c "on('complete'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is at least `1` (the `'complete'` listener is registered — DEVIATIONS Gap 2 / CR-04 wiring).
    - Source assertion: `grep -c "off('complete'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is at least `1` (the `'complete'` listener is unregistered in teardown — DEVIATIONS Gap 2 / CR-04 wiring).
    - Source assertion: `grep -c "setSensorState('trainer', 'disconnected')" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is at least `1` (the disconnected transition is wired — required for CR-04).
    - Source assertion: `grep -c "completeListener" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is at least `2` (private field declaration + at least one usage).
    - CLI: `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm typecheck` exits 0.
    - Behavior assertion: The file structure follows RESEARCH §"Pattern 1" (private fields are exactly: `inner`, `telemetryHandler`, `innerDispose`, `rideStateUnsub`, `completeListener` — no `scheduler`/`records`/`frameBuffer`).
  </acceptance_criteria>
  <done>
    `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is rewritten as a ~80-110 LOC adapter that composes trainer-sim's `createFakeTransport` with the `{ buffer }` source variant. VW's 9-method `ITrainerTransport` is implemented unchanged. Pause/resume uses Path A (disconnect/reconnect on rideState transitions). The adapter registers a `'complete'` listener that calls `useBleStore.getState().setSensorState('trainer', 'disconnected')` and unregisters it in `teardown()` — CR-04 wiring landed. `pnpm typecheck` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite VW unit-test mocks (`fake-trainer-transport.test.ts`) + run local-green gate (typecheck + test + build + grep)</name>
  <files>/Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts</files>
  <read_first>
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts — current 296 LOC state. Read the entire file. The test structure (8-9 `it()` blocks) is preserved; the mocks change.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-PATTERNS.md §"`__tests__/dev/fake-trainer-transport.test.ts`" lines 219-262 — the per-test rewrite map.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-RESEARCH.md §"Open Question 4" lines 695-699 (speed=null assertion) and §"Wire-Format Compatibility" lines 365-383 (6-byte frame format).
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts (post-Task-2 state) — the just-rewritten adapter; the test must mock its dependencies, which are now `'trainer-sim'`, `'../../stores/ble-store'`, `'../../stores/ride-store'`. Confirm the `'complete'` wiring is in place (so test #8's natural-exhaustion path can drive it via the captured listener).
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/packages/ble/src/ftms-codec.ts — VW's `parseIndoorBikeData`. The test #5 assertion on `parsed.instantaneousSpeed` depends on this function's return shape; read it once to confirm the return type is `{ instantaneousPower, instantaneousCadence, instantaneousSpeed?, ... }`.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/src/types.ts — trainer-sim's `FakeTransport` shape (the mock's return value must satisfy this, including the `on/off/once` event surface).
  </read_first>
  <action>
    Rewrite `apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` per PATTERNS §"`__tests__/dev/fake-trainer-transport.test.ts`" lines 219-262. Use the Edit tool for surgical changes (the test structure is largely preserved); use Write only if Edit becomes unwieldy.

    Concrete changes:

    Change 1 (the hoisted-mock block, current lines 14-67):
    - Remove the `mockLoadFitFromBytes` declaration (the `loadFitFromBytes` symbol no longer exists in VW; trainer-sim handles FIT loading).
    - Add new mock declarations for the trainer-sim API: `mockCreateFakeTransport`, `mockInnerConnect`, `mockInnerDisconnect`, `mockInnerOnData`. The `vi.hoisted()` body returns these as `vi.fn()` instances.
    - Replace the `vi.mock('../../dev/fit-loader', () => ({ loadFitFromBytes: mockLoadFitFromBytes }))` block with `vi.mock('trainer-sim', () => ({ createFakeTransport: mockCreateFakeTransport }))`.
    - Preserve `vi.mock('../../../stores/ble-store', ...)` and `vi.mock('../../../stores/ride-store', ...)` blocks verbatim (the store mocks are unaffected).

    Change 2 (test #3 — "connect loads FIT bytes via window.veloworld.dev.readFitFile and starts ReplayScheduler", current lines 139-159):
    - Rename the test to `"connect loads FIT bytes via window.veloworld.dev.readFitFile and delegates to trainer-sim's createFakeTransport"`.
    - The body's primary assertion shifts from `expect(mockLoadFitFromBytes).toHaveBeenCalledWith(bytes)` to `expect(mockCreateFakeTransport).toHaveBeenCalledWith({ source: { buffer: bytes } })` and `expect(mockInnerConnect).toHaveBeenCalledOnce()`.
    - Setup: configure `mockCreateFakeTransport.mockReturnValue({ connect: mockInnerConnect, disconnect: mockInnerDisconnect, onData: mockInnerOnData, sendResistance: vi.fn(), received: { resistance: [] }, reset: vi.fn(), on: vi.fn(), off: vi.fn(), once: vi.fn() })` so the inner shape matches `FakeTransport`. Configure `mockInnerOnData.mockReturnValue(() => {})` (the disposer).

    Change 3 (test #4 — "connect rejects unknown deviceId", current lines 161-166):
    - Preserve verbatim. The deviceId check is in the adapter, not in the inner trainer-sim transport, so this test doesn't need mock changes.

    Change 4 (test #5 — "onTelemetry handler receives DataView frames built from FIT records", current lines 168-204) — THIS IS THE LOAD-BEARING TEST:
    - The current test uses VW's vendored `encodeIndoorBikeFrame` to build expected 8-byte frames with speed. The rewrite asserts trainer-sim's 6-byte frames per `<interfaces>` wire-format expectation:
      1. Setup: capture the inner `onData` callback by configuring `mockInnerOnData` to record its argument: `let capturedOnData: ((dv: DataView) => void) | undefined; mockInnerOnData.mockImplementation((cb) => { capturedOnData = cb; return () => {} })`.
      2. The `mockReturnValue` shape MUST include `on: vi.fn().mockImplementation((event, cb) => { if (event === 'complete') completeListener = cb; return undefined })`, `off: vi.fn()`, `once: vi.fn()` so the captured `'complete'` listener is reachable from test #8 (natural exhaustion). The existing `received: { resistance: [] }`, `reset: vi.fn()` stay.
      3. After `await transport.connect(FAKE_DEVICE_ID)`, register a telemetry listener on the adapter: `const received: ParsedFrame[] = []; transport.onTelemetry((dv) => { received.push(parseIndoorBikeData(dv)) })`.
      4. Synthesize trainer-sim-shaped 6-byte frames in the test (using a hand-rolled little-endian buffer build that matches FTMS-04: bit-0 SET = no speed, bit-2 SET = cadence, bit-6 SET = power, frame body = `<flags-uint16-LE><cadence-uint16-LE><power-sint16-LE>`). Push them through `capturedOnData(dv)`.
      5. Assertions: `expect(received).toHaveLength(N)`, `expect(received[i].instantaneousPower).toBe(<expected-watts>)`, `expect(received[i].instantaneousCadence).toBe(<expected-rpm>)`, `expect(received[i].instantaneousSpeed).toBeUndefined()` (per RESEARCH Open Question 4).
    - This test is the "round-trip-through-parseIndoorBikeData" gate that proves VW-02 (power+cadence read correctly across the full ride). It is the closest thing to a phase-defining test in VW's repo.

    Change 5 (test #6 — "probeControlPointSupport, requestControl..."):
    - Preserve verbatim. The 5 BLE-shaped methods are still no-ops/synthetic.

    Change 6 (test #7 — "disconnect() calls scheduler.stop()", current lines 214-240):
    - Rename to `"disconnect() awaits inner.disconnect() and clears store state"`.
    - The assertion shifts from `expect(mockSchedulerStop).toHaveBeenCalled()` to `expect(mockInnerDisconnect).toHaveBeenCalled()`. The store-clearing assertions (`setSensorState('trainer', 'idle')`, `clearTelemetryForSensor('trainer')`) stay verbatim.

    Change 7 (test #8 — "natural exhaustion tears down rideState subscription (CR-03) AND fires `setSensorState('trainer', 'disconnected')` (CR-04)", current lines 242-263):
    - The natural-exhaustion trigger shifts from "scheduler reaches end-of-records" to "trainer-sim emits `'complete'` event". Setup: capture the `'complete'` listener via the inner mock's `on` (`mockReturnValue.on = vi.fn().mockImplementation((event, cb) => { if (event === 'complete') completeListener = cb })`). Trigger via `completeListener?.()`.
    - NEW for the replan: the adapter's NEW behavior (Task 2) means trainer-sim's `'complete'` event now fires through the adapter's listener to `setSensorState('trainer', 'disconnected')`. Test #8 MUST assert BOTH:
      (a) `expect(mockSetSensorState).toHaveBeenCalledWith('trainer', 'disconnected')` — proves the CR-04 wiring fires; the disconnected transition is observable from VW's store at the moment trainer-sim's scheduler exhausts records.
      (b) The rideState subscription IS NOT torn down by the listener itself (the listener only calls setSensorState; teardown happens on explicit disconnect or on the next connect's top-of-body teardown). If the existing test asserts `expect(mockUnsubscribeRide).toHaveBeenCalled()` immediately after the `'complete'` fire, that assertion needs to be removed or rewritten — the executor decides based on what the post-Task-2 adapter actually does. The CR-03 invariant is that explicit `disconnect()` (separate test event) DOES tear down rideStateUnsub, NOT that the natural-exhaustion onComplete tears it down.
    - PATTERNS notes this as test rewrite, not preservation. Preserve the test intent (CR-03 + CR-04 regression coverage) but adjust the trigger to trainer-sim's `'complete'` surface and align with the adapter's actual post-Task-2 behavior.

    Change 8 (test #9 — "re-connect does not leak the prior rideState subscription", current lines 265-295):
    - Preserve test intent (CR-03 second leg). Adjust mocks to match the new shape: each `connect()` triggers a fresh `createFakeTransport` call → fresh `inner` → fresh `rideStateUnsub` AND fresh `completeListener`. Assert that the FIRST-connect's `rideStateUnsub` was called when the SECOND-connect happens (the top-of-body `teardown()` in connect cleans the prior). Also assert that the FIRST-connect's `'complete'` listener was unregistered via `off` on the first inner before the second `createFakeTransport` call.

    SUB-STEP F — Local-green gate:

    After all test rewrites, run from VW repo root:
    1. `pnpm typecheck` — exit 0.
    2. `pnpm test` — all tests pass. NOTE: `dev-mode-save-flow.test.ts` will still FAIL at this point because it still references the deleted modules (Task 4 hasn't run yet). The executor has two options:
       (a) Run only the unit-test file in this task's gate: `pnpm test -- fake-trainer-transport.test.ts` (and confirm exit 0 for that selector), then defer the full `pnpm test` run to Task 4's gate.
       (b) Run full `pnpm test` and tolerate `dev-mode-save-flow.test.ts` failing for now, then re-run after Task 4.
       Option (a) is recommended for clean task isolation; the full workspace gate is Task 4's job.
    3. `pnpm --filter @veloworld/desktop build` — exit 0. This builds the renderer; `apps/desktop/out/renderer/assets/*.js` is produced.
    4. Production-bundle grep gate: `grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js`. Per VW's existing CI step, this MUST exit 1 (no matches) — proves the dev-mode tree-shake gate strips trainer-sim from production. If it exits 0 (matches found), the dev-mode dynamic-import gate is broken and Phase 5 cannot proceed; surface to human.

    SUB-STEP G — Defer commit until Task 4 lands:

    Do NOT commit at the end of Task 3. The full Wave 1a commit lands at the end of Task 4 once `dev-mode-save-flow.test.ts` is green and the full `pnpm test` workspace passes. This is a change from the original plan (which committed at the end of Task 3); the four-task structure benefits from a single Wave 1a commit covering all 11 files.

    The executor MAY stage all changes via `git add -A` (or selectively) at the end of Task 3 to make the Task 4 commit atomic, but the actual `git commit` happens in Task 4 SUB-STEP H.
  </action>
  <verify>
    <automated>cd /Users/agniveshpatel/dev/agni21/veloworld-ride &amp;&amp; { pnpm typecheck 2>&amp;1 | tail -5; TC=$?; if [ "$TC" != "0" ]; then echo "FAIL: pnpm typecheck exit $TC"; exit 1; fi; pnpm test -- fake-trainer-transport.test.ts 2>&amp;1 | tail -20; TT=$?; if [ "$TT" != "0" ]; then echo "FAIL: pnpm test fake-trainer-transport.test.ts exit $TT"; exit 1; fi; pnpm --filter @veloworld/desktop build 2>&amp;1 | tail -10; TB=$?; if [ "$TB" != "0" ]; then echo "FAIL: pnpm build exit $TB"; exit 1; fi; if grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js; then echo "FAIL: production bundle contains trainer-sim or FakeTransport strings — dev-mode tree-shake broken"; exit 1; fi; echo "OK: Task 3 unit-test rewrite + local build + grep gate passed; full workspace test deferred to Task 4"; }</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "vi.mock('trainer-sim'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` is exactly `1`.
    - Source assertion: `grep -c "vi.mock('../../dev/fit-loader'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` is exactly `0` (the old mock target is gone).
    - Source assertion: `grep -c "instantaneousSpeed" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` is at least `1` AND at least one occurrence is in a `toBeUndefined()` or `?? null` context (the speed=null assertion per Open Question 4).
    - Source assertion: `grep -c "createFakeTransport" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` is at least `1`.
    - Source assertion: The inner-mock shape includes the `'complete'` event surface — `grep -c "on: vi.fn" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` is at least `1`.
    - Source assertion: Test #8 captures and fires the `'complete'` listener — `grep -c "completeListener" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` is at least `1`.
    - Behavior assertion: All 8 surviving test groups in the file have an `it(...)` declaration (the 9 originals minus removed-tests-if-any; if all 9 are preserved, count is 9).
    - CLI: `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm typecheck` exits 0.
    - CLI: `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm test -- fake-trainer-transport.test.ts` exits 0. The terminal output reports passing test counts for the 8 (or 9) `it()` blocks; no test failures within this file.
    - CLI: `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm --filter @veloworld/desktop build` exits 0.
    - CLI: `grep -El 'fake-transport|FakeTransport|trainer-sim' /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/out/renderer/assets/*.js` exits 1 (no matches in production bundle — dev-mode tree-shake holds, CI grep gate will pass on push).
  </acceptance_criteria>
  <done>
    The unit-test file `fake-trainer-transport.test.ts` is rewritten with `vi.mock('trainer-sim', ...)`, the speed=null assertion is in place, the inner-mock shape includes `on/off/once` so `'complete'` is testable, and all 8 (or 9) `it()` blocks pass under the file-scoped test runner. `pnpm typecheck`, `pnpm test -- fake-trainer-transport.test.ts`, `pnpm --filter @veloworld/desktop build` all exit 0 from the VW repo root. The production bundle grep gate exits 1 (no trainer-sim leak). NO commit yet — Task 4 owns the single Wave 1a commit covering all 11 files.
  </done>
</task>

<task type="auto">
  <name>Task 4: Rewrite `dev-mode-save-flow.test.ts` to repoint at trainer-sim + run full-workspace local-green gate + commit Wave 1a</name>
  <files>/Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts</files>
  <read_first>
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts — current 263 LOC state. Read the entire file. The test has CR-01..CR-04 inline comments at the assertions (lines 206-210 = CR-03 unsub, lines 220-223 = CR-01 appState unchanged, lines 225-235 = CR-02 disk file, lines 245-254 = CR-04 disconnected-once, lines 256-261 = CR-03 second leg). The rewrite preserves all four CR-XX assertion intents while shifting the mock target + completion trigger.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-02-DEVIATIONS.md §"Gap 2" — full root-cause + lines 49/64/90/105/143/150/254 verbatim references.
    - The just-rewritten `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` (post-Task-2 state) — confirms the `'complete'` -> `'disconnected'` wiring is in place. Without this wiring, line 254's CR-04 assertion fires a regression.
    - The just-rewritten `apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` (post-Task-3 state) — borrow the mock-shape patterns (capture-`'complete'`-via-`on` mock, inner mock shape including `received`/`reset`/`sendResistance`/`on/off/once`) to keep both test files consistent.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/src/index.ts — confirms `RideRecord` is exported at the trainer-sim package root (it is, per Phase 4's exports).
    - Phase 10 SUMMARY or equivalent in VW: `grep -rln "CR-01" /Users/agniveshpatel/dev/agni21/veloworld-ride/.planning/ 2>/dev/null | head -5` to find the source file documenting the CR-01..CR-04 closure-plan invariants. Read once to preserve CR-XX intent — the rewrite must not weaken any of the four assertions.
    - /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/main/storage/{app-state,ride-storage,dev-lifetime-stats}.js — the test dynamic-imports these via the relative path `../../../../../main/storage/...`. Read the exports once (`initStorage`, `getAppState`, `initRideStorage`, `saveRideMetadata`, `initDevLifetime`, `getDevLifetime`) to confirm the integration test's main-side calls are unchanged by the trainer-sim swap.
  </read_first>
  <action>
    Rewrite `apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` to repoint at trainer-sim's surface. Use the Edit tool for surgical changes; use Write only if the cumulative diff becomes unmanageable. The test's CR-01..CR-04 assertion intent is fully preserved; only the mock target + completion trigger change.

    Concrete sub-steps:

    SUB-STEP 1 — Replace the `RideRecord` import:
    - Line 49: `import type { RideRecord } from '../../dev/replay-scheduler';` -> `import type { RideRecord } from 'trainer-sim';`. trainer-sim DOES export `RideRecord` at the package root per Phase 4's `src/index.ts` (confirmed via the read_first list).

    SUB-STEP 2 — Replace the hoisted-mock symbols:
    - The current `vi.hoisted()` block (lines 54-92) declares `mockLoadFitFromBytes` (line 64, 90) and uses it in `vi.mock('../../dev/fit-loader', ...)` (lines 105-107).
    - Remove `mockLoadFitFromBytes` from the hoisted return. Add `mockCreateFakeTransport: vi.fn()` (and optionally `mockInnerConnect`, `mockInnerDisconnect`, `mockInnerOnData` if the test body benefits from named handles for the inner mock; the executor decides — the simplest form is to declare `mockCreateFakeTransport` only and let the test body shape the `mockReturnValue` inline).
    - Replace the `vi.mock('../../dev/fit-loader', () => ({ loadFitFromBytes: mockLoadFitFromBytes }))` block (lines 105-107) with `vi.mock('trainer-sim', () => ({ createFakeTransport: mockCreateFakeTransport }))`.
    - Remove the `mockLoadFitFromBytes.mockReset()` (line 143) and `mockLoadFitFromBytes.mockResolvedValue(SAMPLE_RECORDS)` (line 150) calls — replace with `mockCreateFakeTransport.mockReset()` (in `beforeEach`) and a new shape that builds the inner mock (see SUB-STEP 3).

    SUB-STEP 3 — Refactor the test setup to expose a captured `'complete'` listener and an inner-mock-shape that satisfies trainer-sim's `FakeTransport`. Pattern (option a — recommended; cleanest test boundary):

    Add at the top of the `describe` block (before `beforeEach`):
    ```ts
    let completeListener: (() => void) | undefined;
    ```

    Inside `beforeEach`, configure the mock (replacing the deleted `mockLoadFitFromBytes.mockResolvedValue(SAMPLE_RECORDS)` line):
    ```ts
    completeListener = undefined;
    const mockInnerConnect = vi.fn().mockResolvedValue(undefined);
    const mockInnerDisconnect = vi.fn().mockResolvedValue(undefined);
    const mockInnerOnData = vi.fn(() => () => {});
    mockCreateFakeTransport.mockReturnValue({
      connect: mockInnerConnect,
      disconnect: mockInnerDisconnect,
      onData: mockInnerOnData,
      sendResistance: vi.fn(),
      received: { resistance: [] },
      reset: vi.fn(),
      on: vi.fn().mockImplementation((evt, cb) => { if (evt === 'complete') completeListener = cb }),
      off: vi.fn(),
      once: vi.fn(),
    });
    ```

    The `SAMPLE_RECORDS` constant (lines 111-114) becomes lightly informational — it's no longer fed to a `mockLoadFitFromBytes.mockResolvedValue(...)` call because trainer-sim is mocked at the `createFakeTransport` boundary, not at the FIT-loader boundary. Keep the constant in the file (the executor MAY choose to reference it in comments documenting CR-XX expectations, but the test no longer needs to drive trainer-sim's records directly).

    SUB-STEP 4 — Replace the natural-exhaustion trigger:
    - Line 201: `vi.advanceTimersByTime(5000);` previously triggered VW's vendored `ReplayScheduler` to walk all records and fire its onComplete callback. The deleted `ReplayScheduler` no longer exists.
    - Replace with: `completeListener?.();` — fires trainer-sim's `'complete'` event manually via the captured listener.
    - The CR-04 assertion (line 251-254): `const disconnectedCalls = mockSetSensorState.mock.calls.filter((c) => c[0] === 'trainer' && c[1] === 'disconnected'); expect(disconnectedCalls).toHaveLength(1);` MUST pass post-rewrite. Why: the adapter's `'complete'` listener (Task 2 wiring) calls `setSensorState('trainer', 'disconnected')` exactly once when `completeListener?.()` fires. If `disconnectedCalls.length` is 0, the Task 2 wiring is missing or wrong; if it's 2+, the wiring is leaking across the connect cycle.

    SUB-STEP 5 — Reconcile the handler-count and unsub-count assertions:
    - Line 204: `expect(handlerSpy).toHaveBeenCalledTimes(2);` — this previously asserted that VW's vendored `ReplayScheduler` walked both `SAMPLE_RECORDS` entries within the 5000ms timer window. With the new emitter-based path, NO records are actually emitted via `capturedOnData` because the test's `mockInnerOnData` returns an empty disposer and the test fires `completeListener` directly without driving any data through. The executor MUST observe the actual count and update the assertion to match reality:
      - Most likely outcome: `expect(handlerSpy).toHaveBeenCalledTimes(0)` — no data emitted because the test bypasses the data path entirely and only exercises the `'complete'` -> `'disconnected'` transition.
      - Alternative: if the executor wants to preserve the "two records emitted" semantic, manually drive `capturedOnData` with two synthesized 6-byte trainer-sim frames before firing `completeListener?.()`. This is more faithful to the original test but requires the test to depend on the FTMS frame format. EITHER outcome is acceptable as long as the assertion reflects the actual post-rewrite behavior; the executor decides based on what produces the cleanest test.
    - Line 210: `expect(unsubSpy).toHaveBeenCalledTimes(1);` — this previously asserted the natural-exhaustion onComplete callback's `teardownReplay()` tore down the rideStateUnsub. The post-Task-2 adapter behavior: the `'complete'` listener does NOT itself call `teardown()`; it only calls `setSensorState('trainer', 'disconnected')`. The rideStateUnsub teardown happens on EXPLICIT disconnect (line 260: `await t.disconnect()`), not on natural exhaustion. The executor MUST either:
      (a) Move the `expect(unsubSpy).toHaveBeenCalledTimes(1)` assertion AFTER line 260's `await t.disconnect()` (where it correctly fires) — preserving CR-03's "explicit disconnect tears down rideStateUnsub" intent.
      (b) Remove the line-210 assertion entirely if it duplicates the line-261 assertion (`expect(unsubSpy).toHaveBeenCalledTimes(1)` post-disconnect).
      Option (a) is the more faithful preservation of CR-03 if the executor wants to assert pre-disconnect behavior is "no unsub yet"; in that case, change line 210 to `expect(unsubSpy).toHaveBeenCalledTimes(0)` (no unsub on natural exhaustion alone) and leave line 261 as `toHaveBeenCalledTimes(1)` (unsub fires on explicit disconnect).
      The executor decides; either preserves CR-03's intent. Document the chosen path in the commit message body.

    SUB-STEP 6 — Verify CR-XX inline comments are still attached to the right assertions:
    - The current file has CR-01 / CR-02 / CR-03 / CR-04 inline-comment markers at lines 206-210, 220-223, 225-235, 245-254, 256-261. The rewrite MUST keep these markers with their assertions; if the executor moves an assertion (per SUB-STEP 5 option (a)), the comment moves with it. The grep `grep -E 'CR-0[1-4]' dev-mode-save-flow.test.ts` should still show 4+ matches (one per CR-ID at minimum), preserving reverse-traceability.

    SUB-STEP 7 — Run the test:
    `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm test -- dev-mode-save-flow` — exit 0. If a CR-XX assertion fails:
    - CR-04 (`disconnectedCalls toHaveLength(1)`) failure -> Task 2's `'complete'` -> `'disconnected'` wiring is missing. Re-read `fake-trainer-transport.ts` and confirm `this.inner.on('complete', this.completeListener)` and `setSensorState('trainer', 'disconnected')` are present.
    - CR-03 failures -> SUB-STEP 5 option choice mismatched the actual adapter behavior; re-read the post-Task-2 `teardown()` body and align the assertion.
    - "Cannot find module 'trainer-sim'" or `RideRecord is not exported` -> Task 1 didn't land or Plan 05-01 didn't export `RideRecord` from trainer-sim's `src/index.ts`. Confirm with `grep "export type.*RideRecord" /Users/agniveshpatel/dev/agni21/trainer-sim/src/index.ts`.

    SUB-STEP 8 — Run the full-workspace local-green gate:
    1. `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm typecheck` — exit 0.
    2. `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm test` — exit 0. The full vitest workspace (4 packages + apps/desktop) MUST be green; this is the gate Task 3 deferred. NO test failures.
    3. `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm --filter @veloworld/desktop build` — exit 0 (re-run to confirm Task 4's edits did not break the renderer build).
    4. Production-bundle grep gate (re-confirm): `grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js` MUST exit 1.

    SUB-STEP 9 — Post-rewrite defense-in-depth grep audit (the EXACT grep that would have caught the 2026-05-18 deviation):
    `grep -rln 'dev/fit-loader\|dev/replay-scheduler' apps/desktop/src/ packages/ 2>/dev/null | grep -v node_modules` MUST output ZERO files. This proves no consumer of the deleted modules remains anywhere in VW's source tree. The pre-Task-1 audit showed two consumers (the adapter + this test); after Tasks 2 + 4 land, the grep should be empty.

    SUB-STEP H — Commit (only if SUB-STEPs 7-9 all pass):

    `git status` — confirm exactly the expected file list:
    - Modified: `package.json` (root), `apps/desktop/package.json`, `pnpm-lock.yaml`, `.github/workflows/ci.yml`, `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts`, `apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts`, `apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts`
    - Deleted: `apps/desktop/src/renderer/src/lib/dev/fit-loader.ts`, `apps/desktop/src/renderer/src/lib/dev/replay-scheduler.ts`, `apps/desktop/src/renderer/src/lib/__tests__/dev/fit-loader.test.ts`, `apps/desktop/src/renderer/src/lib/__tests__/dev/replay-scheduler.test.ts`

    Total: 7 modified + 4 deleted = 11 files (was 9 in the pre-replan plan; +2 from the deviation fixes).

    Stage all + commit with HEREDOC message:
    ```
    feat(phase-5): adopt trainer-sim FakeTransport via git-ref + adapter

    Replace VW's vendored FIT replay path (dev/fit-loader.ts +
    dev/replay-scheduler.ts + 8-byte encodeIndoorBikeFrame) with a thin
    adapter over trainer-sim's createFakeTransport. VW's 9-method
    ITrainerTransport (packages/ble/src/transport.ts) stays VW-owned per
    Anti-Pattern 6; the adapter delegates connect/disconnect/onData to
    trainer-sim and keeps the BLE-shaped methods as no-ops/synthetic.

    + pnpm 10 onlyBuiltDependencies allowlist for trainer-sim's prepare hook
      (root package.json — without this, pnpm 10.33.0 strict-mode blocks
      trainer-sim's Wave 0 prepare lifecycle script and dist/ never
      materializes on the consumer side).

    + adapter wires trainer-sim's 'complete' event to VW's
      setSensorState('trainer', 'disconnected') for CR-04 regression
      coverage. dev-mode-save-flow.test.ts (the Phase 10 CR-01..CR-04
      integration test) is rewritten to mock 'trainer-sim' (not
      '../../dev/fit-loader') and drive natural exhaustion via the
      captured 'complete' listener.

    Pause/resume on useRideStore is Path A: inner.disconnect()/connect()
    with ~5ms FIT re-parse on resume. The 6-byte trainer-sim frame format
    means parsed.instantaneousSpeed is undefined; useBleStore already
    accepts speed: null.

    CI matrix expanded to [ubuntu-latest, macos-latest] with fail-fast:
    false to satisfy trainer-sim VW-03 (cross-platform Node 24 green).

    Files changed: 11 (7 modified + 4 deleted).

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    ```

    DO NOT push the branch — Plan 05-03 owns the push + PR open + cross-platform CI monitoring.
  </action>
  <verify>
    <automated>cd /Users/agniveshpatel/dev/agni21/veloworld-ride &amp;&amp; { F=apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts; if ! grep -q "from 'trainer-sim'" "$F"; then echo "FAIL: $F missing import from 'trainer-sim'"; exit 1; fi; if grep -q "from '../../dev/replay-scheduler'\|from '../../dev/fit-loader'" "$F"; then echo "FAIL: $F still imports from deleted modules"; exit 1; fi; M1=$(grep -c "vi.mock('trainer-sim'" "$F"); if [ "$M1" != "1" ]; then echo "FAIL: $F vi.mock('trainer-sim') count is $M1, expected 1"; exit 1; fi; M2=$(grep -c "vi.mock('../../dev/fit-loader'" "$F"); if [ "$M2" != "0" ]; then echo "FAIL: $F vi.mock('../../dev/fit-loader') count is $M2, expected 0"; exit 1; fi; if ! grep -q "completeListener" "$F"; then echo "FAIL: $F missing captured completeListener"; exit 1; fi; pnpm typecheck 2>&amp;1 | tail -5; TC=$?; if [ "$TC" != "0" ]; then echo "FAIL: pnpm typecheck exit $TC"; exit 1; fi; pnpm test 2>&amp;1 | tail -30; TT=$?; if [ "$TT" != "0" ]; then echo "FAIL: pnpm test (full workspace) exit $TT"; exit 1; fi; pnpm --filter @veloworld/desktop build 2>&amp;1 | tail -10; TB=$?; if [ "$TB" != "0" ]; then echo "FAIL: pnpm build exit $TB"; exit 1; fi; if grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js; then echo "FAIL: production bundle contains trainer-sim or FakeTransport strings — dev-mode tree-shake broken"; exit 1; fi; STALE=$(grep -rln 'dev/fit-loader\|dev/replay-scheduler' apps/desktop/src/ packages/ 2>/dev/null | grep -v node_modules); if [ -n "$STALE" ]; then echo "FAIL: stale references to deleted modules remain:"; echo "$STALE"; exit 1; fi; LAST_MSG=$(git log -1 --pretty=%s); if ! echo "$LAST_MSG" | grep -q 'phase-5'; then echo "FAIL: HEAD commit does not match Phase 5 subject: '$LAST_MSG'"; exit 1; fi; FILE_COUNT=$(git diff --name-only HEAD~1 HEAD | wc -l | tr -d ' '); if [ "$FILE_COUNT" != "11" ]; then echo "FAIL: HEAD commit touches $FILE_COUNT files, expected 11"; git diff --name-only HEAD~1 HEAD; exit 1; fi; echo "OK: Wave 1a complete; integration test rewrite + full-workspace gate passed; trainer-sim integration committed locally (11 files)"; }</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -c "from 'trainer-sim'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` is at least `1` (the `RideRecord` import shifted to trainer-sim).
    - Source assertion: `grep -c "vi.mock('trainer-sim'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` is exactly `1`.
    - Source assertion: `grep -c "vi.mock('../../dev/fit-loader'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` is exactly `0`.
    - Source assertion: `grep -c "from '../../dev/replay-scheduler'" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` is exactly `0`.
    - Source assertion: `grep -c "completeListener" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` is at least `1` (the captured `'complete'` listener).
    - Source assertion: All four CR-XX comment markers are present — `grep -cE 'CR-0[1-4]' /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` is at least `4`.
    - Source assertion: `grep -c "disconnectedCalls" /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts` is at least `1` AND the file contains `expect(disconnectedCalls).toHaveLength(1)` (CR-04 preservation).
    - CLI: `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm test -- dev-mode-save-flow.test.ts` exits 0.
    - CLI: `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm typecheck` exits 0.
    - CLI: `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm test` (full workspace) exits 0. NO test failures across all packages — the integration rewrite did not break the unit tests, and the unit-test rewrite did not break the integration test.
    - CLI: `cd /Users/agniveshpatel/dev/agni21/veloworld-ride && pnpm --filter @veloworld/desktop build` exits 0.
    - CLI: `grep -El 'fake-transport|FakeTransport|trainer-sim' /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/out/renderer/assets/*.js` exits 1 (no matches; dev-mode tree-shake holds).
    - CLI: `grep -rln 'dev/fit-loader\|dev/replay-scheduler' /Users/agniveshpatel/dev/agni21/veloworld-ride/apps/desktop/src/ /Users/agniveshpatel/dev/agni21/veloworld-ride/packages/ 2>/dev/null | grep -v node_modules` outputs ZERO files. (The post-replan defense-in-depth grep that would have caught the 2026-05-18 deviation; now an explicit acceptance criterion.)
    - Source assertion: `git log -1 --pretty=%s` from VW repo root reports a message starting with `feat(phase-5): adopt trainer-sim FakeTransport via git-ref + adapter`.
    - Source assertion: `git diff --name-only HEAD~1 HEAD | wc -l` from VW repo root outputs exactly `11` (the 11 files in Wave 1a's single commit; was `9` in the pre-replan plan).
    - Source assertion: NO file outside `apps/desktop/...`, `package.json` (root), `pnpm-lock.yaml`, or `.github/workflows/ci.yml` appears in the diff (e.g., `packages/ble/src/transport.ts` is NOT modified — VW-01 invariant + user override #1).
  </acceptance_criteria>
  <done>
    `dev-mode-save-flow.test.ts` is rewritten to import `RideRecord` from `'trainer-sim'`, mock `'trainer-sim'`'s `createFakeTransport`, and trigger natural-exhaustion via the captured `'complete'` listener. CR-01..CR-04 regression assertions still pass with intent preserved (`expect(disconnectedCalls).toHaveLength(1)` is the CR-04 round-trip through the Task 2 wiring). `pnpm test -- dev-mode-save-flow.test.ts` exits 0. The full `pnpm test` workspace exits 0 (Task 3's deferred full-workspace gate now lands here). The post-replan defense grep for stale dev/fit-loader|dev/replay-scheduler references outputs zero. A single Wave 1a commit `feat(phase-5): adopt trainer-sim FakeTransport via git-ref + adapter` lands on the feature branch with all 11 files. NOT pushed — Plan 05-03 owns push + PR.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VW renderer (Web Bluetooth context) -> trainer-sim's loadFitFromBuffer | The `{ buffer: Uint8Array }` source variant runs trainer-sim's FIT parser INSIDE the renderer process. trainer-sim's parser was hardened in Phase 2 against malformed FIT files (FitLoadError family). The bytes come from `window.veloworld.dev.readFitFile()`, which is the existing VW IPC reading from `apps/desktop/src/renderer/src/lib/__tests__/dev/fixtures/sample.fit` — a committed, version-controlled fixture. No untrusted user input crosses this boundary. |
| VW dev-mode flag -> dynamic import of fake-trainer-transport.ts | VW's existing `import.meta.env.DEV && VITE_FAKE_TRAINER === 'true'` AND-gate is the production-bundle isolation. trainer-sim becomes a transitive dependency of `dev/fake-trainer-transport.ts`; the dynamic-import-gating ensures it is not bundled into production. The grep gate (`grep -El 'fake-transport|FakeTransport|trainer-sim'`) is the verification of this isolation. |
| VW's 9-method `ITrainerTransport` <-> trainer-sim's 4-method `ITrainerTransport` | The interface boundary. The adapter pattern means VW owns the wider 9-method shape; trainer-sim owns the narrower 4-method shape. The 5 BLE-shaped methods (`scan`, `probeControlPointSupport`, `requestControl`, `setGradeSimulation`, `releaseControl`) are NOT exposed to trainer-sim's import graph (Anti-Pattern 6 / Pitfall 13). |
| pnpm git-ref install -> trainer-sim Wave 0 sha | The git-ref string `github:VeloWorld/trainer-sim#<sha>` is the immutable evidence trail (CONTEXT §"specifics"). The sha is committed in VW's `apps/desktop/package.json` AND `pnpm-lock.yaml`. Tampering would require a force-push to either trainer-sim's main (changing what `<sha>` resolves to) or VW's lockfile (changing the recorded sha) — both visible in code review. |
| pnpm 10 strict-mode allowlist -> trainer-sim's `prepare` lifecycle | NEW for the replan. VW root `package.json` `pnpm.onlyBuiltDependencies` is the consumer-side opt-in for git-ref `prepare` hook execution. Without `"trainer-sim"` in the allowlist, pnpm 10 silently refuses to run trainer-sim's prepare hook. The allowlist entry is committed in VW root and visible in code review. |
| trainer-sim `'complete'` event -> VW store `setSensorState('trainer', 'disconnected')` | NEW for the replan. The adapter's natural-exhaustion path: trainer-sim's scheduler reaches end-of-records -> emits `'complete'` -> the adapter's listener fires `setSensorState('trainer', 'disconnected')`. CR-04 (line 254 of `dev-mode-save-flow.test.ts`) asserts this fires EXACTLY once across the natural-exhaustion + post-save sequence; without the wiring, the integration test fires a regression. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | trainer-sim git-ref dep on Wave 0 sha | mitigate | The sha is recorded in two places: VW's `pnpm-lock.yaml` (regenerated by `pnpm install`) and VW's `apps/desktop/package.json`. Both are committed in the same Wave 1 commit. Lockfile contains `commit: '<sha>'` literal which `pnpm install --frozen-lockfile` checks. Any divergence between package.json and lockfile fails CI with `ERR_PNPM_LOCKFILE_OUTDATED` — deterministic. |
| T-05-02-02 | Spoofing | `window.veloworld.dev.readFitFile()` returns FIT bytes | accept | This IPC is VW's existing dev-mode bridge; the bytes come from `apps/desktop/src/renderer/src/lib/__tests__/dev/fixtures/sample.fit` (version-controlled, real Garmin export). Phase 5 doesn't introduce new IPC surface. Same trust as before. |
| T-05-02-03 | Information Disclosure | trainer-sim leaks into production renderer bundle | mitigate | VW's existing dev-mode AND-gate (`import.meta.env.DEV && VITE_FAKE_TRAINER`) strips the entire `dev/` subtree from production. Trainer-sim is a transitive dep of `dev/fake-trainer-transport.ts`; the same gate strips it. Verification: `grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js` MUST exit 1. The grep gate runs in VW's CI on every build — if it ever fires, the build fails (D-19 / CONTRACTS §22). Phase 5 inherits this protection. |
| T-05-02-04 | Repudiation | Wave 1 PR vs trainer-sim Wave 0 sha | mitigate | The Wave 0 sha is captured in trainer-sim's `.planning/phases/.../05-WAVE-0-SHA.txt` AND in VW's `apps/desktop/package.json`/`pnpm-lock.yaml`. A historian can `git show <wave-0-sha>` against trainer-sim and `git show <vw-pr-merge-sha>` against VW to reconstruct the cross-repo state. Plan 05-04 records both URLs in `05-VERIFICATION.md`. |
| T-05-02-05 | Denial of Service | FIT re-parse on every pause/resume (Path A) | accept | RESEARCH Open Question 1 quantified: ~5ms re-parse per resume on the 594-byte sample.fit. Imperceptible for v1 dev/test. If real users complain, the fix is a Path B (preserve VW's scheduler) OR trainer-sim widens to add a `pause()` seam (D-VW-08 sequencing); both deferred. |
| T-05-02-06 | Tampering | Speed channel becomes `undefined` from trainer-sim frames | accept | VW's `BleManager.init` already coerces `parsed.instantaneousSpeed ?? null` (RESEARCH "Wire-Format Compatibility"). Real-BLE-trainer path also produces undefined-speed frames if a trainer omits speed; the `?? null` pattern is established. The HUD shows speed=null in dev-mode FIT replay; this is documented in Plan 05-04's narrative per D-VW-09. Per FTMS-06 (v2), trainer-sim does not yet expose speed; the appropriate fix is a v1.x trainer-sim API widening, not a Phase 5 hack in the adapter. |
| T-05-02-07 | Elevation of Privilege | trainer-sim runs in VW's renderer (sandboxed Chromium) | accept | Renderer is sandboxed by Electron; trainer-sim's code path is the same as VW's prior vendored code path (renderer-bound JS reading bytes from IPC, parsing them, emitting DataViews). No elevation introduced. |
| T-05-02-08 | Tampering | VW root `package.json` `pnpm.onlyBuiltDependencies` allowlist (NEW for the replan) | mitigate | The `"trainer-sim"` allowlist entry is committed in VW root `package.json` and visible in code review. pnpm 10 strict-mode is the consumer's defense against runaway git-ref `prepare` hooks; allowlisting trainer-sim is a documented opt-in (the trust-boundary tax already accepted in Plan 05-01's threat register T-05-01-01 / T-05-01-05). Rotation requires a coordinated PR removing it; without the entry, `pnpm install` fails loudly with `ERR_PNPM_GIT_DEP_PREPARE_NOT_ALLOWED` — deterministic, not silent. |
| T-05-02-09 | Tampering | `'complete'` event wiring from adapter to `setSensorState('trainer', 'disconnected')` (NEW for the replan) | mitigate | The wiring is asserted by Task 2 acceptance criteria (`grep "on('complete'"` and `grep "setSensorState\('trainer', 'disconnected'\)"` ≥ 1) AND Task 4's CR-04 assertion (`expect(disconnectedCalls).toHaveLength(1)`) provides defense-in-depth. Without the wiring, both test layers fail loudly — the unit test fails because test #8 cannot observe the disconnected transition, and the integration test fails because line 254's CR-04 assertion sees `disconnectedCalls.length === 0`. Tampering would require breaking BOTH the source assertion AND the integration test — a single-source-of-truth invariant. |
</threat_model>

<verification>
Once all four tasks land, run from VW repo root:

```bash
cd /Users/agniveshpatel/dev/agni21/veloworld-ride

# Dependency: trainer-sim git-ref resolves with built dist/ (proves Gap 1 allowlist + Plan 05-01 prepare hook end-to-end)
test -f node_modules/trainer-sim/dist/index.js && echo "OK: trainer-sim resolves"

# pnpm allowlist: trainer-sim is opted into strict-mode lifecycle scripts
grep -c '"trainer-sim"' package.json
# Expect: at least 1

# Source: VW interface preserved, no agni21 references
grep -c 'agni21/trainer-sim' apps/desktop/package.json pnpm-lock.yaml
# Expect: 0 in both files

# Source: Adapter shape correct (with 'complete' wiring)
grep "from 'trainer-sim'" apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts
# Expect: import { createFakeTransport, type FakeTransport } from 'trainer-sim'
grep "on('complete'" apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts
# Expect: at least 1 match (the listener registration)
grep "setSensorState('trainer', 'disconnected')" apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts
# Expect: at least 1 match (the CR-04 wiring)

# Integration test: repointed at trainer-sim
grep -c "from 'trainer-sim'" apps/desktop/src/renderer/src/lib/__tests__/dev/dev-mode-save-flow.test.ts
# Expect: at least 1

# Tests: VW workspace green (full workspace, all packages)
pnpm typecheck && pnpm test && pnpm --filter @veloworld/desktop build
# Expect: all 3 exit 0

# CI matrix expanded
grep -A 3 'matrix:' .github/workflows/ci.yml | grep -E '(ubuntu|macos)-latest'
# Expect: both ubuntu-latest and macos-latest match

# Production-bundle hygiene preserved
grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js
# Expect: exit 1 (no matches)

# Post-replan defense-in-depth: no consumer of the deleted modules remains
grep -rln 'dev/fit-loader\|dev/replay-scheduler' apps/desktop/src/ packages/ 2>/dev/null | grep -v node_modules
# Expect: zero files (the EXACT grep that would have caught the 2026-05-18 deviation)

# Commit landed on feature branch, NOT yet pushed; touches exactly 11 files
git log -1 --pretty='%h %s'
# Expect: <sha> feat(phase-5): adopt trainer-sim FakeTransport via git-ref + adapter
git diff --name-only HEAD~1 HEAD | wc -l
# Expect: 11 (was 9 in the pre-replan plan; +2 from VW root package.json + dev-mode-save-flow.test.ts)
git status
# Expect: nothing to commit, working tree clean
git rev-parse --abbrev-ref --symbolic-full-name '@{u}' 2>/dev/null
# Expect: error or no upstream — branch not pushed
```

If all green, Plan 05-02 done; Plan 05-03 owns push + PR.
</verification>

<success_criteria>
Wave 1a plan complete when:

1. trainer-sim's Wave 0 sha is pinned in `apps/desktop/package.json` AND recorded in `pnpm-lock.yaml`'s `resolution.commit` field for the trainer-sim entry.
2. The four vendored modules (`dev/fit-loader.ts`, `dev/replay-scheduler.ts`, and their `__tests__/` siblings) are deleted via `git rm`.
3. `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` is rewritten as a ~80-110 LOC adapter composing trainer-sim's `createFakeTransport` with the `{ buffer }` source variant. The class implements VW's 9-method `ITrainerTransport` from `@veloworld/ble` UNCHANGED. Pause/resume is Path A (disconnect/reconnect on rideState). The 8-byte `encodeIndoorBikeFrame` is gone.
4. `apps/desktop/src/renderer/src/lib/__tests__/dev/fake-trainer-transport.test.ts` mocks `'trainer-sim'` instead of `'../../dev/fit-loader'`. Test #5 asserts `parsed.instantaneousSpeed` is undefined for trainer-sim's 6-byte frames. The inner-mock shape includes `on/off/once` so the captured `'complete'` listener is reachable.
5. `.github/workflows/ci.yml` `build-and-prod-bundle-grep` job has `strategy.matrix.os = [ubuntu-latest, macos-latest]` with `fail-fast: false` and `runs-on: ${{ matrix.os }}`.
6. From VW repo root: `pnpm typecheck`, `pnpm test`, `pnpm --filter @veloworld/desktop build` all exit 0.
7. `grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js` exits 1 (production-bundle grep gate clean).
8. A single commit subject `feat(phase-5): adopt trainer-sim FakeTransport via git-ref + adapter` lands on the feature branch (e.g., `feat/phase-5-trainer-sim-canonical`), but the branch is NOT pushed (push is Plan 05-03's job).
9. NO files outside `apps/desktop/...`, `package.json` (root), `pnpm-lock.yaml`, and `.github/workflows/ci.yml` are modified. `packages/ble/src/transport.ts` (VW's owned interface) is byte-identical to its pre-edit state.
10. The feature-branch name is recorded in the SUMMARY for Plan 05-03 to push.
11. NEW for the replan (Gap 1 fix): VW root `package.json` `pnpm.onlyBuiltDependencies` array contains `"trainer-sim"`. `node_modules/trainer-sim/dist/index.js` exists locally after `pnpm install`, proving the prepare hook ran end-to-end under pnpm 10.33.0 strict-mode.
12. NEW for the replan (Gap 2 fix — adapter wiring): `fake-trainer-transport.ts` registers `this.inner.on('complete', this.completeListener)` after `inner.connect()` succeeds, and `teardown()` unregisters via `this.inner.off('complete', this.completeListener)`. The listener calls `useBleStore.getState().setSensorState('trainer', 'disconnected')` exactly once per natural-exhaustion event.
13. NEW for the replan (Gap 2 fix — integration test): `dev-mode-save-flow.test.ts` (a) imports `RideRecord` from `'trainer-sim'`, (b) mocks `'trainer-sim'`'s `createFakeTransport`, (c) drives natural exhaustion via the captured `'complete'` listener (NOT `vi.advanceTimersByTime` against the deleted `ReplayScheduler`), (d) all CR-01..CR-04 assertions still pass (`expect(disconnectedCalls).toHaveLength(1)` is the CR-04 round-trip through Task 2's wiring).
14. NEW for the replan (defense-in-depth): `grep -rln 'dev/fit-loader\|dev/replay-scheduler' apps/desktop/src/ packages/ 2>/dev/null | grep -v node_modules` outputs ZERO files. This is the EXACT grep that would have caught the 2026-05-18 deviation pre-execution.
15. NEW for the replan (commit shape): `git diff --name-only HEAD~1 HEAD` lists exactly 11 files (was 9 in the pre-replan plan; +2 from VW root `package.json` + `dev-mode-save-flow.test.ts`).
</success_criteria>

<output>
After completion, create `.planning/phases/05-veloworld-end-to-end-validation/05-02-SUMMARY.md` recording:
- The VW feature branch name created/used.
- The VW commit sha (from `git rev-parse HEAD` in VW repo).
- The pre-rewrite vs post-rewrite LOC for `fake-trainer-transport.ts`.
- The terminal output (last 20 lines) of `pnpm typecheck`, `pnpm test`, and `pnpm --filter @veloworld/desktop build`.
- Confirmation that `grep -El 'fake-transport|FakeTransport|trainer-sim' apps/desktop/out/renderer/assets/*.js` exited 1.
- Confirmation that `grep -rln 'dev/fit-loader\|dev/replay-scheduler' apps/desktop/src/ packages/ 2>/dev/null | grep -v node_modules` outputs zero files.
- Confirmation that VW root `package.json` `pnpm.onlyBuiltDependencies` contains `"trainer-sim"`.
- Confirmation that `git diff --name-only HEAD~1 HEAD | wc -l` is `11`.
- The SUB-STEP 5 path chosen for `dev-mode-save-flow.test.ts` (option (a) move-to-post-disconnect vs option (b) remove-line-210; or the option-1 (data-bypass) vs option-2 (synthesize-frames) choice for `handlerSpy` assertions).
- Any deviations from the plan (e.g., a 9th test discovered or a method renamed in trainer-sim). Plan 05-03 will use this summary as the input for the cross-repo PR push.
</output>
