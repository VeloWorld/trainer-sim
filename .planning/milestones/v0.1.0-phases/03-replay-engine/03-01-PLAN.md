---
phase: 03-replay-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/replay/types.ts
  - src/replay/scheduler.ts
autonomous: true
requirements:
  - REPL-01
  - REPL-02
  - REPL-03
  - REPL-04
  - REPL-06
tags:
  - replay
  - scheduler
  - foundation
  - types
  - drift-correction

must_haves:
  truths:
    - "src/replay/types.ts exports ReplayConfig and ReplayState interfaces, internal-only — NOT re-exported from src/index.ts (D-REPL-12)"
    - "ReplayConfig has fields: records (ReadonlyArray<RideRecord>), speed (number, accepts Infinity), loop (boolean), maxEmissionHz (number) — JSDoc'd with D-REPL-04, D-REPL-05, D-REPL-06 references (D-REPL-13)"
    - "src/replay/scheduler.ts exports a single named function `runScheduler` that returns Promise<void> and accepts SchedulerInput (records, speed, loop, maxEmissionHz, signal, emit, getNow) — pure async function, no class state (D-REPL-13, RESEARCH §Architectural Responsibility Map)"
    - "Scheduler uses absolute-target-time + per-tick recompute drift correction: target = baseline + (record.timestamp - firstTs) / speed; delay = Math.max(target - getNow(), 1000 / maxEmissionHz) (D-REPL-02, REPL-01, REPL-03)"
    - "Scheduler imports `setTimeout as sleep` from 'node:timers/promises' — NOT global setTimeout — and awaits sleep(delay, undefined, { signal }) for each tick (D-REPL-01, D-REPL-09, RESEARCH §AbortController teardown pitfalls)"
    - "Scheduler is the SINGLE module in src/ that imports 'node:timers/promises' (single-source-of-truth seam mirroring D-FIT-08; PATTERNS §Module Structure)"
    - "On loop boundary (cursor wraps to 0 with loop=true), baseline is reset to getNow() before scheduling next emission (D-REPL-06, REPL-04, RESEARCH §Pitfall 7)"
    - "speed=Infinity does NOT branch — it falls through to the maxEmissionHz floor via Math.max clamping (D-REPL-04, REPL-02, RESEARCH §Pitfall 8)"
    - "Empty records array (length === 0) returns immediately without emit (RESEARCH §Pitfall 9 defense-in-depth)"
    - "src/index.ts is BYTE-IDENTICAL to its pre-Phase-3 state — D-REPL-12 forbids public re-export of any replay symbol"
  artifacts:
    - path: "src/replay/types.ts"
      provides: "Internal types ReplayConfig and ReplayState; mirrors src/types.ts JSDoc style; internal-only (PATTERNS §src/replay/types.ts analog)"
      exports: ["ReplayConfig", "ReplayState"]
      min_lines: 25
    - path: "src/replay/scheduler.ts"
      provides: "Pure async drift-corrected setTimeout-chain scheduler; the algorithmic core of REPL-01/02/03/04/06"
      exports: ["runScheduler"]
      min_lines: 60
  key_links:
    - from: "src/replay/scheduler.ts"
      to: "node:timers/promises"
      via: "import { setTimeout as sleep } from 'node:timers/promises' for AbortSignal-aware delay"
      pattern: "from\\s+['\"]node:timers/promises['\"]"
    - from: "src/replay/scheduler.ts"
      to: "src/replay/types.ts"
      via: "import type { ReplayConfig } from './types.js'"
      pattern: "from\\s+['\"]\\./types\\.js['\"]"
    - from: "src/replay/scheduler.ts"
      to: "src/types.ts"
      via: "import type { RideRecord } from '../types.js'"
      pattern: "from\\s+['\"]\\.\\./types\\.js['\"]"
---

<objective>
Wave 1 of Phase 3: ship the replay engine's algorithmic foundation. Two files,
zero dependencies between them and any other phase code (apart from the existing
`RideRecord` type).

1. **`src/replay/types.ts`** — internal `ReplayConfig` and `ReplayState`
   interfaces, mirroring the JSDoc + locked-decision-citation style of
   `src/types.ts`. Internal-only — D-REPL-12 forbids public re-export.

2. **`src/replay/scheduler.ts`** — the pure async function `runScheduler` that
   implements the drift-corrected setTimeout-chain (D-REPL-01) with
   absolute-target-time per-tick recalibration (D-REPL-02), the
   max-emission-Hz floor (D-REPL-04, D-REPL-05), the loop-boundary re-base
   (D-REPL-06), and `node:timers/promises` AbortSignal teardown (D-REPL-09).

Implements (the algorithmic core of):
- REPL-01 — real-time emission respecting FIT timestamps
- REPL-02 — `speed` multiplier including Infinity, with `maxEmissionHz` cap
- REPL-03 — drift correction (proven in 03-04 soak; algorithmic correctness here)
- REPL-04 — loop boundary without drift accumulation
- REPL-06 — clean cancellation via AbortSignal

Honors locked decisions:
- D-REPL-01 (setTimeout-chain, NOT setInterval / setImmediate)
- D-REPL-02 (per-tick recalibration against absolute target)
- D-REPL-03 (`performance.now()`, NOT `Date.now()`) — but injected as
  `getNow` per RESEARCH §Pitfall 6 testability seam
- D-REPL-04 + D-REPL-05 (Infinity falls through to maxEmissionHz floor)
- D-REPL-06 (loop boundary re-base)
- D-REPL-09 (AbortController via `node:timers/promises` setTimeout `{ signal }`)
- D-REPL-12 (internal only — no `src/index.ts` change)
- D-REPL-13 (file split: scheduler.ts pure-async + types.ts internal)
- CLAUDE.md "no abstractions for hypothetical future requirements" — NO
  `IScheduler` interface, NO strategy pattern, NO event-bus

Open question resolutions baked in for the wrapper class to consume in 03-02
(documented here so 03-02's tasks have a concrete contract):
- AbortError semantics: scheduler re-throws whatever `node:timers/promises`
  rejects with (an `AbortError` whose `name === 'AbortError'`). 03-02's wrapper
  routes that rejection to `replay.completed.reject(err)` per RESEARCH §Open
  Question 2.
- Restart-after-stop: NOT decided in this plan — the scheduler is stateless
  per call. 03-02 handles single-use lock per RESEARCH §Open Question 1.

Purpose: Wave 1 delivers a function that 03-02's `Replay` class can wrap with
zero ambiguity. The scheduler is testable in isolation (a 03-03 test passes
its own `emit` and `getNow` and AbortSignal — no class instance needed).

Output: Two TypeScript files (~25 + ~80 lines), no public surface change,
`npm run build` and `npm run typecheck:test` still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/03-replay-engine/03-CONTEXT.md
@.planning/phases/03-replay-engine/03-RESEARCH.md
@.planning/phases/03-replay-engine/03-PATTERNS.md
@src/types.ts
@src/index.ts
@src/fit/normalize.ts
</context>

<interfaces>
<!-- Key types/contracts the executor needs. Extracted from existing code. -->
<!-- Executor must use these directly — no codebase exploration needed. -->

From `src/types.ts` (the SOLE upstream type the scheduler iterates over):
```typescript
export interface RideRecord {
  /** Unix epoch milliseconds (NOT FIT epoch). FIT-03. */
  timestamp: number;
  /** Watts. `undefined` = no signal; `0` = real coasting. */
  power?: number;
  /** RPM. Same absent-vs-zero semantics as `power`. */
  cadence?: number;
}
```

Phase 2 invariants the scheduler RELIES on (verified in 02-VERIFICATION.md):
- `records.length >= 1` when the loader returns successfully (empty input throws `NoRecordMessagesError` upstream, but defense-in-depth in §Pitfall 9 still required).
- `records[i].timestamp <= records[i+1].timestamp` for all i (sorted ascending; `normalize` enforces).
- Timestamps are Unix epoch ms (not FIT epoch). Differences across the array are millisecond-meaningful.

Convention/enforcement (from `tsconfig.json`):
- `strict: true` + `noUncheckedIndexedAccess: true` → every `records[i]` access requires `!`.
- `verbatimModuleSyntax: true` → use `import type` for type-only imports.

Style targets (PATTERNS §src/replay/scheduler.ts analog — `src/fit/normalize.ts`):
```typescript
import { debuglog } from 'node:util';
const log = debuglog('trainer-sim:replay');   // bumped namespace per CONTEXT.md <code_context>

// Step 1 — ...
// Step 2 — ...
// (numbered comments inside long functions)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author src/replay/types.ts (ReplayConfig + ReplayState internal interfaces, D-REPL-13)</name>
  <files>src/replay/types.ts</files>
  <read_first>
    - .planning/phases/03-replay-engine/03-CONTEXT.md (D-REPL-04 speed/Infinity, D-REPL-05 maxEmissionHz default 1000, D-REPL-06 loop, D-REPL-12 internal-only, D-REPL-13 file split)
    - .planning/phases/03-replay-engine/03-RESEARCH.md (§Architectural Responsibility Map — types layer; §Pattern: Replay class wrapping the scheduler — ReplayConfig usage shape lines 481-486)
    - .planning/phases/03-replay-engine/03-PATTERNS.md (§src/replay/types.ts — analog is src/types.ts; "internal-only" rationale — see line 61 "Intentionally NOT exported — D-FIT-08 internal seam")
    - src/types.ts (the JSDoc + module-doc + locked-decision-citation style to mirror exactly)
    - tsconfig.json (verbatimModuleSyntax + strict + noUncheckedIndexedAccess all apply)
  </read_first>
  <action>
    Create `src/replay/types.ts` (NEW file; the `src/replay/` directory does
    not yet exist — `mkdir -p src/replay` first or write the file via the
    editor which will create the directory).

    File structure (target ~25-35 lines):

    1. **Module-doc header** — JSDoc block at the top (mirror src/types.ts:1-19).
       State the file's role: "Internal types for the Phase 3 replay engine.
       NOT exported from src/index.ts — D-REPL-12 keeps the replay surface
       internal in this phase; Phase 4's FakeTransport decides which
       primitives to re-export." Cite the locked decisions: D-REPL-04
       (speed/Infinity), D-REPL-05 (maxEmissionHz default 1000), D-REPL-06
       (loop), D-REPL-13 (this file is the types layer).

    2. **Export `ReplayConfig`** as an interface with four members:
       - `records: ReadonlyArray<RideRecord>` — JSDoc: "Time-ordered ride records
         to emit. Must satisfy Phase 2's normalize invariants: sorted ascending
         by timestamp, no exact duplicates, length >= 1 (empty arrays return
         immediately from runScheduler per RESEARCH §Pitfall 9 defense-in-depth)."
       - `speed: number` — JSDoc: "Replay speed multiplier. `1` = real-time;
         `2` = 2×; `Infinity` = as-fast-as-possible (rate-limited by
         maxEmissionHz). Per D-REPL-04 the Infinity case falls through to the
         maxEmissionHz floor — no special-case branch in the scheduler. Values
         <= 0 are not supported (would invert time)."
       - `loop: boolean` — JSDoc: "When true, replay restarts from cursor 0
         after the last record, with the baseline rebased to getNow() to
         prevent drift accumulation across iterations (D-REPL-06, REPL-04).
         Default is false — stop-at-end (D-REPL-07)."
       - `maxEmissionHz: number` — JSDoc: "Maximum emission frequency in Hz.
         Default 1000 (D-REPL-05). Implementation: scheduler clamps the per-tick
         delay to `Math.max(target - now, 1000 / maxEmissionHz)`. Lowering this
         lets a soak test simulate sparse-record load."

    3. **Export `ReplayState`** as a string-literal union representing the
       lifecycle from RESEARCH §Pattern: Replay class. Suggested:
       `export type ReplayState = 'idle' | 'running' | 'done' | 'aborted';`
       JSDoc: "Replay lifecycle states. Transitions: idle → running (start());
       running → done (cursor exhausted, loop=false); running → aborted
       (stop() or external signal abort). Per RESEARCH §Open Question 1
       (resolved in 03-02): once 'done' or 'aborted', the instance is
       single-use — start() throws on re-call."

    4. **No imports** other than `type { RideRecord } from '../types.js'`
       (use `import type` per `verbatimModuleSyntax: true`). Use `.js`
       extension on the relative specifier (PATTERNS §Cross-Cutting
       Conventions Summary — `.js`-extension on relative imports).

    5. **NO export beyond ReplayConfig + ReplayState.** Do NOT introduce
       `IScheduler`, `ReplayEvent`, `ReplayError`, etc. — CLAUDE.md "no
       abstractions for hypothetical future requirements" + RESEARCH
       §Anti-Patterns to Avoid forbids these for this phase.

    6. **NO modification of `src/index.ts`.** Per D-REPL-12 the public surface
       does not change in Phase 3. Acceptance criteria below verify this.

    7. **NO modification of `src/types.ts`.** That file holds the shared
       `RideRecord` type only; replay-specific types live in `src/replay/types.ts`.

    Commit per Phase 1/2 atomic-commit pattern:
    `feat(03-01): add ReplayConfig + ReplayState internal types (D-REPL-13)`
  </action>
  <acceptance_criteria>
    - `src/replay/types.ts` exists: `test -f src/replay/types.ts`.
    - File exports the `ReplayConfig` interface (named, not default; not type alias): `grep -cE '^export\s+interface\s+ReplayConfig' src/replay/types.ts` reports 1.
    - File exports the `ReplayState` type/alias (named): `grep -cE '^export\s+type\s+ReplayState' src/replay/types.ts` reports 1.
    - ReplayConfig declares all four required fields: `grep -cE '^\s*records:\s*ReadonlyArray<RideRecord>' src/replay/types.ts` reports 1 AND `grep -cE '^\s*speed:\s*number' src/replay/types.ts` reports 1 AND `grep -cE '^\s*loop:\s*boolean' src/replay/types.ts` reports 1 AND `grep -cE '^\s*maxEmissionHz:\s*number' src/replay/types.ts` reports 1.
    - File imports RideRecord with `import type` (verbatimModuleSyntax compliance) using `.js` extension on the relative specifier: `grep -cE "import\s+type\s+\\{\s*RideRecord\s*\\}\s+from\s+['\"]\\.\\./types\\.js['\"]" src/replay/types.ts` reports 1.
    - File does NOT export anything besides ReplayConfig and ReplayState (no IScheduler, ReplayError, etc.): `grep -cE '^export\s+' src/replay/types.ts` reports 2.
    - File cites at least one D-REPL-* decision in a comment/JSDoc (traceability): `grep -cE 'D-REPL-(04|05|06|12|13)' src/replay/types.ts` reports >= 1.
    - File does NOT use `export default`: `grep -cE 'export\s+default' src/replay/types.ts` reports 0.
    - **D-REPL-12 enforcement: `src/index.ts` is unchanged from its pre-Phase-3 state.** No mention of `replay/` in src/index.ts: `grep -cE 'replay' src/index.ts` reports 0. (The string `'replay'` does not appear in any pre-existing src/index.ts content as of Phase 2 completion — verified.)
    - tsup build exits 0: `npm run build`. Note the new file isn't imported by anything yet, but tsc-via-tsup type-checks all of `src/`.
    - Test-mode typecheck exits 0: `npm run typecheck:test`.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/replay/types.ts && [ "$(grep -cE '^export\s+interface\s+ReplayConfig' src/replay/types.ts)" = "1" ] && [ "$(grep -cE '^export\s+type\s+ReplayState' src/replay/types.ts)" = "1" ] && grep -E '^\s*records:\s*ReadonlyArray<RideRecord>' src/replay/types.ts && grep -E '^\s*speed:\s*number' src/replay/types.ts && grep -E '^\s*loop:\s*boolean' src/replay/types.ts && grep -E '^\s*maxEmissionHz:\s*number' src/replay/types.ts && grep -E "import\s+type\s+\{\s*RideRecord\s*\}\s+from\s+['\"]\.\./types\.js['\"]" src/replay/types.ts && [ "$(grep -cE '^export\s+' src/replay/types.ts)" = "2" ] && [ "$(grep -cE 'export\s+default' src/replay/types.ts)" = "0" ] && [ "$(grep -cE 'replay' src/index.ts)" = "0" ] && npm run build && npm run typecheck:test</automated>
  </verify>
  <done>
    `src/replay/types.ts` exports `ReplayConfig` (interface, four required fields with JSDoc citing D-REPL-04/05/06) and `ReplayState` (string-literal union with four states), imports `RideRecord` via `import type` with `.js` extension. `src/index.ts` unchanged. Build and typecheck pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author src/replay/scheduler.ts (drift-corrected runScheduler async fn, D-REPL-01..06/09)</name>
  <files>src/replay/scheduler.ts</files>
  <read_first>
    - .planning/phases/03-replay-engine/03-RESEARCH.md (§Drift correction code sketch — lines 234-281, the COPY-PASTEABLE algorithm; §AbortController teardown pitfalls — lines 294-357 for why node:timers/promises is non-negotiable; §Common Pitfalls 1, 2, 3, 6, 7, 8, 9 — the eight traps to avoid)
    - .planning/phases/03-replay-engine/03-CONTEXT.md (D-REPL-01 setTimeout-chain, D-REPL-02 absolute-target-time, D-REPL-03 performance.now, D-REPL-04 Infinity-via-clamp, D-REPL-05 maxEmissionHz config, D-REPL-06 loop re-base, D-REPL-09 AbortController, D-REPL-13 file split)
    - .planning/phases/03-replay-engine/03-PATTERNS.md (§src/replay/scheduler.ts — analog is src/fit/normalize.ts; module-header convention lines 71-82; debuglog namespace bump to 'trainer-sim:replay' lines 102-112; numbered-step comments lines 151-163; conditional debuglog emit lines 166-176; single-import-of-external-dep enforcement lines 568-583)
    - src/fit/normalize.ts (the canonical pure-function module to mirror — header style, step-numbered comments, debuglog conditional emit, `!`-postfix index access, named export, .js relative imports)
    - src/replay/types.ts (the file authored in Task 1; this file imports ReplayConfig from there)
    - src/types.ts (RideRecord shape — the iteration target)
  </read_first>
  <action>
    Create `src/replay/scheduler.ts` per RESEARCH §Drift correction code sketch.
    The algorithm body is ~10 lines of business logic plus a module header and
    helper-interface declaration; total file ~70-100 lines.

    Required structure:

    1. **Module-doc header** (mirror `src/fit/normalize.ts` lines 1-32 exactly).
       Sections in this order:
       - One-line summary: "Pure async drift-corrected setTimeout-chain
         scheduler — the algorithmic core of Phase 3's replay engine."
       - "Implements (per .planning/phases/03-replay-engine/03-CONTEXT.md):"
         followed by a bulleted list naming D-REPL-01 (setTimeout chain),
         D-REPL-02 (absolute-target-time + per-tick recompute), D-REPL-03
         (performance.now via injected getNow), D-REPL-04 (Infinity + cap),
         D-REPL-05 (maxEmissionHz config), D-REPL-06 (loop re-base),
         D-REPL-09 (AbortSignal via node:timers/promises), D-REPL-13 (this
         file's role).
       - "Pitfalls addressed (per 03-RESEARCH §Common Pitfalls):"
         numbered cross-references — §1 setInterval drift (avoided by
         setTimeout-chain), §2 relative-delta drift (avoided by absolute
         target), §3 listener leak (handled by node:timers/promises),
         §7 loop-boundary drift (re-base on each iteration), §8 NaN-on-Infinity
         (handled by Math.max clamp), §9 empty records (early return).

    2. **Imports** (the file is the SINGLE place in src/ that imports
       node:timers/promises — mirroring the D-FIT-08 single-import seam from
       Phase 2; PATTERNS §Module Structure):
       ```
       import { setTimeout as sleep } from 'node:timers/promises';
       import { debuglog } from 'node:util';
       import type { ReplayConfig } from './types.js';
       import type { RideRecord } from '../types.js';
       ```
       Do NOT import `performance` from `node:perf_hooks` — RESEARCH §Pitfall 6
       proves the import-binding is captured before fake timers initialize. The
       caller (replay.ts in plan 03-02) injects `getNow` via the
       `SchedulerInput` parameter.

    3. **`debuglog` instance** at module top:
       `const log = debuglog('trainer-sim:replay');`
       Namespace per CONTEXT.md `<code_context>` lines 109-110.

    4. **`SchedulerInput` interface** (compile-time-only, declared inside
       scheduler.ts NOT in types.ts because it is a function-signature type,
       not a public-shape type — PATTERNS lines 130-131). Fields:
       - All four fields from `ReplayConfig` (records, speed, loop,
         maxEmissionHz) — preferable to extend via TS `&`-intersection or
         declare explicitly. Choose explicit re-declaration so the SchedulerInput
         shape can be read in isolation (PATTERNS analogue to ParsedFitMinimal
         in `src/fit/normalize.ts:38-48`).
       - `signal: AbortSignal` — required; runScheduler does not run without
         a signal because cancellation is the contract (D-REPL-09).
       - `emit: (record: RideRecord) => void` — synchronous emit callback.
       - `getNow: () => number` — monotonic clock, sub-ms precision; tests
         inject a fake (RESEARCH §Pitfall 6); production wiring in 03-02
         passes `() => performance.now()` against globalThis.performance.

    5. **`export async function runScheduler(input: SchedulerInput): Promise<void>`** —
       named export, no default. Body follows RESEARCH §Drift correction code
       sketch exactly. Use numbered-step comments (PATTERNS §step-numbered
       comments — `// Step 1 — ...`, `// Step 2 — ...`):

       - **Step 1 — Defense-in-depth: empty-records guard.** Per RESEARCH
         §Pitfall 9, `if (input.records.length === 0) return;`
         (resolves the Promise immediately). Phase 2 throws on empty FIT
         upstream, but the scheduler is a public-to-Phase-4 surface and
         must not crash on misuse.

       - **Step 2 — Destructure and compute invariants.** Pull `records,
         speed, loop, maxEmissionHz, signal, emit, getNow` from input. Compute
         `const minIntervalMs = 1000 / maxEmissionHz;` and `const firstTs =
         records[0]!.timestamp;` (the `!` is required by
         `noUncheckedIndexedAccess`).

       - **Step 3 — Initialize loop state.** `let baseline = getNow();`
         and `let cursor = 0;`.

       - **Step 4 — Main loop body.** `while (true) { ... }`:
         a. `const record = records[cursor]!;`
         b. `const targetSinceStart = (record.timestamp - firstTs) / speed;`
            — RESEARCH §Pitfall 8 note: when `speed === Infinity` and
            `record.timestamp === firstTs` (i.e., cursor=0), this evaluates
            to `0/Infinity === NaN`. The next line's `Math.max(NaN,
            minIntervalMs)` returns NaN; `setTimeout(NaN, ...)` clamps to 1ms.
            That's correct-by-accident but fragile. Add an explicit guard:
            `const targetSinceStart = speed === Infinity ? 0 : (record.timestamp - firstTs) / speed;`
            so the test for §Pitfall 8 passes deterministically.
         c. `const target = baseline + targetSinceStart;`
         d. `const delay = Math.max(target - getNow(), minIntervalMs);`
         e. `await sleep(delay, undefined, { signal });` — wrapped in a
            try/catch is NOT required at this level: per RESEARCH §Pitfall 4
            the AbortError rejection propagates out of `runScheduler`; the
            wrapper class in 03-02 catches it via the `.then(success,
            failure)` chain. Do NOT swallow the error here.
         f. `emit(record);`
         g. `cursor++;`
         h. `if (cursor >= records.length) { ... }` — loop boundary handling.

       - **Step 5 — Loop boundary (D-REPL-06, RESEARCH §Pitfall 7).**
         Inside the `if (cursor >= records.length)`:
         a. `if (!loop) return;` — stop-at-end (default).
         b. `cursor = 0;`
         c. `baseline = getNow();` — re-base. THIS IS THE CRITICAL LINE
            that prevents drift accumulation across loop iterations.
         d. Optional: emit a debuglog at this boundary (printf-style):
            `log('loop iteration restart at baseline=%d', baseline);`

    6. **Conditional debuglog observability** (mirror `src/fit/normalize.ts`
       lines 99-107). Track when the scheduler had to clamp delay to
       `minIntervalMs` (i.e., the host fell behind). Suggested: keep a
       `clampedTicks` counter, and at `if (!loop) return;` (the natural-end
       branch) emit a single summary log if `clampedTicks > 0`:
       `log('runScheduler: %d/%d ticks clamped to minIntervalMs=%d ms',
       clampedTicks, totalTicks, minIntervalMs);`
       This is observability-by-NODE_DEBUG, not a contract — keep it
       conditional, do NOT log per-tick (that would dwarf the actual signal).

    7. **No `console.log`.** Production code uses `debuglog` only (PATTERNS
       §Logging/Observability — "NEVER `console.log` from production code —
       only test files").

    8. **No additional exports.** Only `runScheduler` is exported. The
       `SchedulerInput` interface stays internal to the file (no `export`).

    9. **No `src/index.ts` modification** — D-REPL-12 lock; acceptance below
       verifies the public surface is unchanged.

    Commit per Phase 1/2 atomic-commit pattern:
    `feat(03-01): add runScheduler drift-corrected setTimeout-chain (D-REPL-01..06,09)`
  </action>
  <acceptance_criteria>
    - `src/replay/scheduler.ts` exists: `test -f src/replay/scheduler.ts`.
    - Exports a single async function `runScheduler` (named, not default): `grep -cE '^export\s+async\s+function\s+runScheduler\s*\(' src/replay/scheduler.ts` reports 1.
    - Imports the promisified setTimeout from node:timers/promises (NOT global setTimeout): `grep -cE "import\s+\\{\s*setTimeout\s+as\s+sleep\s*\\}\s+from\s+['\"]node:timers/promises['\"]" src/replay/scheduler.ts` reports 1.
    - Does NOT import the global setTimeout / clearTimeout (would invite the listener-leak antipattern from RESEARCH §Pitfall 3): `grep -cE "^import\s+\\{\s*(setTimeout|clearTimeout)" src/replay/scheduler.ts | head -1` and verify only the `as sleep` form matches — manual check via `grep -nE 'global.*setTimeout|^import.*setTimeout(?!.*as\s+sleep)' src/replay/scheduler.ts | wc -l` reports 0.
    - Imports debuglog from node:util and instantiates the 'trainer-sim:replay' namespace: `grep -F "debuglog('trainer-sim:replay')" src/replay/scheduler.ts` exits 0.
    - Uses `import type` for ReplayConfig and RideRecord (verbatimModuleSyntax compliance): `grep -cE "import\s+type\s+\\{\s*ReplayConfig\s*\\}\s+from\s+['\"]\\./types\\.js['\"]" src/replay/scheduler.ts` reports 1 AND `grep -cE "import\s+type\s+\\{\s*RideRecord\s*\\}\s+from\s+['\"]\\.\\./types\\.js['\"]" src/replay/scheduler.ts` reports 1.
    - Does NOT import performance from node:perf_hooks (RESEARCH §Pitfall 6 — getNow injected instead): `grep -cE "from\s+['\"]node:perf_hooks['\"]" src/replay/scheduler.ts` reports 0.
    - Uses `await sleep(` with the `{ signal }` option for AbortSignal cancellation: `grep -E 'await\s+sleep\s*\(' src/replay/scheduler.ts` exits 0 AND `grep -E '\\{\s*signal\s*\\}' src/replay/scheduler.ts` exits 0.
    - Implements absolute-target-time drift correction (the `target = baseline + ...` line and the `delay = Math.max(target - ..., ...)` line): `grep -E 'baseline\s*\+\s*' src/replay/scheduler.ts` exits 0 AND `grep -E 'Math\.max\s*\(.*target' src/replay/scheduler.ts` exits 0.
    - Implements loop boundary re-base (D-REPL-06, RESEARCH §Pitfall 7) — the rebase is at cursor wrap inside an `if (!loop) return;` branch: `grep -E 'if\s*\(\s*!\s*loop\s*\)\s*return' src/replay/scheduler.ts` exits 0 AND `grep -E 'baseline\s*=\s*getNow\s*\(\s*\)' src/replay/scheduler.ts` exits 0 (occurs at least twice — once at init, once at re-base).
    - Has the `speed === Infinity` explicit guard (RESEARCH §Pitfall 8): `grep -E 'speed\s*===\s*Infinity' src/replay/scheduler.ts` exits 0.
    - Has the empty-records-guard early return (RESEARCH §Pitfall 9): `grep -E 'records\.length\s*===\s*0' src/replay/scheduler.ts` exits 0 AND immediately followed by a `return` statement (manual check confirmed in code review during execute-plan).
    - Uses `!`-postfix on bracket indexing (noUncheckedIndexedAccess compliance): `grep -cE 'records\[(cursor|0)\]!' src/replay/scheduler.ts` reports >= 2.
    - Cites locked decisions in comments (traceability — at least 4 distinct D-REPL-* references): `grep -cE 'D-REPL-(01|02|03|04|05|06|09|12|13)' src/replay/scheduler.ts` reports >= 4.
    - Does NOT use `console.log` / `console.warn` / `console.error` in production source: `grep -cE 'console\.(log|warn|error)' src/replay/scheduler.ts` reports 0.
    - Does NOT export anything besides `runScheduler`: `grep -cE '^export\s+' src/replay/scheduler.ts` reports 1.
    - Does NOT introduce an `IScheduler` / strategy-pattern abstraction (CLAUDE.md anti-pattern): `grep -cE 'IScheduler|interface\s+Scheduler\s' src/replay/scheduler.ts` reports 0 AND `grep -cE 'class\s+\w+Scheduler' src/replay/scheduler.ts` reports 0.
    - **Single-source-of-truth seam: scheduler.ts is the ONLY file in src/ importing 'node:timers/promises'** (mirrors D-FIT-08 enforcement): `grep -rlE "from\s+['\"]node:timers/promises['\"]" src/ | grep -v 'src/replay/scheduler.ts' | wc -l | tr -d ' '` reports 0.
    - **D-REPL-12 enforcement: src/index.ts STILL has zero mention of 'replay':** `grep -cE 'replay' src/index.ts` reports 0.
    - tsup build exits 0: `npm run build`.
    - Test-mode typecheck exits 0: `npm run typecheck:test`.
    - The whole pre-existing Phase 1+2 test suite still passes (no regression): `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>test -f src/replay/scheduler.ts && [ "$(grep -cE '^export\s+async\s+function\s+runScheduler\s*\(' src/replay/scheduler.ts)" = "1" ] && grep -E "import\s+\{\s*setTimeout\s+as\s+sleep\s*\}\s+from\s+['\"]node:timers/promises['\"]" src/replay/scheduler.ts && grep -F "debuglog('trainer-sim:replay')" src/replay/scheduler.ts && grep -E "import\s+type\s+\{\s*ReplayConfig\s*\}\s+from\s+['\"]\./types\.js['\"]" src/replay/scheduler.ts && grep -E "import\s+type\s+\{\s*RideRecord\s*\}\s+from\s+['\"]\.\./types\.js['\"]" src/replay/scheduler.ts && [ "$(grep -cE "from\s+['\"]node:perf_hooks['\"]" src/replay/scheduler.ts)" = "0" ] && grep -E 'await\s+sleep\s*\(' src/replay/scheduler.ts && grep -E '\{\s*signal\s*\}' src/replay/scheduler.ts && grep -E 'speed\s*===\s*Infinity' src/replay/scheduler.ts && grep -E 'records\.length\s*===\s*0' src/replay/scheduler.ts && [ "$(grep -cE 'D-REPL-(01|02|03|04|05|06|09|12|13)' src/replay/scheduler.ts)" -ge "4" ] && [ "$(grep -cE 'console\.(log|warn|error)' src/replay/scheduler.ts)" = "0" ] && [ "$(grep -cE '^export\s+' src/replay/scheduler.ts)" = "1" ] && [ "$(grep -cE 'IScheduler|class\s+\w+Scheduler' src/replay/scheduler.ts)" = "0" ] && [ "$(grep -rlE "from\s+['\"]node:timers/promises['\"]" src/ | grep -v 'src/replay/scheduler.ts' | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -cE 'replay' src/index.ts)" = "0" ] && npm run build && npm run typecheck:test && npm test</automated>
  </verify>
  <done>
    `src/replay/scheduler.ts` exports a single `runScheduler` async function implementing the absolute-target-time setTimeout-chain with `node:timers/promises` AbortSignal cancellation, loop re-base, Infinity-speed guard, and empty-records defense-in-depth. Single-import seam (only file importing node:timers/promises in src/). `src/index.ts` unchanged. Pre-existing test suite still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 4 (FakeTransport) ↔ Replay scheduler | The scheduler is the trusted timing primitive; Phase 4 wraps it but does not duplicate timing logic. Drift-correction lives in ONE place. |
| External AbortSignal ↔ scheduler.ts | Caller (Phase 4 / tests) provides a signal; scheduler honors it via node:timers/promises. The scheduler does NOT mutate signals it receives. |
| RideRecord[] (from Phase 2) ↔ runScheduler | Input is pre-validated (Phase 2 normalize enforces sorted ascending, no duplicates, length >= 1). Defense-in-depth handles `length === 0` regardless. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | A future code change replaces `await sleep(delay, undefined, { signal })` with `await new Promise(r => setTimeout(r, delay))`, re-introducing the listener-leak antipattern (RESEARCH §Pitfall 3) | mitigate | Acceptance criteria grep-enforce that scheduler.ts imports from `'node:timers/promises'` and that `src/replay/scheduler.ts` is the ONLY file in src/ doing so. The single-source-of-truth seam means a future regression is caught by the grep at the next plan that touches scheduler.ts. |
| T-03-02 | Tampering | A future change reverts the loop boundary `baseline = getNow()` re-base, re-introducing drift accumulation across iterations (RESEARCH §Pitfall 7) | mitigate | The scheduler emits a debuglog at loop iteration boundaries naming the new baseline. Plan 03-03's loop.test.ts asserts <5 ms inter-iteration drift; a regression that drops the re-base produces drift linear in iteration count and fails the test. |
| T-03-03 | Denial of Service | A pathological caller passes `maxEmissionHz = 0` (or negative), making `minIntervalMs = Infinity`. The scheduler then awaits forever on tick 0. | accept | TypeScript types do not constrain this (number is unbounded). Phase 4's FakeTransport will validate config in its factory; scheduler is the LOWER layer and assumes pre-validated input. JSDoc on ReplayConfig.maxEmissionHz documents the contract. Plan 03-02 may add a fail-fast check in `Replay.start()` if needed. |
| T-03-04 | Tampering | Public API surface drift — someone re-exports `Replay` or `runScheduler` from `src/index.ts` to "make it easier to import" | mitigate | Acceptance criteria in BOTH tasks grep `src/index.ts` for the absence of 'replay'. D-REPL-12 lock cited in JSDoc and acceptance criteria. The discrepancy between Phase 3 (internal-only) and Phase 4 (public API) must be a deliberate change, not drift. |
| T-03-05 | Information Disclosure | debuglog drift / clamp messages leak record contents (timestamps, power values) to NODE_DEBUG-enabled logs | accept | RideRecord contains sport telemetry only — no PII. The scheduler logs aggregate counts (clampedTicks/totalTicks) and baseline timestamps, not record content. NODE_DEBUG is a developer-debug channel (not user-visible), gated on env var. |
</threat_model>

<verification>
- `src/replay/types.ts` exists with the two internal exports (ReplayConfig, ReplayState).
- `src/replay/scheduler.ts` exists with the single `runScheduler` export, the `node:timers/promises` import, the absolute-target-time drift correction, the loop re-base, the Infinity-speed guard, and the empty-records defense-in-depth.
- `src/index.ts` is byte-identical to its Phase-2-completion state — D-REPL-12 lock holds.
- `src/replay/scheduler.ts` is the SINGLE file in `src/` importing `'node:timers/promises'` — single-source-of-truth seam (mirrors D-FIT-08).
- `npm run build`, `npm run typecheck:test`, and `npm test` all exit 0 (Phase 1+2 tests still pass; no Phase 3 tests yet — those land in 03-03 and 03-04).
- Two atomic commits land cleanly: `feat(03-01)` × 2.
</verification>

<success_criteria>
- The replay engine's algorithmic foundation is in place: types in `src/replay/types.ts`, drift-corrected scheduler in `src/replay/scheduler.ts`.
- Plan 03-02's `Replay` class can import `runScheduler` and `ReplayConfig` directly — no further codebase exploration required.
- Locked decisions D-REPL-01, D-REPL-02, D-REPL-03 (via getNow injection), D-REPL-04, D-REPL-05, D-REPL-06, D-REPL-09, D-REPL-12, D-REPL-13 are all reflected in source.
- The single-import-of-node:timers/promises seam mirrors Phase 2's D-FIT-08 enforcement; future plans cannot accidentally import the global `setTimeout` and re-introduce listener leaks.
- Phase 1+2 test suite remains green — no regression.
- No public surface change (D-REPL-12).
</success_criteria>

<output>
After completion, create `.planning/phases/03-replay-engine/03-01-SUMMARY.md` documenting:
- Line counts of `src/replay/types.ts` and `src/replay/scheduler.ts`.
- Confirmation that `src/index.ts` is byte-identical to its pre-Phase-3 state (output of `git diff HEAD~2 HEAD -- src/index.ts` should be empty).
- Confirmation that `src/replay/scheduler.ts` is the SOLE importer of `node:timers/promises` in src/ (output of the single-source-of-truth grep).
- The exact `SchedulerInput` interface shape (so plan 03-02's Replay class can wire it without re-reading the source).
- Whether `Promise.withResolvers()` was used or whether a `withDeferred()` helper is deferred to 03-02 (RESEARCH §Don't Hand-Roll Promise.withResolvers note + Assumptions A5).
- The two atomic commits' SHAs for traceability into 03-02.
</output>
