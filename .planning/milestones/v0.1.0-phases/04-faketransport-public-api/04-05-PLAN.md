---
phase: 04
plan: 05
type: execute
wave: 3
depends_on: [03]
files_modified:
  - test/transport/path-and-buffer.test.ts
autonomous: true
requirements:
  - API-01
  - API-04
tags: [test, fake-transport, integration, fit-fixture]

must_haves:
  truths:
    - "Vitest test file at test/transport/path-and-buffer.test.ts exercises the { path } and { buffer } source variants of createFakeTransport against test/fixtures/fit/basic.fit"
    - "Tests prove path-source and buffer-source variants emit the same number of records and the same emitted byte stream when run with identical config"
    - "Tests prove a FitLoadError thrown from the loader bubbles unchanged through connect()'s Promise rejection (NOT wrapped or translated)"
    - "Tests use vi.useFakeTimers() with the lifted fakeAwareSleep helper for fast deterministic execution"
    - "All new tests pass under `npm test -- test/transport/path-and-buffer.test.ts`"
  artifacts:
    - path: "test/transport/path-and-buffer.test.ts"
      provides: "Integration tests covering API-01 (factory across all 3 source variants) and API-04 (echo-only sendResistance — verified path matches buffer matches records when sendResistance is called)"
      min_lines: 100
      contains: "basic.fit"
  key_links:
    - from: "test/transport/path-and-buffer.test.ts"
      to: "src/index.ts"
      via: "named imports through public surface"
      pattern: "from '\\.\\./\\.\\./src/index\\.js'"
    - from: "test/transport/path-and-buffer.test.ts"
      to: "test/fixtures/fit/basic.fit"
      via: "filesystem path + readFileSync"
      pattern: "basic\\.fit"
---

<objective>
Create the integration test suite for the FakeTransport factory's `{ path }` and `{ buffer }` source variants at `test/transport/path-and-buffer.test.ts`. This is the only Phase 4 test that exercises the FIT load path end-to-end (Plan 04-04 uses the `{ records }` fast path; Plan 04-06 only validates the published artifact). Per D-API-22 + D-API-23, the test uses the existing Phase 2 fixture `test/fixtures/fit/basic.fit` (443 records, 7 minutes, ROUVY clean 1Hz, 28 zero-power records — exercises the `rec.power ?? 0` collapse path).

Implements: D-API-04 (sync factory + deferred FIT load → connect() Promise carries FIT errors), D-API-05 (discriminated union variant coverage for `{ path }` and `{ buffer }`), D-API-22 (test layout under test/transport/), D-API-23 (no new fixtures — reuse Phase 2 corpus).

Requirements addressed: API-01 (factory works across path / buffer / records variants — this plan covers the first two; 04-04 covered records), API-04 (echo-only sendResistance — verified that calling sendResistance during a fixture-driven replay does not mutate the emitted bytes).

Output: `test/transport/path-and-buffer.test.ts` (~100-150 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-faketransport-public-api/04-CONTEXT.md
@.planning/phases/04-faketransport-public-api/04-RESEARCH.md
@.planning/phases/04-faketransport-public-api/04-PATTERNS.md
@.planning/phases/04-faketransport-public-api/04-03-SUMMARY.md
@src/transport/fake-transport.ts
@src/index.ts
@src/types.ts
@src/fit/errors.ts
@test/fit/loader.test.ts
@test/_helpers/fake-aware-sleep.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/transport/path-and-buffer.test.ts (path/buffer source variants + FitLoadError bubble)</name>
  <files>test/transport/path-and-buffer.test.ts</files>
  <read_first>
    - src/transport/fake-transport.ts (entire file — confirm the loadRecords inner helper switches on path / buffer / records and bubbles FitLoadError)
    - src/index.ts (confirm createFakeTransport, FakeTransport, FitLoadError, InvalidFitHeaderError, FitCrcError, FitTruncatedError, NoRecordMessagesError are reachable)
    - src/fit/errors.ts (confirm error class names)
    - test/fit/loader.test.ts (lines 33-76 — primary analog for fixture path resolution + path-vs-buffer parity assertion pattern)
    - test/fixtures/fit/README.md (fixture provenance — confirm basic.fit has 443 records and is the right fixture per D-API-22)
    - test/_helpers/fake-aware-sleep.ts (the lifted helper; tests pass it through createFakeTransport's second-arg sleep seam)
    - .planning/phases/04-faketransport-public-api/04-CONTEXT.md (D-API-04, D-API-05, D-API-22, D-API-23)
    - .planning/phases/04-faketransport-public-api/04-RESEARCH.md (§Open Questions resolutions 6 + 9 — once() under fake timers + basic.fit fixture choice)
    - .planning/phases/04-faketransport-public-api/04-PATTERNS.md (TPB1 fixture path resolution; TPB2 path-and-buffer parity test)
  </read_first>
  <action>
    Create `test/transport/path-and-buffer.test.ts`. Mirror Pattern TR1 + TPB1 (module preamble) — open with a long comment block citing the SUT (`src/transport/fake-transport.ts`'s loadRecords helper specifically), the locked decisions exercised (D-API-04, D-API-05, D-API-22), the requirements addressed (API-01, API-04), the fixture used (`test/fixtures/fit/basic.fit` — 443 records, ROUVY clean 1Hz), and the cooperation pattern (vi.useFakeTimers + speed: Infinity + maxEmissionHz: 1000 → ~443ms wall-clock under fake timers).

    **Imports:**
    - `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
    - `import { once } from 'node:events';`
    - `import { readFileSync } from 'node:fs';`
    - `import { fileURLToPath } from 'node:url';`
    - `import { dirname, resolve } from 'node:path';`
    - `import { createFakeTransport, FitLoadError, FitCrcError } from '../../src/index.js';` (public-surface — value imports for runtime instanceof checks)
    - `import type { FakeTransport } from '../../src/index.js';` (type-only)
    - `import { fakeAwareSleep } from '../_helpers/fake-aware-sleep.js';`

    **Constants (Pattern TPB1):**
    - `const __dirname = dirname(fileURLToPath(import.meta.url));`
    - `const FIXTURE = resolve(__dirname, '../fixtures/fit/basic.fit');`

    **Lifecycle hooks (Pattern TR2):** `beforeEach(vi.useFakeTimers); afterEach(vi.useRealTimers);` at the top-level describe.

    **Helper for byte snapshotting:** Define a small inner function inside the test file: `function snapshot(dv: DataView): Uint8Array` returning `new Uint8Array(dv.buffer.slice(dv.byteOffset, dv.byteOffset + dv.byteLength))`. Used to copy the per-emission DataView bytes into a stable form (the encoder shares the same DataView across all subscribers per Pattern 5; tests must snapshot to compare).

    **Test groups:**

    1. **Group 1 — D-API-05: { path } source variant emits records (API-01).**
       - Test: `createFakeTransport({ source: { path: FIXTURE }, speed: Infinity, maxEmissionHz: 1000 }, { sleep: fakeAwareSleep })`; subscribe via onData; `await connect()` then `await vi.advanceTimersByTimeAsync(60_000)` (cap at 60s of fake time — basic.fit is 7min real but speed=Infinity capped at 1000Hz takes ~443ms); `await once(transport, 'complete')`; assert subscriber received >= 440 emissions and <= 445 (basic.fit has 443 records per fixture provenance, allowing minor parser drift). Each emission has `byteLength === 6`.

    2. **Group 2 — D-API-05: { buffer } source variant emits the same record stream (API-01).**
       - Test: `const buf = readFileSync(FIXTURE);` then `createFakeTransport({ source: { buffer: buf }, speed: Infinity, maxEmissionHz: 1000 }, { sleep: fakeAwareSleep })`; same lifecycle as Group 1; assert the emitted byte snapshots are identical to Group 1's (path and buffer produce the SAME DataView byte sequence per Phase 2 D-FIT-07 sync/async parity). Use the `snapshot()` helper to convert each emission to a stable Uint8Array, then `expect(fromBufferBytes).toEqual(fromPathBytes)`.

    3. **Group 3 — D-API-04: FitLoadError bubbles unchanged from connect() (API-01 error path).**
       - Test: `createFakeTransport({ source: { buffer: new Uint8Array(50) /* random bytes — not a FIT */ } }, { sleep: fakeAwareSleep })` constructs synchronously; `await expect(transport.connect()).rejects.toBeInstanceOf(FitLoadError)`. The specific subclass depends on what fails first (header magic mismatch → `InvalidFitHeaderError`; truncated → `FitTruncatedError`); assert the rejection IS-A `FitLoadError` (the abstract base) without locking to a specific subclass. This proves D-API-04 + Reusable-Asset note (FakeTransport does NOT wrap FitLoadError).
       - Test: ENOENT bubbles. Construct with `source: { path: '/nonexistent/path/file.fit' }`; `await expect(connect()).rejects.toThrow(/ENOENT|no such file/i)`. NOT wrapped in FitLoadError (Phase 2 contract — fs errors bubble unchanged per loader.ts:266-269 docstring).

    4. **Group 4 — API-04: sendResistance is echo-only against the FIT-driven path.**
       - Test: with `{ path: FIXTURE, speed: Infinity, maxEmissionHz: 1000 }` + sleep seam, snapshot the first 5 emitted byte sequences (call this baseline). Reset, re-connect, call `await sendResistance(0.10); await sendResistance(0.20); await sendResistance(0.30);` while replay is running, advance through completion. Snapshot the first 5 emissions of the SECOND run. Assert `secondRunFirst5 === baselineFirst5` (sendResistance did NOT mutate any emitted byte). Also assert `received.resistance.toEqual([0.10, 0.20, 0.30])` (the log captured all three calls in order). This is the binding API-04 verification: echo-only against a real fixture.

    **Performance / timeout note:** Each test runs under fake timers, so even the 60-second `advanceTimersByTimeAsync(60_000)` is instant wall-clock. The vitest default timeout (5s real time) is plenty; do NOT add `it.timeout(...)` overrides.

    **Comment posture:** Per CLAUDE.md "default to writing no comments" — describe / it titles document the test intent; do NOT add line-by-line commentary in the test bodies. The single header preamble citing decisions is acceptable WHY-non-obvious.
  </action>
  <verify>
    <automated>tsc --noEmit -p tsconfig.test.json &amp;&amp; test -f test/transport/path-and-buffer.test.ts &amp;&amp; test -f test/fixtures/fit/basic.fit &amp;&amp; npm test -- test/transport/path-and-buffer.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit -p tsconfig.test.json` exits 0.
    - `test/transport/path-and-buffer.test.ts` exists.
    - File imports `createFakeTransport`, `FitLoadError`, `FakeTransport` types through `../../src/index.js` (NOT internal paths).
    - File imports `fakeAwareSleep` from `../_helpers/fake-aware-sleep.js`.
    - File references the fixture path `../fixtures/fit/basic.fit`.
    - File contains at least 4 `describe` or `it` blocks (one per group above).
    - File contains a path-and-buffer parity assertion (`expect(fromBufferBytes).toEqual(fromPathBytes)` or equivalent).
    - File contains an `await expect(...).rejects.toBeInstanceOf(FitLoadError)` assertion (D-API-04 error path).
    - File contains an ENOENT bubble test (`/ENOENT/` or `/no such file/` regex).
    - File contains an API-04 echo-only test with snapshot comparison.
    - `grep -E "advanceTimersByTime[^A]" test/transport/path-and-buffer.test.ts` returns ZERO matches (sync-variant ban).
    - `grep -n "from '\\.\\./\\.\\./src/transport" test/transport/path-and-buffer.test.ts` returns ZERO matches (public-surface discipline).
    - `npm test -- test/transport/path-and-buffer.test.ts` reports all tests passing.
  </acceptance_criteria>
  <done>The integration test suite exists, exercises both fixture-backed source variants + the FitLoadError bubble + API-04 echo-only across the FIT path, and passes under fake timers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → Public surface | Tests import via `../../src/index.js` to validate the public re-export end-to-end |
| Test file → Phase 2 fixture (filesystem) | Reads `test/fixtures/fit/basic.fit` — read-only access; fixture is committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | A test silently uses `vi.advanceTimersByTime` (sync variant) and produces non-deterministic pass/fail | mitigate | Acceptance grep `grep -E "advanceTimersByTime[^A]" test/transport/path-and-buffer.test.ts` returns zero matches. Phase 3 RESEARCH §Pitfall 5 + 04-RESEARCH §Pitfall 4. |
| T-04-05-02 | Tampering | Test bypasses public surface and imports from `src/transport/...` directly | mitigate | Acceptance grep `grep -n "from '\\.\\./\\.\\./src/transport" test/transport/path-and-buffer.test.ts` returns zero matches. |
| T-04-05-03 | Information Disclosure | basic.fit contains real ride data with a real timestamp — could leak rider identity if the fixture preamble was wrong | accept | Phase 2 already verified fixture provenance: basic.fit is a ROUVY-generated indoor-trainer FIT with no GPS / no rider identity. Documented in test/fixtures/fit/README.md. |
| T-04-05-04 | DoS | `vi.advanceTimersByTimeAsync(60_000)` could starve real CPU under fake timers if a microtask loop spins | accept | Phase 3 already proved this works (test/replay/replay.test.ts uses identical patterns); the scheduler's clamped delay (max(0, 1000/maxEmissionHz)) guarantees forward progress. |
</threat_model>

<verification>
- `tsc --noEmit -p tsconfig.test.json` exits 0.
- `npm test -- test/transport/path-and-buffer.test.ts` reports all tests passing.
- Acceptance greps in Task 1 all pass.
- Both `{ path }` and `{ buffer }` source variants are exercised end-to-end against a real Phase 2 FIT fixture (D-API-05).
- The FitLoadError bubble path is proven (D-API-04 — bubbled, not wrapped).
- API-04 echo-only is verified against the FIT-driven path (sendResistance does NOT mutate emitted bytes).
</verification>

<success_criteria>
- `{ path }` source variant: tested end-to-end against `basic.fit`; emission count and byte stream observable.
- `{ buffer }` source variant: tested end-to-end against `basic.fit` bytes; produces identical emission stream to the `{ path }` variant.
- FitLoadError bubble: corrupt buffer → connect() rejects with `instanceof FitLoadError` (NOT wrapped).
- ENOENT bubble: nonexistent path → connect() rejects with a Node ENOENT-style Error (NOT wrapped in FitLoadError).
- API-04 echo-only: sendResistance during replay does NOT modify any emitted DataView byte.
- All tests pass under `vi.useFakeTimers()` + the lifted `fakeAwareSleep` helper.
</success_criteria>

<output>
After completion, create `.planning/phases/04-faketransport-public-api/04-05-SUMMARY.md` documenting:
- Test file created at `test/transport/path-and-buffer.test.ts`
- Test count + group count
- Fixture used: `test/fixtures/fit/basic.fit` (no new fixtures per D-API-23)
- Path/buffer parity assertion outcome
- FitLoadError bubble assertion outcome
- API-04 echo-only assertion outcome
- Requirements addressed: API-01 (factory across all 3 variants — combined with 04-04), API-04 (echo-only against FIT-driven path)
</output>
