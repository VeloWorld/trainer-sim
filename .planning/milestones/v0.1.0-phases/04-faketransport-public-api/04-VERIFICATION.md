---
phase: 04-faketransport-public-api
verified: 2026-05-16T20:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 4: FakeTransport & Public API Verification Report

**Phase Goal:** Library exposes a `createFakeTransport(config)` factory that satisfies `ITrainerTransport` and ships as a dual ESM/CJS package importable cleanly into a TypeScript Node 24 project.

**Verified:** 2026-05-16T20:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth (ROADMAP SC)                                                                                                                                                                                                                  | Status     | Evidence                                                                                                                                                                                                                                                                                                                |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `createFakeTransport(config)` returns an object with `connect`, `disconnect`, `onData`, `sendResistance` matching the `ITrainerTransport` type exported from the package root                                                       | VERIFIED  | `src/transport/fake-transport.ts:138-306` returns plain object literal with all four methods. `src/index.ts:25-31` re-exports `createFakeTransport` (value) + `ITrainerTransport`/`FakeTransport`/`FakeTransportConfig`/`FakeTransportSource` (types). `src/types.ts:105-140` defines the `ITrainerTransport` interface. Test Group 1 ("returned object exposes the full FakeTransport surface") passes. |
| 2   | `onData(handler)` accepts a `(data: DataView) => void` handler and returns a disposer that, when called, stops further deliveries                                                                                                  | VERIFIED  | `src/transport/fake-transport.ts:265-270` adds to a `Set` and returns `() => subscribers.delete(handler)`. Test Group 4 ("disposer returned by onData removes the subscriber") passes; "handler that disposes itself mid-fan-out" also passes (Pitfall §3 / ECMA-262 Set iteration semantics).                            |
| 3   | `sendResistance(grade)` records the grade in `received.resistance` (in order) and does NOT modify any subsequent emitted power or cadence value                                                                                    | VERIFIED  | `src/transport/fake-transport.ts:272-278` awaits `Promise.resolve()` (microtask boundary) THEN pushes; `received` getter exposes `resistanceLog` as `ReadonlyArray<number>` (lines 291-293). Test Group 6 ("sendResistance during a running replay does NOT modify emitted DataView bytes") + `path-and-buffer.test.ts` "sendResistance during a fixture-driven replay does NOT mutate emitted bytes" both pass. |
| 4   | `reset()` clears `received.resistance` and rewinds the replay cursor so a single instance can be reused across `afterEach()`-isolated tests                                                                                       | VERIFIED  | `src/transport/fake-transport.ts:280-283` awaits `disconnect()` then truncates `resistanceLog` in place; `disconnect()` discards `replay` (lines 256-263) so the next `connect()` constructs a fresh `Replay`. Test Group 8 ("reset() clears received.resistance, preserves subscribers, and recycles for the next connect()") passes. |
| 5   | `publint` and `attw` both pass against the published package shape; importing into a fresh strict-mode TypeScript Node 24 project requires no `@types/*` shim                                                                       | VERIFIED  | `npm run validate` (build + publint + attw) executed and exited 0. publint output: "All good!". attw output: all six resolution modes green (node10 / node16-CJS / node16-ESM / bundler / ESM consumer / CJS consumer). `dist/index.{js,cjs,d.ts,d.cts}` all exist and contain `createFakeTransport`. publish.test.ts asserts the same in CI. |

**Score:** 5/5 ROADMAP success criteria verified.

### Observable Truths (Plan Frontmatter must_haves — additive)

| #   | Plan-level truth                                                                                                                                                                              | Status   | Evidence                                                                                                                                                                                                                  |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 6   | NO BLE-specific tokens (`bleno|gatt|advertis`) in `src/index.ts`, `src/types.ts`, `src/transport/fake-transport.ts` (D-API-03 / threat T-04-03-01)                                            | VERIFIED | `grep -rn "bleno\|gatt\|advertis" src/index.ts src/types.ts src/transport/fake-transport.ts` returned exit=1 (zero matches).                                                                                          |
| 7   | `src/transport/fake-transport.ts` is the SINGLE-IMPORT-SEAM for `Replay` — no other `src/*` file imports from `../replay/` (D-API-18 / Pitfall 5)                                                | VERIFIED | `grep -rln "from '\.\./replay" src/` returns only `src/transport/fake-transport.ts`. The `grep ... | grep -v` exit=1 (filtered list empty).                                                                                |
| 8   | Test discipline: 115 tests pass, 2 skipped (intentional opt-ins TEST_FIT_DIR + RUN_SOAK), 117 total. Group 10 regression tests for resolved CR-01/CR-02/CR-03 all pass.                          | VERIFIED | `npm test` reported "Test Files 14 passed | 2 skipped (16); Tests 115 passed | 2 skipped (117)". `npx vitest run test/transport/fake-transport.test.ts` reported 28 passed (includes Group 10's 3 regression tests). |

**Score:** 3/3 plan-level truths verified.

**Combined Score: 8/8 must-haves verified.**

### Critical Issues from REVIEW.md (CR-01 / CR-02 / CR-03 — fix verification)

| #     | Critical Issue                                                                              | Code Fix                                                                                                                                                                                                  | Regression Test                                                                                              | Status     |
| ----- | ------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ---------- |
| CR-01 | Concurrent `connect()` calls orphan a Replay (idempotency contract violation)               | `fake-transport.ts:185, 194-247` — `connectInFlight` Promise sentinel set BEFORE `await loadRecords()`; `disconnect()` and `reset()` await it before their early-return guards (also closes WR-03).         | Group 10 test 1: "two concurrent connect() calls do not orphan a Replay" — asserts exactly 3 emissions, not 6. PASS | VERIFIED  |
| CR-02 | Empty-records source wedges the transport (deadlock on subsequent disconnect)                | `fake-transport.ts:200-206, 237-241` — `connect()` rejects with `createFakeTransport.connect: source produced zero records` BEFORE `new Replay(...)`; `replay` only commits AFTER `start()` returns without throwing. | Group 10 test 2: "connect() with zero records rejects AND leaves the transport reconnectable" — second connect() rejects identically; disconnect() resolves. PASS | VERIFIED  |
| CR-03 | A throwing `'complete'` listener becomes `unhandledRejection`                                 | `fake-transport.ts:222-236` — `emitter.emit('complete')` wrapped in try/catch with `debuglog('trainer-sim:transport')` swallow; matches the per-handler discipline already in the `onRecord` fan-out.        | Group 10 test 3: "a throwing 'complete' listener does NOT register as unhandledRejection" — `process.on('unhandledRejection', ...)` listener observes zero rejections. PASS | VERIFIED  |

REVIEW.md frontmatter reads `status: critical_resolved`, `findings.critical: 3`, `resolved: 3` — internally consistent with code + tests.

### Required Artifacts

| Artifact                                              | Expected                                                                          | Status     | Details                                                                                                                                                                                                                                                                                                                              |
| ----------------------------------------------------- | --------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/types.ts`                                        | Adds `ITrainerTransport`, `FakeTransport`, `FakeTransportConfig`, `FakeTransportSource` | VERIFIED  | Lines 105-140 (`ITrainerTransport`), 155-158 (`FakeTransportSource` discriminated union of `{path}` / `{buffer}` / `{records}`), 175-205 (`FakeTransportConfig`), 229-265 (`FakeTransport extends ITrainerTransport` with literal-`'complete'`-typed `on`/`off`/`once` per D-API-13).                                                                |
| `src/transport/fake-transport.ts`                     | `createFakeTransport` factory (>=120 LOC, plain object literal, no class)         | VERIFIED  | 306 LOC. Single Replay import (line 107). `EventEmitter<{ complete: [] }>` composed (line 172). Per-handler try/catch (lines 213-219). Microtask boundary in `sendResistance` (line 276). Synchronous validation of `speed > 0` and `maxEmissionHz > 0` (lines 145-152). CR-01/02/03 fixes inline with `// CR-01` / `// CR-02` / `// CR-03` annotations. |
| `src/index.ts`                                        | Re-exports `createFakeTransport` + 4 types                                          | VERIFIED  | Lines 25-31 — runtime `createFakeTransport` value export + 4 `export type` lines.                                                                                                                                                                                                                                                       |
| `test/_helpers/fake-aware-sleep.ts`                   | Lifted helper, exports `fakeAwareSleep`                                              | VERIFIED  | 50 LOC. `globalThis.setTimeout`-based AbortSignal-aware sleep.                                                                                                                                                                                                                                                                            |
| `test/transport/fake-transport.test.ts`               | Unit tests covering API-01..06, Group 10 CR regressions                              | VERIFIED  | 513 LOC. 28 tests across 10 describe groups. All pass under `vi.useFakeTimers()`.                                                                                                                                                                                                                                                         |
| `test/transport/path-and-buffer.test.ts`              | Integration tests against `basic.fit`, FitLoadError bubble                          | VERIFIED  | 233 LOC. 4 tests cover `{ path }`, `{ buffer }`, FitLoadError bubbling unchanged, ENOENT bubbling as plain Node error, sendResistance echo-only against fixture-driven path. All pass.                                                                                                                                              |
| `test/transport/publish.test.ts`                      | build + publint + attw against built `dist/`                                         | VERIFIED  | 153 LOC. 5 tests assert build emits 4 dual-publish artifacts; built artifacts contain Phase 4 surface; publint exits 0; attw `--pack .` exits 0; `package.json`/`tsup.config.ts` D-API-08 invariants intact (single-rooted exports, types-first conditional, Node 24 engines pin, files whitelist).                                  |
| `dist/index.{js,cjs,d.ts,d.cts}`                      | All four dual-publish artifacts contain `createFakeTransport`                       | VERIFIED  | `grep -c createFakeTransport dist/index.{js,cjs,d.ts,d.cts}` → 5/5/6/6 hits.                                                                                                                                                                                                                                                                |

### Key Link Verification

| From                                              | To                                  | Via                                                                                                  | Status   | Details                                                                                                                                          |
| ------------------------------------------------- | ----------------------------------- | ---------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/index.ts`                                    | `src/transport/fake-transport.ts`   | `export { createFakeTransport } from './transport/fake-transport.js'`                                 | WIRED    | Line 25.                                                                                                                                          |
| `src/index.ts`                                    | `src/types.ts`                      | `export type { ITrainerTransport, FakeTransport, FakeTransportConfig, FakeTransportSource } from './types.js'` | WIRED    | Lines 26-31.                                                                                                                                       |
| `src/transport/fake-transport.ts`                 | `src/replay/replay.ts`              | `import { Replay } from '../replay/replay.js'` — SOLE seam                                            | WIRED    | Line 107. No other `src/*` file imports from `../replay/` (verified via grep).                                                                    |
| `src/transport/fake-transport.ts`                 | `src/fit/loader.ts`                 | `import { loadFitFromBuffer, loadFitFromPath } from '../fit/loader.js'`                                | WIRED    | Line 103. Used inside `loadRecords()` (lines 187-192) on the `{ path }` and `{ buffer }` source variants.                                            |
| `src/transport/fake-transport.ts`                 | `src/ftms/indoor-bike-data.ts`      | `import { encodeIndoorBikeData } from '../ftms/indoor-bike-data.js'`                                  | WIRED    | Line 102. Called inside `replay.onRecord` callback (lines 209-212) with `rec.power ?? 0` / `rec.cadence ?? 0` collapse per D-API-20.                  |
| `test/transport/*.test.ts`                        | `src/index.ts` (public surface)     | `import { createFakeTransport, FitLoadError } from '../../src/index.js'`                              | WIRED    | All three `test/transport/*.test.ts` files import through the public surface; no internal-path imports (T-04-04-02 / T-04-05-02 enforced).      |
| `test/transport/fake-transport.test.ts`           | `test/_helpers/fake-aware-sleep.ts` | `import { fakeAwareSleep } from '../_helpers/fake-aware-sleep.js'`                                    | WIRED    | Line 43; passed through `createFakeTransport(..., { sleep: fakeAwareSleep })` test-only seam.                                                       |
| `FakeTransport` interface                         | `ITrainerTransport` interface       | `interface FakeTransport extends ITrainerTransport`                                                   | WIRED    | `src/types.ts:229`.                                                                                                                                |

### Data-Flow Trace (Level 4)

| Artifact                                              | Data Variable          | Source                                                                                  | Produces Real Data | Status    |
| ----------------------------------------------------- | ---------------------- | --------------------------------------------------------------------------------------- | ------------------ | --------- |
| `src/transport/fake-transport.ts` (DataView emissions) | per-record `dv` from `encodeIndoorBikeData(...)` inside `replay.onRecord` callback | `Replay.onRecord` driven by Phase 3 scheduler iterating real `RideRecord[]` from FIT loader (Phase 2) or `{ records }` variant. Test Group 3: byteLength === 6 confirms encoder produced real bytes. `path-and-buffer.test.ts` 443 records from real `basic.fit`. | Yes                | FLOWING  |
| `src/transport/fake-transport.ts` (`received.resistance`) | `resistanceLog: number[]` | `sendResistance` pushes; `reset()` truncates                                              | Tests assert `received.resistance` matches `[0.05, 0.10]` etc.                                                                                                                                                                  | Yes                | FLOWING  |

### Behavioral Spot-Checks

| Behavior                                                        | Command                                              | Result                                                                                                                                                                                                                                                                                                                              | Status |
| --------------------------------------------------------------- | ---------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| Library publishes 4 dual artifacts and validates clean          | `npm run validate`                                   | Exit 0. `dist/index.cjs` 17.60 KB, `dist/index.js` 17.12 KB, `dist/index.d.ts` 28.37 KB, `dist/index.d.cts` 28.37 KB. publint "All good!". attw all 4 modes green.                                                                                                                                                                       | PASS   |
| Full test suite                                                  | `npm test`                                           | 14 test files passed + 2 skipped (16). 115 tests passed + 2 skipped (117). Duration 30.22s.                                                                                                                                                                                                                                          | PASS   |
| FakeTransport unit tests (Group 10 regression of CR-01/02/03)   | `npx vitest run test/transport/fake-transport.test.ts` | 28 tests passed (1 file). Includes Group 10 with the three CR regression tests.                                                                                                                                                                                                                                                       | PASS   |
| TypeScript test sources type-check                              | `npm run typecheck:test`                             | `tsc --noEmit -p tsconfig.test.json` — exit 0.                                                                                                                                                                                                                                                                                          | PASS   |
| BLE-token grep on Phase 4 source files (D-API-03 / T-04-03-01)  | `grep -rn "bleno\|gatt\|advertis" src/index.ts src/types.ts src/transport/fake-transport.ts` | Exit 1 (no matches).                                                                                                                                                                                                                                                                                                                                  | PASS   |
| Single-Replay-import seam (D-API-18 / Pitfall 5)                | `grep -rln "from '\.\./replay" src/`                 | Only match: `src/transport/fake-transport.ts`. Filter (excluding that one file) is empty.                                                                                                                                                                                                                                              | PASS   |

### Probe Execution

No probes declared in PLAN/SUMMARY/CONTEXT files. No conventional `scripts/*/tests/probe-*.sh` files exist in this repo. Skipped — Phase 4 is a library-API phase whose verification surface is `npm run validate` + `npm test` (both run above).

### Requirements Coverage

| Requirement | Source Plan(s)                  | Description                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                |
| ----------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| API-01      | 04-03, 04-04, 04-05             | Library exports `createFakeTransport(config)` factory returning an `ITrainerTransport`-shaped object                                          | SATISFIED | `src/index.ts:25` exports the factory; `fake-transport.ts:138` returns `FakeTransport` (extends `ITrainerTransport`). Test Group 1 + `path-and-buffer.test.ts` exercise all 3 source variants.                                                          |
| API-02      | 04-02, 04-03, 04-04             | `ITrainerTransport` interface exported as a TypeScript type from the package root                                                            | SATISFIED | `src/types.ts:105` defines the interface; `src/index.ts:27` re-exports as type. `dist/index.d.ts` contains `interface ITrainerTransport`.                                                                                                                |
| API-03      | 04-03, 04-04                    | `onData(handler)` accepts `(data: DataView) => void` and returns a disposer for unsubscription                                                 | SATISFIED | `fake-transport.ts:265-270` returns the disposer. Test Group 4 covers fan-out + disposer + self-removal during fan-out.                                                                                                                                  |
| API-04      | 04-03, 04-04, 04-05             | `sendResistance(grade)` is echo-only — records the call and does NOT modify replayed power/cadence                                              | SATISFIED | `fake-transport.ts:272-278`. Test Group 6 ("does NOT modify emitted DataView bytes") + `path-and-buffer.test.ts` "echo run vs baseline" prove emitted bytes are byte-identical.                                                                          |
| API-05      | 04-03, 04-04                    | `received.resistance` is a public read-only array of every `sendResistance(grade)` call in order                                                | SATISFIED | `fake-transport.ts:291-293` exposes via getter. `src/types.ts:236` types as `ReadonlyArray<number>`. Test Group 6 asserts ordering across 3 calls.                                                                                                       |
| API-06      | 04-03, 04-04                    | `reset()` clears the resistance log and replay cursor for cross-test reuse                                                                  | SATISFIED | `fake-transport.ts:280-283`. Test Group 8 covers full lifecycle 1 → reset → lifecycle 2.                                                                                                                                                                  |
| API-07      | 04-06                           | Library ships as ESM-first with dual ESM/CJS publish, validated by `publint` and `@arethetypeswrong/cli`                                       | SATISFIED | `npm run validate` exits 0. publint "All good!". attw all 6 modes green. `publish.test.ts` test 3 + 4 assert this in CI.                                                                                                                                |
| API-08      | 04-02, 04-06                    | Library is importable into TypeScript Node 24 projects with strict-mode types out of the box                                                  | SATISFIED | attw all 6 resolution modes (node10, node16-CJS, node16-ESM, bundler) green; types-first conditional in `package.json` exports map; `engines.node` pinned `>=24.0`. `tsc --noEmit` of test sources (which import via the public surface) passes.        |

All 8 phase requirement IDs (API-01..08) are SATISFIED. No orphaned IDs in REQUIREMENTS.md (Phase 4 maps exactly to API-01..08).

### Anti-Patterns Found

| File                                              | Line | Pattern | Severity | Impact |
| ------------------------------------------------- | ---- | ------- | -------- | ------ |

(None.) Debt-marker grep across the seven Phase 4 files (`TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER`) returned exit=1 — zero matches. The CR-01/02/03 marker comments inside `fake-transport.ts` reference resolved review issues, not deferred work.

### Human Verification Required

(None.) Phase 4 is a library-API phase whose entire surface is observable via `npm run validate` + `npm test`. Both ran clean above. Human visual / UX / external-service verification is not applicable.

### Gaps Summary

No gaps. The phase goal — "Library exposes a `createFakeTransport(config)` factory that satisfies `ITrainerTransport` and ships as a dual ESM/CJS package importable cleanly into a TypeScript Node 24 project" — is achieved end-to-end:

1. The factory exists, returns the documented shape, and is the only `src/*` file that imports `Replay` (D-API-18 single-import seam).
2. The four types (`ITrainerTransport`, `FakeTransport`, `FakeTransportConfig`, `FakeTransportSource`) are defined in `src/types.ts`, re-exported from `src/index.ts`, and present in the built `dist/index.d.{ts,cts}`.
3. The dual ESM/CJS dist artifacts pass both publint and attw (all 6 resolution modes) — confirmed by direct `npm run validate` execution (exit 0).
4. All 8 phase requirements (API-01..08) are satisfied with traceable evidence.
5. The 3 review BLOCKERs (CR-01 connect race, CR-02 empty-records deadlock, CR-03 throwing complete listener) are fixed inline AND covered by Group 10 regression tests — all 3 tests pass.
6. The D-API-03 BLE-token grep returns zero matches across the three target source files; the dual-publish CJS path therefore does not require a `@stoprocent/bleno` install (T-04-03-01 mitigated).
7. Test count matches the brief: 115 passed + 2 skipped (intentional opt-ins TEST_FIT_DIR + RUN_SOAK) + 117 total.

Phase 4 is complete and may proceed to Phase 5 (VeloWorld End-to-End Validation).

---

_Verified: 2026-05-16T20:00:00Z_
_Verifier: Claude (gsd-verifier)_
