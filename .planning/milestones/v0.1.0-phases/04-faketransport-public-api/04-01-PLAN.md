---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/_helpers/fake-aware-sleep.ts
  - test/replay/scheduler.test.ts
  - test/replay/abort.test.ts
  - test/replay/replay.test.ts
  - test/replay/loop.test.ts
autonomous: true
requirements: []
tags: [test-helpers, refactor, phase-3-followup]

must_haves:
  truths:
    - "test/_helpers/fake-aware-sleep.ts exists and exports a single named function `fakeAwareSleep`"
    - "Each of the 4 Phase 3 test files imports `fakeAwareSleep` from '../_helpers/fake-aware-sleep.js' and contains zero local copies of the helper definition"
    - "All 4 Phase 3 test files still pass under `npm test` after the lift (zero behavioral change)"
  artifacts:
    - path: "test/_helpers/fake-aware-sleep.ts"
      provides: "AbortSignal-aware sleep using globalThis.setTimeout (lifted shared helper)"
      exports: ["fakeAwareSleep"]
    - path: "test/replay/scheduler.test.ts"
      provides: "Scheduler unit tests now consuming the lifted helper"
      contains: "from '../_helpers/fake-aware-sleep.js'"
    - path: "test/replay/abort.test.ts"
      provides: "Abort tests now consuming the lifted helper"
      contains: "from '../_helpers/fake-aware-sleep.js'"
    - path: "test/replay/replay.test.ts"
      provides: "Replay class tests now consuming the lifted helper"
      contains: "from '../_helpers/fake-aware-sleep.js'"
    - path: "test/replay/loop.test.ts"
      provides: "Loop tests now consuming the lifted helper"
      contains: "from '../_helpers/fake-aware-sleep.js'"
  key_links:
    - from: "test/replay/scheduler.test.ts"
      to: "test/_helpers/fake-aware-sleep.ts"
      via: "named import"
      pattern: "from '\\.\\./_helpers/fake-aware-sleep\\.js'"
    - from: "test/replay/abort.test.ts"
      to: "test/_helpers/fake-aware-sleep.ts"
      via: "named import"
      pattern: "from '\\.\\./_helpers/fake-aware-sleep\\.js'"
    - from: "test/replay/replay.test.ts"
      to: "test/_helpers/fake-aware-sleep.ts"
      via: "named import"
      pattern: "from '\\.\\./_helpers/fake-aware-sleep\\.js'"
    - from: "test/replay/loop.test.ts"
      to: "test/_helpers/fake-aware-sleep.ts"
      via: "named import"
      pattern: "from '\\.\\./_helpers/fake-aware-sleep\\.js'"
---

<objective>
Lift the byte-identical `fakeAwareSleep` helper out of 4 Phase 3 test files and into a single shared helper at `test/_helpers/fake-aware-sleep.ts`. This is a pure refactor with zero behavioral change — the helper bytes are byte-for-byte identical across `test/replay/{scheduler,abort,replay,loop}.test.ts` (verified in 04-RESEARCH §Open Questions resolution 8). Closes Phase 3 followup IN-01 per D-API-24 / D-API-25.

Purpose: Removes a fifth duplication that Wave 2's new `test/transport/fake-transport.test.ts` would otherwise introduce. Lands as Wave 1 so subsequent transport tests can import from the new location without conflict.

Output: `test/_helpers/fake-aware-sleep.ts` (new) + 4 in-place edits removing local definitions and adding the import line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-faketransport-public-api/04-CONTEXT.md
@.planning/phases/04-faketransport-public-api/04-RESEARCH.md
@.planning/phases/04-faketransport-public-api/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/_helpers/fake-aware-sleep.ts</name>
  <files>test/_helpers/fake-aware-sleep.ts</files>
  <read_first>
    - test/replay/scheduler.test.ts (lines 49-82 — source-of-truth body for the lift)
    - test/replay/abort.test.ts (lines 32-62 — confirm byte-for-byte match)
    - test/replay/replay.test.ts (lines 37-67 — confirm byte-for-byte match)
    - test/replay/loop.test.ts (lines 23-53 — confirm byte-for-byte match)
    - .planning/phases/04-faketransport-public-api/04-PATTERNS.md (Pattern TH1 — verbatim body, JSDoc preamble for the lift)
    - .planning/phases/04-faketransport-public-api/04-RESEARCH.md (§Code Example 4 — the lifted-helper file shape)
  </read_first>
  <action>
    Create the new directory `test/_helpers/` and file `test/_helpers/fake-aware-sleep.ts`. Export ONE named function `fakeAwareSleep` with signature `(delay: number, _value?: undefined, options?: { signal?: AbortSignal }) => Promise<void>`. Body must be byte-for-byte identical to the existing 4 copies (Promise constructor with `signal?.aborted` early-reject path setting `(err as { name: string }).name = 'AbortError'`; abort listener registered with `{ once: true }`; `globalThis.setTimeout` with `clearTimeout` cleanup on abort; `signal?.removeEventListener('abort', onAbort)` on natural resolve). File preamble JSDoc must cite Phase 3 followup IN-01, Phase 4 D-API-24, and 04-RESEARCH §Pitfall 4 root cause (vi.useFakeTimers does NOT intercept node:timers/promises module-level binding). Use `.js` extension discipline is N/A here (no relative imports). Do NOT export anything else; do NOT add `as const` or other decorators. Per CLAUDE.md "default to no comments" — JSDoc preamble is acceptable WHY-non-obvious; do NOT add line-by-line inline comments.
  </action>
  <verify>
    <automated>test -f test/_helpers/fake-aware-sleep.ts &amp;&amp; grep -q "export function fakeAwareSleep" test/_helpers/fake-aware-sleep.ts &amp;&amp; grep -q "globalThis.setTimeout" test/_helpers/fake-aware-sleep.ts &amp;&amp; grep -q "AbortError" test/_helpers/fake-aware-sleep.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `test/_helpers/fake-aware-sleep.ts` exists.
    - Exactly one named export: `export function fakeAwareSleep(...)`.
    - Function signature is `(delay: number, _value?: undefined, options?: { signal?: AbortSignal }) => Promise&lt;void&gt;`.
    - Body uses `globalThis.setTimeout` (NOT `setTimeout` from `node:timers/promises`).
    - Body sets `name = 'AbortError'` on the rejection error in BOTH the early-reject branch and the listener branch.
    - Body uses `{ once: true }` on `signal?.addEventListener('abort', onAbort, ...)`.
    - `tsc --noEmit -p tsconfig.test.json` exits 0.
  </acceptance_criteria>
  <done>The shared helper file exists with the correct exported signature and byte-equivalent body; typecheck stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate 4 Phase 3 test files to consume the lifted helper</name>
  <files>test/replay/scheduler.test.ts, test/replay/abort.test.ts, test/replay/replay.test.ts, test/replay/loop.test.ts</files>
  <read_first>
    - test/_helpers/fake-aware-sleep.ts (just created in Task 1)
    - test/replay/scheduler.test.ts (entire file — confirm where local helper lives and where imports are)
    - test/replay/abort.test.ts (entire file)
    - test/replay/replay.test.ts (entire file)
    - test/replay/loop.test.ts (entire file)
    - .planning/phases/04-faketransport-public-api/04-PATTERNS.md (Pattern TH2 — migration form)
  </read_first>
  <action>
    For each of the 4 files (`test/replay/scheduler.test.ts`, `test/replay/abort.test.ts`, `test/replay/replay.test.ts`, `test/replay/loop.test.ts`): (1) Delete the local `function fakeAwareSleep(...)` block including its preceding JSDoc comment block. (2) Add a single import line `import { fakeAwareSleep } from '../_helpers/fake-aware-sleep.js';` placed in the import group near the other relative imports (e.g., right after the `import type { RideRecord } from '../../src/types.js';` line, or grouped with other relative `../_helpers/` imports if any are added later). Preserve all OTHER content of each file unchanged — same describe blocks, same test bodies, same call sites of `fakeAwareSleep`. Do NOT rename or refactor anything else in these files in this task.
  </action>
  <verify>
    <automated>! grep -n "function fakeAwareSleep" test/replay/scheduler.test.ts test/replay/abort.test.ts test/replay/replay.test.ts test/replay/loop.test.ts &amp;&amp; grep -l "from '../_helpers/fake-aware-sleep.js'" test/replay/scheduler.test.ts test/replay/abort.test.ts test/replay/replay.test.ts test/replay/loop.test.ts | wc -l | grep -q "^4$" &amp;&amp; npm test -- test/replay 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "function fakeAwareSleep" test/replay/{scheduler,abort,replay,loop}.test.ts` returns ZERO matches (local definitions gone).
    - All 4 files contain `import { fakeAwareSleep } from '../_helpers/fake-aware-sleep.js';`.
    - `npm test -- test/replay` reports the same pass count as before the lift (Phase 3 reported 77/79 — 2 intentional opt-in skips).
    - `tsc --noEmit -p tsconfig.test.json` exits 0.
  </acceptance_criteria>
  <done>4 Phase 3 test files import from the shared helper; no local copies remain; all replay tests pass with no behavioral regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → repository | Test-only code; not shipped in `dist/` (excluded by `package.json` `files: ["dist", "README.md", "LICENSE.md"]`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Migrated test files behavioral drift | mitigate | Byte-for-byte verified-identical helper body across all 4 source files (04-RESEARCH §Open Questions resolution 8); `npm test -- test/replay` regression check in Task 2's verify ensures zero behavior delta |
| T-04-01-02 | Information Disclosure | `test/_helpers/` accidentally shipped to consumers | accept | `package.json` `files` array whitelists ONLY `dist/`, `README.md`, `LICENSE.md` — `test/` is excluded. Verified by inspecting current `package.json:26-30`. |
</threat_model>

<verification>
- `npm test -- test/replay` reports the same pass/skip counts as before the refactor (Phase 3 baseline: 77 passed, 2 skipped per 03-VERIFICATION.md).
- `tsc --noEmit -p tsconfig.test.json` exits 0 (no type regressions).
- `grep -rn "function fakeAwareSleep" test/replay/ test/_helpers/` returns exactly ONE match — the export declaration in `test/_helpers/fake-aware-sleep.ts`.
- `grep -rln "from '\\.\\./_helpers/fake-aware-sleep\\.js'" test/replay/` matches all 4 files.
</verification>

<success_criteria>
- Helper file exists at `test/_helpers/fake-aware-sleep.ts` with the verbatim body from Phase 3 test files.
- All 4 Phase 3 test files (`scheduler.test.ts`, `abort.test.ts`, `replay.test.ts`, `loop.test.ts`) import from the shared location.
- Local `function fakeAwareSleep(...)` definitions removed from all 4 files.
- `npm test -- test/replay` passes with zero behavioral regression vs the Phase 3 baseline (77 passed, 2 skipped).
- Phase 3 followup IN-01 closed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-faketransport-public-api/04-01-SUMMARY.md` documenting:
- Helper file created at `test/_helpers/fake-aware-sleep.ts`
- 4 Phase 3 test files migrated
- Test counts before/after (must match)
- Phase 3 followup IN-01 closed
- Wave 2's `test/transport/fake-transport.test.ts` (later plan) can now import from the same helper
</output>
