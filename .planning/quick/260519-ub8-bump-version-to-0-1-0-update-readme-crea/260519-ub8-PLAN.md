---
phase: 260519-ub8-bump-version-to-0-1-0-update-readme-crea
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
autonomous: false
requirements:
  - RELEASE-0.1.0
must_haves:
  truths:
    - "`package.json` reports version `0.1.0`"
    - "`CHANGELOG.md` exists at repo root in Keep a Changelog format with a `## [0.1.0] - 2026-05-19` section"
    - "`README.md` reflects shipped v0.1.0 status (not 'Phase 1 in progress')"
    - "An annotated git tag `v0.1.0` exists locally pointing at the release commit"
    - "After the human gate, the release commit and `v0.1.0` tag are pushed to `origin`"
    - "After the human gate, a GitHub release `v0.1.0` exists at `VeloWorld/trainer-sim` with the CHANGELOG entry as its body"
  artifacts:
    - path: "package.json"
      provides: "version field set to 0.1.0"
      contains: '"version": "0.1.0"'
    - path: "CHANGELOG.md"
      provides: "Keep a Changelog v0.1.0 release notes summarizing phases 01–05"
      contains: "## [0.1.0] - 2026-05-19"
    - path: "README.md"
      provides: "Status section + install snippet reflecting v0.1.0 (git-ref consumption)"
      contains: "v0.1.0"
  key_links:
    - from: "annotated tag v0.1.0"
      to: "release commit on main"
      via: "git tag -a v0.1.0 -m '...'"
      pattern: "git tag -a v0\\.1\\.0"
    - from: "GitHub release v0.1.0"
      to: "CHANGELOG.md [0.1.0] section"
      via: "gh release create --notes-file"
      pattern: "gh release create v0\\.1\\.0"
---

<objective>
Mark the v0.1.0 milestone for trainer-sim. Phases 01–05 are complete and Phase 05's
VeloWorld end-to-end validation makes the v1 surface real-world proven, so the library
crosses the threshold from `0.0.1` placeholder to its first published milestone tag.

Purpose: Give consumers (VeloWorld today, future cycling-app developers tomorrow) a
stable git-ref they can pin against, with a human-readable changelog explaining what
shipped and a GitHub release page that surfaces the same notes.

Output:
- `package.json` version bumped `0.0.1` → `0.1.0`
- `CHANGELOG.md` (new file, Keep a Changelog 1.1.0 format) with `[0.1.0] - 2026-05-19` entry
- `README.md` updated to reflect shipped status and v0.1.0 git-ref install snippet
- Single release commit on `main`
- Annotated git tag `v0.1.0` (local)
- After human gate: pushed commit + tag, GitHub release at `VeloWorld/trainer-sim`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@PROJECT.md
@README.md
@package.json
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-vendored-ftms-codec/01-05-SUMMARY.md
@.planning/phases/02-fit-loader-normalization/02-05-SUMMARY.md
@.planning/phases/03-replay-engine/03-04-SUMMARY.md
@.planning/phases/04-faketransport-public-api/04-06-SUMMARY.md
@.planning/phases/05-veloworld-end-to-end-validation/05-04-SUMMARY.md

<authoritative_sources>
- ROADMAP.md is the authoritative list of phases, goals, and success criteria for the
  CHANGELOG. Each phase's terminal SUMMARY.md (highest plan number) gives the final
  set of provided artifacts and decisions.
- The git remote is `git@github.com:VeloWorld/trainer-sim.git` — `gh release create`
  must target this repo. `gh auth status` is already green.
- Today's date for the CHANGELOG header is 2026-05-19.
- The library is currently consumed via git-ref (no npm publish in scope per
  README's existing posture). Install snippet should reflect that.
</authoritative_sources>

<keep_a_changelog_skeleton>
The CHANGELOG.md must follow https://keepachangelog.com/en/1.1.0/ exactly:

```
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

## [0.1.0] - 2026-05-19

### Added
- ...

### Changed
- ...

### Fixed
- ...

[Unreleased]: https://github.com/VeloWorld/trainer-sim/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/VeloWorld/trainer-sim/releases/tag/v0.1.0
```

Section content (Added) MUST cover, at minimum, one bullet per phase, derived from
ROADMAP.md goals + terminal SUMMARY.md `provides` lists:

- Phase 1 — Vendored FTMS IndoorBikeData encoder (`encodeIndoorBikeData`,
  `IndoorBikeRecord` exported from package root); spec-cited MIT decoder fixture;
  three-gate verification (byte fixtures + round-trip + nRF Connect manual).
- Phase 2 — FIT loader & normalization: `loadFitFromPath`, `loadFitFromBuffer`,
  `RideRecord` type, `FitLoadError` + 4 concrete subclasses
  (`InvalidFitHeaderError`, `FitTruncatedError`, `FitCrcError`,
  `NoRecordMessagesError`). `fit-file-parser@~3.0.0` (MIT) chosen behind
  `FitRecordSource` seam.
- Phase 3 — Drift-corrected replay engine: `Replay` class with `speed`, `loop`,
  `maxEmissionHz`, `AbortController` cancellation, awaitable `completed` promise.
- Phase 4 — `createFakeTransport(config)` factory + `ITrainerTransport` public
  contract; dual ESM/CJS publish via tsup; publint + attw clean; CI on macOS +
  Ubuntu, Node 24.
- Phase 5 — VeloWorld end-to-end validation green on macOS + Linux; v1 surface
  proven against a real consumer.

Place release-engineering items (CI, dual-publish, validate scripts) under `### Added`
unless they replace something — there's nothing to put under `### Changed` or
`### Fixed` for a 0.0.1 → 0.1.0 first-release entry, so those subsections may be
omitted from the 0.1.0 block per Keep a Changelog convention.
</keep_a_changelog_skeleton>

<readme_target_state>
The current README is 19 lines and says "Status: Phase 1 in progress." After this plan
the README should:

1. Keep the existing tagline, two-modes section, and link to ROADMAP.
2. Replace the Status section with `Status: v0.1.0 — first milestone (FakeTransport
   path; BLE peripheral deferred to v2).`
3. Add an Install section reflecting git-ref consumption, e.g.:

   ```
   ## Install

   trainer-sim is consumed by git-ref (no npm publish yet). Pin to the v0.1.0 tag:

       npm install github:VeloWorld/trainer-sim#v0.1.0
   ```

4. Add a "What's shipped in v0.1.0" section with 5 bullets, one per phase, mirroring
   the CHANGELOG `### Added` list but in a single short sentence each. Link to
   `CHANGELOG.md` for the full list.
5. Add a single CI badge line near the top (optional but expected for a milestone
   release):

       ![CI](https://github.com/VeloWorld/trainer-sim/actions/workflows/ci.yml/badge.svg)

Do NOT touch the existing "Two modes" or ROADMAP-link sections beyond what's listed
above.
</readme_target_state>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version + create CHANGELOG.md</name>
  <files>package.json, CHANGELOG.md</files>
  <action>
Bump `package.json` `"version"` from `"0.0.1"` to `"0.1.0"` (single-line edit; do not
touch any other field, including `engines`, `exports`, or `dependencies`).

Then create `CHANGELOG.md` at the repo root following the Keep a Changelog 1.1.0
skeleton in `<keep_a_changelog_skeleton>` above. Populate the `## [0.1.0] - 2026-05-19`
section's `### Added` subsection with one bullet per shipped phase, derived from
ROADMAP.md (phase goals + plan list) and each phase's terminal `*-SUMMARY.md`
(`provides` block). Use the per-phase prose given in `<keep_a_changelog_skeleton>` as
the seed; expand each into 1–3 sub-bullets if useful, but keep total length under ~60
lines for the v0.1.0 block. Include the two reference-link lines at the bottom
(`[Unreleased]` compare URL and `[0.1.0]` tag URL) targeting
`https://github.com/VeloWorld/trainer-sim`.

Do NOT include `### Changed` or `### Fixed` subsections in the 0.1.0 block — this is
the first release. Keep the empty `## [Unreleased]` header.
  </action>
  <verify>
    <automated>node -e "const v=require('./package.json').version; if(v!=='0.1.0'){console.error('version is',v);process.exit(1)}" && grep -F '## [0.1.0] - 2026-05-19' CHANGELOG.md && grep -F 'Keep a Changelog' CHANGELOG.md && grep -cE '^- ' CHANGELOG.md | awk '{if($1<5){print "expected at least 5 bullets, got",$1; exit 1}}'</automated>
  </verify>
  <done>
    - `package.json` version is `0.1.0` (no other fields touched)
    - `CHANGELOG.md` exists with Keep a Changelog header, an `## [Unreleased]` block,
      a `## [0.1.0] - 2026-05-19` block whose `### Added` covers all five phases, and
      the two reference-link lines at the bottom
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md for v0.1.0</name>
  <files>README.md</files>
  <action>
Update `README.md` to reflect shipped status per `<readme_target_state>`:

1. Add a CI badge line after the H1.
2. Replace the existing "Status: Phase 1 in progress." line with
   `Status: v0.1.0 — first milestone (FakeTransport path; BLE peripheral deferred to v2).`
3. Add an `## Install` section after the Status section showing the git-ref pin
   command `npm install github:VeloWorld/trainer-sim#v0.1.0` in a fenced code block.
4. Add a `## What's shipped in v0.1.0` section listing the 5 per-phase bullets in the
   same order as the CHANGELOG `### Added` block (one short sentence per phase).
   Close that section with a link line: `See [CHANGELOG.md](./CHANGELOG.md) for the
   full release notes.`
5. Leave the existing "Two modes" section and the ROADMAP link unchanged.

Do NOT add unrelated polish (no logos, no contributing section, no LICENSE expansion).
The diff should be additive plus the Status-line replacement; existing prose is
otherwise preserved.
  </action>
  <verify>
    <automated>grep -F 'v0.1.0 — first milestone' README.md && grep -F 'github:VeloWorld/trainer-sim#v0.1.0' README.md && grep -F "What's shipped in v0.1.0" README.md && grep -F 'CHANGELOG.md' README.md && ! grep -F 'Phase 1 in progress' README.md</automated>
  </verify>
  <done>
    - README contains the new Status line, install snippet pointing at the
      `v0.1.0` git-ref, the "What's shipped in v0.1.0" section with 5 bullets,
      and a link to CHANGELOG.md
    - The old "Phase 1 in progress" line is gone
    - "Two modes" section and ROADMAP link are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit release artifacts and create annotated tag v0.1.0 (local only)</name>
  <files>(no source edits — git operations only)</files>
  <action>
Stage and commit the three files modified above (no others). Do NOT use `git add -A`
or `git add .`; stage only the explicit paths.

```
git add package.json CHANGELOG.md README.md
git commit -m "$(cat <<'EOF'
chore(release): v0.1.0

Bump version 0.0.1 -> 0.1.0 to mark the first milestone. Phases 01-05 are
complete and Phase 05's VeloWorld end-to-end validation makes the v1 surface
real-world proven.

See CHANGELOG.md [0.1.0] for the full set of shipped artifacts.
EOF
)"
```

Then create an ANNOTATED tag (NOT lightweight) on this commit. The tag message body
should reference the CHANGELOG entry. Use a HEREDOC for the message:

```
git tag -a v0.1.0 -m "$(cat <<'EOF'
v0.1.0 — first milestone

trainer-sim's first published milestone. Ships:
  - Vendored FTMS IndoorBikeData encoder (Phase 1)
  - FIT loader + normalization (Phase 2)
  - Drift-corrected replay engine (Phase 3)
  - createFakeTransport public API, dual ESM/CJS (Phase 4)
  - VeloWorld end-to-end validation green on macOS + Linux (Phase 5)

See CHANGELOG.md [0.1.0] - 2026-05-19 for the full release notes.
EOF
)"
```

DO NOT push the commit or tag yet — Task 4 is a checkpoint that gates the push and
the GitHub release. The branch and tag remain local until the human approves.

If `git tag v0.1.0` already exists locally (rerun), abort with a clear message
instructing the operator to delete it manually with `git tag -d v0.1.0` rather than
silently overwriting — this is a release tag and overwriting must be deliberate.
  </action>
  <verify>
    <automated>git log -1 --format='%s' | grep -F 'chore(release): v0.1.0' && git tag -l v0.1.0 | grep -F v0.1.0 && git for-each-ref refs/tags/v0.1.0 --format='%(objecttype)' | grep -F tag && git diff --quiet HEAD -- package.json CHANGELOG.md README.md</automated>
  </verify>
  <done>
    - Single commit `chore(release): v0.1.0` on the current branch with exactly the
      three files (package.json, CHANGELOG.md, README.md) changed
    - `git tag -l v0.1.0` returns `v0.1.0`
    - `git for-each-ref refs/tags/v0.1.0 --format='%(objecttype)'` returns `tag`
      (annotated, not `commit`)
    - Working tree clean against HEAD for the three files
    - Nothing pushed to `origin` yet
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human gate before pushing to origin and publishing GitHub release</name>
  <what-built>
    - Release commit `chore(release): v0.1.0` on local `main`
    - Annotated tag `v0.1.0` pointing at that commit
    - `package.json` at `0.1.0`, `CHANGELOG.md` with `[0.1.0] - 2026-05-19`, README
      updated to reflect shipped status
    - Nothing has been pushed; no GitHub release exists yet
  </what-built>
  <how-to-verify>
1. Verify the commit is what you expect:

       git show --stat v0.1.0

   Confirm: only `package.json`, `CHANGELOG.md`, `README.md` are in the diff; the
   commit message is `chore(release): v0.1.0`; the tag points at the same commit as
   `HEAD`.

2. Verify the tag is annotated:

       git for-each-ref refs/tags/v0.1.0 --format='%(objecttype) %(taggername) %(subject)'

   Expect output starting with `tag ` (not `commit `).

3. Read the CHANGELOG entry once more:

       sed -n '/## \[0.1.0\]/,/## \[/{/## \[/!p; /^## \[0.1.0\]/p}' CHANGELOG.md

   Confirm the per-phase bullets are accurate and you're happy publishing them.

4. Decide:
   - If everything looks right, type `approved` and Task 5 will push + release.
   - If you want changes, describe them and the run will pause for fixes before
     pushing. The push and the release are partially irreversible (the tag and the
     GitHub release page become visible to consumers immediately on push).
  </how-to-verify>
  <resume-signal>Type "approved" to push and publish, or describe required changes.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Push commit + tag and create GitHub release</name>
  <files>(no source edits — remote git operations + gh release)</files>
  <action>
This task runs ONLY after Task 4 returns `approved`.

1. Push the release commit on the current branch:

       git push origin HEAD

   (If the current branch is `main`, this is `git push origin main`. The orchestrator
   should not assume a branch name — use `HEAD` or read the branch from `git rev-parse
   --abbrev-ref HEAD` and push that explicitly.)

2. Push the annotated tag:

       git push origin v0.1.0

   Do NOT use `git push --tags` — that pushes every local tag. Push only `v0.1.0` so
   any unrelated stale local tags don't accidentally land on the remote.

3. Extract the v0.1.0 section from CHANGELOG.md into a temporary notes file. Use awk
   so the extraction is exact and doesn't include the `## [Unreleased]` header or the
   reference-link block:

       awk '/^## \[0.1.0\]/{flag=1; next} /^## \[/{flag=0} flag' CHANGELOG.md > /tmp/trainer-sim-v0.1.0-notes.md

   Confirm the file is non-empty (`test -s /tmp/trainer-sim-v0.1.0-notes.md`) before
   the next step.

4. Create the GitHub release. The repo is `VeloWorld/trainer-sim` (per `git remote -v`
   in the discovery step). `gh auth status` is already green.

       gh release create v0.1.0 \
         --repo VeloWorld/trainer-sim \
         --title "v0.1.0 — first milestone" \
         --notes-file /tmp/trainer-sim-v0.1.0-notes.md

   Do NOT pass `--draft` (the human gate already approved publication). Do NOT pass
   `--prerelease` (this is a real milestone, not a pre-release). Do NOT attach build
   artifacts — the package is git-ref consumed; consumers fetch source.

5. Clean up the temp notes file (`rm /tmp/trainer-sim-v0.1.0-notes.md`).

6. Print the release URL returned by `gh release create` so the operator has a direct
   link.

If `gh release create` fails because the release already exists (rerun), abort with a
clear message instructing the operator to delete it manually
(`gh release delete v0.1.0 --repo VeloWorld/trainer-sim`) rather than silently
upserting — same reasoning as the tag in Task 3.
  </action>
  <verify>
    <automated>git ls-remote --tags origin v0.1.0 | grep -F refs/tags/v0.1.0 && gh release view v0.1.0 --repo VeloWorld/trainer-sim --json tagName,name,isDraft,isPrerelease | grep -F '"tagName":"v0.1.0"' && gh release view v0.1.0 --repo VeloWorld/trainer-sim --json isDraft | grep -F '"isDraft":false'</automated>
  </verify>
  <done>
    - `git ls-remote --tags origin v0.1.0` shows the tag on origin
    - `git ls-remote origin <branch>` includes the release commit on the pushed branch
    - `gh release view v0.1.0 --repo VeloWorld/trainer-sim` returns a non-draft,
      non-prerelease release titled `v0.1.0 — first milestone`
    - The release body matches the CHANGELOG `[0.1.0]` section
    - Operator has the release URL printed in the run output
  </done>
</task>

</tasks>

<verification>
After Task 5 completes, the milestone state is:

- Local: release commit on `main` (or whatever branch was current), annotated tag
  `v0.1.0` pointing at it, working tree clean.
- Remote: same commit and tag pushed to `origin`; GitHub release page live at
  `https://github.com/VeloWorld/trainer-sim/releases/tag/v0.1.0`.
- Files: `package.json` at `0.1.0`, `CHANGELOG.md` with the v0.1.0 entry, README
  reflecting shipped status — all three under git, no untracked release artifacts.

Phase-wide automated check (run from repo root):

```
node -p "require('./package.json').version" && \
git tag -l v0.1.0 | grep -F v0.1.0 && \
git for-each-ref refs/tags/v0.1.0 --format='%(objecttype)' | grep -F tag && \
git ls-remote --tags origin v0.1.0 | grep -F refs/tags/v0.1.0 && \
gh release view v0.1.0 --repo VeloWorld/trainer-sim --json tagName | grep -F '"tagName":"v0.1.0"'
```

All five lines must succeed.
</verification>

<success_criteria>
- `package.json` reports `0.1.0`
- `CHANGELOG.md` exists, follows Keep a Changelog 1.1.0, has a `[0.1.0] - 2026-05-19`
  entry covering phases 01–05
- `README.md` reflects v0.1.0 shipped status with a git-ref install snippet
- One release commit `chore(release): v0.1.0` on `main`
- Annotated tag `v0.1.0` exists locally AND on `origin`
- GitHub release `v0.1.0` is published (non-draft, non-prerelease) at
  `VeloWorld/trainer-sim` with the CHANGELOG entry as its body
- The human gate (Task 4) was reached and explicitly approved before any push or
  release happened
</success_criteria>

<output>
After completion, create
`.planning/quick/260519-ub8-bump-version-to-0-1-0-update-readme-crea/260519-ub8-SUMMARY.md`
recording: final version, tag SHA, GitHub release URL, files modified, and the
release-commit hash.
</output>
