---
phase: 03-replay-engine
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - test/replay/scheduler.test.ts
  - test/replay/replay.test.ts
  - test/replay/abort.test.ts
  - test/replay/loop.test.ts
autonomous: true
requirements:
  - REPL-01
  - REPL-02
  - REPL-04
  - REPL-05
  - REPL-06
tags:
  - replay
  - tests
  - fake-timers
  - vitest

must_haves:
  truths:
    - "test/replay/scheduler.test.ts exercises the pure runScheduler async fn under vi.useFakeTimers(): emission cadence (REPL-01), speed multiplier (REPL-02), speed=Infinity + maxEmissionHz cap (REPL-02), empty-records guard (RESEARCH §Pitfall 9), Infinity-speed-on-cursor-0 (RESEARCH §Pitfall 8)"
    - "test/replay/replay.test.ts exercises the Replay class lifecycle: completed Promise resolves on stop-at-end (REPL-05), single-subscriber lock (D-REPL-11), single-use throw (D-REPL-07), missing-subscriber throw (RESEARCH §Pitfall 10), pre-aborted-external-signal throw (RESEARCH §Pitfall 4)"
    - "test/replay/abort.test.ts exercises clean cancellation (REPL-06): zero emissions in 100 ms after stop(), replay.completed rejects with AbortError, external signal abort path, AbortSignal.any composition"
    - "test/replay/loop.test.ts exercises loop boundary (REPL-04, D-REPL-06): three iterations without drift accumulation (each iteration's first-record offset within ±5 ms of expected), loop=true never hits 'done' state until aborted"
    - "All four test files use `beforeEach(() => vi.useFakeTimers()); afterEach(() => vi.useRealTimers());` and `await vi.advanceTimersByTimeAsync(...)` (NEVER the sync variant — RESEARCH §Pitfall 5)"
    - "All four test files import Replay/runScheduler from `'../../src/replay/replay.js'` and `'../../src/replay/scheduler.js'` directly (NOT through `'../../src/index.js'`) — D-REPL-12 keeps the public surface unchanged"
    - "All four test files defensively assert that `vi.advanceTimersByTimeAsync(N)` advances `globalThis.performance.now()` by N (RESEARCH Assumptions A1) — failure surfaces as a setup-level test fail, not a mysterious cascade"
    - "Tests group cases under `describe('Group N — D-REPL-XX: <behavior>')` per PATTERNS §Group-by-decision describe blocks"
  artifacts:
    - path: "test/replay/scheduler.test.ts"
      provides: "Pure-fn unit tests for runScheduler; covers REPL-01, REPL-02, RESEARCH §Pitfalls 8 + 9; vi.useFakeTimers throughout"
      min_lines: 100
    - path: "test/replay/replay.test.ts"
      provides: "Lifecycle unit tests for Replay class; covers REPL-05, D-REPL-07/11, RESEARCH §Pitfalls 4 + 10"
      min_lines: 90
    - path: "test/replay/abort.test.ts"
      provides: "Cancellation invariant tests; covers REPL-06, D-REPL-09/10, AbortSignal.any composition"
      min_lines: 80
    - path: "test/replay/loop.test.ts"
      provides: "Loop boundary tests; covers REPL-04, D-REPL-06, RESEARCH §Pitfall 7"
      min_lines: 70
  key_links:
    - from: "test/replay/*.test.ts"
      to: "../../src/replay/replay.js (Replay class)"
      via: "named import of Replay from internal path (D-REPL-12 keeps it out of src/index.ts)"
      pattern: "from\\s+['\"]\\.\\./\\.\\./src/replay/replay\\.js['\"]"
    - from: "test/replay/scheduler.test.ts"
      to: "../../src/replay/scheduler.js (runScheduler)"
      via: "named import of runScheduler from internal path"
      pattern: "from\\s+['\"]\\.\\./\\.\\./src/replay/scheduler\\.js['\"]"
    - from: "test/replay/*.test.ts"
      to: "vitest fake timers"
      via: "vi.useFakeTimers + vi.advanceTimersByTimeAsync (NEVER the sync variant — Pitfall 5)"
      pattern: "vi\\.advanceTimersByTimeAsync"
---

<objective>
Wave 3 (parallel branch A): the unit-test suite for Phase 3, covering five of
the six requirements (REPL-01, REPL-02, REPL-04, REPL-05, REPL-06) under
vitest fake timers. The drift-correction acceptance gate REPL-03 is verified
by plan 03-04's real-clock soak suite — this plan does NOT touch real timers.

Four focused test files. Each file is one concern, one fake-timer regime, and
imports the SUT directly from `src/replay/...js` (NOT through `src/index.ts`)
because D-REPL-12 keeps the replay surface internal in this phase.

1. **scheduler.test.ts** — pure runScheduler tests:
   - Group 1 — REPL-01: emission cadence at FIT-relative timestamps.
   - Group 2 — REPL-02: speed multiplier (1x, 2x, 0.5x).
   - Group 3 — REPL-02: speed=Infinity + maxEmissionHz cap (RESEARCH §Code Examples 4).
   - Group 4 — RESEARCH §Pitfall 8: speed=Infinity at cursor=0 single-record case.
   - Group 5 — RESEARCH §Pitfall 9: empty-records early return.
   - Setup-sanity test: `vi.advanceTimersByTimeAsync(100)` advances `performance.now()` by ~100 (RESEARCH Assumptions A1).

2. **replay.test.ts** — Replay class lifecycle:
   - Group 1 — D-REPL-08 + REPL-05: completed Promise resolves on stop-at-end.
   - Group 2 — D-REPL-11: single-subscriber lock (second onRecord throws); subscriber-not-set throw at start (RESEARCH §Pitfall 10).
   - Group 3 — D-REPL-07: single-use lock (second start() throws after done).
   - Group 4 — RESEARCH §Pitfall 4: pre-aborted external signal throws synchronously from start().
   - Group 5 — D-REPL-10 partial: stop() before start() is a no-op (idempotent stop).

3. **abort.test.ts** — REPL-06 cancellation:
   - Group 1 — REPL-06: stop() mid-replay → zero emissions in 100 ms wait, completed rejects with AbortError (RESEARCH §Code Examples 2).
   - Group 2 — D-REPL-09: external signal abort path → completed rejects.
   - Group 3 — RESEARCH §Open Question 3: external + internal abort race (whichever fires first wins, both work).
   - Group 4 — single pending tick → no leftover timers / ghost emissions after abort (D-REPL-10).

4. **loop.test.ts** — REPL-04 loop boundary:
   - Group 1 — D-REPL-06 + REPL-04: three iterations of a 3-record/1-second fixture, each iteration's first-emission offset within ±5 ms of expected (RESEARCH §Code Examples 3).
   - Group 2 — RESEARCH §Pitfall 7 negative test: a hypothetical scheduler that DIDN'T re-base would burst-emit iteration 2 — but our scheduler DOES re-base, so iteration 2's emissions are spread across wall-clock time. Assert by checking inter-emission intervals within iteration 2.
   - Group 3 — loop=true does NOT transition to 'done' (currentState stays 'running' across iterations until stop() is called).

Implements (test side of):
- REPL-01 (scheduler.test.ts Group 1)
- REPL-02 (scheduler.test.ts Groups 2, 3, 4)
- REPL-04 (loop.test.ts all groups)
- REPL-05 (replay.test.ts Group 1)
- REPL-06 (abort.test.ts all groups)

Honors locked decisions: D-REPL-06 (loop re-base), D-REPL-07 (single-use),
D-REPL-08 (Promise-first), D-REPL-09 (AbortController), D-REPL-10 (no
emissions after abort), D-REPL-11 (single subscriber), D-REPL-12 (internal
import path), D-REPL-14 (vi.useFakeTimers throughout).

Purpose: Wave 3 verifies the Phase 3 contract end-to-end at the unit level.
Plan 03-04 (parallel) covers the REPL-03 drift gate via real-clock soak.

Output: Four vitest test files under `test/replay/`. All run under
`npm test` (CI tier; no env vars required). ~340 LOC total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/03-replay-engine/03-CONTEXT.md
@.planning/phases/03-replay-engine/03-RESEARCH.md
@.planning/phases/03-replay-engine/03-PATTERNS.md
@.planning/phases/03-replay-engine/03-01-SUMMARY.md
@.planning/phases/03-replay-engine/03-02-SUMMARY.md
@src/replay/scheduler.ts
@src/replay/replay.ts
@src/replay/types.ts
@src/types.ts
@test/fit/normalize.test.ts
@test/fit/perf.test.ts
</context>

<interfaces>
<!-- Contracts the executor consumes from plans 03-01 and 03-02. -->

From `src/replay/scheduler.ts` (plan 03-01):
```typescript
export async function runScheduler(input: SchedulerInput): Promise<void>;
// SchedulerInput is internal to scheduler.ts (not exported). Tests construct
// it inline. Required fields:
//   records: ReadonlyArray<RideRecord>
//   speed: number
//   loop: boolean
//   maxEmissionHz: number
//   signal: AbortSignal
//   emit: (record: RideRecord) => void
//   getNow: () => number
```

From `src/replay/replay.ts` (plan 03-02):
```typescript
export class Replay {
  constructor(config: ReplayConfig);
  get completed(): Promise<void>;
  get currentState(): ReplayState;   // 'idle' | 'running' | 'done' | 'aborted'
  onRecord(handler: (r: RideRecord) => void): () => void;
  start(config?: { signal?: AbortSignal }): void;
  stop(): void;
}
```

Vitest 4 fake-timer recipe (RESEARCH §Vitest fake-timer interaction recipe):
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';

beforeEach(() => { vi.useFakeTimers(); });   // fakes setTimeout AND performance.now()
afterEach(() => { vi.useRealTimers(); });

// ALWAYS use the Async variant — NEVER vi.advanceTimersByTime() (sync)
await vi.advanceTimersByTimeAsync(500);
```

Key Vitest-fake-timer assertions:
- `vi.useFakeTimers()` (no args) fakes setTimeout, clearTimeout, AND `globalThis.performance.now()` (RESEARCH Assumptions A1; @sinonjs/fake-timers default toFake set).
- The scheduler reads `getNow` (injected) which the production wiring binds to `() => globalThis.performance.now()`. Under fake timers, `globalThis.performance.now()` IS faked.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write test/replay/scheduler.test.ts (REPL-01, REPL-02, RESEARCH §Pitfalls 8 + 9)</name>
  <files>test/replay/scheduler.test.ts</files>
  <read_first>
    - .planning/phases/03-replay-engine/03-RESEARCH.md (§Vitest fake-timer interaction recipe — lines 360-408 the canonical pattern; §Code Examples 4 — REPL-02 speed=Infinity recipe lines 736-754; §Common Pitfalls 5 (advanceTimersByTimeAsync), 6 (performance.now binding), 8 (Infinity NaN), 9 (empty records))
    - .planning/phases/03-replay-engine/03-CONTEXT.md (D-REPL-04 Infinity, D-REPL-05 maxEmissionHz, D-REPL-12 import path, D-REPL-14 fake-timer discipline)
    - .planning/phases/03-replay-engine/03-PATTERNS.md (§test/replay/scheduler.test.ts — analog test/fit/normalize.test.ts; §Group-by-decision describe blocks lines 309-323; §Test-file header lines 268-282)
    - test/fit/normalize.test.ts (the canonical group-by-decision test structure to mirror)
    - src/replay/scheduler.ts (the SUT — runScheduler exact signature, Pitfall guards, behavior)
    - src/types.ts (RideRecord — the record shape tests construct inline)
  </read_first>
  <action>
    Create `test/replay/scheduler.test.ts`. The `test/replay/` directory does
    not exist — `mkdir -p test/replay` first or create via the editor.

    File header (mirror PATTERNS lines 268-282 — the `test/fit/normalize.test.ts:1-30` shape). Sections:
    - One-line summary naming "Phase 3 Plan 03-03 Task 1 — unit tests for the pure runScheduler function".
    - "SUT" block stating the SUT is `src/replay/scheduler.ts` (built in plan 03-01) and that direct unit-test import is acceptable because D-REPL-12 keeps the replay surface internal.
    - "Locked decisions / requirements exercised" block listing REPL-01, REPL-02, D-REPL-04, D-REPL-14, RESEARCH §Pitfalls 5, 8, 9.

    Imports — exact:
    `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
    `import { runScheduler } from '../../src/replay/scheduler.js';`
    `import type { RideRecord } from '../../src/types.js';`

    Test structure — single `describe('runScheduler — fake-timer unit tests', () => { ... })` outer block. Inside:
    - `beforeEach(() => { vi.useFakeTimers(); });`
    - `afterEach(() => { vi.useRealTimers(); });`

    Setup-sanity test (RESEARCH Assumptions A1 — fails immediately if Vitest 4 drops `performance` from default toFake):
    - `it('sanity: vi.advanceTimersByTimeAsync advances globalThis.performance.now', async () => { ... })`
    - Capture `t0 = globalThis.performance.now()`; `await vi.advanceTimersByTimeAsync(100)`; capture `t1`. Assert `t1 - t0 >= 99` AND `<= 101` (±1ms tolerance for fake-timer arithmetic).

    Group 1 — REPL-01: emission cadence at FIT-relative timestamps. `describe('Group 1 — REPL-01: emission cadence at FIT-relative timestamps', () => { ... })`. Required test:
    - 3-record array `[{timestamp: 1000}, {timestamp: 1500}, {timestamp: 2200}]` at speed=1, loop=false, maxEmissionHz=1000.
    - Construct AbortController, run scheduler with emit-pushing into an array, advance fake time, assert emission count after 500ms (=2: t=0 and t=+500), assert count after additional 700ms (=3 total: t=+1200 fired).
    - Use the canonical pattern from RESEARCH §Vitest fake-timer interaction recipe lines 379-408.
    - After scheduler returns naturally (`await done`), assert `emitted.map(r => r.timestamp)` equals `[1000, 1500, 2200]`.

    Group 2 — REPL-02: speed multiplier. `describe('Group 2 — REPL-02: speed multiplier', () => { ... })`. Required tests:
    - 2-record array `[{timestamp: 0}, {timestamp: 1000}]` at speed=2. After advancing 500ms (= half the original 1-second gap at 2x speed), assert both records emitted.
    - Same array at speed=0.5. After 1500ms, assert only one emission; after 2000ms total, both emitted.

    Group 3 — REPL-02: speed=Infinity + maxEmissionHz cap (RESEARCH §Code Examples 4). `describe('Group 3 — REPL-02: speed=Infinity + maxEmissionHz cap', () => { ... })`. Required test:
    - 1000-record array at speed=Infinity, maxEmissionHz=100. Construct: `Array.from({length: 1000}, (_, i) => ({timestamp: i * 100}))`.
    - maxEmissionHz=100 → 10ms minimum delay → 1000 records × 10ms = 10000ms total wall-clock.
    - After `vi.advanceTimersByTimeAsync(10_000)` and `await done`, assert `count === 1000`.
    - Helper: define `function makeRecords(count: number, cadenceMs = 100): RideRecord[]` near the top of the file (PATTERNS §Helper functions defined inside the test file).

    Group 4 — RESEARCH §Pitfall 8: speed=Infinity at cursor=0 single-record case. `describe('Group 4 — RESEARCH §Pitfall 8: speed=Infinity at cursor=0', () => { ... })`. Required test:
    - 1-record array `[{timestamp: 5000}]` at speed=Infinity, maxEmissionHz=1000.
    - The scheduler's explicit guard (Step 4b in 03-01 task 2: `targetSinceStart = speed === Infinity ? 0 : ...`) makes this deterministic at 1ms.
    - Assert: after `vi.advanceTimersByTimeAsync(2)`, `count === 1`.

    Group 5 — RESEARCH §Pitfall 9: empty-records early return. `describe('Group 5 — RESEARCH §Pitfall 9: empty-records early return', () => { ... })`. Required test:
    - 0-record array. Scheduler should return immediately. Assert `count === 0` AND `done` resolves within `await Promise.resolve()`.

    Forbidden in this file: `vi.advanceTimersByTime(...)` (sync — RESEARCH §Pitfall 5); imports through `'../../src/index.js'` (D-REPL-12); lifecycle / class tests (those go in replay.test.ts); real-clock assertions (those go in plan 03-04's soak suite).

    Commit per Phase 1/2 atomic-commit pattern:
    `test(03-03): scheduler.test.ts — REPL-01/02 + Pitfalls 8 + 9`
  </action>
  <acceptance_criteria>
    - `test/replay/scheduler.test.ts` exists: `test -f test/replay/scheduler.test.ts`.
    - Imports runScheduler from internal path (NOT src/index.js — D-REPL-12): `grep -cE "import\s+\\{\s*runScheduler\s*\\}\s+from\s+['\"]\\.\\./\\.\\./src/replay/scheduler\\.js['\"]" test/replay/scheduler.test.ts` reports 1.
    - Does NOT import from public surface src/index.js: `grep -cE "from\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]" test/replay/scheduler.test.ts` reports 0.
    - Imports vitest fake-timer triplet: `grep -E "import\s+\\{[^}]*vi[^}]*\\}\s+from\s+['\"]vitest['\"]" test/replay/scheduler.test.ts` exits 0 AND `grep -F 'beforeEach' test/replay/scheduler.test.ts` exits 0 AND `grep -F 'afterEach' test/replay/scheduler.test.ts` exits 0.
    - Uses vi.useFakeTimers in beforeEach and vi.useRealTimers in afterEach: `grep -F 'vi.useFakeTimers' test/replay/scheduler.test.ts` exits 0 AND `grep -F 'vi.useRealTimers' test/replay/scheduler.test.ts` exits 0.
    - Uses ASYNC fake-timer advancement (NEVER the sync variant — RESEARCH §Pitfall 5): `grep -cE 'vi\.advanceTimersByTimeAsync\s*\(' test/replay/scheduler.test.ts` reports >= 4 AND `grep -vE '^[[:space:]]*//' test/replay/scheduler.test.ts | grep -cE 'vi\.advanceTimersByTime\s*\(' | tr -d ' '` reports the SAME number as the Async variant count (sync calls only appear inside `Async`, never bare).
    - Includes the setup-sanity test asserting performance.now is faked (RESEARCH Assumptions A1): `grep -F 'globalThis.performance.now' test/replay/scheduler.test.ts` exits 0 AND `grep -iE 'sanity|advances' test/replay/scheduler.test.ts` exits 0.
    - All five test groups appear (Group 1 REPL-01, Group 2 REPL-02 multiplier, Group 3 REPL-02 Infinity, Group 4 Pitfall 8, Group 5 Pitfall 9): `grep -cE "Group\s+1\s+—" test/replay/scheduler.test.ts` reports 1 AND `grep -cE "Group\s+2\s+—" test/replay/scheduler.test.ts` reports 1 AND `grep -cE "Group\s+3\s+—" test/replay/scheduler.test.ts` reports 1 AND `grep -cE "Group\s+4\s+—" test/replay/scheduler.test.ts` reports 1 AND `grep -cE "Group\s+5\s+—" test/replay/scheduler.test.ts` reports 1.
    - Tests speed=Infinity (Groups 3 + 4): `grep -cF 'speed: Infinity' test/replay/scheduler.test.ts` reports >= 2.
    - Tests maxEmissionHz cap (Group 3): `grep -E 'maxEmissionHz\s*:\s*100' test/replay/scheduler.test.ts` exits 0.
    - vitest runs and passes: `npx vitest run test/replay/scheduler.test.ts`.
    - Whole pre-existing + new test suite still passes: `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/replay/scheduler.test.ts && npm test && grep -E "import\s+\{\s*runScheduler\s*\}\s+from\s+['\"]\.\./\.\./src/replay/scheduler\.js['\"]" test/replay/scheduler.test.ts && [ "$(grep -cE "from\s+['\"]\.\./\.\./src/index\.js['\"]" test/replay/scheduler.test.ts)" = "0" ] && grep -F 'vi.useFakeTimers' test/replay/scheduler.test.ts && grep -F 'vi.useRealTimers' test/replay/scheduler.test.ts && [ "$(grep -cE 'vi\.advanceTimersByTimeAsync\s*\(' test/replay/scheduler.test.ts)" -ge "4" ] && grep -F 'globalThis.performance.now' test/replay/scheduler.test.ts && [ "$(grep -cF 'speed: Infinity' test/replay/scheduler.test.ts)" -ge "2" ] && grep -E 'maxEmissionHz\s*:\s*100' test/replay/scheduler.test.ts && [ "$(grep -cE 'Group\s+[12345]\s+—' test/replay/scheduler.test.ts)" = "5" ]</automated>
  </verify>
  <done>
    `test/replay/scheduler.test.ts` ships and passes. Five test groups cover REPL-01 emission cadence, REPL-02 speed multiplier, REPL-02 speed=Infinity + maxEmissionHz cap (1000-record × 100Hz = 10s wall-clock), RESEARCH §Pitfall 8 (Infinity-on-cursor-0 single-record), RESEARCH §Pitfall 9 (empty-records early return). Setup-sanity test asserts vi.useFakeTimers fakes performance.now (Assumptions A1). vi.advanceTimersByTimeAsync used throughout. Imports from internal path only.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write test/replay/replay.test.ts (REPL-05 + D-REPL-07/08/11 + Pitfalls 4 + 10)</name>
  <files>test/replay/replay.test.ts</files>
  <read_first>
    - .planning/phases/03-replay-engine/03-RESEARCH.md (§Pattern: Replay class wrapping the scheduler — lines 477-525 the public surface; §Common Pitfalls 4 (pre-aborted), 10 (subscriber-not-set))
    - .planning/phases/03-replay-engine/03-CONTEXT.md (D-REPL-07 single-use, D-REPL-08 Promise-first, D-REPL-11 single-subscriber)
    - .planning/phases/03-replay-engine/03-PATTERNS.md (§test/replay/replay.test.ts — analog test/fit/normalize.test.ts + test/ftms/indoor-bike-data.test.ts; §Helper functions defined inside the test file lines 354-358 — define `makeRecords` helper)
    - .planning/phases/03-replay-engine/03-02-SUMMARY.md (the exact public surface of the Replay class, including currentState getter)
    - src/replay/replay.ts (the SUT — Replay class, especially throw messages)
    - src/replay/types.ts (ReplayState — 'idle' | 'running' | 'done' | 'aborted')
  </read_first>
  <action>
    Create `test/replay/replay.test.ts`. Same fake-timer setup as scheduler.test.ts.

    File header: mirror Task 1's header, swap SUT to `src/replay/replay.ts`, list locked decisions REPL-05, D-REPL-07, D-REPL-08, D-REPL-11, RESEARCH §Pitfalls 4 + 10.

    Imports — exact:
    `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
    `import { Replay } from '../../src/replay/replay.js';`
    `import type { RideRecord } from '../../src/types.js';`

    Helper at top of file:
    `function makeRecords(count: number, cadenceMs = 100): RideRecord[] { return Array.from({ length: count }, (_, i) => ({ timestamp: 1000 + i * cadenceMs })); }`

    Test structure: single outer `describe('Replay — fake-timer lifecycle tests', () => { ... })` with `vi.useFakeTimers()` / `vi.useRealTimers()` setup.

    Group 1 — D-REPL-08 + REPL-05: completed Promise resolves on stop-at-end. Required tests:
    - Construct Replay with 3 records over 200ms, speed=1, loop=false. Wire onRecord, start, advance time past end, assert `await replay.completed` resolves (does NOT reject) AND `replay.currentState === 'done'`.
    - Same scenario asserts emission count matches records length BEFORE completed resolves (the resolution semantics — completed only fires after the LAST record is emitted, not before).

    Group 2 — D-REPL-11: single-subscriber lock + RESEARCH §Pitfall 10: subscriber-not-set throw at start. Required tests:
    - `replay.onRecord(h1); expect(() => replay.onRecord(h2)).toThrow(/D-REPL-11|single.subscriber/i);`
    - Construct Replay; do NOT call onRecord; expect `replay.start()` to throw with a message naming D-REPL-11 or 'subscriber'.
    - Disposer returned from onRecord works — call dispose(), then onRecord(h2) succeeds (re-attach is allowed only after the slot is freed). NOTE: this depends on plan 03-02's lock interpretation. Per plan 03-02 task 1 step 3d, onRecord throws if state !== 'idle' regardless of subscriber slot freedom. If you find that re-attach is NOT allowed even after dispose because state is checked before subscriber slot, document the discovered behavior in 03-03-SUMMARY.md and assert the actual semantics (e.g., that re-attach after dispose() fails with a state-related throw rather than a subscriber-slot throw). Either contract is acceptable — what matters is the test asserts the actual behavior so future regressions are caught.

    Group 3 — D-REPL-07: single-use lock. Required tests:
    - Complete a stop-at-end run, then call `replay.start()` again → expect throw matching `/D-REPL-07|single.use|state/i`.
    - After stop() (mid-replay), call start() again → expect throw. `replay.currentState` should be 'aborted' at this point.

    Group 4 — RESEARCH §Pitfall 4: pre-aborted external signal throws synchronously. Required test:
    - Construct Replay, attach onRecord, construct external AbortController and call `controller.abort('reason X')` BEFORE start.
    - `expect(() => replay.start({ signal: controller.signal })).toThrow(/aborted|D-REPL-09/i);` — the throw is synchronous (Replay.start fail-fast guards per plan 03-02 task 1 step 3e guard 4).

    Group 5 — D-REPL-10 partial: stop() before start() is idempotent (no-op). Required tests:
    - Construct Replay, do NOT start, call `replay.stop()` repeatedly → no throw, currentState stays 'idle'.
    - After stop-at-end completion, calling stop() again is a no-op (currentState stays 'done').

    Forbidden: tests exercising REPL-06 cancellation mid-replay (those go in `abort.test.ts`); tests exercising REPL-04 loop boundary (those go in `loop.test.ts`); imports through `'../../src/index.js'`.

    Commit:
    `test(03-03): replay.test.ts — REPL-05 + D-REPL-07/08/11 + Pitfalls 4/10`
  </action>
  <acceptance_criteria>
    - `test/replay/replay.test.ts` exists: `test -f test/replay/replay.test.ts`.
    - Imports Replay from internal path (D-REPL-12): `grep -cE "import\s+\\{\s*Replay\s*\\}\s+from\s+['\"]\\.\\./\\.\\./src/replay/replay\\.js['\"]" test/replay/replay.test.ts` reports 1.
    - Does NOT import from src/index.js: `grep -cE "from\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]" test/replay/replay.test.ts` reports 0.
    - Uses vi.useFakeTimers / vi.useRealTimers and the Async timer advancement: `grep -F 'vi.useFakeTimers' test/replay/replay.test.ts` exits 0 AND `grep -F 'vi.advanceTimersByTimeAsync' test/replay/replay.test.ts` exits 0.
    - Defines a makeRecords helper for clean test setup (PATTERNS §Helper functions): `grep -F 'makeRecords' test/replay/replay.test.ts` exits 0.
    - Asserts `await replay.completed` resolves on stop-at-end (REPL-05): `grep -F 'replay.completed' test/replay/replay.test.ts` exits 0 AND `grep -E "currentState.*===\\s*['\"]done['\"]|toBe\\(\\s*['\"]done['\"]" test/replay/replay.test.ts` exits 0.
    - Asserts onRecord throws on second call (D-REPL-11): `grep -E "toThrow.*(D-REPL-11|single.?subscriber)" test/replay/replay.test.ts` exits 0.
    - Asserts start() throws when subscriber not set (RESEARCH §Pitfall 10): `grep -E "toThrow.*(subscriber|onRecord)" test/replay/replay.test.ts` exits 0.
    - Asserts start() throws on second call (D-REPL-07 single-use): `grep -E "toThrow.*(D-REPL-07|single.?use|state)" test/replay/replay.test.ts` exits 0.
    - Asserts pre-aborted signal causes synchronous throw from start() (RESEARCH §Pitfall 4): `grep -E "controller\.abort\\(" test/replay/replay.test.ts` exits 0 AND `grep -E "toThrow.*(aborted|D-REPL-09)" test/replay/replay.test.ts` exits 0.
    - Tests stop() idempotency (D-REPL-10 partial): `grep -F 'replay.stop()' test/replay/replay.test.ts` exits 0 AND (`grep -i 'idempotent' test/replay/replay.test.ts` exits 0 OR a Group 5 mention of stop-before-start exists).
    - All five test groups appear: `grep -cE 'Group\s+[12345]\s+—' test/replay/replay.test.ts` reports 5.
    - vitest runs and passes: `npx vitest run test/replay/replay.test.ts`.
    - Whole suite still passes: `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/replay/replay.test.ts && npm test && grep -E "import\s+\{\s*Replay\s*\}\s+from\s+['\"]\.\./\.\./src/replay/replay\.js['\"]" test/replay/replay.test.ts && [ "$(grep -cE "from\s+['\"]\.\./\.\./src/index\.js['\"]" test/replay/replay.test.ts)" = "0" ] && grep -F 'vi.useFakeTimers' test/replay/replay.test.ts && grep -F 'vi.advanceTimersByTimeAsync' test/replay/replay.test.ts && grep -F 'makeRecords' test/replay/replay.test.ts && grep -F 'replay.completed' test/replay/replay.test.ts && grep -E "toThrow" test/replay/replay.test.ts && grep -F 'replay.stop()' test/replay/replay.test.ts && [ "$(grep -cE 'Group\s+[12345]\s+—' test/replay/replay.test.ts)" = "5" ]</automated>
  </verify>
  <done>
    `test/replay/replay.test.ts` ships and passes. Five test groups cover D-REPL-08/REPL-05 (completed resolves on stop-at-end + currentState transitions to 'done'), D-REPL-11 (single-subscriber + subscriber-not-set throws), D-REPL-07 (single-use lock), RESEARCH §Pitfall 4 (pre-aborted signal sync throw), D-REPL-10 (idempotent stop). Internal-path import only.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write test/replay/abort.test.ts (REPL-06 cancellation invariants)</name>
  <files>test/replay/abort.test.ts</files>
  <read_first>
    - .planning/phases/03-replay-engine/03-RESEARCH.md (§Code Examples 2 — REPL-06 cancellation invariant test, lines 670-700, COPY-PASTEABLE; §AbortController teardown pitfalls; §Open Question 3 — AbortSignal.any composition)
    - .planning/phases/03-replay-engine/03-CONTEXT.md (D-REPL-09 AbortController, D-REPL-10 no emissions after abort)
    - .planning/phases/03-replay-engine/03-PATTERNS.md (§test/replay/abort.test.ts — analog test/fit/normalize.test.ts; same fake-timer recipe as scheduler.test.ts)
    - src/replay/replay.ts (Replay.start({ signal? }) signature, .stop() behavior, .completed rejection semantics)
    - src/replay/scheduler.ts (the AbortError that propagates up — `.name === 'AbortError'`)
  </read_first>
  <action>
    Create `test/replay/abort.test.ts`. Fake-timer setup as before.

    File header: list REPL-06, D-REPL-09, D-REPL-10, RESEARCH §Open Question 3 (AbortSignal.any).

    Imports — exact:
    `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
    `import { Replay } from '../../src/replay/replay.js';`
    `import type { RideRecord } from '../../src/types.js';`

    Helper: define the same `makeRecords` helper as Task 2.

    Group 1 — REPL-06: stop() mid-replay → zero emissions in 100ms wait + completed rejects. Use the canonical pattern from RESEARCH §Code Examples 2 lines 670-700 verbatim adapted:
    - 5-record array with 100ms cadence: timestamps `[1000, 1100, 1200, 1300, 1400]`.
    - Wire onRecord, start, advance 150ms (=2 emissions: t=0 and t=+100).
    - Assert `emitted.toEqual([1000, 1100])`.
    - Call `replay.stop()`, capture `before = emitted.length`.
    - Advance 100ms more: `await vi.advanceTimersByTimeAsync(100);`.
    - Assert `emitted.length === before` (zero new emissions in 100ms after stop — REPL-06).
    - Assert `await expect(replay.completed).rejects.toMatchObject({ name: 'AbortError' })` (RESEARCH §Open Question 2 resolution).
    - Assert `replay.currentState === 'aborted'`.

    Group 2 — D-REPL-09: external signal abort path → completed rejects. Required test:
    - Construct Replay, wire onRecord.
    - Construct external AbortController with NOT-yet-aborted signal.
    - `replay.start({ signal: controller.signal });`
    - Advance 50ms (some emissions fire).
    - Call `controller.abort();` (NOT `replay.stop()` — exercises the external signal path).
    - Advance 100ms more.
    - Assert no further emissions AND `await expect(replay.completed).rejects.toMatchObject({ name: 'AbortError' })`.
    - Assert `replay.currentState === 'aborted'`.

    Group 3 — RESEARCH §Open Question 3: external + internal abort race. Required tests:
    - Construct Replay, attach onRecord, start with external signal. Both `controller.abort()` AND `replay.stop()` fire in the same tick. Whichever fires first wins; both reach 'aborted' state without throwing a second time. Assert no double-rejection (the completed Promise's settle handler runs once — `await replay.completed.catch(() => {})` cleanly).
    - External signal aborts AFTER replay.stop() — a no-op (replay is already aborted; calling controller.abort() does NOT cause a second rejection).

    Group 4 — D-REPL-10: single pending tick (no leftover timers / ghost emissions after abort). Required test:
    - 10-record array, start, advance 250ms (=3 emissions: t=0/100/200).
    - Call replay.stop().
    - Advance 5000ms more.
    - Assert: NO further emissions. (The setTimeout-chain primitive only ever has ONE pending tick — a single clearTimeout-equivalent. If a regression added multiple timer schedules, this test catches ghost emissions.)
    - Optionally use `vi.getTimerCount()` if available in Vitest 4 to assert zero pending timers AFTER stop(). If the API behaves differently, fall back to the wait-and-assert-no-emissions approach.

    Forbidden: asserting on AbortError's `cause` field (Node's AbortError shape is implementation-controlled — assert only `name === 'AbortError'`); mid-replay subscriber throws (out of scope here; a separate concern); loop=true scenarios (those go in loop.test.ts).

    Commit:
    `test(03-03): abort.test.ts — REPL-06 cancellation invariants (D-REPL-09/10)`
  </action>
  <acceptance_criteria>
    - `test/replay/abort.test.ts` exists: `test -f test/replay/abort.test.ts`.
    - Internal-path import (D-REPL-12): `grep -cE "import\s+\\{\s*Replay\s*\\}\s+from\s+['\"]\\.\\./\\.\\./src/replay/replay\\.js['\"]" test/replay/abort.test.ts` reports 1.
    - Does NOT import from src/index.js: `grep -cE "from\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]" test/replay/abort.test.ts` reports 0.
    - Fake-timer setup with Async advancement: `grep -F 'vi.useFakeTimers' test/replay/abort.test.ts` exits 0 AND `grep -F 'vi.advanceTimersByTimeAsync' test/replay/abort.test.ts` exits 0.
    - Asserts replay.completed rejects with AbortError (RESEARCH §Open Question 2 resolution): `grep -E "rejects\.toMatchObject" test/replay/abort.test.ts` exits 0 AND `grep -F "name: 'AbortError'" test/replay/abort.test.ts` exits 0.
    - Tests stop() mid-replay → zero-emissions wait pattern (Group 1, REPL-06): `grep -F 'replay.stop()' test/replay/abort.test.ts` exits 0 AND `grep -E 'emitted\.length' test/replay/abort.test.ts` exits 0.
    - Tests external AbortController path (Group 2, D-REPL-09): `grep -E 'controller\.abort\\(' test/replay/abort.test.ts` exits 0 AND `grep -E 'start\s*\\(\s*\\{\s*signal' test/replay/abort.test.ts` exits 0.
    - Tests external+internal abort race (Group 3): `grep -iE 'race|both|external|composition' test/replay/abort.test.ts` exits 0.
    - Asserts currentState transitions to 'aborted': `grep -E "currentState.*===\\s*['\"]aborted['\"]|toBe\\(\\s*['\"]aborted['\"]" test/replay/abort.test.ts` exits 0.
    - All four groups present: `grep -cE 'Group\s+[1234]\s+—' test/replay/abort.test.ts` reports 4.
    - vitest runs and passes: `npx vitest run test/replay/abort.test.ts`.
    - Whole suite passes: `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/replay/abort.test.ts && npm test && grep -E "import\s+\{\s*Replay\s*\}\s+from\s+['\"]\.\./\.\./src/replay/replay\.js['\"]" test/replay/abort.test.ts && [ "$(grep -cE "from\s+['\"]\.\./\.\./src/index\.js['\"]" test/replay/abort.test.ts)" = "0" ] && grep -F 'vi.advanceTimersByTimeAsync' test/replay/abort.test.ts && grep -E "rejects\.toMatchObject" test/replay/abort.test.ts && grep -F "name: 'AbortError'" test/replay/abort.test.ts && grep -F 'replay.stop()' test/replay/abort.test.ts && grep -E 'controller\.abort\(' test/replay/abort.test.ts && grep -E "currentState.*===\s*['\"]aborted['\"]|toBe\(\s*['\"]aborted['\"]" test/replay/abort.test.ts && [ "$(grep -cE 'Group\s+[1234]\s+—' test/replay/abort.test.ts)" = "4" ]</automated>
  </verify>
  <done>
    `test/replay/abort.test.ts` ships and passes. Four test groups cover REPL-06 stop()-mid-replay (zero-emissions-in-100ms + completed rejects with AbortError), D-REPL-09 external-signal-abort-path, AbortSignal.any composition race, D-REPL-10 single-pending-tick / no-ghost-emissions guarantee. Internal-path import only.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write test/replay/loop.test.ts (REPL-04 loop boundary, no drift accumulation)</name>
  <files>test/replay/loop.test.ts</files>
  <read_first>
    - .planning/phases/03-replay-engine/03-RESEARCH.md (§Code Examples 3 — REPL-04 loop boundary test, lines 705-731, COPY-PASTEABLE; §Common Pitfall 7 — loop boundary leaks drift across iterations)
    - .planning/phases/03-replay-engine/03-CONTEXT.md (D-REPL-06 loop re-base; D-REPL-07 stop-at-end; REPL-04 loop:true restarts without drift)
    - .planning/phases/03-replay-engine/03-PATTERNS.md (§test/replay/loop.test.ts — analog test/fit/normalize.test.ts)
    - src/replay/scheduler.ts (the loop boundary re-base implementation — `baseline = getNow()` at cursor wrap)
    - src/replay/replay.ts (loop=true behavior in the constructor → scheduler invocation chain)
  </read_first>
  <action>
    Create `test/replay/loop.test.ts`. Fake-timer setup as before.

    File header: list REPL-04, D-REPL-06, RESEARCH §Pitfall 7 (loop boundary drift).

    Imports — exact:
    `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
    `import { Replay } from '../../src/replay/replay.js';`
    `import type { RideRecord } from '../../src/types.js';`

    Group 1 — D-REPL-06 + REPL-04: three iterations, each first-emission within ±5ms of expected. Use RESEARCH §Code Examples 3 (lines 705-731) verbatim adapted:
    - 3-record array `[{timestamp: 0}, {timestamp: 500}, {timestamp: 1000}]` (1 second period).
    - `const emissionTimes: number[] = [];`
    - `replay.onRecord(() => emissionTimes.push(globalThis.performance.now()));`
    - `replay.start();`
    - `await vi.advanceTimersByTimeAsync(3500);` (advance through 3 iterations + a bit).
    - `replay.stop();`
    - `await replay.completed.catch(() => {});` (swallow the abort rejection — loop.test focuses on timing, not abort semantics).
    - Compute the first-emission-of-each-iteration timestamps (every 3rd entry: indices 0, 3, 6).
    - Assert: `Math.abs(firsts[1] - firsts[0] - 1000) < 5` AND `Math.abs(firsts[2] - firsts[1] - 1000) < 5` (each iteration's first-record-offset within ±5ms of the FIT period).
    - Justification of ±5ms tolerance: under fake timers the math is exact modulo microtask ordering — the slop covers the emit-microtask boundary.

    Group 2 — RESEARCH §Pitfall 7 negative test: iteration 2 does NOT burst-emit. Required test:
    - 3-record array `[{timestamp: 0}, {timestamp: 500}, {timestamp: 1000}]`, loop=true.
    - Advance through iteration 1 entirely (advance 1000ms; assert 3 emissions).
    - Then advance 100ms (should be inside iteration 2 between record 0 and record 1 → assert ONE additional emission, not two).
    - This proves the loop boundary re-base happened: without re-base, all 3 iteration-2 records would be in the past and emit instantly upon `cursor=0` re-armament.
    - If the assertion is off-by-one due to microtask ordering, document the adjustment in 03-03-SUMMARY.md and assert the correct count after a known-good trace; do NOT widen the tolerance to "at least N" without reason — the precise count is the contract.

    Group 3 — loop=true does NOT transition to 'done'. Required test:
    - 3-record array, loop=true. Start, advance through 5 iterations worth of fake time. `replay.currentState` should still be 'running' — loop=true never reaches 'done' on its own. Then call `replay.stop()` and assert `currentState === 'aborted'`.

    Forbidden: REPL-03 30-min real-clock drift gate (that's plan 03-04's soak suite); tolerance > 5ms on Group 1 timing assertions; real-clock measurements via Date.now() inside test logic (use `globalThis.performance.now()` which is faked).

    Commit:
    `test(03-03): loop.test.ts — REPL-04 loop boundary (D-REPL-06, Pitfall 7)`
  </action>
  <acceptance_criteria>
    - `test/replay/loop.test.ts` exists: `test -f test/replay/loop.test.ts`.
    - Internal-path import: `grep -cE "import\s+\\{\s*Replay\s*\\}\s+from\s+['\"]\\.\\./\\.\\./src/replay/replay\\.js['\"]" test/replay/loop.test.ts` reports 1.
    - Does NOT import from src/index.js: `grep -cE "from\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]" test/replay/loop.test.ts` reports 0.
    - Fake-timer setup with Async advancement: `grep -F 'vi.useFakeTimers' test/replay/loop.test.ts` exits 0 AND `grep -F 'vi.advanceTimersByTimeAsync' test/replay/loop.test.ts` exits 0.
    - Tests loop=true configuration: `grep -F 'loop: true' test/replay/loop.test.ts` exits 0.
    - Records emission timestamps via globalThis.performance.now (faked under vi): `grep -F 'globalThis.performance.now' test/replay/loop.test.ts` exits 0.
    - Asserts inter-iteration drift bounds (RESEARCH §Code Examples 3 — ±5 ms): `grep -E 'toBeLessThan\\(\\s*5\\s*\\)|<\\s*5\\b' test/replay/loop.test.ts` exits 0.
    - Asserts loop=true does NOT auto-transition to 'done' (Group 3): `grep -E "currentState.*===\\s*['\"]running['\"]|toBe\\(\\s*['\"]running['\"]" test/replay/loop.test.ts` exits 0.
    - All three groups present: `grep -cE 'Group\s+[123]\s+—' test/replay/loop.test.ts` reports 3.
    - vitest runs and passes: `npx vitest run test/replay/loop.test.ts`.
    - Whole suite passes: `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/replay/loop.test.ts && npm test && grep -E "import\s+\{\s*Replay\s*\}\s+from\s+['\"]\.\./\.\./src/replay/replay\.js['\"]" test/replay/loop.test.ts && [ "$(grep -cE "from\s+['\"]\.\./\.\./src/index\.js['\"]" test/replay/loop.test.ts)" = "0" ] && grep -F 'vi.advanceTimersByTimeAsync' test/replay/loop.test.ts && grep -F 'loop: true' test/replay/loop.test.ts && grep -F 'globalThis.performance.now' test/replay/loop.test.ts && grep -E 'toBeLessThan\(\s*5\s*\)|<\s*5\b' test/replay/loop.test.ts && grep -E "currentState.*===\s*['\"]running['\"]|toBe\(\s*['\"]running['\"]" test/replay/loop.test.ts && [ "$(grep -cE 'Group\s+[123]\s+—' test/replay/loop.test.ts)" = "3" ]</automated>
  </verify>
  <done>
    `test/replay/loop.test.ts` ships and passes. Three test groups cover D-REPL-06 + REPL-04 (three iterations within ±5ms drift), RESEARCH §Pitfall 7 negative test (iteration 2 does NOT burst-emit), and loop=true never auto-transitions to 'done'. Internal-path import only. Fake-timer discipline maintained.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test imports ↔ src/replay/* | Tests bypass src/index.ts (D-REPL-12 keeps the surface internal). The internal-import contract is the ONLY way to test Phase 3 in isolation. |
| Vitest fake-timer regime ↔ scheduler/Replay code under test | The fake-timer regime fakes `setTimeout`, `clearTimeout`, AND `globalThis.performance.now()`. The scheduler's injected `getNow` reads through `globalThis.performance` — RESEARCH Assumptions A1 verified. Setup-sanity test in scheduler.test.ts catches a regression where Vitest 4 silently drops `performance` from default toFake. |
| AbortError shape ↔ test assertion | Tests assert `{ name: 'AbortError' }` — the stable invariant from `node:timers/promises` setTimeout cancellation. Tests do NOT assert on `cause` or message format — those are implementation-controlled. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | Test uses `vi.advanceTimersByTime()` (sync) instead of Async, producing silently-wrong results when promise chains span multiple ticks (RESEARCH §Pitfall 5) | mitigate | Acceptance criteria require `vi.advanceTimersByTimeAsync` call count >= 4 in scheduler.test.ts; the sync variant is forbidden in all four files. CI enforcement: `grep -cE 'vi\.advanceTimersByTime\s*\(' === count of 'Async' calls (sync variant only appears as part of the Async name). |
| T-03-12 | Tampering | Test imports from `'../../src/index.js'` (the public surface), inadvertently demanding that Replay/runScheduler be exported there — violating D-REPL-12 | mitigate | Acceptance criteria grep-ban `'../../src/index.js'` imports in all four files. |
| T-03-13 | Repudiation | A regression in scheduler's loop re-base (D-REPL-06) silently passes loop.test.ts because the `±5 ms` tolerance is widened to `±50 ms` "to fix flakiness" | mitigate | Action explicitly forbids tolerance > 5ms on Group 1 timing assertions; acceptance criteria grep `toBeLessThan(5)` literally. Under fake timers the math is exact; widening the tolerance hides the regression. |
| T-03-14 | Tampering | A test imports `setTimeout as sleep` from `'node:timers/promises'` directly, bypassing the SUT's seam | accept | Tests are allowed to use Node built-ins where useful (e.g., a future test of subscriber-throw behavior might `await sleep(1)` to sequence). The SUT seam enforcement (only scheduler.ts imports node:timers/promises in src/) is a SOURCE constraint, not a TEST constraint. |
| T-03-15 | Information Disclosure | Test failure logs leak record values (timestamps, power) | accept | RideRecord has no PII; sport telemetry is fine in test logs. |
</threat_model>

<verification>
- All four test files run under `npm test` (CI tier; no env vars required).
- `test/replay/scheduler.test.ts` covers REPL-01 emission cadence, REPL-02 speed multiplier (1x/2x/0.5x + Infinity+cap), RESEARCH §Pitfall 8 (Infinity-on-cursor-0), §Pitfall 9 (empty-records), and Assumptions A1 (vi fakes performance.now).
- `test/replay/replay.test.ts` covers REPL-05 (completed resolves on stop-at-end), D-REPL-07 (single-use), D-REPL-11 (single-subscriber), RESEARCH §Pitfall 4 (pre-aborted signal), §Pitfall 10 (subscriber-not-set), and D-REPL-10 partial (idempotent stop before/after start).
- `test/replay/abort.test.ts` covers REPL-06 (zero-emissions-in-100ms-after-stop, completed rejects with AbortError), D-REPL-09 (external signal path), §Open Question 3 (external+internal race), D-REPL-10 (no ghost emissions after abort).
- `test/replay/loop.test.ts` covers REPL-04 + D-REPL-06 (three iterations within ±5ms drift), §Pitfall 7 (iteration 2 does NOT burst-emit), and loop=true never transitions to 'done'.
- All four files use the canonical `vi.useFakeTimers` / `vi.useRealTimers` setup with `vi.advanceTimersByTimeAsync` (NEVER the sync variant).
- All four files import the SUT from internal paths (`'../../src/replay/scheduler.js'`, `'../../src/replay/replay.js'`) — NEVER from `'../../src/index.js'`. D-REPL-12 lock holds.
- Four atomic conventional-prefix commits land cleanly: `test(03-03)` × 4.
</verification>

<success_criteria>
- The Phase 3 unit-test discipline established by D-REPL-14 is in place: `vi.useFakeTimers()` for all fake-timer tests, `vi.advanceTimersByTimeAsync()` for advancement, the setup-sanity test verifying Assumptions A1.
- Five of six REPL-* requirements have CI-tier test coverage (REPL-01, REPL-02, REPL-04, REPL-05, REPL-06 — REPL-03 is plan 03-04's soak gate).
- All eight RESEARCH §Common Pitfalls relevant to Phase 3's algorithm + class are exercised by at least one test (Pitfalls 4, 5, 6 — implicit in setup, 7, 8, 9, 10).
- `npm test` exits 0 with all four new files contributing tests.
- The internal-import discipline (D-REPL-12) is enforced by acceptance criteria — no test can drift to importing through src/index.ts without the next plan's checker catching it.
</success_criteria>

<output>
After completion, create `.planning/phases/03-replay-engine/03-03-SUMMARY.md` documenting:
- Each test file's line count and tests-added count.
- Confirmation that `vi.advanceTimersByTime()` (sync) is absent from all four files.
- Confirmation that `'../../src/index.js'` is absent from all four files (D-REPL-12 enforcement).
- The actual measured tolerance achieved on loop.test.ts Group 1 (well under 5ms or right at the boundary?).
- Whether `vi.getTimerCount()` was used in abort.test.ts Group 4 or whether the wait-and-assert-no-emissions fallback was needed.
- Any discovered behavior in Group 2 of replay.test.ts where the disposer-then-re-attach contract was tested (does the implementation allow re-attach after dispose, or does it throw?).
- Confirmation that Phase 1+2 tests still pass (no regression).
- The four atomic commit SHAs.
</output>
