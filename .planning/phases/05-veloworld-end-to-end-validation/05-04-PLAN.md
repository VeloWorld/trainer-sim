---
phase: 05-veloworld-end-to-end-validation
plan: 04
type: execute
wave: 2
depends_on: [03]
files_modified:
  - .planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md
autonomous: true
requirements: [VW-01, VW-02, VW-03]
must_haves:
  truths:
    - "`.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md` exists with frontmatter (`phase`, `verified`, `status: passed`, `score`, `re_verification`) following the 04-VERIFICATION.md template per PATTERNS.md."
    - "The 5-item D-VW-09 acceptance bundle is present in the verification doc, in a clearly-labeled section: (1) merged VW PR URL, (2) VW CI run URL on macOS Node 24 with conclusion: success, (3) VW CI run URL on Linux Node 24 with conclusion: success, (4) trainer-sim sha pinned by the merged PR (the Wave 0 sha or Wave 0.5 if iteration happened), (5) narrative."
    - "The narrative (item 5) names: which VW E2E suite was run (per VW's vitest.workspace.ts: physics + route + ble + apps/desktop, invoked via `pnpm test`), what FIT fixture VW committed (the existing `apps/desktop/src/renderer/src/lib/__tests__/dev/fixtures/sample.fit` per RESEARCH §"Don't Hand-Roll" + Pitfall 7), what shape diffs surfaced (VW's 9-method ITrainerTransport vs trainer-sim's 4-method core, resolved via the adapter pattern in `dev/fake-trainer-transport.ts` per Anti-Pattern 6), and how they were resolved (in trainer-sim's favor: trainer-sim's contract did NOT widen; VW's adapter absorbs the diff)."
    - "The narrative records the speed=null wire-format diff (trainer-sim emits 6-byte frames, VW's parser returns instantaneousSpeed: undefined, useBleStore accepts speed: null) and identifies it as expected per FTMS-06 being v2-deferred."
    - "The narrative records any Wave 0.5 trainer-sim fixes that happened during iteration (per D-VW-10), with each Wave 0.5 sha and a one-line description of the bug fixed. If no Wave 0.5 fix happened, the narrative says so explicitly."
    - "Requirements VW-01, VW-02, VW-03 are mapped to evidence in a `### Requirements Coverage` table per the 04-VERIFICATION.md template."
    - "The verification doc is committed to trainer-sim's `main` branch with subject `docs(05): record VW E2E acceptance bundle (D-VW-09)`."
    - "trainer-sim's `.github/workflows/ci.yml` is unchanged in this commit (D-VW-06 absolute), `src/` and `test/` are unchanged (D-VW-07 — no source changes in Wave 2), and `package.json` is unchanged (no version bump per Phase 5 non-edits)."
  artifacts:
    - path: ".planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md"
      provides: "The 5-item D-VW-09 acceptance bundle + ROADMAP-success-criteria mapping + Requirements Coverage table for VW-01..03"
      contains: "Acceptance Bundle (D-VW-09)"
      min_lines: 80
  key_links:
    - from: "05-VERIFICATION.md `## Acceptance Bundle (D-VW-09)` section, item 1"
      to: "merged VW PR on VeloWorld/veloworld-ride"
      via: "Verbatim PR URL captured in `/tmp/05-03-pr-url.txt` by Plan 05-03"
      pattern: "https://github.com/VeloWorld/veloworld-ride/pull/"
    - from: "05-VERIFICATION.md `## Acceptance Bundle (D-VW-09)` section, item 4"
      to: "trainer-sim Wave 0 (or 0.5) commit on VeloWorld/trainer-sim"
      via: "The 40-character sha lifted from `05-WAVE-0-SHA.txt` (or the iteration log's final sha if Wave 0.5 happened)"
      pattern: "VeloWorld/trainer-sim/commit/"
---

<objective>
Compose `05-VERIFICATION.md` — the trainer-sim-side artifact that closes Phase 5. Mechanically lifts the 5 D-VW-09 bundle items from Plan 05-03's outputs (`/tmp/05-03-pr-url.txt`, `/tmp/05-03-merge-sha.txt`, `/tmp/05-03-ci-runs-final.json`, `/tmp/05-03-iteration-log.md`, `/tmp/05-03-acceptance-bundle.md`) into the structured verification template established by `04-VERIFICATION.md`.

Per D-VW-07 (with Wave 0 as the documented exception, which already happened in Plan 05-01): trainer-sim's Phase 5 deliverable inside this repo is "documentation + a stable commit, not new source code." Plan 05-04 is the documentation deliverable. NO source changes, NO test changes, NO CI changes.

Per D-VW-09: the acceptance bundle is exactly 5 items. The plan-checker will count.

Per the PATTERNS §"05-VERIFICATION.md" guidance: the structure follows the 04-VERIFICATION.md template (frontmatter + Phase Goal + Goal Achievement + ROADMAP Success Criteria table + Requirements Coverage table + Anti-Patterns Found + Gaps Summary), but with two Phase 5 specific deviations:
1. A dedicated `## Acceptance Bundle (D-VW-09)` section before "Gaps Summary" containing the 5 items.
2. The "Required Artifacts" table is sparse (Phase 5 has no `src/` artifacts) — likely 2 rows: the Wave 0 `package.json` patch and this `05-VERIFICATION.md` itself.

Purpose: Make the v1 acceptance evidence permanent and discoverable. Output: a single trainer-sim commit on `main` containing the new verification doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-veloworld-end-to-end-validation/05-CONTEXT.md
@.planning/phases/05-veloworld-end-to-end-validation/05-RESEARCH.md
@.planning/phases/05-veloworld-end-to-end-validation/05-PATTERNS.md
@.planning/phases/05-veloworld-end-to-end-validation/05-01-SUMMARY.md
@.planning/phases/05-veloworld-end-to-end-validation/05-02-SUMMARY.md
@.planning/phases/05-veloworld-end-to-end-validation/05-03-SUMMARY.md
@.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt
@.planning/phases/04-faketransport-public-api/04-VERIFICATION.md

<inputs_to_lift>
<!-- All inputs are produced by Plans 05-01 / 05-02 / 05-03. -->
<!-- The executor reads them and copies content into 05-VERIFICATION.md. -->

From Plan 05-01:
- `.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt` — trainer-sim Wave 0 sha (40 chars)
- `.planning/phases/05-veloworld-end-to-end-validation/05-01-SUMMARY.md` — Wave 0 commit details, smoke-test stdout

From Plan 05-02:
- `.planning/phases/05-veloworld-end-to-end-validation/05-02-SUMMARY.md` — VW feature branch name, VW commit sha, LOC stats, local-green gate output

From Plan 05-03:
- `/tmp/05-03-pr-url.txt` — merged VW PR URL
- `/tmp/05-03-merge-sha.txt` — VW merge commit sha (40 chars)
- `/tmp/05-03-ci-runs-final.json` — JSON with 2 CI run details (name, detailsUrl, conclusion)
- `/tmp/05-03-iteration-log.md` — path-to-green log; lists Wave 0.5 shas if any
- `/tmp/05-03-acceptance-bundle.md` — scaffolded 5-item bundle, Plan 05-03 wrote this for Plan 05-04 to lift
- `.planning/phases/05-veloworld-end-to-end-validation/05-03-SUMMARY.md` — final state recap

If a Wave 0.5 trainer-sim fix happened during Plan 05-03's iteration: `05-WAVE-0-SHA.txt` was overwritten with the new (Wave 0.5) sha; the original Wave 0 sha lives only in the iteration log. Plan 05-04 lifts the FINAL sha (whatever's in `05-WAVE-0-SHA.txt` after iteration) for D-VW-09 item 4 — that's the sha the merged PR pins.
</inputs_to_lift>

<template_skeleton>
<!-- Lifted from 04-VERIFICATION.md per PATTERNS §"05-VERIFICATION.md" -->
<!-- The Phase 5 deviations are noted inline. -->

```markdown
---
phase: 05-veloworld-end-to-end-validation
verified: <ISO-8601 timestamp at the moment of writing>
status: passed
score: 3/3 ROADMAP success criteria verified + 8/8 plan-level truths
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 5: VeloWorld End-to-End Validation Verification Report

**Phase Goal:** VeloWorld's dev/test build runs green end-to-end against FakeTransport replaying a real Garmin/Wahoo FIT file, on both macOS and Linux on Node 24.

**Verified:** <ISO-8601 timestamp>
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|---|---|---|
| 1 | VeloWorld's existing `ITrainerTransport`-consuming code runs unchanged when FakeTransport is swapped in for the real BLE transport | VERIFIED | <PR diff: packages/ble/src/transport.ts is byte-identical; ride scene + physics code untouched. The diff scope is `apps/desktop/src/renderer/src/lib/dev/*` + CI matrix + dep pin. PR URL: <PR-URL>> |
| 2 | A real Garmin/Wahoo FIT file replayed through FakeTransport yields power and cadence values that VeloWorld's existing FTMS decoder reads correctly across the full ride | VERIFIED | <Test #5 in fake-trainer-transport.test.ts: round-trip-through-parseIndoorBikeData passes. The 6-byte trainer-sim frames decode with instantaneousPower + instantaneousCadence equal to expected, and instantaneousSpeed: undefined as documented per FTMS-06 v2-deferred. CI run URLs: <2 URLs>> |
| 3 | CI runs the VeloWorld E2E suite green on both macOS and Linux on Node 24 | VERIFIED | <2 CI run URLs (one per OS), both conclusion: SUCCESS. Node 24, pnpm install --frozen-lockfile, typecheck, test, build, prod-bundle grep gate all green.> |

**Score:** 3/3 ROADMAP success criteria verified.

### Observable Truths (Plan Frontmatter must_haves — additive)

| # | Plan-level truth | Status | Evidence |
|---|---|---|---|
| 4 | trainer-sim Wave 0 sha is pinned in VW's apps/desktop/package.json | VERIFIED | <package.json contains "trainer-sim": "github:VeloWorld/trainer-sim#<sha>"; pnpm-lock.yaml records resolution.commit: <sha>> |
| 5 | VW's vendored fit-loader.ts + replay-scheduler.ts + their tests are deleted | VERIFIED | <git diff at PR merge shows 4 file deletions> |
| 6 | VW's adapter rewrites in <apps/.../dev/fake-trainer-transport.ts>, ~80 LOC, composes trainer-sim's createFakeTransport with { source: { buffer } } | VERIFIED | <PR diff shows the rewrite; LOC delta from 273 -> ~80 per Plan 05-02 SUMMARY> |
| 7 | VW's 9-method ITrainerTransport (packages/ble/src/transport.ts) is byte-identical (Anti-Pattern 6 / user override #1) | VERIFIED | <git diff at PR merge: packages/ble/src/transport.ts has no changes> |
| 8 | trainer-sim's ITrainerTransport (src/types.ts) is byte-identical to post-Phase-4 (no widening per D-VW-05 + Anti-Pattern 6) | VERIFIED | <git diff <wave-0-sha>..HEAD -- src/types.ts shows no interface changes> |
| 9 | trainer-sim's CI was not modified (D-VW-06) | VERIFIED | <git diff <pre-wave-0-sha>..HEAD -- .github/workflows/ci.yml is empty> |
| 10 | trainer-sim's src/ + test/ were not modified in Wave 2 (D-VW-07) | VERIFIED | <git diff HEAD~1 HEAD -- src/ test/ is empty> |
| 11 | The 6-byte FTMS frame format is preserved end-to-end; speed=null is the documented v1 wire-format reality | VERIFIED | <05-VERIFICATION.md Acceptance Bundle item 5 narrative discusses; VW's BleManager.init's `?? null` coerce is the existing handler> |

**Score:** 8/8 plan-level truths verified.

**Combined Score: 11/11 must-haves verified.**

### Critical Issues from REVIEW.md

(None for Phase 5; this phase has no internal review since the review surface lives in VW's PR. Iteration cycles per D-VW-08 are recorded in Acceptance Bundle item 5 narrative.)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `package.json` (trainer-sim) | Adds `"prepare": "npm run build"` script + corrected `repository.url` | VERIFIED | Plan 05-01 commit `<wave-0-sha>`; smoke test confirmed dist/ materializes on consumer install |
| `.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md` | This file | VERIFIED | Self-referential — the existence of this verified row is itself the evidence |
| (cross-repo) VW PR | merged with both CI legs green | VERIFIED | <PR URL>; <2 CI URLs> |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| trainer-sim `package.json` `prepare` | trainer-sim `package.json` `build` (tsup) | `npm run build` invoked at consumer install | WIRED | Plan 05-01 smoke test |
| VW `apps/desktop/package.json` `dependencies.trainer-sim` | trainer-sim Wave 0 commit on VeloWorld/trainer-sim | `github:VeloWorld/trainer-sim#<sha>` resolved by pnpm | WIRED | Confirmed by `node_modules/trainer-sim/dist/index.js` existing post-install during Plan 05-02 + 05-03 CI runs |
| VW `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` | trainer-sim `createFakeTransport` | `import { createFakeTransport } from 'trainer-sim'` resolves through git-ref | WIRED | Plan 05-02 typecheck + test green; Plan 05-03 cross-platform CI green |
| VW PR `apps/desktop/src/renderer/src/lib/dev/fake-trainer-transport.ts` (DataView from inner.onData) | VW `parseIndoorBikeData` (packages/ble/src/ftms-codec.ts) | adapter's onTelemetry handler fans out inner.onData; VW's existing parser consumes the DataView | WIRED | Test #5 in fake-trainer-transport.test.ts asserts the round-trip; CI green |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| trainer-sim's local test suite still green after Wave 0 | `npm test` (in trainer-sim) | <copy from Plan 05-01 SUMMARY: 115 passed + 2 skipped> | PASS |
| trainer-sim's validate (publint + attw) still green after Wave 0 | `npm run validate` | <copy from Plan 05-01 SUMMARY: exit 0> | PASS |
| VW's typecheck + test + build green locally on macOS | `pnpm typecheck && pnpm test && pnpm --filter @veloworld/desktop build` | <copy from Plan 05-02 SUMMARY> | PASS |
| VW's CI green on ubuntu-latest Node 24 | (CI run URL) | conclusion: SUCCESS | PASS |
| VW's CI green on macos-latest Node 24 | (CI run URL) | conclusion: SUCCESS | PASS |
| VW's production-bundle grep gate clean post-PR | `grep -El 'fake-transport\|FakeTransport\|trainer-sim' apps/desktop/out/renderer/assets/*.js` | exit 1 (no matches) | PASS |

### Probe Execution

(None declared. Phase 5's probe surface is the cross-repo PR + green CI bundle.)

### Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| VW-01 | 05-02, 05-03 | VeloWorld's existing `ITrainerTransport`-consuming code runs unchanged when `FakeTransport` is swapped in | SATISFIED | git diff at merge: `packages/ble/src/transport.ts` byte-identical; `apps/desktop/src/renderer/src/lib/ble-manager.ts` byte-identical (only `dev/` subtree + CI + deps changed) |
| VW-02 | 05-02, 05-03 | A real Garmin/Wahoo FIT file replayed through FakeTransport produces power and cadence values that VeloWorld's existing FTMS decoder reads correctly across the full ride | SATISFIED | Test #5 in fake-trainer-transport.test.ts (rewritten by Plan 05-02) asserts round-trip; CI green on both OSes proves repeatability. Speed=undefined is the documented FTMS-06-v2-deferred wire-format reality. |
| VW-03 | 05-02, 05-03 | CI runs the VW E2E suite green on macOS and Linux on Node 24 | SATISFIED | 2 CI run URLs (`ci (ubuntu-latest)` + `ci (macos-latest)`), both conclusion: SUCCESS, both Node 24 |

All 3 phase requirement IDs (VW-01..03) are SATISFIED.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|

(None. trainer-sim's `ITrainerTransport` did NOT widen during Phase 5 — Anti-Pattern 6 / user override #1 hard gate held. VW's 9-method interface remained VW-owned. The adapter pattern absorbed the shape diff per the original CONTEXT.md design intent.)

### Human Verification Required

(Optional smoke test in Plan 05-03 Task 3: pair the synthetic "Trainer Sim (FIT replay)" device, run a ride end-to-end in dev mode, observe pause/resume cycles. If performed and clean, note in Acceptance Bundle item 5; if skipped, note as such.)

## Acceptance Bundle (D-VW-09)

The 5-item v1 acceptance evidence per CONTEXT §D-VW-09:

1. **VW PR (merged):** <PR URL from /tmp/05-03-pr-url.txt>
   Merge commit sha: <sha from /tmp/05-03-merge-sha.txt>

2. **VW CI macOS Node 24:** <macOS run URL from /tmp/05-03-ci-runs-final.json>
   Status: SUCCESS
   Run includes: pnpm install --frozen-lockfile, pnpm typecheck, pnpm test, pnpm --filter @veloworld/desktop build, "Verify FakeTransport stripped from production bundle" grep gate (exit 1 = clean).

3. **VW CI Linux Node 24:** <Linux run URL from /tmp/05-03-ci-runs-final.json>
   Status: SUCCESS
   Same step list as item 2.

4. **trainer-sim sha pinned:** `<sha from 05-WAVE-0-SHA.txt>`
   Commit subject: `build(05): add prepare hook for git-ref install + correct repo org` (or the Wave 0.5 subject if iteration happened — see iteration log).
   Source diff scope: `package.json` (one new script line + repository.url org fix), `.planning/PROJECT.md`, `.planning/phases/05-veloworld-end-to-end-validation/05-CONTEXT.md`, `.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt`. NO source changes (D-VW-07 honored). NO CI changes (D-VW-06 honored). NO version bump.
   Commit URL: https://github.com/VeloWorld/trainer-sim/commit/<sha>

5. **Narrative:**
   - **Suite run:** VW's vitest workspace (4 packages: physics + route + ble + apps/desktop), invoked via `pnpm test` on each CI runner. Total test count and per-package counts: <copy from Plan 05-03 SUMMARY's terminal output capture>.
   - **Fixture:** `apps/desktop/src/renderer/src/lib/__tests__/dev/fixtures/sample.fit` (594 bytes, real Garmin export, manufacturer 257 product 16896 verified by `file(1)`). The existing fixture satisfied D-VW-03 per RESEARCH §"Don't Hand-Roll" + Pitfall 7; no new FIT trim was required.
   - **Shape diffs surfaced:** VW's `packages/ble/src/transport.ts` interface is 9-method (scan/connect-with-deviceId/onTelemetry/probeControlPointSupport/requestControl/setGradeSimulation/releaseControl/disconnect/reconnect); trainer-sim's `ITrainerTransport` is 4-method (connect/disconnect/onData/sendResistance). Resolved via the **adapter pattern** (RESEARCH §"Pattern 1"): VW's `dev/fake-trainer-transport.ts` (rewritten in Plan 05-02 from 273 LOC down to ~<actual LOC from 05-02 SUMMARY> LOC) implements VW's 9-method interface and delegates the FIT-replay verbs to trainer-sim's `createFakeTransport`. The 5 BLE-shaped methods (scan, probeControlPointSupport, requestControl, setGradeSimulation, releaseControl) stay as VW-owned no-ops or synthetic implementations; reconnect re-runs connect. Per Anti-Pattern 6 + user override #1, **trainer-sim's contract did NOT widen** — `git diff <pre-wave-0-sha>..HEAD -- src/types.ts` shows the `ITrainerTransport` interface is byte-identical to its post-Phase-4 state.
   - **Pause/resume:** Path A (per user override #2). VW's adapter implements pause/resume as `inner.disconnect()` / `inner.connect()` on `useRideStore` rideState transitions. The ~5ms FIT re-parse on resume is accepted for v1; if it surfaces UX pain, the v1.x fix is to add a `pause()` seam to trainer-sim per D-VW-08, not a Phase 5 deliverable.
   - **Speed channel:** trainer-sim emits 6-byte FTMS IndoorBikeData frames without speed (bit-0 SET per FTMS-04). VW's `parseIndoorBikeData` returns `instantaneousSpeed: undefined` for these frames. VW's `BleManager.init` already coerces `parsed.instantaneousSpeed ?? null` (line 99-102 — VW's pre-existing handling for trainers that omit speed). UX impact: dev-mode HUD shows speed=null, which is consistent with FTMS-06 (Heart rate / speed FTMS fields) being v2-deferred per REQUIREMENTS.md.
   - **trainer-sim contract changes during iteration:** <if Plan 05-03 iteration log records a Wave 0.5 fix, document it here: "A Wave 0.5 fix was required during CI iteration. Bug: <description>. Fix sha: <0.5 sha>. Source diff: <files touched>. Confirmed Anti-Pattern-6-compliant: src/types.ts ITrainerTransport interface is byte-identical to its post-Phase-4 state.". If no Wave 0.5 fix happened: "No Wave 0.5 trainer-sim fixes were required during Plan 05-03's CI iteration cycles. The Wave 0 sha pinned at PR open was the same sha pinned at PR merge.">

### Gaps Summary

No gaps. The phase goal — "VeloWorld's dev/test build runs green end-to-end against FakeTransport replaying a real Garmin/Wahoo FIT file, on both macOS and Linux on Node 24" — is achieved end-to-end:

1. trainer-sim's `createFakeTransport` is the FIT replay engine VW consumes (via the git-ref + prepare hook).
2. VW's adapter (~<actual LOC> LOC) wraps trainer-sim's 4-method core to expose VW's 9-method ITrainerTransport interface, preserving VW's existing ride-scene + physics code unchanged (VW-01).
3. The round-trip-through-VW's-parser test (test #5 in fake-trainer-transport.test.ts) proves power+cadence are decoded correctly (VW-02); the speed=null wire-format diff is documented and consistent with v2-deferred FTMS-06.
4. CI is green on both ubuntu-latest and macos-latest with Node 24 (VW-03).
5. trainer-sim's source contract did NOT widen during Phase 5; Anti-Pattern 6 / D-VW-05 / user override #1 hard gate held.
6. The trainer-sim sha pinned by VW's PR is `<wave-0-or-0.5-sha>`, locked into VW's git history as the immutable evidence trail.

Phase 5 is complete. v1 is shippable per PROJECT.md.

---

_Verified: <timestamp>_
_Verifier: Claude (gsd-executor, Plan 05-04)_
```
</template_skeleton>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose 05-VERIFICATION.md from Plan 05-01/02/03 inputs</name>
  <files>.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md</files>
  <read_first>
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-WAVE-0-SHA.txt — the trainer-sim sha to record in item 4 (verbatim).
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-01-SUMMARY.md — Wave 0 commit details, smoke-test stdout for Behavioral Spot-Checks.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-02-SUMMARY.md — VW LOC stats, local-green gate output for the narrative + Behavioral Spot-Checks.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-03-SUMMARY.md — PR URL, merge sha, CI URLs, iteration log for the 5-item bundle.
    - /tmp/05-03-pr-url.txt — PR URL (item 1).
    - /tmp/05-03-merge-sha.txt — merge commit sha (item 1 sub-detail).
    - /tmp/05-03-ci-runs-final.json — JSON with the 2 CI run URLs (items 2 + 3). Parse with `python3 -c "import json; data=json.load(open('/tmp/05-03-ci-runs-final.json')); for r in data: print(r['name'], r['conclusion'], r['detailsUrl'])"` to extract the macOS row vs Linux row.
    - /tmp/05-03-iteration-log.md — to determine if Wave 0.5 fixes happened (informs item 5 narrative).
    - /tmp/05-03-acceptance-bundle.md — Plan 05-03's scaffolded bundle to lift verbatim if usable.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/04-faketransport-public-api/04-VERIFICATION.md — the structural template per PATTERNS §"05-VERIFICATION.md".
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-PATTERNS.md §"05-VERIFICATION.md" lines 79-119 — the pattern map for this file.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/REQUIREMENTS.md §"VeloWorld Integration" — VW-01..03 binding text for Requirements Coverage table.
  </read_first>
  <action>
    Use the Write tool to create `/Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md` following the `<template_skeleton>` block in this plan's `<context>` section verbatim, with `<placeholder>` substitutions:

    Substitution recipe:

    1. `<ISO-8601 timestamp>` (3 occurrences in frontmatter, header, footer) — current UTC timestamp at write time. Use `date -u +'%Y-%m-%dT%H:%M:%SZ'`.

    2. `<PR URL from /tmp/05-03-pr-url.txt>` — read the file and use the URL verbatim. Should be `https://github.com/VeloWorld/veloworld-ride/pull/<N>`.

    3. `<sha from /tmp/05-03-merge-sha.txt>` — 40-char merge commit sha.

    4. `<macOS run URL from /tmp/05-03-ci-runs-final.json>` — parse the JSON for the row whose `name` contains `macos` and use its `detailsUrl`.

    5. `<Linux run URL from /tmp/05-03-ci-runs-final.json>` — same for `ubuntu`.

    6. `<sha from 05-WAVE-0-SHA.txt>` — 40-char trainer-sim sha (Wave 0, OR Wave 0.5 if iteration log records a fix; the file holds the FINAL sha pinned at merge).

    7. `<copy from Plan 05-01 SUMMARY: 115 passed + 2 skipped>` — verbatim from Plan 05-01's SUMMARY, the `npm test` output line.

    8. `<copy from Plan 05-01 SUMMARY: exit 0>` — verbatim.

    9. `<copy from Plan 05-02 SUMMARY>` — the local-green gate output (typecheck + test + build last-20-lines as recorded in the SUMMARY).

    10. `<actual LOC from 05-02 SUMMARY>` — the rewritten LOC count for `dev/fake-trainer-transport.ts` (Plan 05-02 records pre-rewrite vs post-rewrite LOC).

    11. `<copy from Plan 05-03 SUMMARY's terminal output capture>` — the per-package test pass counts.

    12. `<wave-0-or-0.5-sha>` — same as item 6.

    13. `<pre-wave-0-sha>` — the trainer-sim sha BEFORE Wave 0. Use `cd /Users/agniveshpatel/dev/agni21/trainer-sim && git log --pretty=%H` and find the sha immediately before the Wave 0 commit (i.e., HEAD~N where N is the count of Phase 5 commits — typically `git rev-parse HEAD~1` if Wave 0 is the most recent commit on `main`).

    14. `<if Plan 05-03 iteration log records a Wave 0.5 fix...>` — read `/tmp/05-03-iteration-log.md`. If a Wave 0.5 fix is recorded, document it (bug + fix sha + diff scope + Anti-Pattern-6 confirmation). If no Wave 0.5: write the explicit "No Wave 0.5 trainer-sim fixes were required" sentence.

    15. `<PR diff: ... PR URL: <PR-URL>>` and similar contextual placeholders in the ROADMAP Success Criteria table — fill with concrete evidence references (PR URL + line-count summaries from `gh pr view --json additions,deletions`).

    16. The "Suite run" sub-bullet in item 5 narrative: extract from `/tmp/05-03-ci-runs-final.json` or rerun `gh run view <run-id> --log` snippet for the test step's terminal output — usually a `Test Files X passed` line.

    17. The "trainer-sim contract changes during iteration" sub-bullet: read `/tmp/05-03-iteration-log.md`. If Wave 0.5 happened, document each one (one-line per fix). If not, the "No Wave 0.5..." sentence stands.

    Sanity checks before saving:

    a. The frontmatter has all 5 fields (`phase`, `verified`, `status`, `score`, `re_verification`).

    b. The `## Acceptance Bundle (D-VW-09)` section heading is exactly that string (Plan-checker greps it).

    c. The bundle has exactly 5 numbered items (D-VW-09 verbatim count).

    d. Each item is filled with concrete content (no `<placeholder>` strings remain).

    e. The Requirements Coverage table maps VW-01, VW-02, VW-03 each to a SATISFIED row.

    f. The "Anti-Patterns Found" table is empty (no rows under the header) AND has a parenthetical noting "trainer-sim's `ITrainerTransport` did NOT widen during Phase 5".

    Save the file. Do NOT commit yet — Task 2 owns the commit.
  </action>
  <verify>
    <automated>cd /Users/agniveshpatel/dev/agni21/trainer-sim &amp;&amp; { F=.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md; if [ ! -f "$F" ]; then echo "FAIL: $F not created"; exit 1; fi; LOC=$(wc -l &lt; "$F"); if [ "$LOC" -lt "80" ]; then echo "FAIL: $F is $LOC lines, expected ≥80 per must_haves min_lines"; exit 1; fi; if ! grep -q "^## Acceptance Bundle (D-VW-09)" "$F"; then echo "FAIL: missing '## Acceptance Bundle (D-VW-09)' section heading"; exit 1; fi; BUNDLE_ITEMS=$(awk '/^## Acceptance Bundle/,/^## /{if(/^[1-5]\. /)print}' "$F" | wc -l); if [ "$BUNDLE_ITEMS" -lt "5" ]; then echo "FAIL: Acceptance Bundle has $BUNDLE_ITEMS numbered items, expected 5"; exit 1; fi; if grep -q '&lt;[A-Z]\|&lt;ISO-8601\|&lt;sha\|&lt;PR URL\|&lt;copy from\|&lt;actual LOC\|&lt;Linux run\|&lt;macOS run\|&lt;wave-0\|&lt;pre-wave-0\|&lt;if Plan' "$F"; then echo "FAIL: $F still contains placeholders"; grep -n '&lt;[A-Z]\|&lt;ISO-8601\|&lt;sha\|&lt;PR URL\|&lt;copy from\|&lt;actual LOC\|&lt;Linux run\|&lt;macOS run\|&lt;wave-0\|&lt;pre-wave-0\|&lt;if Plan' "$F" | head -5; exit 1; fi; if ! grep -q "VW-01" "$F" || ! grep -q "VW-02" "$F" || ! grep -q "VW-03" "$F"; then echo "FAIL: VW-01/02/03 not all mentioned in $F"; exit 1; fi; if ! grep -q "VeloWorld/veloworld-ride/pull/" "$F"; then echo "FAIL: $F has no VW PR URL"; exit 1; fi; if ! grep -q "VeloWorld/trainer-sim/commit/" "$F"; then echo "FAIL: $F has no trainer-sim commit URL"; exit 1; fi; PHASE_FM=$(awk '/^---$/{c++; if(c==2)exit; next} c==1' "$F" | grep -c '^phase: 05-veloworld-end-to-end-validation'); if [ "$PHASE_FM" != "1" ]; then echo "FAIL: frontmatter phase field missing or wrong"; exit 1; fi; STATUS_FM=$(awk '/^---$/{c++; if(c==2)exit; next} c==1' "$F" | grep -c '^status: passed'); if [ "$STATUS_FM" != "1" ]; then echo "FAIL: frontmatter status not passed"; exit 1; fi; echo "OK: $F has $LOC lines, 5-item bundle, no placeholders, VW-01/02/03 mapped"; }</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `/Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md` exists.
    - Source assertion: The file has at least 80 lines (per must_haves `min_lines`).
    - Source assertion: The file contains the literal section heading `## Acceptance Bundle (D-VW-09)`.
    - Source assertion: Within the Acceptance Bundle section, exactly 5 numbered items (`1.`, `2.`, `3.`, `4.`, `5.`) appear at line-start.
    - Source assertion: No `<placeholder>` strings remain in the file (`grep -E '<[A-Z]|<ISO|<sha|<PR URL|<copy|<actual|<Linux|<macOS|<wave-0|<pre-wave-0|<if Plan'` exits 1).
    - Source assertion: The file mentions `VW-01`, `VW-02`, `VW-03` each at least once.
    - Source assertion: The file contains at least one `https://github.com/VeloWorld/veloworld-ride/pull/` URL (item 1).
    - Source assertion: The file contains at least one `https://github.com/VeloWorld/trainer-sim/commit/` URL (item 4).
    - Source assertion: The frontmatter has `phase: 05-veloworld-end-to-end-validation`, `status: passed`, and a populated `verified:` ISO timestamp.
    - Source assertion: The "Anti-Patterns Found" section is present AND its table has zero data rows (only the header).
    - Behavior: The narrative (item 5) explicitly states whether a Wave 0.5 trainer-sim fix happened (matches `/tmp/05-03-iteration-log.md`'s record).
    - Behavior: The narrative explicitly states the speed=null wire-format diff (per `<interfaces>` in Plan 05-02 + RESEARCH "Wire-Format Compatibility").
  </acceptance_criteria>
  <done>
    `05-VERIFICATION.md` is written with the 5-item D-VW-09 acceptance bundle, ROADMAP success criteria mapped, Requirements Coverage table for VW-01..03, no placeholder strings. Anti-Patterns Found is empty (with the parenthetical noting trainer-sim contract didn't widen). The file references concrete URLs and shas — no abstract `<...>` markers.
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit 05-VERIFICATION.md to trainer-sim main</name>
  <files>(no source edits — git operations)</files>
  <read_first>
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md (post-Task-1 state).
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.gitignore — confirm `.planning/` is NOT gitignored.
    - /Users/agniveshpatel/dev/agni21/trainer-sim/.planning/phases/05-veloworld-end-to-end-validation/05-CONTEXT.md §"Non-edits" lines 141-147 — confirm Wave 2 makes no source/test/CI/package.json changes.
  </read_first>
  <action>
    `cd /Users/agniveshpatel/dev/agni21/trainer-sim` and:

    1. `git status` — confirm only `.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md` is added/modified. If anything under `src/`, `test/`, `.github/`, `package.json`, `tsup.config.ts`, etc. shows up, surface the error (D-VW-06 + D-VW-07 violation).

    2. `git diff HEAD -- src/ test/ .github/workflows/ci.yml package.json tsup.config.ts` — should be empty.

    3. Stage just the verification file: `git add .planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md`. (NOT `git add -A`; the explicit path prevents accidental inclusion of other files.)

    4. Commit with HEREDOC message:
       ```
       docs(05): record VW E2E acceptance bundle (D-VW-09)

       Phase 5 close: 05-VERIFICATION.md captures the 5-item acceptance
       bundle per CONTEXT D-VW-09 — merged VW PR URL, both CI run URLs
       (macOS + Linux Node 24, conclusion: SUCCESS), trainer-sim sha
       pinned by the merged PR, and the narrative covering shape diffs,
       wire-format diffs (speed=null per FTMS-06 v2-deferred), and any
       Wave 0.5 iteration cycles.

       Per D-VW-07: no src/, test/, or CI changes in this commit. The
       only file touched is the verification doc.

       v1 acceptance gate satisfied. Phase 5 complete.

       Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
       ```

    5. `git log -1 --pretty=%s` — confirm subject is `docs(05): record VW E2E acceptance bundle (D-VW-09)`.

    6. Push: `git push origin main`.
  </action>
  <verify>
    <automated>cd /Users/agniveshpatel/dev/agni21/trainer-sim &amp;&amp; { LAST_MSG=$(git log -1 --pretty=%s); if ! echo "$LAST_MSG" | grep -q 'docs(05): record VW E2E acceptance bundle'; then echo "FAIL: HEAD subject is '$LAST_MSG'"; exit 1; fi; FILES=$(git diff HEAD~1 HEAD --name-only); if [ "$FILES" != ".planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md" ]; then echo "FAIL: Wave 2 commit touched unexpected files: $FILES"; exit 1; fi; SRC_DIFF=$(git diff HEAD~1 HEAD -- src/ test/ .github/workflows/ci.yml package.json tsup.config.ts | wc -l); if [ "$SRC_DIFF" != "0" ]; then echo "FAIL: D-VW-06/07 violated — src/test/CI/package changed in Wave 2 commit"; exit 1; fi; UPSTREAM=$(git rev-parse @{u} 2>/dev/null); HEAD=$(git rev-parse HEAD); if [ "$UPSTREAM" != "$HEAD" ]; then echo "FAIL: HEAD not pushed (upstream $UPSTREAM != HEAD $HEAD)"; exit 1; fi; echo "OK: Wave 2 commit landed and pushed cleanly"; }</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `git log -1 --pretty=%s` outputs `docs(05): record VW E2E acceptance bundle (D-VW-09)`.
    - Source assertion: `git diff HEAD~1 HEAD --name-only` outputs exactly the line `.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md` (one and only one file changed).
    - Source assertion: `git diff HEAD~1 HEAD -- src/ test/ .github/workflows/ci.yml package.json tsup.config.ts` is empty (D-VW-06 + D-VW-07 honored).
    - CLI: `git rev-parse @{u}` matches `git rev-parse HEAD` (the commit is pushed to `origin/main`).
    - Behavior: `git log --oneline -3` shows the Wave 0 + Wave 2 commits in order (Wave 0 from Plan 05-01, then potentially Wave 0.5 from Plan 05-03 iteration if it happened, then this Wave 2 doc commit).
  </acceptance_criteria>
  <done>
    `05-VERIFICATION.md` is committed to trainer-sim's `main` with subject `docs(05): record VW E2E acceptance bundle (D-VW-09)` and pushed to `origin`. The diff is exactly one file. No source/test/CI/package.json changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 05-03 outputs (`/tmp/*` files) -> 05-VERIFICATION.md content | The five `/tmp/05-03-*` files are produced by a previous plan in the same session. They are local-machine artifacts; standard process trust. |
| 05-VERIFICATION.md -> trainer-sim main branch | A new commit to a documented-only path under `.planning/`. No source code, no test, no CI. Standard git operations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | Mismatch between recorded sha and actual sha pinned at merge | mitigate | Task 1's substitution recipe lifts shas verbatim from `05-WAVE-0-SHA.txt` and `/tmp/05-03-merge-sha.txt`. The acceptance criteria explicitly checks for substituted-not-placeholder content. A reader can cross-reference the PR URL → `gh pr view ... --json mergeCommit` to detect any mismatch. |
| T-05-04-02 | Repudiation | Verification doc claims "passed" without underlying evidence | mitigate | The doc references concrete PR + run URLs. Anyone reading `05-VERIFICATION.md` can click the URLs and verify CI conclusion + merge state. The cross-repo sha pin is the immutable evidence trail per CONTEXT §"specifics". |
| T-05-04-03 | Information Disclosure | Verification doc captures internal `/tmp/` paths | accept | The `/tmp/` paths in the `<inputs_to_lift>` block are session-local; the FINAL doc references concrete URLs and shas, not `/tmp/` paths. (Acceptance criteria checks no `<placeholder>` strings remain — `/tmp/...` markers would be placeholders too.) |
| T-05-04-04 | Tampering | A bad-actor amends the Wave 2 commit to record false success | mitigate | Standard repo hygiene (branch protection on `main` if VW configures it; PR review for Phase 5's verification commit). The plan-checker can be re-run against `05-VERIFICATION.md` to detect: (a) missing `## Acceptance Bundle (D-VW-09)` heading, (b) <5 items, (c) sha mismatch with `gh pr view`. |
| T-05-04-05 | Denial of Service | n/a | n/a | No DoS surface; the plan is local file edits + a git push. |
| T-05-04-06 | Elevation of Privilege | The Wave 2 commit accidentally includes source changes | mitigate | Acceptance criteria's grep (`git diff HEAD~1 HEAD -- src/ test/ .github/workflows/ci.yml package.json tsup.config.ts | wc -l == 0`) is the hard gate. The commit is rejected if anything outside `.planning/` shows up. |
</threat_model>

<verification>
After both tasks complete, the cumulative state is:

```bash
cd /Users/agniveshpatel/dev/agni21/trainer-sim

# The verification doc exists
ls -la .planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md
# Expect: file present, ≥80 lines

# The 5-item bundle is structurally correct
grep "^## Acceptance Bundle (D-VW-09)" .planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md
# Expect: 1 match

# Concrete URLs (not placeholders)
grep -c "VeloWorld/veloworld-ride/pull/" .planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md
# Expect: ≥1
grep -c "VeloWorld/trainer-sim/commit/" .planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md
# Expect: ≥1

# The commit is on main and pushed
git log -1 --pretty='%h %s'
# Expect: <sha> docs(05): record VW E2E acceptance bundle (D-VW-09)
git rev-parse @{u}
# Expect: matches HEAD

# D-VW-06 + D-VW-07 honored: only the doc file changed in Wave 2
git diff HEAD~1 HEAD --name-only
# Expect: exactly one line, .planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md
```

If all green, Phase 5 is closed; v1 is shippable per PROJECT.md.
</verification>

<success_criteria>
Wave 2 plan complete when:

1. `.planning/phases/05-veloworld-end-to-end-validation/05-VERIFICATION.md` exists with ≥80 lines, valid frontmatter (`phase`, `verified`, `status: passed`, `score`, `re_verification`), and the structural sections from PATTERNS §"05-VERIFICATION.md" template.
2. The `## Acceptance Bundle (D-VW-09)` section contains exactly 5 numbered items (PR URL + merge sha; macOS CI URL + SUCCESS; Linux CI URL + SUCCESS; trainer-sim sha pinned; narrative).
3. The Requirements Coverage table maps VW-01, VW-02, VW-03 each to a SATISFIED row with concrete evidence references.
4. The "Anti-Patterns Found" table is empty (with the parenthetical noting trainer-sim's `ITrainerTransport` did NOT widen during Phase 5).
5. The narrative records the speed=null wire-format diff explicitly and identifies it as expected per FTMS-06 v2-deferred.
6. The narrative records whether a Wave 0.5 trainer-sim fix happened during Plan 05-03's iteration (with shas + bug descriptions, OR an explicit "no Wave 0.5" sentence).
7. No `<placeholder>` strings remain in the doc.
8. The verification doc is committed to trainer-sim's `main` with subject `docs(05): record VW E2E acceptance bundle (D-VW-09)`.
9. The Wave 2 commit's diff is EXACTLY one file: the verification doc itself. No source, test, CI, or package.json changes (D-VW-06 + D-VW-07 honored).
10. The commit is pushed to `origin/main`.
</success_criteria>

<output>
After completion, create `.planning/phases/05-veloworld-end-to-end-validation/05-04-SUMMARY.md` recording:
- The path of the verification doc.
- The commit sha for the Wave 2 commit.
- The trainer-sim sha that the merged VW PR pinned (lifted into item 4 of the bundle).
- The merged VW PR URL (lifted into item 1).
- A confirmation that the diff scope matches D-VW-06 + D-VW-07 (only `.planning/.../05-VERIFICATION.md` modified).
- Phase 5 close status: "complete" + "v1 shippable per PROJECT.md".
</output>
