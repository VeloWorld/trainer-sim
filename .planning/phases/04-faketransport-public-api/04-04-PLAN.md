---
phase: 04
plan: 04
type: execute
wave: 3
depends_on: [03]
files_modified:
  - test/transport/fake-transport.test.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
tags: [test, fake-transport, unit, fake-timers]

must_haves:
  truths:
    - "Vitest test file at test/transport/fake-transport.test.ts exercises the factory shape, lifecycle, fan-out, throw isolation, ordering, complete event, microtask boundary, and reset semantics — all under vi.useFakeTimers()"
    - "Tests use the { records: [...] } source variant for speed (D-API-05 fast path)"
    - "Tests import fakeAwareSleep from test/_helpers/fake-aware-sleep.js (Plan 04-01) and pass it through createFakeTransport's test-only second-arg sleep seam"
    - "Tests import createFakeTransport, FakeTransport, RideRecord through the public surface (../../src/index.js) NOT through internal paths"
    - "All new tests pass under `npm test -- test/transport/fake-transport.test.ts`"
  artifacts:
    - path: "test/transport/fake-transport.test.ts"
      provides: "Unit tests covering API-01, API-02, API-03, API-04, API-05, API-06 against the factory's records-source fast path"
      min_lines: 200
      contains: "describe('FakeTransport"
  key_links:
    - from: "test/transport/fake-transport.test.ts"
      to: "src/index.ts"
      via: "named imports through public surface"
      pattern: "from '\\.\\./\\.\\./src/index\\.js'"
    - from: "test/transport/fake-transport.test.ts"
      to: "test/_helpers/fake-aware-sleep.ts"
      via: "named import"
      pattern: "from '\\.\\./_helpers/fake-aware-sleep\\.js'"
---

<objective>
Create the unit-test suite for the FakeTransport factory at `test/transport/fake-transport.test.ts`. Uses the `{ records: [...] }` source variant (D-API-05 fast path) so tests run fast under `vi.useFakeTimers()`. Covers: factory shape, validation throw paths, lifecycle (connect → emit → 'complete' → disconnect), multi-subscriber Set fan-out, subscriber-throws isolation (D-API-10), sendResistance microtask boundary + ordering (D-API-04 + PITFALLS §12), received.resistance shape (D-API-05/16/17), reset() semantics (D-API-14 — clear log + recycle replay, preserve subscribers), and the disconnect-quiet-after-resolve invariant (REPL-06).

Imports go through the PUBLIC surface (`../../src/index.js`) — Phase 4 makes FakeTransport public, so tests prove the export shape end-to-end.

Requirements addressed: API-01, API-02, API-03, API-04, API-05, API-06.

Output: `test/transport/fake-transport.test.ts` (~200-280 LOC, ~7-9 describe groups).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-faketransport-public-api/04-CONTEXT.md
@.planning/phases/04-faketransport-public-api/04-RESEARCH.md
@.planning/phases/04-faketransport-public-api/04-PATTERNS.md
@.planning/phases/04-faketransport-public-api/04-03-SUMMARY.md
@src/transport/fake-transport.ts
@src/types.ts
@src/index.ts
@test/replay/replay.test.ts
@test/replay/abort.test.ts
@test/_helpers/fake-aware-sleep.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/transport/fake-transport.test.ts (factory unit tests)</name>
  <files>test/transport/fake-transport.test.ts</files>
  <read_first>
    - src/transport/fake-transport.ts (the SUT — confirm the second-arg `options?: { sleep?: SleepFn }` shape so tests call the factory correctly)
    - src/index.ts (confirm createFakeTransport + FakeTransport + RideRecord are all reachable through the public surface)
    - src/types.ts (confirm FakeTransport / RideRecord shape)
    - src/ftms/indoor-bike-data.ts (encoder byte layout — tests assert payload byteLength === 6 for power+cadence emissions per D-API-21)
    - test/_helpers/fake-aware-sleep.ts (Plan 04-01 helper — tests pass it through the factory's sleep seam)
    - test/replay/replay.test.ts (entire file — primary analog for the lifecycle test pattern; vi.useFakeTimers + makeRecords helper + await replay.completed)
    - test/replay/abort.test.ts (lines 80-130 — analog for "wait 100ms after stop, zero emissions" regression pattern; mirror at the transport layer for REPL-06)
    - .planning/phases/04-faketransport-public-api/04-CONTEXT.md (D-API-04 through D-API-22 — every locked decision the test must exercise)
    - .planning/phases/04-faketransport-public-api/04-RESEARCH.md (Code Example 5 — once() + fake timers; Pitfalls 1, 3, 6, 8 — each pitfall maps to a specific test case)
    - .planning/phases/04-faketransport-public-api/04-PATTERNS.md (TR1 module preamble; TR2 vi.useFakeTimers hooks; TR3 makeRecords helper; TR4 once() + fake timers; TR6 subscriber-throws; TR7 reset)
  </read_first>
  <action>
    Create `test/transport/fake-transport.test.ts`. Mirror Pattern TR1 (module preamble) — open with a long comment block citing the SUT (`src/transport/fake-transport.ts`), the locked decisions exercised (D-API-04 through D-API-22), the requirements addressed (API-01..06), and the pitfalls covered (04-RESEARCH §Pitfalls 1, 3, 6, 8 plus the fake-timers invariant from Phase 3 RESEARCH §Pitfall 5).

    **Imports:**
    - `import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';`
    - `import { once } from 'node:events';`
    - `import { createFakeTransport } from '../../src/index.js';` (public-surface import — Pattern TR1)
    - `import type { FakeTransport, RideRecord } from '../../src/index.js';` (type-only, public surface)
    - `import { fakeAwareSleep } from '../_helpers/fake-aware-sleep.js';` (lifted helper from Plan 04-01)

    **Top-of-file helper (Pattern TR3):** define `function makeRecords(count: number, cadenceMs = 100): RideRecord[]` returning records `{ timestamp: 1000 + i * cadenceMs }` for i in 0..count-1 (mirrors test/replay/replay.test.ts:33-35 verbatim).

    **Top-of-file factory helper:** define `function newTransport(records: RideRecord[]): FakeTransport` returning `createFakeTransport({ source: { records } }, { sleep: fakeAwareSleep })`. Centralizes the `{ sleep: fakeAwareSleep }` wiring so each test body stays readable.

    **Test groups (use `describe` per locked decision; one `describe` per concern). Apply Pattern TR2 hooks at the TOP-LEVEL describe — `beforeEach(() => vi.useFakeTimers()); afterEach(() => vi.useRealTimers());` — covering all groups below.**

    1. **Group 1 — D-API-04: factory is synchronous and returns the FakeTransport shape (API-01, API-02).**
       - Test: `createFakeTransport({ source: { records: makeRecords(3) } })` returns synchronously (no await needed).
       - Test: returned object has `connect`, `disconnect`, `onData`, `sendResistance`, `received`, `reset`, `on`, `off`, `once` — typed as `FakeTransport`.
       - Test: `received.resistance` starts as an empty array.
       - Test: typeof each method is `'function'`; `received` is an object with a `resistance` array property.

    2. **Group 2 — D-API-06: factory validates speed and maxEmissionHz synchronously.**
       - Test: `createFakeTransport({ source: { records: [...] }, speed: 0 })` throws synchronously with message matching `/speed must be > 0/`.
       - Test: `createFakeTransport({ source: { records: [...] }, speed: -1 })` throws (`/speed must be > 0/`).
       - Test: `createFakeTransport({ source: { records: [...] }, speed: NaN })` throws (`/speed must be > 0/`).
       - Test: `createFakeTransport({ source: { records: [...] }, maxEmissionHz: 0 })` throws (`/maxEmissionHz must be > 0/`).
       - Test: defaults applied — `createFakeTransport({ source: { records: [...] } })` succeeds (speed defaults to 1, maxEmissionHz to 1000, loop to false).

    3. **Group 3 — D-API-04 + connect() lifecycle: connect emits records, completes naturally, fires 'complete' (REPL-05 surfacing, API-01).**
       - Test (Pattern TR4 — once() + fake timers from 04-RESEARCH §Code Example 5): with 3 records at 100ms cadence and one onData subscriber, `await connect()` then `await vi.advanceTimersByTimeAsync(300)` then `await once(transport, 'complete')` resolves; the subscriber received exactly 3 DataView emissions; each emission has `byteLength === 6` (Flags + Cadence + Power per D-API-21, no speed bytes).
       - Test: 'complete' does NOT fire before connect (test that `once(transport, 'complete')` does not race resolve a previously-attached promise from a different lifecycle).

    4. **Group 4 — D-API-09 + D-API-20: multi-subscriber Set fan-out (API-03).**
       - Test: register 3 subscribers via `onData(...)` BEFORE connect; advance timers; all three subscribers receive the SAME number of emissions in the SAME order; the DataView reference passed to all three is the SAME object (Pattern 5 sharing — verifies the encode-once-fan-out-many recipe).
       - Test: disposer returned by `onData` removes the subscriber — register handler, dispose, connect, advance: handler receives ZERO emissions while other registered handlers receive all of them.
       - Test: handler removing itself during emit (Pitfall 3) — register handler that calls its disposer on first invocation; verify it receives the FIRST emission but NOT subsequent emissions; verify other handlers continue receiving.

    5. **Group 5 — D-API-10: subscriber throw isolation (Pitfall 8 / Pattern TR6).**
       - Test: register handler1 that throws on every call; register handler2 that pushes to an array; advance through all records; handler2 received all emissions (throw did not abort the loop). Use `vi.spyOn(console, 'error')` is NOT needed — debuglog is silent unless `NODE_DEBUG=trainer-sim:transport` is set.
       - Test: throwing handler does NOT prevent 'complete' from firing.

    6. **Group 6 — D-API-04 + PITFALLS §12: sendResistance microtask boundary + ordering (API-04, API-05).**
       - Test: `await sendResistance(0.05)` then immediately `expect(received.resistance).toEqual([0.05])` — the push is observable AFTER the await resolves (the microtask boundary lets `await` schedule the push).
       - Test: ordering across three sequential `await sendResistance(g)` calls produces `received.resistance === [a, b, c]` in call order.
       - Test: `sendResistance` does NOT modify any subsequent emitted DataView's bytes — register a subscriber that snapshots emitted bytes; call sendResistance during connect's running phase; advance timers; verify the snapshot bytes only contain power+cadence values matching the input records (NOT modified by the resistance grade).

    7. **Group 7 — D-API-12 + REPL-06: disconnect-then-quiet (Pitfall 6 / 04-RESEARCH §Pattern 3).**
       - Test (analog of test/replay/abort.test.ts:100-113): `connect`, register subscriber, advance halfway through records, call `await disconnect()`, then `await vi.advanceTimersByTimeAsync(100)`; assert the subscriber received emissions ONLY from the pre-disconnect window — zero further emissions after disconnect resolved.
       - Test: 'complete' does NOT fire when disconnect aborts mid-stream (D-API-12). Attach a `'complete'` listener that flips a boolean; disconnect mid-stream; advance 100ms; assert the boolean is still false.

    8. **Group 8 — D-API-14/15: reset() semantics (API-06, Pattern TR7).**
       - Test: connect, await complete, sendResistance × 2, reset; `received.resistance` is `[]`; subscribers preserved (the same handler registered before reset receives emissions in the SECOND lifecycle).
       - Test: after reset, calling connect again succeeds (the previous Replay was discarded — Phase 3's single-use lock would have thrown if reset reused the instance).
       - Test: `reset()` returns a Promise; `await reset()` resolves; assertions after the await observe the cleared state.
       - Test: reset() during a running replay — connect, advance halfway, await reset(); the cleared resistance log is observable; subscribers receive zero emissions during the wait-100ms-after-reset window (REPL-06 contract via reset's internal disconnect).

    9. **Group 9 — D-API-13 + Pattern 2: composed EventEmitter, on/off/once typed for 'complete' only.**
       - Test: `transport.once('complete', listener)` fires the listener exactly once on natural completion.
       - Test: `transport.on('complete', listener)` fires the listener; `transport.off('complete', listener)` removes it (a follow-up `'complete'` after a re-connect+reset would not fire the off'd listener).
       (No test for non-'complete' event names — the literal `'complete'` type-narrowing is a TypeScript-only constraint, not a runtime one.)

    **Test conventions:**
    - All groups use `vi.advanceTimersByTimeAsync()` (NOT the sync variant — Phase 3 RESEARCH §Pitfall 5; reaffirmed in 04-RESEARCH §Pitfall 4).
    - Each test creates a fresh transport via `newTransport(...)` to avoid state bleed (the FakeTransport's reset() is itself one of the things under test, not a setup tool here).
    - Use `await once(transport, 'complete')` for natural-completion synchronization (Pattern TR4 — verified to cooperate with vi.useFakeTimers per 04-RESEARCH §Open Questions resolution 6).
    - JSDoc decision-citation per Pattern S6: each `describe` group name includes the D-API-XX it exercises; the leading describe block JSDoc cites the test file's covered decisions.
    - Per CLAUDE.md "default to writing no comments": describe / it titles are the documentation; do NOT add line-by-line `// arrange / // act / // assert` comments inside test bodies. The body structure makes intent clear.
  </action>
  <verify>
    <automated>tsc --noEmit -p tsconfig.test.json &amp;&amp; test -f test/transport/fake-transport.test.ts &amp;&amp; npm test -- test/transport/fake-transport.test.ts 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit -p tsconfig.test.json` exits 0.
    - `test/transport/fake-transport.test.ts` exists and contains at least 9 `describe` blocks (one per locked decision group above).
    - File imports `createFakeTransport` and `FakeTransport`, `RideRecord` from `../../src/index.js` (NOT from internal paths — public-surface validation).
    - File imports `fakeAwareSleep` from `../_helpers/fake-aware-sleep.js`.
    - File contains the `vi.useFakeTimers()` / `vi.useRealTimers()` lifecycle hooks at the top-level describe (Pattern TR2).
    - File contains at least one `await once(transport, 'complete')` site (Pattern TR4).
    - File contains at least one `vi.advanceTimersByTimeAsync(` call (NEVER the sync variant — `grep "advanceTimersByTime\\b" test/transport/fake-transport.test.ts` should return ZERO matches when filtered to exclude the `Async` variant: `grep -E "advanceTimersByTime[^A]" test/transport/fake-transport.test.ts` returns zero).
    - File contains a test for sendResistance ordering (`received.resistance` ordering matches sendResistance call order).
    - File contains a test for subscriber-throws isolation.
    - File contains a "wait 100ms after disconnect, zero emissions" regression test.
    - File contains a "subscribers preserved across reset" test.
    - `npm test -- test/transport/fake-transport.test.ts` reports all tests passing (zero failures, zero unexpected skips).
  </acceptance_criteria>
  <done>The unit-test suite exists, exercises every D-API decision and API-01..06 requirement against the records-source fast path, and passes under fake timers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → Public surface | Tests import via `../../src/index.js` to validate the public re-export end-to-end |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | A test silently uses `vi.advanceTimersByTime` (sync variant) and produces non-deterministic pass/fail under microtask interleaving | mitigate | Acceptance grep `grep -E "advanceTimersByTime[^A]" test/transport/fake-transport.test.ts` returns zero matches. Phase 3 RESEARCH §Pitfall 5 + 04-RESEARCH §Pitfall 4 establish this discipline; the test file's preamble cites both. |
| T-04-04-02 | Tampering | Test bodies bypass the public surface and import from `src/transport/fake-transport.ts` directly, allowing invariants like the index.ts re-export to silently regress | mitigate | All test imports go through `../../src/index.js`. Verified by acceptance grep — `grep -n "from '\\.\\./\\.\\./src/transport" test/transport/fake-transport.test.ts` returns zero matches. |
| T-04-04-03 | Information Disclosure | A test asserts on debuglog output (which is opt-in via `NODE_DEBUG=trainer-sim:transport`), tying the test to an env-var that may not be set in CI | accept | Tests do NOT assert on debuglog output. Subscriber-throws isolation (Group 5) verifies BEHAVIOR (handler2 still received emissions), NOT log output. The debuglog is a developer tool, not a test surface. |
</threat_model>

<verification>
- `tsc --noEmit -p tsconfig.test.json` exits 0.
- `npm test -- test/transport/fake-transport.test.ts` reports all tests passing.
- `grep -E "advanceTimersByTime[^A]" test/transport/fake-transport.test.ts` returns ZERO matches (sync-variant ban).
- `grep -n "from '\\.\\./\\.\\./src/transport" test/transport/fake-transport.test.ts` returns ZERO matches (public-surface discipline).
- `grep -c "describe(" test/transport/fake-transport.test.ts` returns at least 9.
- `grep -n "await once(transport, 'complete')" test/transport/fake-transport.test.ts` matches at least once.
</verification>

<success_criteria>
- All 6 requirements (API-01, API-02, API-03, API-04, API-05, API-06) covered by at least one passing test.
- Every locked decision D-API-04 / 06 / 09 / 10 / 12 / 13 / 14 / 15 / 17 / 20 has a test asserting its observable behavior.
- The "wait 100ms after disconnect, zero emissions" REPL-06 regression test passes (analog of test/replay/abort.test.ts).
- The subscriber-throws-does-not-abort-loop test passes (D-API-10 / Pitfall 8).
- The reset-preserves-subscribers test passes (D-API-14 step 4).
- The `await Promise.resolve()` microtask-boundary contract is observable (sendResistance ordering test).
</success_criteria>

<output>
After completion, create `.planning/phases/04-faketransport-public-api/04-04-SUMMARY.md` documenting:
- Test file created at `test/transport/fake-transport.test.ts`
- Test count + group count
- Confirmation that all imports go through `src/index.js` (public-surface validation)
- Coverage map: D-API-XX → test name(s)
- Requirements addressed: API-01, API-02, API-03, API-04, API-05, API-06
- Note: Plan 04-05 covers the `{ path }` and `{ buffer }` integration paths against test/fixtures/fit/basic.fit; Plan 04-06 covers publint+attw against the built artifact
</output>
