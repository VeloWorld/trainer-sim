---
phase: 04
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/transport/fake-transport.ts
  - src/index.ts
autonomous: true
requirements:
  - API-01
  - API-02
  - API-03
  - API-04
  - API-05
  - API-06
tags: [factory, fake-transport, public-surface]

must_haves:
  truths:
    - "createFakeTransport(config) is a synchronous factory returning a FakeTransport-shaped object literal (NOT a class instance)"
    - "createFakeTransport throws synchronously when speed <= 0 or maxEmissionHz <= 0 (Phase 3 followup WR-05 fold per D-API-25)"
    - "connect() is async; defers FIT load via loadFitFromPath/loadFitFromBuffer/records; constructs Replay; wires per-record encode + Set fan-out; calls replay.start()"
    - "disconnect() calls replay.stop() then awaits replay.completed.catch(() => undefined) so REPL-06 is observable"
    - "onData(handler) adds the handler to a Set<(data: DataView) => void> and returns a disposer that removes it"
    - "sendResistance(grade) awaits Promise.resolve() (microtask boundary per PITFALLS §12) THEN pushes grade to the resistance log"
    - "received.resistance is a ReadonlyArray<number> backed by an internal mutable number[] (NO Object.freeze per D-API-17)"
    - "reset() awaits disconnect(), truncates resistanceLog in place, and discards the Replay so the next connect() constructs a fresh one (D-API-14)"
    - "A subscriber that throws does NOT abort the loop or other subscribers (D-API-10 — per-handler try/catch with debuglog('trainer-sim:transport'))"
    - "FakeTransport composes EventEmitter<{ complete: [] }>; on/off/once pass-through ONLY for the literal 'complete' event"
    - "'complete' fires when replay.completed resolves naturally; does NOT fire when disconnect() aborts (D-API-12)"
    - "src/transport/fake-transport.ts is the ONLY src/* file that imports from '../replay/' (single-import-seam — D-API-18 + Pitfall 5)"
    - "No BLE-specific tokens (bleno|gatt|advertis) appear in src/index.ts, src/types.ts, or src/transport/fake-transport.ts (D-API-03)"
    - "src/index.ts re-exports createFakeTransport (value) and ITrainerTransport, FakeTransport, FakeTransportConfig, FakeTransportSource (types)"
  artifacts:
    - path: "src/transport/fake-transport.ts"
      provides: "createFakeTransport factory + FakeTransport object-literal shape"
      exports: ["createFakeTransport"]
      min_lines: 120
      contains: "createFakeTransport"
    - path: "src/index.ts"
      provides: "Public package barrel — Phase 4 adds 4 exports per D-API-07"
      exports: ["createFakeTransport"]
      contains: "createFakeTransport"
  key_links:
    - from: "src/transport/fake-transport.ts"
      to: "src/replay/replay.ts"
      via: "named import (Replay class) — single import seam"
      pattern: "import \\{ Replay \\} from '\\.\\./replay/replay\\.js'"
    - from: "src/transport/fake-transport.ts"
      to: "src/ftms/indoor-bike-data.ts"
      via: "named import (encodeIndoorBikeData) — called per record"
      pattern: "import \\{ encodeIndoorBikeData \\} from '\\.\\./ftms/indoor-bike-data\\.js'"
    - from: "src/transport/fake-transport.ts"
      to: "src/fit/loader.ts"
      via: "named imports (loadFitFromPath, loadFitFromBuffer) — called inside connect()"
      pattern: "import \\{ loadFitFromBuffer, loadFitFromPath \\} from '\\.\\./fit/loader\\.js'"
    - from: "src/index.ts"
      to: "src/transport/fake-transport.ts"
      via: "value re-export"
      pattern: "export \\{ createFakeTransport \\} from '\\./transport/fake-transport\\.js'"
    - from: "src/index.ts"
      to: "src/types.ts"
      via: "type-only re-export"
      pattern: "export type \\{[^}]*ITrainerTransport"
---

<objective>
Implement the FakeTransport factory at `src/transport/fake-transport.ts` and re-export it from `src/index.ts` per D-API-07. This is the keystone plan: the factory composes the Phase 3 Replay, the Phase 1 encoder, and the Phase 2 loader behind the `ITrainerTransport` contract from Plan 04-02.

Implements: D-API-01 (canonical definer site), D-API-02 (Promise<void> + microtask boundary on sendResistance), D-API-03 (no BLE leak — full grep enforcement here), D-API-04 (sync factory + deferred FIT load), D-API-05 (discriminated-union source), D-API-06 (factory-level validation of speed/maxEmissionHz — folds Phase 3 followup WR-05), D-API-07 (the four index.ts exports), D-API-09 (Set<handler> fan-out), D-API-10 (per-handler try/catch + debuglog), D-API-11 (composed EventEmitter, NOT extended), D-API-12 ('complete' on natural end only), D-API-14/15 (reset semantics), D-API-16/17 (received shape + readonly type, no Object.freeze), D-API-18 (src/transport/ module layout + single-import seam for Replay), D-API-20 (per-record collapse + fan-out recipe), D-API-25 (Phase 3 followups WR-05 + IN-01 folded — IN-01 closed in Plan 04-01).

Requirements addressed: API-01, API-02, API-03, API-04, API-05, API-06.

Output: New `src/transport/fake-transport.ts` (~150-200 LOC) + 4 new export lines in `src/index.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-faketransport-public-api/04-CONTEXT.md
@.planning/phases/04-faketransport-public-api/04-RESEARCH.md
@.planning/phases/04-faketransport-public-api/04-PATTERNS.md
@.planning/phases/04-faketransport-public-api/04-02-SUMMARY.md
@src/types.ts
@src/index.ts
@src/replay/replay.ts
@src/ftms/indoor-bike-data.ts
@src/fit/loader.ts
@src/fit/errors.ts

<interfaces>
<!-- Phase 3 Replay (single-import-seam target) — src/replay/replay.ts -->
export class Replay {
  constructor(config: ReplayConfig);
  get completed(): Promise<void>;
  get currentState(): ReplayState;
  onRecord(handler: (r: RideRecord) => void): () => void;
  start(config?: {
    signal?: AbortSignal;
    sleep?: (delay: number, value?: undefined, options?: { signal?: AbortSignal }) => Promise<void>;
  }): void;
  stop(): void;
}

<!-- Phase 3 ReplayConfig — src/replay/types.ts -->
interface ReplayConfig {
  records: ReadonlyArray<RideRecord>;
  speed: number;
  loop: boolean;
  maxEmissionHz: number;
}

<!-- Phase 1 encoder — src/ftms/indoor-bike-data.ts -->
export interface IndoorBikeRecord {
  power: number;     // sint16, watts (NOT optional — Phase 4 collapses RideRecord.power ?? 0)
  cadence: number;   // uint16, 0.5 rpm resolution (NOT optional — Phase 4 collapses RideRecord.cadence ?? 0)
  speed?: number;    // optional; Phase 4 omits per D-API-21
}
export function encodeIndoorBikeData(record: IndoorBikeRecord): DataView;

<!-- Phase 2 loader — src/fit/loader.ts -->
export function loadFitFromBuffer(input: Buffer | Uint8Array): RideRecord[];
export async function loadFitFromPath(path: string): Promise<RideRecord[]>;

<!-- Phase 4 types (added in Plan 04-02) — src/types.ts -->
export interface ITrainerTransport { connect/disconnect/onData/sendResistance }
export interface FakeTransport extends ITrainerTransport {
  readonly received: { resistance: ReadonlyArray<number> };
  reset(): Promise<void>;
  on(event: 'complete', listener: () => void): void;
  off(event: 'complete', listener: () => void): void;
  once(event: 'complete', listener: () => void): void;
}
export interface FakeTransportConfig { source; speed?; loop?; maxEmissionHz?; }
export type FakeTransportSource = { path: string } | { buffer: Buffer | Uint8Array } | { records: ReadonlyArray<RideRecord> };
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement src/transport/fake-transport.ts (factory + composition + 'complete' wiring + reset)</name>
  <files>src/transport/fake-transport.ts</files>
  <read_first>
    - src/replay/replay.ts (the public-to-Phase-4 Replay class — verify onRecord / start / stop / completed / currentState shape)
    - src/ftms/indoor-bike-data.ts (encodeIndoorBikeData signature + IndoorBikeRecord shape — confirm power/cadence are required `number`, speed is optional)
    - src/fit/loader.ts (loadFitFromPath async / loadFitFromBuffer sync signatures)
    - src/fit/errors.ts (FitLoadError hierarchy — bubbles unchanged through connect())
    - src/types.ts (FakeTransport / FakeTransportConfig / FakeTransportSource / ITrainerTransport from Plan 04-02)
    - src/replay/scheduler.ts (lines 63-70 — single-import-seam comment style to mirror)
    - src/fit/loader.ts (lines 21-23 — single-import-seam comment style to mirror)
    - .planning/phases/04-faketransport-public-api/04-CONTEXT.md (D-API-04, D-API-06, D-API-09 through D-API-15, D-API-17 through D-API-20)
    - .planning/phases/04-faketransport-public-api/04-RESEARCH.md (Code Example 1 — factory skeleton; Patterns 1-5; Pitfalls 1, 2, 3, 6, 8)
    - .planning/phases/04-faketransport-public-api/04-PATTERNS.md (Pattern A1 module-doc preamble; A2 single-import-seam; A3 debuglog; A4 .then(success,failure) defuse; A5 sync validation block; A7 closure-state JSDoc; S1 .js extensions; S2 verbatimModuleSyntax; S5 debuglog convention; S6 decision-citation JSDoc)
  </read_first>
  <action>
    Create file `src/transport/fake-transport.ts` (and the parent directory `src/transport/`). Implement the synchronous factory `createFakeTransport(config: FakeTransportConfig, options?: { sleep?: SleepFn }): FakeTransport`.

    **Imports (top of file, after the module-doc preamble — single-import-seam discipline per Pattern A2):**
    - `import { EventEmitter } from 'node:events';`
    - `import { debuglog } from 'node:util';`
    - `import { encodeIndoorBikeData } from '../ftms/indoor-bike-data.js';`
    - `import { loadFitFromBuffer, loadFitFromPath } from '../fit/loader.js';`
    - `import { Replay } from '../replay/replay.js';` — preceded by a comment block matching Phase 2 D-FIT-08 / Phase 3 scheduler.ts:63-70 single-seam style ("THE SINGLE Replay IMPORT IN ALL OF src/. No other src/* file may import from `../replay/` — D-API-18 seam (v2 BlenoTransport will be the second).")
    - `import type { FakeTransport, FakeTransportConfig, RideRecord } from '../types.js';` — type-only per `verbatimModuleSyntax`
    - Define a local type alias `type SleepFn = (delay: number, value?: undefined, options?: { signal?: AbortSignal }) => Promise<void>;` matching Phase 3's Replay.start `sleep?` signature exactly (replay.ts:201-205)
    - Module-top-level constant `const log = debuglog('trainer-sim:transport');` (Pattern A3 — third instance after `:fit` and `:replay`)

    **Module-doc preamble (Pattern A1):** Long block JSDoc citing D-API-01..21 (locked) + D-API-25 (folds), Pitfalls 1, 2, 3, 6, 8 from 04-RESEARCH (microtask boundary, no instanceof leak, mid-fan-out disposer, disconnect-completes-after-scheduler, subscriber-throws isolation), and references to `.planning/phases/04-faketransport-public-api/04-CONTEXT.md` and `04-RESEARCH.md`.

    **Factory signature (synchronous, returns plain object literal — D-API-04 + Discretion default):** `export function createFakeTransport(config: FakeTransportConfig, options?: { sleep?: SleepFn }): FakeTransport`. The `options.sleep?` is the test-only injection seam per 04-RESEARCH §Pitfall 4. Document with JSDoc as "test-only — do not use in production code." NOT part of `FakeTransportConfig` (kept off the public config to avoid encouraging consumer use).

    **Factory body — execute in this exact order:**

    1. **Sync validation (D-API-06 + Phase 3 followup WR-05 fold per D-API-25 + Pattern A5):** read `speed = config.speed ?? 1`, `loop = config.loop ?? false`, `maxEmissionHz = config.maxEmissionHz ?? 1000`. Throw with the messages `createFakeTransport: speed must be > 0, got <value>` and `createFakeTransport: maxEmissionHz must be > 0, got <value>`. Use the `!(x > 0)` form (catches NaN, 0, negative, AND non-numeric coercions in one expression — preferred over `<= 0`).

    2. **Closure-scope state (Pattern A7 — JSDoc each one with the D-API citation):**
       - `subscribers: Set<(data: DataView) => void>` initialized as `new Set()` — D-API-09
       - `resistanceLog: number[]` initialized as `[]` — D-API-16/17 (mutable backing for received.resistance)
       - `emitter: EventEmitter<{ complete: [] }>` initialized via `new EventEmitter()` — D-API-11 (composed; typed)
       - `replay: Replay | undefined` initialized as `undefined` — lazily constructed in connect(), discarded in reset()

    3. **Inner helper `loadRecords()` (async, returns ReadonlyArray<RideRecord>):** switches on the discriminated union (`'records' in src` / `'buffer' in src` / fallthrough is `path`). Calls `loadFitFromPath(src.path)` (await), `loadFitFromBuffer(src.buffer)` (sync), or returns `src.records` directly. FitLoadError + filesystem errors bubble unchanged.

    4. **`async function connect(): Promise<void>`:**
       - Idempotent: if `replay !== undefined` return early.
       - `const records = await loadRecords();`
       - `replay = new Replay({ records, speed, loop, maxEmissionHz });`
       - `replay.onRecord((rec) => { ... })` — the per-record handler does:
         - `const dv = encodeIndoorBikeData({ power: rec.power ?? 0, cadence: rec.cadence ?? 0 });` (D-API-20 — the ONLY place absent→0 collapses; speed omitted per D-API-21)
         - `for (const h of subscribers) { try { h(dv); } catch (err) { log('subscriber threw: %O', err); } }` (D-API-10 — per-handler try/catch with debuglog)
       - **Wire 'complete' on natural completion only (D-API-12 + Pattern A4):** `replay.completed.then(() => emitter.emit('complete'), () => undefined);`. The `.then(success, failure)` form attaches BOTH handlers eagerly so the abort rejection never surfaces as `unhandledRejection`. Mirrors `replay.ts:247-256`.
       - `replay.start(options?.sleep ? { sleep: options.sleep } : undefined);` Pass the test-only sleep seam through to Replay.start when supplied; otherwise pass undefined so Replay uses its production node:timers/promises path.

    5. **`async function disconnect(): Promise<void>` (Pattern 3 from 04-RESEARCH):**
       - Idempotent: if `replay === undefined` return early.
       - Capture local `const r = replay; replay = undefined;` — so a re-entrant connect() during the await constructs cleanly.
       - `r.stop();`
       - `await r.completed.catch(() => undefined);` — consumes the rejection caused by our own stop(), and lets the scheduler's last microtask fully unwind so REPL-06 ("after disconnect resolves, no further onData callbacks fire") is observable.

    6. **`function onData(handler: (data: DataView) => void): () => void`:** `subscribers.add(handler);` then return a disposer arrow function `() => { subscribers.delete(handler); }`.

    7. **`async function sendResistance(grade: number): Promise<void>` (PITFALLS §12 microtask boundary):** `await Promise.resolve();` BEFORE the push (canonical idiom; matches v2 BlenoTransport timing). Then `resistanceLog.push(grade);`.

    8. **`async function reset(): Promise<void>` (D-API-14 + Pattern 4 from 04-RESEARCH):**
       - `await disconnect();` (idempotent step 1).
       - `resistanceLog.length = 0;` — truncate IN PLACE (NOT `resistanceLog = []`) so the same array reference returned by `received.resistance` stays stable across reset().
       - **Do NOT clear `subscribers`** (D-API-14 step 4).
       - The replay was already set to undefined inside disconnect(); the next connect() will construct a fresh one (D-API-14 step 3).

    9. **Return the FakeTransport object literal (D-API-04 default — NOT a class instance to avoid `instanceof` leak per Pitfall 2):** Object literal with members `connect`, `disconnect`, `onData`, `sendResistance`, a `received` getter that returns `{ resistance: resistanceLog as ReadonlyArray<number> }`, `reset`, and three pass-through methods `on(event: 'complete', listener: () => void): void`, `off(event: 'complete', listener: () => void): void`, `once(event: 'complete', listener: () => void): void` that delegate to `emitter.on/off/once`. The on/off/once method signatures use the LITERAL `'complete'` event-name (NOT a `string` parameter — Pattern 2 / D-API-13). The `received` getter MUST alias the SAME `resistanceLog` array so reset's truncate-in-place is observable to consumers who hold a reference (Pattern 4 rationale).

    **Naming inside the closure (Discretion):** Use `subscribers`, `resistanceLog`, `replay`, `emitter`, `log`, `speed`, `loop`, `maxEmissionHz` exactly. Do NOT introduce additional abstractions (no SubscriberRegistry class, no ResistanceLog object, no encoder wrapper) — CLAUDE.md "Don't add features, refactor, or introduce abstractions beyond what the task requires."

    **No defensive validation beyond D-API-06:** CLAUDE.md "Trust internal code and framework guarantees. Only validate at system boundaries (user input, external APIs)." The factory boundary IS the user-input boundary (createFakeTransport is the public API entry point), so the speed / maxEmissionHz validation per D-API-06 stays. Do NOT add `if (typeof grade !== 'number')` inside sendResistance — TypeScript handles the boundary. Do NOT add `if (!Array.isArray(records))` inside loadRecords — the discriminated-union typing handles it.

    **Comment posture (CLAUDE.md "default to writing no comments. Only WHY-non-obvious"):** The module-doc preamble + JSDoc on the closure-state lets and on the async-microtask-boundary `await Promise.resolve()` is acceptable WHY-non-obvious. Do NOT add `// closure-scope state` / `// per-record handler` / `// idempotent` line comments to the body — the JSDoc already says it.
  </action>
  <verify>
    <automated>tsc --noEmit -p tsconfig.test.json &amp;&amp; test -f src/transport/fake-transport.ts &amp;&amp; grep -q "export function createFakeTransport" src/transport/fake-transport.ts &amp;&amp; grep -q "debuglog('trainer-sim:transport')" src/transport/fake-transport.ts &amp;&amp; grep -q "EventEmitter<{ complete: \[\] }>" src/transport/fake-transport.ts &amp;&amp; grep -q "await Promise.resolve()" src/transport/fake-transport.ts &amp;&amp; grep -q "rec.power ?? 0" src/transport/fake-transport.ts &amp;&amp; grep -q "subscribers.delete(handler)" src/transport/fake-transport.ts &amp;&amp; grep -q "resistanceLog.length = 0" src/transport/fake-transport.ts &amp;&amp; ! grep -E "bleno|gatt|advertis" src/transport/fake-transport.ts &amp;&amp; ! grep -E "class FakeTransport extends" src/transport/fake-transport.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit -p tsconfig.test.json` exits 0.
    - `src/transport/fake-transport.ts` exists and exports a single function `createFakeTransport`.
    - Function signature is `(config: FakeTransportConfig, options?: { sleep?: SleepFn }) => FakeTransport`.
    - File contains `debuglog('trainer-sim:transport')` (D-API-10 + Pattern A3).
    - File contains `EventEmitter<{ complete: [] }>` (D-API-11 typed-EE form).
    - File contains the literal text `await Promise.resolve()` inside the sendResistance body BEFORE the push (PITFALLS §12).
    - File contains `rec.power ?? 0` and `rec.cadence ?? 0` (D-API-20 per-record collapse).
    - File contains `subscribers.delete(handler)` (D-API-09 disposer pattern).
    - File contains `resistanceLog.length = 0` inside reset() (Pattern 4 — truncate in place, NOT reassign).
    - File contains `replay.completed.then(` with two arguments (success / failure — Pattern A4 eager defuse).
    - File contains `r.completed.catch(() => undefined)` (or equivalent) after `r.stop()` (Pattern 3 — disconnect awaits scheduler unwind).
    - File DOES NOT contain `class FakeTransport extends` (D-API-11 prohibition + Pitfall 2).
    - File DOES NOT contain `bleno|gatt|advertis` tokens (D-API-03 acceptance grep).
    - `grep -rln "from '\\.\\./replay" src/ | grep -v "^src/transport/fake-transport.ts"` returns ZERO matches (single-import-seam — Pitfall 5).
  </acceptance_criteria>
  <done>The factory file exists, typechecks, satisfies all D-API contracts above, and is the sole `../replay/` importer in src/.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 4 exports to src/index.ts (D-API-07)</name>
  <files>src/index.ts</files>
  <read_first>
    - src/index.ts (entire file — current Phase 1 + Phase 2 exports + the Phase-4-pending preamble)
    - src/transport/fake-transport.ts (just created in Task 1 — confirm `createFakeTransport` is exported)
    - src/types.ts (Plan 04-02 added the four types — confirm names match)
    - .planning/phases/04-faketransport-public-api/04-CONTEXT.md (D-API-07 — exact export list)
    - .planning/phases/04-faketransport-public-api/04-PATTERNS.md (Pattern I1 — value vs `export type` discipline)
    - .planning/phases/04-faketransport-public-api/04-RESEARCH.md (§Code Example 3 — five-name list including the optional `FakeTransport` convenience type)
  </read_first>
  <action>
    Modify `src/index.ts`. Update the file preamble's "Phase 4 will add ITrainerTransport and createFakeTransport" sentence to "Phase 4: FakeTransport public surface (createFakeTransport + ITrainerTransport / FakeTransport / FakeTransportConfig / FakeTransportSource)."

    Append at the bottom of the file (preserve all Phase 1 + Phase 2 exports unchanged):
    - One value-export line: `export { createFakeTransport } from './transport/fake-transport.js';`
    - One type-only export block re-exporting these four names from `./types.js`: `ITrainerTransport`, `FakeTransport`, `FakeTransportConfig`, `FakeTransportSource`.

    Discipline (Pattern I1 + S2): `createFakeTransport` is a runtime value — exported with the value form (`export {}`). The four types are type-only — exported with `export type {}`. The `.js` extension on relative imports is preserved (Pattern S1). Per CLAUDE.md "default to writing no comments" — a single-line decision-citation comment such as `// Phase 4: FakeTransport public surface (D-API-07).` is acceptable WHY-non-obvious; do NOT add per-line commentary.

    **Note on the optional fifth name:** D-API-07 strictly names three types (`ITrainerTransport`, `FakeTransportConfig`, `FakeTransportSource`). 04-RESEARCH §Code Example 3 / 04-PATTERNS Pattern I1 add `FakeTransport` (the wider return-shape type) as a Discretion-level convenience: it lets consumers annotate `const t: FakeTransport = createFakeTransport(...)` without `ReturnType<typeof ...>` gymnastics. Include `FakeTransport` in the type-export list — costs nothing and improves consumer ergonomics.

    Do NOT modify `package.json` or `tsup.config.ts` (D-API-08 + 04-RESEARCH §Open Questions resolution 10 — both stay unchanged in Phase 4).
  </action>
  <verify>
    <automated>tsc --noEmit -p tsconfig.test.json &amp;&amp; grep -q "export { createFakeTransport } from './transport/fake-transport.js'" src/index.ts &amp;&amp; grep -q "ITrainerTransport" src/index.ts &amp;&amp; grep -q "FakeTransportConfig" src/index.ts &amp;&amp; grep -q "FakeTransportSource" src/index.ts &amp;&amp; grep -q "FakeTransport," src/index.ts &amp;&amp; ! grep -E "bleno|gatt|advertis" src/index.ts &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit -p tsconfig.test.json` exits 0.
    - `src/index.ts` contains the exact value-export line `export { createFakeTransport } from './transport/fake-transport.js';`.
    - `src/index.ts` re-exports all four types: `ITrainerTransport`, `FakeTransport`, `FakeTransportConfig`, `FakeTransportSource` from `./types.js` via `export type {}`.
    - The Phase 1 + Phase 2 export blocks are preserved unchanged.
    - `grep -E "bleno|gatt|advertis" src/index.ts` returns ZERO matches.
    - `npm run build` exits 0 (tsup produces dist/index.{js,cjs,d.ts,d.cts} containing the new exports).
    - `node -e "import('./dist/index.js').then(m => { if (typeof m.createFakeTransport !== 'function') process.exit(1); })"` exits 0 (the runtime export resolves).
    - `package.json` and `tsup.config.ts` are unchanged from before this plan (D-API-08 + Phase 4 zero-config-change invariant).
  </acceptance_criteria>
  <done>src/index.ts re-exports the Phase 4 surface; the build succeeds; runtime resolution of `createFakeTransport` is verified; package.json and tsup.config.ts are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public package surface (consumers) → trainer-sim | `src/index.ts` is the only file consumers import from; this plan adds the first runtime value (`createFakeTransport`) reachable from there since Phase 2 |
| Consumer-supplied FIT path → fs/promises.readFile | `loadFitFromPath` reads a consumer-supplied path; ENOENT/EACCES bubble unchanged |
| Consumer-supplied FIT buffer → fit-file-parser | Phase 2's loader already validates header + CRC + magic before parser sees the bytes |
| Consumer subscriber callback → fan-out loop | Untrusted callback that may throw or mutate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| **T-04-03-01** | **Tampering / Information Disclosure** | **BLE-type leak through public type graph (HIGH severity — silently breaks dual-publish; CJS consumers without bleno installed would fail to load the library)** | mitigate | **HIGH severity for this phase.** D-API-03 + 04-RESEARCH §Pitfall 13. Acceptance grep MUST pass: `grep -rn "bleno\|gatt\|advertis" src/index.ts src/types.ts src/transport/fake-transport.ts` returns zero matches. Verified in Task 1 + Task 2 verify blocks AND in the phase-level verification grep below. Plan 04-06's publint+attw run against the BUILT dist/ provides defense-in-depth — if a future plan accidentally adds a `import { ... } from '@stoprocent/bleno'`, attw will surface the dual-publish breakage. |
| T-04-03-02 | DoS | A subscriber that throws aborts the for-of fan-out, starving other subscribers | mitigate | D-API-10 + 04-RESEARCH §Pitfall 8: per-handler `try { h(dv); } catch (err) { log('subscriber threw: %O', err); }` wraps each invocation. The `debuglog('trainer-sim:transport')` swallow ensures observability without propagation. Plan 04-04 includes a regression test (test/transport/fake-transport.test.ts — Pattern TR6). |
| T-04-03-03 | DoS | A subscriber registered then never disposed (memory leak across many connect/disconnect cycles) | accept | The `onData` disposer return is the contract; consumers who don't call it leak their own callback. Documented in JSDoc. CLAUDE.md "Trust internal code" — we don't add a `WeakRef` runtime guard. |
| T-04-03-04 | Tampering | reset() racing with in-flight emit → ghost record after reset | mitigate | reset() awaits disconnect(), which awaits `replay.completed.catch(() => undefined)`. By the time reset() resolves, the scheduler's last microtask has unwound (Pattern 3 + REPL-06 + Phase 3 CR-01 fix at commit e4b04a9). Plan 04-04 regression test asserts "wait 100ms after disconnect, zero emissions" — analog of test/replay/abort.test.ts:100-113. |
| T-04-03-05 | Tampering | Consumer mutates the same DataView passed to multiple subscribers mid-fan-out, corrupting the bytes for later subscribers | accept | 04-RESEARCH §Pattern 5 + Assumptions A1: documented as consumer responsibility; JSDoc on the FakeTransport return type instructs consumers to treat the DataView as read-only and copy if they retain it. Mitigation is documentation, not code (CLAUDE.md "no abstractions for hypothetical concerns"). |
| T-04-03-06 | DoS | Consumer-supplied FIT path triggers ENOENT/EACCES inside connect() | mitigate | Bubbles unchanged through `connect()` Promise rejection (D-API-04). Phase 2's loader already caps file size at 50MB (PITFALLS.md). Phase 4 adds NO new file surface. |
| T-04-03-07 | Information Disclosure | The FitLoadError family bubbling from connect() exposes file structure details (CRC mismatches, truncation offsets) | accept | This is the contract — consumers `catch (e instanceof FitLoadError)` for diagnosis. The error messages do NOT include file paths or buffer contents. Phase 2 review verified. |
</threat_model>

<verification>
- `tsc --noEmit -p tsconfig.test.json` exits 0.
- `npm run build` exits 0 — `dist/index.{js,cjs,d.ts,d.cts}` regenerate; the four new exports are present.
- `grep -rn "bleno\|gatt\|advertis" src/index.ts src/types.ts src/transport/fake-transport.ts` returns ZERO matches (D-API-03 / T-04-03-01 — full BLE-leak grep enforcement; runs against all three files now that src/transport/fake-transport.ts exists).
- `grep -rln "from '\\.\\./replay" src/ | grep -v "^src/transport/fake-transport.ts"` returns ZERO matches (D-API-18 single-import-seam for Replay — Pitfall 5).
- `grep -c "debuglog('trainer-sim:" src/` returns at least 3 (`:fit` + `:replay` + new `:transport`).
- `node -e "import('./dist/index.js').then(m => process.exit(typeof m.createFakeTransport === 'function' ? 0 : 1))"` exits 0.
- `package.json` and `tsup.config.ts` are unchanged (verified by `git diff --stat` listing only src/transport/fake-transport.ts (new), src/index.ts, src/types.ts in this phase's modifications across Plans 04-01..04-03).
</verification>

<success_criteria>
- `createFakeTransport(config, options?)` factory exists with the exact signature from the action above.
- All locked decisions D-API-01 through D-API-21 (and folds D-API-25) implemented and grep-verifiable.
- Requirements satisfied: API-01 (factory returns ITrainerTransport-shaped object), API-02 (ITrainerTransport published from package root), API-03 (onData disposer), API-04 (sendResistance is echo-only — does NOT modify replayed payloads; the per-record path does NOT consult resistanceLog), API-05 (received.resistance ReadonlyArray), API-06 (reset() reusable across afterEach()).
- BLE-leak HIGH-severity threat T-04-03-01 mitigated; full grep enforcement now active.
- Single-import-seam invariant for Replay enforced.
- Phase 3 followup WR-05 closed (factory-level validation of speed/maxEmissionHz).
- `npm run build` succeeds; runtime resolution of `createFakeTransport` works through the built dist/.
- package.json and tsup.config.ts remain unchanged (D-API-08).
</success_criteria>

<output>
After completion, create `.planning/phases/04-faketransport-public-api/04-03-SUMMARY.md` documenting:
- Files created / modified (`src/transport/fake-transport.ts`, `src/index.ts`)
- Confirmation that `package.json` and `tsup.config.ts` are unchanged
- BLE-leak grep result: clean (T-04-03-01 mitigated)
- Single-import-seam grep result: clean (Pitfall 5 enforced)
- Phase 3 followup WR-05 closed
- Requirements addressed: API-01, API-02, API-03, API-04, API-05, API-06
- Note: Plan 04-04/04-05 add the unit + integration tests; Plan 04-06 runs publint+attw against the built artifact
</output>
