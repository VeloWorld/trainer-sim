---
phase: 04
plan: 06
type: execute
wave: 4
depends_on: [03, 04, 05]
files_modified:
  - test/transport/publish.test.ts
autonomous: true
requirements:
  - API-07
  - API-08
tags: [test, publish, publint, attw, dual-publish, slow]

must_haves:
  truths:
    - "Vitest test file at test/transport/publish.test.ts shells out to `npm run build`, `publint`, and `attw --pack .` (or equivalents) and asserts they exit 0"
    - "Test runs against the BUILT dist/ — proves the public package shape (D-API-07 exports + types-first conditional) is valid for both ESM consumers and CJS consumers"
    - "Test marked test.slow (or describe.slow) so the default `npm test` runs the whole suite without a 30+ second build delay being the dominant cost"
    - "Test passes — confirming API-07 + API-08 against the actual published artifact"
  artifacts:
    - path: "test/transport/publish.test.ts"
      provides: "Build + publint + attw smoke test against the dual-published dist/ — guards against future regressions to the exports map"
      contains: "validate"
  key_links:
    - from: "test/transport/publish.test.ts"
      to: "package.json (validate scripts)"
      via: "child_process.execSync invoking `npm run validate:publint` / `npm run validate:attw`"
      pattern: "execSync"
---

<objective>
Create the publish-hygiene smoke test at `test/transport/publish.test.ts`. This test runs the package's existing `npm run build`, `publint`, and `attw --pack .` chain against the freshly-built `dist/` and asserts they all exit 0. It catches regressions where the Phase 4 additions (or any future plan's additions) accidentally break the exports map, the types-first conditional, or dual-publish hygiene.

Per 04-RESEARCH §Tooling Validation, both `publint` and `attw` are currently GREEN against the existing `dist/` (verified mid-research). This test exists to keep them green as the public surface evolves.

Per D-API-22, this test is marked SLOW (it shells out to a multi-second `npm run build`) — the default `npm test` should not be dominated by build time. The slow-mark also documents to a future contributor that this is a heavyweight integration test, not a unit test.

Per D-API-08, `package.json` and `tsup.config.ts` are NOT modified in Phase 4 — this test simply runs the existing `npm run validate` chain (build + publint + attw, defined in package.json:38) against the new content of `dist/index.{js,cjs,d.ts,d.cts}`.

Requirements addressed: API-07 (dual ESM/CJS publish validated by publint + attw), API-08 (strict-mode TypeScript Node 24 import works without `@types/*` shim — attw verifies type-resolution works for ESM, CJS, node10, node16-CJS, node16-ESM, bundler).

Output: `test/transport/publish.test.ts` (~50-80 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-faketransport-public-api/04-CONTEXT.md
@.planning/phases/04-faketransport-public-api/04-RESEARCH.md
@.planning/phases/04-faketransport-public-api/04-PATTERNS.md
@.planning/phases/04-faketransport-public-api/04-03-SUMMARY.md
@package.json
@tsup.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test/transport/publish.test.ts (build + publint + attw smoke test, marked slow)</name>
  <files>test/transport/publish.test.ts</files>
  <read_first>
    - package.json (lines 31-40 — confirm `build`, `validate:publint`, `validate:attw`, `validate` script names; confirm `files: ["dist", "README.md", "LICENSE.md"]`)
    - tsup.config.ts (entire file — confirm `entry: ['src/index.ts']` and the `format: ['esm', 'cjs']` + `dts: true` declarations; this test passes if these are unchanged)
    - src/index.ts (Plan 04-03's edits — confirm `createFakeTransport` and the four type re-exports are present)
    - .planning/phases/04-faketransport-public-api/04-CONTEXT.md (D-API-08 — package.json/tsup.config.ts unchanged in Phase 4; D-API-22 — publish.test.ts is test.slow)
    - .planning/phases/04-faketransport-public-api/04-RESEARCH.md (§Tooling Validation — publint + attw currently GREEN; §Open Questions resolution 1 + 10 — no package.json or tsup.config.ts changes needed)
    - .planning/phases/04-faketransport-public-api/04-PATTERNS.md (TPB3 — shell-out test marked test.slow; the `package.json` validate chain is the analog)
  </read_first>
  <action>
    Create `test/transport/publish.test.ts`. Mirror Pattern TR1 (module preamble) — open with a comment block citing the SUT (the BUILT package shape under `dist/`), the locked decisions exercised (D-API-07 — public exports list, D-API-08 — single-rooted exports map, D-API-22 — test.slow marker), the requirements addressed (API-07, API-08), and a note that this test guards against future regressions (it does NOT fix current state — current state is already GREEN per 04-RESEARCH §Tooling Validation).

    **Imports:**
    - `import { describe, it, expect } from 'vitest';`
    - `import { execSync } from 'node:child_process';`
    - `import { fileURLToPath } from 'node:url';`
    - `import { dirname, resolve } from 'node:path';`
    - `import { readFileSync, existsSync } from 'node:fs';`

    **Constants:**
    - `const __dirname = dirname(fileURLToPath(import.meta.url));`
    - `const REPO_ROOT = resolve(__dirname, '../..');`

    **Helper:** `function runScript(script: string): void { execSync(\`npm run \${script}\`, { cwd: REPO_ROOT, stdio: 'pipe', encoding: 'utf8' }); }` — uses `stdio: 'pipe'` (NOT `'inherit'`) so the test runner output stays clean; the captured stdout/stderr is available on the thrown error if a sub-script fails. Per CLAUDE.md "no comments unless WHY-non-obvious" — the `'pipe'` choice is a WHY ("keep test output clean; failures still surface stdout via the error") that warrants a single-line comment.

    **Test groups:**

    1. **describe.skip ON CI without build access? NO — describe with concurrent:false (the build is global state).** Use a top-level `describe('FakeTransport publish hygiene (API-07 / API-08, test.slow)', () => { ... })`. Per D-API-22, mark the entire describe as slow. In Vitest 4, the slow marker is set per-test via `it.slow` is NOT a Vitest API — instead, use the longer `it.concurrent` discipline NOT applicable here, and rely on Vitest's auto-detection of slow tests + the test name suffix `(slow)` to communicate intent. Acceptable form:
       - Use `describe('FakeTransport publish hygiene (API-07 / API-08) [slow]', ...)` and `it(...)` blocks; the `[slow]` suffix in the title is a documentation-only convention (Vitest 4 reports test durations in its output).
       - **OR** if `vitest` 4.1.x exposes `describe.todo / .skip / .only` only and not `.slow`, use `it.concurrent` is NOT what we want — drop the marker but include a top-of-file JSDoc note that this file shells out and takes 5–30 seconds; CI runs it via the regular test invocation; humans iterating locally can skip it via `npm test -- --exclude '**/publish.test.ts'` when iterating on unit tests only.
       - **Read the vitest 4 API for the actual slow-marker syntax during execution; if `test.slow` / `describe.slow` does not exist, document the absence in the file's JSDoc preamble + use the `[slow]` suffix convention.**

    2. **Test 1 — `npm run build` succeeds (the build step Phase 4's index.ts edits depend on):**
       - `expect(() => runScript('build')).not.toThrow();`
       - Then assert `existsSync(resolve(REPO_ROOT, 'dist/index.js'))` and similarly for `dist/index.cjs`, `dist/index.d.ts`, `dist/index.d.cts` — proves all four artifacts emitted (the dual-publish output).

    3. **Test 2 — `dist/index.js` contains the new createFakeTransport export:**
       - Read the built ESM file: `const dist = readFileSync(resolve(REPO_ROOT, 'dist/index.js'), 'utf8');`
       - Assert it contains the literal text `createFakeTransport` (proves the export survived tree-shaking and tsup's transform).
       - Read `dist/index.d.ts` and assert it contains `ITrainerTransport` and `FakeTransportConfig` (proves the types are emitted into the `.d.ts`).

    4. **Test 3 — `npm run validate:publint` exits 0:**
       - `expect(() => runScript('validate:publint')).not.toThrow();`
       - This catches `EXPORTS_VALUE_NOT_PROVIDED`, `EXPORTS_TYPES_SHOULD_BE_FIRST`, `EXPORTS_DEFAULT_SHOULD_BE_LAST`, and the other publint rules per 04-RESEARCH §State of the Art.

    5. **Test 4 — `npm run validate:attw` exits 0 (API-08):**
       - `expect(() => runScript('validate:attw')).not.toThrow();`
       - This proves: ESM consumers resolve types, CJS consumers resolve types, node10 / node16-CJS / node16-ESM / bundler all green. Per 04-RESEARCH §Tooling Validation, attw currently emits "No problems found 🌟" — the test pins this.

    6. **Test 5 — `package.json` and `tsup.config.ts` unchanged from before Phase 4 (D-API-08):**
       - This is a defensive sanity check — read `package.json`, parse JSON, assert that `exports['.']` has the dual-publish shape with `import.types` first and `require.types` first (the publint rule). The exact expected shape:
         - `pkg.exports['.'].import.types === './dist/index.d.ts'`
         - `pkg.exports['.'].import.default === './dist/index.js'`
         - `pkg.exports['.'].require.types === './dist/index.d.cts'`
         - `pkg.exports['.'].require.default === './dist/index.cjs'`
       - Assert `pkg.engines.node === '>=24.0'` (Node 24 LTS pin per Phase 1 D-16).
       - Assert `pkg.files` array equals `['dist', 'README.md', 'LICENSE.md']` (the publish whitelist per Phase 1).
       - Assert `tsup.config.ts` content (read as text) includes `entry: ['src/index.ts']` (the single-entry build per D-API-08) and `format: ['esm', 'cjs']` and `dts: true`.

    **Test runtime budget:** Build is the dominant cost (~3-10 seconds depending on machine). publint runs in <1s; attw runs in <3s. Total: <20 seconds. Vitest's default 5-second per-test timeout will fire on the build test; use `it('builds dist artifacts', { timeout: 60_000 }, () => { ... })` (Vitest 4 per-test timeout) for Test 1 and Test 4 (attw can be slow on cold cache).

    **Comment posture:** Per CLAUDE.md "default to writing no comments. Only WHY-non-obvious" — describe / it titles document intent; the `stdio: 'pipe'` choice in `runScript` is one acceptable WHY-non-obvious comment. Do NOT add line-by-line commentary; the test bodies are short and clear.
  </action>
  <verify>
    <automated>tsc --noEmit -p tsconfig.test.json &amp;&amp; test -f test/transport/publish.test.ts &amp;&amp; npm test -- test/transport/publish.test.ts 2>&amp;1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit -p tsconfig.test.json` exits 0.
    - `test/transport/publish.test.ts` exists.
    - File imports `execSync` from `node:child_process` (NOT a third-party library — vendor-lite per CLAUDE.md "no abstractions").
    - File contains a test that runs `npm run build` and asserts non-throwing.
    - File contains a test that runs `npm run validate:publint` and asserts non-throwing.
    - File contains a test that runs `npm run validate:attw` and asserts non-throwing.
    - File contains an assertion that `dist/index.js`, `dist/index.cjs`, `dist/index.d.ts`, `dist/index.d.cts` all exist after build.
    - File contains an assertion that the built `dist/index.js` (or `.cjs`) contains the literal text `createFakeTransport` and that `dist/index.d.ts` contains `ITrainerTransport`.
    - File contains a `package.json` shape assertion (exports map dual-publish form with types-first).
    - File contains a `tsup.config.ts` content assertion (`entry: ['src/index.ts']` + `format: ['esm', 'cjs']` + `dts: true`).
    - File uses `{ timeout: 60_000 }` (or equivalent) on at least the build test to defeat Vitest's default 5s timeout.
    - `npm test -- test/transport/publish.test.ts` reports all tests passing (this is the binding gate — current state is GREEN per 04-RESEARCH; the test pins it).
  </acceptance_criteria>
  <done>The publish-hygiene smoke test exists, runs the build + publint + attw chain against dist/, asserts all four artifacts emit, and passes against current state — proving API-07 + API-08 are satisfied for the BUILT package.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → `npm run build` (child process) | Shells out to the local npm; trust assumption: `package.json` scripts are not malicious |
| Test file → `dist/` filesystem | Reads built artifacts after the build step; trust assumption: tsup output is what we asked for |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering | A future plan accidentally modifies `package.json` `exports` map and breaks dual-publish (CJS consumers can't resolve types) | mitigate | This test runs `npm run validate:publint` + `npm run validate:attw` against the BUILT dist — both will fail loudly if the exports map regresses. Plus the explicit `pkg.exports['.']` shape assertion in Test 5 catches structural changes before publint sees them. |
| T-04-06-02 | Tampering | A future plan accidentally adds an `import { ... } from '@stoprocent/bleno'` somewhere reachable from `src/index.ts` | mitigate | attw will surface the dual-publish breakage (CJS consumers without bleno installed would fail to resolve types). The defense-in-depth complement to Plan 04-03's grep-based BLE-leak guard (T-04-03-01). |
| T-04-06-03 | DoS | The shell-out test takes >60s on a slow machine and fires the Vitest test timeout | mitigate | `{ timeout: 60_000 }` per-test override on Tests 1 + 4 (build + attw — the two longest). publint test inherits the default 5s which is plenty. |
| T-04-06-04 | DoS | The test runs `npm run build` in-process on every full test suite run, bloating local dev iteration | accept | This is the cost of the smoke test. Developers iterating on unit tests can skip it via `npm test -- --exclude '**/publish.test.ts'`. The `[slow]` describe-name suffix documents the cost. |
| T-04-06-05 | Information Disclosure | `execSync` with `stdio: 'inherit'` would leak npm output (potentially noisy or with package-version info) into CI logs | mitigate | Use `stdio: 'pipe'` (single-line WHY-non-obvious comment in code). On failure, the captured output surfaces via the thrown error's `stdout`/`stderr` properties. |
| T-04-06-06 | Tampering | A future contributor changes `tsup.config.ts` to a multi-entry build (e.g. for v2's BlenoTransport) and accidentally breaks the single-rooted exports invariant | mitigate | Test 5's `tsup.config.ts` content assertion (`entry: ['src/index.ts']`) will fail loudly if the entry array changes. v2 BlenoTransport WILL change this — at that time, this test gets updated as part of v2's plan, intentionally. |
</threat_model>

<verification>
- `tsc --noEmit -p tsconfig.test.json` exits 0.
- `npm test -- test/transport/publish.test.ts` exits 0 (all 5 sub-tests passing).
- `npm run validate` (the existing chain `build + publint + attw`) still exits 0 standalone — confirming the test isn't masking failures.
- `dist/index.js` (post-build) contains `createFakeTransport`; `dist/index.d.ts` contains `ITrainerTransport`.
- `package.json` and `tsup.config.ts` are unchanged from before Phase 4 (D-API-08 invariant).
</verification>

<success_criteria>
- `npm run build`, `publint`, `attw --pack .` all exit 0 against the freshly-built dist/.
- All four expected artifacts emitted: `dist/index.js`, `dist/index.cjs`, `dist/index.d.ts`, `dist/index.d.cts`.
- The new public surface (createFakeTransport + 4 types) is present in the built `dist/index.{js,d.ts}`.
- `package.json` and `tsup.config.ts` shape assertions pass — confirming D-API-08's "no Phase 4 changes to publish config" invariant.
- API-07 (dual ESM/CJS validated by publint + attw) and API-08 (strict-mode TS Node 24 import works without @types shim — attw verifies all 6 resolution modes green) are satisfied against the BUILT package.
- Test ROADMAP success-criterion 5 (publint + attw both pass against the published package shape) is observable end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/04-faketransport-public-api/04-06-SUMMARY.md` documenting:
- Test file created at `test/transport/publish.test.ts`
- Build + publint + attw all green against the post-Phase-4 dist/
- Confirmation that `package.json` and `tsup.config.ts` are unchanged in Phase 4 (D-API-08 invariant verified by test)
- Phase 4 closing checklist:
  - [x] API-01..06 covered by Plan 04-04 (records source) + Plan 04-05 (path/buffer + FitLoadError bubble + echo-only)
  - [x] API-07 + API-08 covered by Plan 04-06 (this plan) — publint + attw against built artifact
  - [x] BLE-type-leak HIGH-severity threat T-04-03-01 mitigated by Plan 04-03's grep + defense-in-depth from this plan's attw run
  - [x] Phase 3 followups: WR-05 closed in Plan 04-03 (factory validation gate); IN-01 closed in Plan 04-01 (fakeAwareSleep lift)
  - [x] Phase 3 followups NOT addressed (per scope): WR-02 (currentState async transition docstring), WR-04 (config "frozen" claim without Object.freeze)
  - [x] Phase 2 followups NOT addressed (per scope per D-API-26): WR-01, WR-03, WR-05
- Phase 4 done.
</output>
