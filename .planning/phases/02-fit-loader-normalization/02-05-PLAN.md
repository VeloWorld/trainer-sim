---
phase: 02-fit-loader-normalization
plan: 05
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - test/fit/normalize.test.ts
  - test/fit/local.test.ts
autonomous: true
requirements:
  - FIT-02
  - FIT-03
  - FIT-04
tags:
  - fit
  - tests
  - normalize
  - local-dev
  - opt-in

must_haves:
  truths:
    - "test/fit/normalize.test.ts asserts FIT-02 (sort + dedup keep-first), FIT-03 (Date.getTime() to Unix ms), and D-FIT-01 wire-honest semantics (real `0` preserved as `0`, undefined-vs-zero distinction maintained, missing fields produce omitted properties not explicit `undefined`) — by importing the pure `normalize` function directly from src/fit/normalize.ts (unit-test of a pure function; allowed to bypass public-API surface)"
    - "test/fit/normalize.test.ts asserts D-FIT-03 dedup behavior using duplicates.fit (parsed via fit-file-parser in-test, then normalized) — output count is input count minus 13 duplicates"
    - "test/fit/normalize.test.ts asserts D-FIT-09 debuglog emission on duplicate-drop via subprocess (npx tsx, NOT node --experimental-strip-types) + NODE_DEBUG=trainer-sim:fit (same pattern as plan 02-04 task 3)"
    - "test/fit/local.test.ts is the local-dev TEST_FIT_DIR opt-in suite (D-FIT-04). When TEST_FIT_DIR env var is unset (CI default), the suite is skipped via vitest's describe.skipIf. When set, the suite walks the directory, loads every .fit file, and asserts each load produces >0 records and does not throw."
    - "test/fit/local.test.ts uses describe.skipIf(!process.env.TEST_FIT_DIR) so CI runs are silent (no skipped-test noise) and local runs without the env var are also silent. The unset-skip behavior IS the binding contract per D-FIT-04 — the verify command exercises the unset path, not a developer-host path."
    - "Both test files import only the public surface or the pure normalize function — no test imports fit-file-parser directly (the test corpus is loaded via loadFitFromBuffer / loadFitFromPath; the duplicates.fit test uses fit-file-parser ONLY to feed normalize a parsed input shape that's hard to construct by hand)"
  artifacts:
    - path: "test/fit/normalize.test.ts"
      provides: "FIT-02 + FIT-03 + D-FIT-01 + D-FIT-03 + D-FIT-09 unit tests against the pure normalize function"
      min_lines: 90
    - path: "test/fit/local.test.ts"
      provides: "D-FIT-04 local-dev opt-in smoke suite gated on TEST_FIT_DIR env var"
      min_lines: 30
  key_links:
    - from: "test/fit/normalize.test.ts"
      to: "src/fit/normalize.ts"
      via: "direct unit-test import (pure function — allowed to bypass public surface)"
      pattern: "from\\s+['\"]\\.\\./\\.\\./src/fit/normalize\\.js['\"]"
    - from: "test/fit/local.test.ts"
      to: "process.env.TEST_FIT_DIR"
      via: "vitest describe.skipIf"
      pattern: "TEST_FIT_DIR|skipIf"
---

<objective>
Cover the unit-test side of the normalize pure function and ship the local-dev
TEST_FIT_DIR opt-in suite.

Implements:
- FIT-02 (sort + dedup keep-first; time-ordered output)
- FIT-03 (FIT timestamp to Unix epoch ms via Date.getTime())
- FIT-04 (smoke pass against developer's actual real-world Garmin/Wahoo exports — gated on TEST_FIT_DIR)
- D-FIT-01 (wire-honest undefined vs zero, omitted vs explicit undefined)
- D-FIT-03 (drop count: 13 duplicates in duplicates.fit)
- D-FIT-04 (two-tier strategy: local-dev tier opt-in)
- D-FIT-09 (drop count via util.debuglog)

Honors locked decisions:
- D-FIT-01, D-FIT-03, D-FIT-04, D-FIT-09 (see above)

Purpose: Plan 02-04 covered the loader-level end-to-end behavior (FIT-01 parity,
error paths, shadow, perf). This plan covers the normalize function's pure-logic
unit-test edge cases AND the developer-only smoke pass against an unmocked
real-world corpus. Splitting across two plans keeps each plan within ~40-50%
context budget.

This plan has TWO tasks (parallel-safe — different test files; no shared
infrastructure):
1. test/fit/normalize.test.ts (FIT-02 + FIT-03 + D-FIT-01 + D-FIT-03 + D-FIT-09)
2. test/fit/local.test.ts (D-FIT-04 opt-in TEST_FIT_DIR suite)

Output: Two vitest test files, ~120 LOC total. Task 1 runs in CI; task 2 only
runs locally when the dev sets TEST_FIT_DIR.

This plan and plan 02-04 are both in Wave 4 and have no `files_modified` overlap
— execute-phase can run them in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-fit-loader-normalization/02-CONTEXT.md
@.planning/phases/02-fit-loader-normalization/02-RESEARCH.md
@.planning/phases/02-fit-loader-normalization/02-02-SUMMARY.md
@.planning/phases/02-fit-loader-normalization/02-03-SUMMARY.md
@src/fit/normalize.ts
@src/index.ts
@test/fixtures/fit/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write test/fit/normalize.test.ts (unit tests for the pure normalize function)</name>
  <files>test/fit/normalize.test.ts</files>
  <read_first>
    - .planning/phases/02-fit-loader-normalization/02-CONTEXT.md (D-FIT-01 wire-honest 0; D-FIT-02 gap preservation; D-FIT-03 sort + dedup keep-first; D-FIT-09 debuglog channel)
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Code Examples Example 2 reference shape for normalize input; §Pattern 4 util.debuglog usage; §Pattern 5 wire-honest optionals)
    - src/fit/normalize.ts (the SUT — built in plan 02-03 task 1)
    - test/fixtures/fit/duplicates.fit (the fixture exercising D-FIT-03 dedup; 702 records with 13 duplicate timestamps)
    - test/fixtures/fit/README.md (provenance — confirms the duplicate count is 13 after scrub)
    - CLAUDE.md (tsx is in devDeps "because node --experimental-strip-types doesn't yet handle tsconfig paths or all syntax" — relevant to the subprocess approach below; mirrors plan 02-04 task 3's reasoning)
  </read_first>
  <action>
    Create `test/fit/normalize.test.ts`. Unit-test the pure `normalize` function
    by importing it directly from `src/fit/normalize.ts` (with `.js` extension).
    Direct import is acceptable here (NOT public-surface) because:
    - normalize is a pure function with a well-defined input/output contract.
    - Most test inputs are hand-constructed `ParsedFitMinimal`-shaped objects.
    - We avoid coupling the test to fit-file-parser's internal output shape changing.

    Required test groups:

    **Group 1 — FIT-03: Date to Unix ms via getTime()**
    - Test: `normalize({records: [{timestamp: new Date('2025-01-01T00:00:00Z')}]})` returns `[{timestamp: Date.UTC(2025,0,1)}]`. Asserts `getTime()` is applied; no FIT-epoch math leaks (the parser handles that).
    - Test: timestamps are returned as the JS `number` type (NOT string per RESEARCH §Pitfall 7).

    **Group 2 — D-FIT-01: wire-honest power/cadence semantics**
    - Test: `normalize({records: [{timestamp: T, power: 0}]})` returns `[{timestamp: T, power: 0}]` — real `0` preserved as `0`. (RESEARCH §Pitfall 6.)
    - Test: `normalize({records: [{timestamp: T, power: 0, cadence: 0}]})` returns `[{timestamp: T, power: 0, cadence: 0}]` — both zeros preserved.
    - Test: `normalize({records: [{timestamp: T}]})` returns `[{timestamp: T}]` — `power` and `cadence` keys are OMITTED (not set to explicit `undefined`). Assert via `'power' in result[0] === false` AND `'cadence' in result[0] === false`. Why this matters: Phase 1's encoder uses `value === undefined` to gate flag bits; using `'in'` operator confirms the key isn't present at all (not just falsy).
    - Test: `normalize({records: [{timestamp: T, power: undefined}]})` returns `[{timestamp: T}]` — explicit-undefined input collapses to omitted-key output (D-FIT-01: undefined and missing-field are equivalent).
    - Test: `normalize({records: [{timestamp: T, power: 200, cadence: 90}]})` returns `[{timestamp: T, power: 200, cadence: 90}]` — non-zero values pass through.

    **Group 3 — FIT-02 + D-FIT-03: sort + dedup keep-first**
    - Test: out-of-order input is sorted ascending: input `[{ts: T+2, p: a}, {ts: T, p: b}, {ts: T+1, p: c}]` returns sorted by timestamp ascending; values associated with each timestamp are preserved (b at T, c at T+1, a at T+2).
    - Test: exact-duplicate timestamps are deduped keep-first: input `[{ts: T, p: 100}, {ts: T, p: 200}]` returns ONE record with `power: 100` (the first occurrence wins).
    - Test: dedup applies AFTER sort. Input `[{ts: T+1, p: 200}, {ts: T, p: 100}, {ts: T+1, p: 999}]` — sort makes the order T, T+1, T+1; dedup keeps T and the FIRST T+1 (which after sort is the original `{ts: T+1, p: 200}`). Verify the kept value (this is a tricky case worth pinning).
    - Test: `normalize({records: []})` returns `[]`. No throws, no debuglog emit.
    - Test: `normalize({records: undefined})` returns `[]`.
    - Test: a record with no timestamp is skipped defensively. Input `[{ts: T, p: 100}, {power: 200}]` returns one record (the first); the second is dropped by normalize's defensive `if (!rec.timestamp) continue` check.

    **Group 4 — D-FIT-03 + D-FIT-09: dedup count via debuglog (real fixture coverage)**
    Use `test/fixtures/fit/duplicates.fit` (committed in plan 02-02; 702 records, 13 dupes per D-FIT-05 mapping). Approach:
    1. Subprocess + NODE_DEBUG capture, mirroring plan 02-04 task 3's pattern.
       Spawn a child process via `npx tsx` (NOT `node
       --experimental-strip-types` — see rationale below) that runs
       `loadFitFromBuffer(readFileSync('test/fixtures/fit/duplicates.fit'))`
       with `NODE_DEBUG=trainer-sim:fit`.
    2. Capture stderr; assert it contains a message matching the normalize debuglog format from `src/fit/normalize.ts` — e.g. `/normalize.*duplicates dropped/i` and a number near 13 (`/1[0-9]/` to allow ±2 tolerance after scrub-induced minor count drift).
    3. Also assert that the load itself succeeds and returns `702 - 13 = 689` records (±2 tolerance).

    **Use `npx tsx`, NOT `node --experimental-strip-types`.** Per CLAUDE.md, tsx
    is in devDependencies precisely "because `node --experimental-strip-types`
    doesn't yet handle tsconfig paths or all syntax" — and the subprocess
    snippet imports `'./src/fit/loader.ts'`, which itself imports
    `'./normalize.js'` (Phase 1 .js-extension convention). `node
    --experimental-strip-types` has incomplete `.js`→`.ts` resolution rewriting,
    so the subprocess would fail to resolve `./normalize.js` even though tsx
    handles it correctly. Plan 02-04 task 3 documents the same constraint.

    Implementation:
    ```ts
    import { spawnSync } from 'node:child_process';
    // ... inside test ...
    const result = spawnSync('npx', [
      'tsx',
      '-e',
      `import {loadFitFromBuffer} from './src/fit/loader.ts';
       import {readFileSync} from 'node:fs';
       const out = loadFitFromBuffer(readFileSync('test/fixtures/fit/duplicates.fit'));
       console.log('records=' + out.length);`,
    ], {
      env: { ...process.env, NODE_DEBUG: 'trainer-sim:fit' },
      encoding: 'utf8',
    });
    expect(result.status).toBe(0);
    expect(result.stderr).toMatch(/normalize.*duplicates.*dropped/i);
    // ±2 tolerance around 13 duplicates
    expect(result.stdout).toMatch(/records=68[7-9]|records=69[0-1]/);
    ```

    **Group 5 — D-FIT-02: gaps preserved as plain timestamp jumps**
    - Test: input with a 60-second gap is preserved (no backfill). `normalize({records: [{ts: T, p: 100}, {ts: T + 60_000, p: 200}]})` returns two records with the gap intact (delta = 60_000 ms). No phantom records inserted.

    Forbidden:
    - Skipping the duplicates.fit-driven debuglog test if the subprocess approach is awkward — D-FIT-09 is observable contract; if the test is hard, the test reveals a real coupling problem worth investigating, not a reason to skip.
    - Asserting `power` is `undefined` when missing — assert `!('power' in result[0])` instead. The two are semantically different in TypeScript (the latter is what D-FIT-01 actually requires).
    - Direct import of fit-file-parser: NOT needed for this test file. Construct hand-rolled `ParsedFitMinimal` inputs for groups 1-3 and 5; group 4 invokes the loader (which uses fit-file-parser internally) via the public API.
    - Using `node --experimental-strip-types` for the subprocess in Group 4. Use `npx tsx` per CLAUDE.md guidance and the rationale above. Plan 02-04 task 3 has a parallel constraint.

    Commit:
    `test(02-05): normalize unit tests (FIT-02/03, D-FIT-01/02/03/09)`
  </action>
  <acceptance_criteria>
    - `test/fit/normalize.test.ts` exists: `test -f test/fit/normalize.test.ts`.
    - File imports normalize directly from src: `grep -E "from\s+['\"]\\.\\./\\.\\./src/fit/normalize\\.js['\"]" test/fit/normalize.test.ts` exits 0 AND `grep -F 'normalize' test/fit/normalize.test.ts` exits 0.
    - Asserts FIT-03 epoch conversion (Date.UTC anchor): `grep -F 'Date.UTC' test/fit/normalize.test.ts` exits 0.
    - Asserts wire-honest 0 preservation (D-FIT-01): `grep -E 'power.*toBe\(0\)|toEqual.*power.*0' test/fit/normalize.test.ts` exits 0 OR `grep -E 'power:\s*0' test/fit/normalize.test.ts` exits 0.
    - Asserts omitted-vs-explicit-undefined distinction (D-FIT-01): `grep -E "\\'in\\'|in result|in r" test/fit/normalize.test.ts` exits 0 OR `grep -E "in\s+\w+\[0\]" test/fit/normalize.test.ts` exits 0.
    - Asserts sort behavior (FIT-02): `grep -i 'sort' test/fit/normalize.test.ts` exits 0 OR a test assertion verifies ascending timestamps explicitly.
    - Asserts dedup keep-first (D-FIT-03): the test file describes "keep-first" or asserts that the first occurrence's value (e.g. power: 100) survives a duplicate-timestamp collision: `grep -E 'keep.first|first.*wins|first.*occurrence' test/fit/normalize.test.ts` exits 0 OR `grep -E 'power.*toBe\(100\)' test/fit/normalize.test.ts` exits 0 (where 100 is the first-occurrence value from the test's hand-rolled input).
    - References duplicates.fit for the debuglog test (D-FIT-03 + D-FIT-09 real-fixture coverage): `grep -F 'duplicates.fit' test/fit/normalize.test.ts` exits 0.
    - Uses NODE_DEBUG subprocess capture for debuglog (D-FIT-09): `grep -F 'NODE_DEBUG' test/fit/normalize.test.ts` exits 0 AND `grep -F 'spawnSync' test/fit/normalize.test.ts` exits 0.
    - The subprocess uses `tsx` (NOT `node --experimental-strip-types`): `grep -F 'tsx' test/fit/normalize.test.ts` exits 0 AND `grep -F 'experimental-strip-types' test/fit/normalize.test.ts | wc -l` reports 0.
    - Asserts the captured debuglog contains a duplicates-dropped message: `grep -E 'toMatch.*(duplicates|dropped)' test/fit/normalize.test.ts` exits 0.
    - Does NOT import fit-file-parser directly (we test through the seam): `grep -F 'fit-file-parser' test/fit/normalize.test.ts | grep -v '^[[:space:]]*//' | wc -l` reports 0.
    - Asserts D-FIT-02 gap preservation (no backfill): `grep -E '60_000|60000' test/fit/normalize.test.ts` exits 0 OR `grep -i 'gap' test/fit/normalize.test.ts` exits 0.
    - vitest runs and passes: `npx vitest run test/fit/normalize.test.ts`.
    - Whole suite still passes: `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/fit/normalize.test.ts && npm test && grep -E "from\s+['\"]\\.\\./\\.\\./src/fit/normalize\\.js['\"]" test/fit/normalize.test.ts && grep -F 'Date.UTC' test/fit/normalize.test.ts && grep -F 'duplicates.fit' test/fit/normalize.test.ts && grep -F 'NODE_DEBUG' test/fit/normalize.test.ts && grep -F 'tsx' test/fit/normalize.test.ts && [ "$(grep -F 'experimental-strip-types' test/fit/normalize.test.ts | wc -l | tr -d ' ')" = "0" ] && grep -E 'toMatch.*(duplicates|dropped)' test/fit/normalize.test.ts && [ "$(grep -F 'fit-file-parser' test/fit/normalize.test.ts | grep -v '^[[:space:]]*//' | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    `test/fit/normalize.test.ts` ships and passes. Tests FIT-03 epoch conversion, FIT-02 sort, D-FIT-01 wire-honest 0/undefined/omitted, D-FIT-02 gap preservation, D-FIT-03 dedup keep-first (against hand-rolled inputs AND the duplicates.fit fixture), and D-FIT-09 debuglog emission via NODE_DEBUG + npx tsx subprocess capture. No `node --experimental-strip-types` invocation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write test/fit/local.test.ts (D-FIT-04 local-dev TEST_FIT_DIR opt-in suite)</name>
  <files>test/fit/local.test.ts</files>
  <read_first>
    - .planning/phases/02-fit-loader-normalization/02-CONTEXT.md (D-FIT-04 — two-tier strategy; local-dev tier is opt-in via TEST_FIT_DIR env var, skipped in CI)
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Fixture Strategy → Local-dev tier — describe.skipIf pattern; minimal assertion: each .fit parses to >=1 record without throwing)
    - src/index.ts (loadFitFromPath public re-export — what the local suite calls)
  </read_first>
  <action>
    Create `test/fit/local.test.ts`. The suite is a real-world smoke pass — it
    walks the developer's `TEST_FIT_DIR` directory and asserts every `.fit` file
    in there parses cleanly. CI doesn't set TEST_FIT_DIR, so the suite is
    skipped silently.

    **Important contract note (per D-FIT-04):** the unset-skip behavior IS the
    binding contract. CI does NOT set TEST_FIT_DIR; the suite must report as
    skipped (NOT failed) under default `npm test`. The "when set" path is a
    convenience for local development, NOT a CI guarantee, and the verify
    command below exercises the unset path — not a developer-host path.

    Required structure:

    1. Imports:
       - `import { describe, it, expect } from 'vitest';`
       - `import { readdirSync, statSync } from 'node:fs';`
       - `import { resolve, join, extname } from 'node:path';`
       - `import { loadFitFromPath } from '../../src/index.js';`
    2. Resolve the env var: `const dir = process.env.TEST_FIT_DIR;`
    3. `describe.skipIf(!dir)('local-dev FIT smoke (TEST_FIT_DIR)', () => { ... })`. Note: `describe.skipIf` takes a boolean; `!dir` is `true` when unset (skip) and `false` when set (run).
    4. Inside the `describe`, enumerate `.fit` files in `dir`:
       - `const files = readdirSync(dir!).filter(f => extname(f).toLowerCase() === '.fit');`
       - For each file, define `it(file + ' parses without throwing', async () => { ... })`:
         - `const fullPath = join(dir!, file);`
         - `const records = await loadFitFromPath(fullPath);`
         - `expect(records.length).toBeGreaterThan(0);`
         - For each record, optionally assert `typeof r.timestamp === 'number'` (light shape check).
    5. If `files.length === 0` (TEST_FIT_DIR is set but empty), define ONE test that fails with a clear message: `it('expected at least one .fit file in TEST_FIT_DIR', () => { expect.fail('TEST_FIT_DIR=' + dir + ' contains zero .fit files'); });`.
    6. File header JSDoc: cite "D-FIT-04 — local-dev tier; opt-in via TEST_FIT_DIR. CI does not set this env var so the suite is silently skipped. Run locally before release: `TEST_FIT_DIR=/path/to/your/rides npm test`."

    Forbidden:
    - Hard-coding any path under `/Users/agniveshpatel/dev/agni21/test-sim/data/` — the source corpus location is a developer-machine detail, not a test contract. The opt-in suite must accept any TEST_FIT_DIR value.
    - Asserting against specific record counts (the developer may put any files in their dir; we only smoke-check that load succeeds).
    - Running this suite in CI: the `describe.skipIf(!dir)` clause prevents that. CI's vitest run reports the suite as skipped or omitted — that's the locked behavior per D-FIT-04.
    - Failing the WHOLE test run if TEST_FIT_DIR points to a non-existent directory: `readdirSync` throws ENOENT in that case. Wrap in try/catch and fail just the local-suite tests with a clear message: "TEST_FIT_DIR=X is not a directory or is unreadable".

    Commit:
    `test(02-05): TEST_FIT_DIR opt-in local-dev suite (D-FIT-04)`
  </action>
  <acceptance_criteria>
    - `test/fit/local.test.ts` exists: `test -f test/fit/local.test.ts`.
    - Uses describe.skipIf gating on TEST_FIT_DIR: `grep -F 'describe.skipIf' test/fit/local.test.ts` exits 0 AND `grep -F 'TEST_FIT_DIR' test/fit/local.test.ts` exits 0.
    - Imports loadFitFromPath from public surface: `grep -E "from\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]" test/fit/local.test.ts` exits 0 AND `grep -F 'loadFitFromPath' test/fit/local.test.ts` exits 0.
    - Walks the directory via readdirSync: `grep -F 'readdirSync' test/fit/local.test.ts` exits 0.
    - Filters to `.fit` extension: `grep -E "\\.fit['\"]?\)" test/fit/local.test.ts` exits 0 OR `grep -F "extname" test/fit/local.test.ts` exits 0.
    - Asserts records.length > 0: `grep -E 'records\.length.*toBeGreaterThan|length\.toBeGreaterThan' test/fit/local.test.ts` exits 0.
    - Does NOT hard-code any developer-machine path: `grep -F '/Users/' test/fit/local.test.ts | wc -l` reports 0 AND `grep -F '/home/' test/fit/local.test.ts | wc -l` reports 0.
    - Does NOT reference fit-file-parser directly (uses public loadFitFromPath): `grep -F 'fit-file-parser' test/fit/local.test.ts | grep -v '^[[:space:]]*//' | wc -l` reports 0.
    - **Unset-skip contract (D-FIT-04 binding behavior):** with TEST_FIT_DIR unset, vitest reports the suite as skipped, NOT failed. Run `unset TEST_FIT_DIR; npx vitest run test/fit/local.test.ts` (or use `env -u TEST_FIT_DIR npx vitest run test/fit/local.test.ts` for portability). The exit code MUST be 0 AND vitest output MUST contain "skip" (case-insensitive) referring to this suite. The "when set" path is a convenience, not a binding contract — see D-FIT-04 — so this acceptance criterion does NOT pin TEST_FIT_DIR to any host-specific path.
    - Whole suite still passes (no breakage when local suite is skipped): `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>env -u TEST_FIT_DIR npx vitest run test/fit/local.test.ts 2>&1 | tee /tmp/local-test-out && env -u TEST_FIT_DIR npx vitest run test/fit/local.test.ts && grep -i 'skip' /tmp/local-test-out && npm test && grep -F 'describe.skipIf' test/fit/local.test.ts && grep -F 'TEST_FIT_DIR' test/fit/local.test.ts && grep -F 'loadFitFromPath' test/fit/local.test.ts && grep -F 'readdirSync' test/fit/local.test.ts && grep -E 'records\.length.*toBeGreaterThan|length\.toBeGreaterThan' test/fit/local.test.ts && [ "$(grep -F '/Users/' test/fit/local.test.ts | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    `test/fit/local.test.ts` ships. With TEST_FIT_DIR unset (CI default and the binding D-FIT-04 contract), the suite reports as skipped — verified by `env -u TEST_FIT_DIR npx vitest run test/fit/local.test.ts` exiting 0 with "skip" in the output. With TEST_FIT_DIR set locally (developer convenience, not a CI contract), the suite walks the directory and asserts each .fit loads to >0 records. No hard-coded paths.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hand-rolled `ParsedFitMinimal` inputs ↔ normalize | Tests construct minimal input shapes; normalize is the SUT |
| TEST_FIT_DIR env var ↔ local suite | Developer-controlled directory; arbitrary .fit files; loader is the SUT |
| Subprocess stderr ↔ debuglog assertion | normalize.test.ts captures stderr from a child process (via npx tsx) to verify D-FIT-09 emission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Tampering | A future change to normalize's debuglog format breaks the regex assertion in normalize.test.ts (e.g. message wording changes) | mitigate | The assertion targets a regex (`/duplicates.*dropped/i`), not exact wording. Loader/normalize cannot rephrase the message arbitrarily without breaking this test, which is the desired coupling. The regex is loose enough to allow word-order tweaks but tight enough to catch a complete removal of drop-count emission. |
| T-02-22 | Information Disclosure | TEST_FIT_DIR points to a directory containing real (un-scrubbed) Garmin/Wahoo files with PII; the test suite's verbose output could log paths or telemetry to CI logs | accept (CI does not set TEST_FIT_DIR) | The local suite only runs when the developer explicitly sets the env var. CI never sets it. The test asserts `records.length > 0` and does NOT log record contents — even on local runs no PII reaches stdout. If a developer pastes their local test output into a public bug report, paths are exposed but record contents are not. |
| T-02-23 | Denial of Service | TEST_FIT_DIR points to a 50 GB directory of FIT files | accept | Developer-machine choice; out of scope to defend against. The suite walks the directory once and parses each file; on absurd inputs the developer's terminal shows the slowness immediately. |
| T-02-24 | Tampering | A future PR widens `RideRecord` to include speed/HR (deferred from v2) and the dedup key changes from "exact timestamp match" to something else, breaking the test's hand-rolled inputs | mitigate | Tests pin the exact contract for v1. Widening `RideRecord` is a deliberate decision that requires re-running these tests; if they fail, the PR author must update them with intent. D-FIT-01 lock makes this an explicit user-decision boundary. |
| T-02-27 | Tampering | A subprocess test invokes `node --experimental-strip-types` instead of `npx tsx`, hitting Node 24's incomplete `.js`→`.ts` import-specifier rewriting and either falsely passing (silent skip on resolution failure) or falsely failing (resolution error masquerading as feature error) | mitigate | normalize.test.ts group 4 explicitly uses `npx tsx` for the subprocess. Acceptance criterion grep-bans `experimental-strip-types` in this test file. CLAUDE.md's tsx-in-devDeps rationale is cited in the task's read_first; plan 02-04 task 3 has a parallel constraint. |
| T-02-28 | Tampering | The local.test.ts verify command pins TEST_FIT_DIR to a developer-host path, masking a regression in the unset-skip behavior (the actual D-FIT-04 contract) | mitigate | The verify command exercises `env -u TEST_FIT_DIR` (the binding contract per D-FIT-04). The "when set" path is documented in the test file but not part of acceptance criteria — D-FIT-04 explicitly says CI does not set the env var, so the unset-skip is the contract worth pinning. |
</threat_model>

<verification>
- `test/fit/normalize.test.ts` runs in CI and passes — covers FIT-02 (sort), FIT-03 (epoch ms via getTime), D-FIT-01 (wire-honest 0 / undefined / omitted), D-FIT-02 (gap preservation), D-FIT-03 (dedup keep-first against hand-rolled inputs AND duplicates.fit), D-FIT-09 (debuglog emission via NODE_DEBUG + npx tsx subprocess capture).
- `test/fit/local.test.ts` is silently skipped in CI (TEST_FIT_DIR unset) — verified by `env -u TEST_FIT_DIR npx vitest run test/fit/local.test.ts` exiting 0 with "skip" in the output. The unset-skip behavior IS the binding D-FIT-04 contract.
- The "when TEST_FIT_DIR is set" path is documented in the test file as a convenience for local development; it is NOT pinned in acceptance criteria because D-FIT-04 explicitly excludes it from the CI contract.
- No test file hard-codes a developer-machine path. No test imports fit-file-parser directly. No test invokes `node --experimental-strip-types` for subprocesses (use `npx tsx` per CLAUDE.md).
- `npm test` exits 0 with both new files contributing tests (one runs, one is skipped).
</verification>

<success_criteria>
- The Phase 2 normalize function and the local-dev tier both have shipped tests.
- All locked decisions in scope are reflected in test assertions: D-FIT-01 (omitted-key check), D-FIT-02 (gap delta preserved), D-FIT-03 (dedup keep-first; 13 dropped from duplicates.fit), D-FIT-04 (TEST_FIT_DIR unset-skip is the binding contract), D-FIT-09 (debuglog stderr capture).
- Two atomic conventional-prefix commits land cleanly: `test(02-05)` × 2.
- `npm test` exits 0; CI does not run the local suite (verified via `env -u TEST_FIT_DIR`); developers can opt-in locally with `TEST_FIT_DIR=... npm test`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fit-loader-normalization/02-05-SUMMARY.md` documenting:
- Each test file's line count and tests-added count.
- Confirmation that local.test.ts is skipped under default `npm test` and under `env -u TEST_FIT_DIR npx vitest run test/fit/local.test.ts` (the binding D-FIT-04 contract).
- A note that the "when TEST_FIT_DIR is set" path was exercised manually during local dev (cite the count of .fit files walked if run; this is informational, not a CI contract).
- Whether the duplicates.fit + NODE_DEBUG + npx tsx subprocess test passed (or any deviation from the planned approach).
- Aggregate Phase 2 test count and pass count post-commit (combining plans 02-04 and 02-05).
</output>
</content>
