---
phase: 02-fit-loader-normalization
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - test/fit/loader.test.ts
  - test/fit/error-paths.test.ts
  - test/fit/dev-field-shadow.test.ts
  - test/fit/perf.test.ts
autonomous: true
requirements:
  - FIT-01
  - FIT-04
  - FIT-05
tags:
  - fit
  - tests
  - loader
  - error-paths
  - perf-gate

must_haves:
  truths:
    - "test/fit/loader.test.ts asserts FIT-01 (loadFitFromPath and loadFitFromBuffer return identical RideRecord[] for basic.fit), FIT-04 (autopause.fit + zero-power.fit + dev-fields-non-shadow.fit load without throwing), and the sync-return invariant (loadFitFromBuffer result is NOT a Promise) per RESEARCH §Open Questions #3"
    - "test/fit/error-paths.test.ts asserts each FitLoadError subclass fires on the right corrupt input: InvalidFitHeaderError on bad magic / wrong header length, FitTruncatedError on too-short buffer + on truncated data, FitCrcError on hand-corrupted CRC trailer, NoRecordMessagesError on a hand-rolled valid FIT with file_id but zero record messages — and asserts each is `instanceof FitLoadError` (D-FIT-06 generic catch surface)"
    - "test/fit/dev-field-shadow.test.ts asserts shadow.fit loads WITHOUT throwing (D-FIT-10 amendment), returns at least one RideRecord with `power` populated, and emits a util.debuglog('trainer-sim:fit') message naming the shadowed field — verified by spawning a subprocess with NODE_DEBUG=trainer-sim:fit and grepping stderr"
    - "test/fit/perf.test.ts asserts perf-1hr.fit (4562 records, 76m) parses in <50 ms median over 11 runs after 3 warm-up runs (50 ms threshold leaves margin under the ROADMAP <100 ms gate; RESEARCH measured 1.85 ms median on a synthetic 3600-record file)"
    - "All tests run under vitest with no env vars (CI-safe); no test depends on TEST_FIT_DIR (that's plan 02-05)"
    - "Tests assert against the public API surface (loadFitFromPath, loadFitFromBuffer, FitLoadError, etc. imported from '../../src/index.js') — NOT against internals"
  artifacts:
    - path: "test/fit/loader.test.ts"
      provides: "FIT-01 path/buffer parity + FIT-04 real-world quirks load without throwing + sync-return invariant"
      min_lines: 60
    - path: "test/fit/error-paths.test.ts"
      provides: "All four FitLoadError subclasses fire on the appropriate corrupt input (D-FIT-06)"
      min_lines: 80
    - path: "test/fit/dev-field-shadow.test.ts"
      provides: "FIT-05 amended (D-FIT-10) — shadow.fit loads without throwing AND emits util.debuglog"
      min_lines: 40
    - path: "test/fit/perf.test.ts"
      provides: "ROADMAP perf gate (<100 ms parse for typical 1-hour file) — asserts <50 ms median for headroom"
      min_lines: 30
  key_links:
    - from: "test/fit/*.test.ts"
      to: "../../src/index.js (public API surface)"
      via: "import {loadFitFromPath, loadFitFromBuffer, FitLoadError, ...} from '../../src/index.js'"
      pattern: "from\\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]"
    - from: "test/fit/dev-field-shadow.test.ts"
      to: "node:child_process (spawnSync) for NODE_DEBUG capture"
      via: "subprocess with env NODE_DEBUG=trainer-sim:fit"
      pattern: "spawnSync|NODE_DEBUG"
---

<objective>
Verify the Phase 2 loader's public-API contract end-to-end against the seven
committed fixtures from plan 02-02. Four focused vitest test files cover:

1. **loader.test.ts** — FIT-01 path/buffer parity; FIT-04 real-world quirks load
   without throwing; sync-return invariant (loadFitFromBuffer is NOT async).
2. **error-paths.test.ts** — Each FitLoadError subclass fires on the right
   corrupt input; all are `instanceof FitLoadError` (D-FIT-06).
3. **dev-field-shadow.test.ts** — FIT-05 amended (D-FIT-10): shadow.fit loads
   without throwing, returns records, emits util.debuglog warning.
4. **perf.test.ts** — ROADMAP <100 ms gate (asserts <50 ms median for headroom).

Implements the test side of:
- FIT-01 (path/buffer parity test against basic.fit)
- FIT-04 (autopause + zero-power + dev-fields-non-shadow load tests)
- FIT-05 amended (shadow.fit non-throwing + debuglog emission)
- D-FIT-06 (all four error subclasses fire correctly; instanceof FitLoadError)
- ROADMAP perf gate

Honors locked decisions:
- D-FIT-01 (zero-power.fit test asserts a real `0` watt reading is preserved)
- D-FIT-06 (instanceof FitLoadError for generic catch)
- D-FIT-07 (sync-return invariant for loadFitFromBuffer)
- D-FIT-10 (shadow non-fatal — debuglog only; replaces RESEARCH §Pattern 3 reject path)

Purpose: Wave 4 verifies the loader behaves correctly across the structural
matrix. Plan 02-05 separately tests the normalize pure function and the
local-dev TEST_FIT_DIR opt-in suite. Splitting tests across two plans keeps
each plan in the ~50% context budget.

Output: Four vitest test files, ~250 LOC total. All run under `npm test` (CI tier).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/02-fit-loader-normalization/02-CONTEXT.md
@.planning/phases/02-fit-loader-normalization/02-RESEARCH.md
@.planning/phases/02-fit-loader-normalization/02-02-SUMMARY.md
@.planning/phases/02-fit-loader-normalization/02-03-SUMMARY.md
@src/index.ts
@test/fixtures/fit/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write test/fit/loader.test.ts (FIT-01 parity + FIT-04 real-world quirks + sync invariant)</name>
  <files>test/fit/loader.test.ts</files>
  <read_first>
    - .planning/phases/02-fit-loader-normalization/02-CONTEXT.md (FIT-01 path==buffer; FIT-04 autopause + zero-power + sparse cadence + null power; D-FIT-01 wire-honest 0; D-FIT-07 sync invariant)
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Code Examples Example 5 path/buffer parity skeleton; §Open Questions #3 + Assumptions A1 — sync-return defensive test)
    - test/fixtures/fit/README.md (provenance — confirms record counts of basic.fit, zero-power.fit, autopause.fit, dev-fields-non-shadow.fit)
    - src/index.ts (public surface; tests import from '../../src/index.js' to exercise the published shape)
  </read_first>
  <action>
    Create `test/fit/loader.test.ts`. Use vitest's `describe`/`it`/`expect` API.
    Import the public surface from `'../../src/index.js'` (with `.js` extension
    per phase convention; vitest 4.1 + Node 24 supports this with `tsconfig.test.json`'s
    `moduleResolution: bundler`).

    Required test groups:

    **Group 1 — FIT-01: loadFitFromPath and loadFitFromBuffer parity**
    - Test: loading `test/fixtures/fit/basic.fit` via path returns the same `RideRecord[]` as loading via buffer.
      - `const path = resolve(__dirname, '../fixtures/fit/basic.fit');`
      - `const buf = readFileSync(path);`
      - `const fromPath = await loadFitFromPath(path);`
      - `const fromBuf = loadFitFromBuffer(buf);`
      - `expect(fromPath).toEqual(fromBuf);`
      - `expect(fromPath.length).toBeGreaterThan(0);`
      - `expect(fromPath.length).toBeCloseTo(443, -1)` OR explicit range check (basic.fit's record count from D-FIT-05 mapping ± fixture-rounding tolerance).
    - Test: each record has a numeric timestamp (NOT a string per RESEARCH §Pitfall 1/7).
      - For each record `r` in `fromBuf`, `expect(typeof r.timestamp).toBe('number')`.
    - Test: timestamps are anchored near 2025-01-01 UTC (synthetic epoch from the scrubber per D-FIT-05).
      - `const start = Date.UTC(2025, 0, 1);`
      - `expect(fromBuf[0]!.timestamp).toBeGreaterThanOrEqual(start - 86400000);`
      - `expect(fromBuf[0]!.timestamp).toBeLessThanOrEqual(start + 86400000 * 2);`

    **Group 2 — D-FIT-07 sync invariant for loadFitFromBuffer (defensive)**
    - Test: `loadFitFromBuffer(buf)` returns a non-Promise. Per RESEARCH Assumptions A1 — fails immediately if a future fit-file-parser version makes the callback async.
      - `const result = loadFitFromBuffer(readFileSync(resolve(__dirname, '../fixtures/fit/basic.fit')));`
      - `expect(result).not.toBeInstanceOf(Promise);`
      - `expect(Array.isArray(result)).toBe(true);`

    **Group 3 — FIT-04: real-world quirks load without throwing**
    For each of: `autopause.fit`, `zero-power.fit`, `dev-fields-non-shadow.fit`:
    - Test: `loadFitFromBuffer(buf)` does not throw.
    - Test: returns `RideRecord[]` with length consistent with the D-FIT-05 mapping (within ±2 to allow for normalize's dedup).
    - Specific assertions per fixture:
      - `autopause.fit` (3172 records, 2 gaps max 68s): assert at least one consecutive timestamp delta in the returned array is > 60_000 ms (a 60s+ gap is preserved per D-FIT-02).
      - `zero-power.fit` (541 records, 142 zero-power): assert at least 50 records have `power === 0` (NOT undefined). This proves D-FIT-01 wire-honesty: the parser's invalid-sentinel filter strips the FIT-spec `0xFFFF` sentinel to undefined, but a real `0x0000` watt reading round-trips as `0`. Threshold 50 leaves margin if scrubbing affected counts slightly.
      - `dev-fields-non-shadow.fit` (2501 records, 4 dev fields with non-shadowing names like `UUID`, `Title`, `CurrentRouteId`, `IsEvent`): assert load succeeds and returns records with `power !== undefined` for the majority (proving the non-shadow dev fields don't break the standard `record.power` path). At least 1500 records have power defined.

    Forbidden in this file:
    - Tests that depend on `TEST_FIT_DIR` (that's plan 02-05's job).
    - Tests that touch the perf gate (that's task 4 of this plan).
    - Tests that import internals (`src/fit/loader.ts`, `src/fit/normalize.ts`) — assert against the public surface only. Plan 02-05 may import normalize directly because that's a unit test of the pure function; loader.test.ts is end-to-end.

    Commit:
    `test(02-04): FIT-01 parity + FIT-04 real-world quirks + sync invariant`
  </action>
  <acceptance_criteria>
    - `test/fit/loader.test.ts` exists: `test -f test/fit/loader.test.ts`.
    - File imports loadFitFromPath and loadFitFromBuffer from the public surface: `grep -E "from\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]" test/fit/loader.test.ts` exits 0 AND `grep -F 'loadFitFromPath' test/fit/loader.test.ts` exits 0 AND `grep -F 'loadFitFromBuffer' test/fit/loader.test.ts` exits 0.
    - File does NOT import internals (loader.ts, normalize.ts) directly: `grep -nE "from\s+['\"][^'\"]*src/fit/(loader|normalize)" test/fit/loader.test.ts | wc -l` reports 0.
    - File references all four CI fixtures it should test: `grep -F 'basic.fit' test/fit/loader.test.ts` exits 0 AND `grep -F 'autopause.fit' test/fit/loader.test.ts` exits 0 AND `grep -F 'zero-power.fit' test/fit/loader.test.ts` exits 0 AND `grep -F 'dev-fields-non-shadow.fit' test/fit/loader.test.ts` exits 0.
    - Asserts sync-return invariant (RESEARCH Assumptions A1): `grep -F 'not.toBeInstanceOf(Promise)' test/fit/loader.test.ts` exits 0 OR `grep -E 'instanceof\s+Promise' test/fit/loader.test.ts` exits 0.
    - Asserts wire `0` is preserved (D-FIT-01) for zero-power.fit: `grep -E 'power\s*===\s*0' test/fit/loader.test.ts` exits 0.
    - Asserts at least one timestamp delta > 60s in autopause.fit (D-FIT-02 gap preservation): `grep -E '60[_]?000' test/fit/loader.test.ts` exits 0 OR `grep -i 'gap' test/fit/loader.test.ts` exits 0.
    - Does NOT reference TEST_FIT_DIR (separation from plan 02-05): `grep -F 'TEST_FIT_DIR' test/fit/loader.test.ts | wc -l` reports 0.
    - Does NOT include perf-gate assertions (separation from task 4): `grep -i 'performance' test/fit/loader.test.ts | grep -v "^[[:space:]]*//" | wc -l` reports 0.
    - vitest runs the new test file successfully: `npx vitest run test/fit/loader.test.ts` exits 0.
    - Whole suite still passes: `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/fit/loader.test.ts && npm test && grep -F 'loadFitFromPath' test/fit/loader.test.ts && grep -F 'loadFitFromBuffer' test/fit/loader.test.ts && grep -F 'not.toBeInstanceOf(Promise)' test/fit/loader.test.ts && grep -F 'basic.fit' test/fit/loader.test.ts && grep -F 'autopause.fit' test/fit/loader.test.ts && grep -F 'zero-power.fit' test/fit/loader.test.ts && [ "$(grep -nE 'from\s+[\\\"'\\'']([^'\\\"]*)src/fit/(loader|normalize)' test/fit/loader.test.ts | wc -l | tr -d ' ')" = "0" ] && [ "$(grep -F 'TEST_FIT_DIR' test/fit/loader.test.ts | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    `test/fit/loader.test.ts` ships and passes. Asserts FIT-01 path/buffer parity, FIT-04 real-world quirks (autopause + zero-power + dev-fields-non-shadow), D-FIT-01 wire-honest `0`, D-FIT-02 gap preservation, and D-FIT-07 sync-return invariant. No TEST_FIT_DIR coupling, no perf assertions, no internal imports.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write test/fit/error-paths.test.ts (D-FIT-06 — all four FitLoadError subclasses)</name>
  <files>test/fit/error-paths.test.ts</files>
  <read_first>
    - .planning/phases/02-fit-loader-normalization/02-CONTEXT.md (D-FIT-06 — abstract base + 4 concrete subclasses; "loud, actionable failures" doctrine)
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Pattern 2 — header validation + CRC algorithm; §Code Examples Example 1 — error message shapes; §Pitfall 4 — CRC TODO-commented)
    - src/fit/errors.ts (the four subclasses + abstract base)
    - src/index.ts (confirm public re-exports of the error classes for instanceof checks)
  </read_first>
  <action>
    Create `test/fit/error-paths.test.ts`. Each test corrupts a fixture input
    in-test (does NOT modify committed fixture bytes) and asserts the right
    subclass throws.

    Required test groups:

    **Group 1 — InvalidFitHeaderError**
    - Test: empty buffer throws FitTruncatedError (boundary case — the empty case fires the truncated path BEFORE it reaches header magic check). `expect(() => loadFitFromBuffer(Buffer.alloc(0))).toThrowError(FitTruncatedError);`
    - Test: buffer with bad magic throws InvalidFitHeaderError. Build a minimal 14-byte buffer where bytes 0..7 are valid header preamble (length=12, protocol=0x10, profile=0x00, dataLength=0) but bytes 8..11 are `'JUNK'` not `'.FIT'`. `expect(() => loadFitFromBuffer(badMagicBuf)).toThrowError(InvalidFitHeaderError);`
    - Test: buffer with header length neither 12 nor 14 throws InvalidFitHeaderError. Set `buf[0] = 13;` on an otherwise-valid header (data + valid CRC). `expect(...).toThrowError(InvalidFitHeaderError);`
    - Test: thrown error is instanceof FitLoadError (D-FIT-06 generic catch surface): `try { loadFitFromBuffer(badMagicBuf); } catch (e) { expect(e).toBeInstanceOf(FitLoadError); expect(e).toBeInstanceOf(InvalidFitHeaderError); }`

    **Group 2 — FitTruncatedError**
    - Test: buffer of length < 14 throws FitTruncatedError (already covered by empty-buffer case in group 1; that covers FitTruncatedError instanceof check too).
    - Test: buffer with valid header but truncated data throws FitTruncatedError. Read `basic.fit`, slice to the first `headerLength + 100` bytes (cuts off the data section + CRC), pass to loadFitFromBuffer. `expect(...).toThrowError(FitTruncatedError);`

    **Group 3 — FitCrcError**
    - Test: hand-corrupt the CRC trailer of `basic.fit` and assert FitCrcError. Read `basic.fit`, copy to a mutable Buffer, flip the last byte (or last two bytes — the CRC trailer). Assert thrown error.
      ```ts
      const buf = Buffer.from(readFileSync(resolve(__dirname, '../fixtures/fit/basic.fit')));
      buf[buf.length - 1] = buf[buf.length - 1] ^ 0xFF; // flip CRC byte
      expect(() => loadFitFromBuffer(buf)).toThrowError(FitCrcError);
      ```
    - Test: thrown error is instanceof FitLoadError.

    **Group 4 — NoRecordMessagesError**
    Hand-craft a minimal valid FIT byte buffer in-test that contains a file_id message but ZERO record messages. Approach: re-use the same byte-writer pattern as `test/fixtures/generate-shadow.ts` BUT simpler — header + file_id definition + file_id data + correct CRC, no record messages. Approximately ~50 LOC inline in the test file.

    Alternative (simpler) approach: read `shadow.fit` (which has 30 records), parse it via fit-file-parser to confirm structure, then construct a buffer that has the SAME header + same file_id but truncates after the file_id data message. This requires knowing the shadow.fit byte layout and is fragile. **Choose the inline-hand-rolled approach** — copy the relevant ~30 LOC from `test/fixtures/generate-shadow.ts` into a helper inside this test file (or extract to a `test/fit/helpers.ts` if both task 2 and other tests need it; for this single use, inlining is fine).

    Acceptance: `expect(() => loadFitFromBuffer(noRecordsBuf)).toThrowError(NoRecordMessagesError);` AND `expect(e).toBeInstanceOf(FitLoadError)`.

    Forbidden:
    - Mutating the committed fixtures (build temp buffers in-test, never write back to test/fixtures/fit/).
    - Asserting against `instanceof DeveloperFieldShadowError` (does not exist; D-FIT-10).
    - Catching all errors as a generic Error and string-matching the message — use `instanceof`. Per D-FIT-06 the typed hierarchy IS the contract.

    Commit:
    `test(02-04): all four FitLoadError subclasses fire on corrupt input (D-FIT-06)`
  </action>
  <acceptance_criteria>
    - `test/fit/error-paths.test.ts` exists: `test -f test/fit/error-paths.test.ts`.
    - File imports the four FitLoadError subclasses + abstract base from public surface: `grep -E "from\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]" test/fit/error-paths.test.ts` exits 0 AND for each E in FitLoadError InvalidFitHeaderError FitCrcError FitTruncatedError NoRecordMessagesError: `grep -F "$E" test/fit/error-paths.test.ts` exits 0.
    - Tests for all four concrete subclasses present (each appears in a test name or expect): `grep -cE 'InvalidFitHeaderError|FitCrcError|FitTruncatedError|NoRecordMessagesError' test/fit/error-paths.test.ts` reports >= 4.
    - Asserts instanceof FitLoadError (D-FIT-06 generic catch): `grep -F 'toBeInstanceOf(FitLoadError)' test/fit/error-paths.test.ts` exits 0 OR `grep -E 'instanceof\s+FitLoadError' test/fit/error-paths.test.ts` exits 0.
    - Does NOT reference DeveloperFieldShadowError: `grep -cF 'DeveloperFieldShadowError' test/fit/error-paths.test.ts` reports 0.
    - Does NOT modify committed fixture files (in-test buffer mutation only): the test does NOT write to `test/fixtures/fit/*.fit`. Verified by checking `git status` after running the test reports those files clean.
    - vitest runs and passes: `npx vitest run test/fit/error-paths.test.ts`.
    - Whole suite still passes: `npm test`.
    - After running tests, no committed fixtures were modified: `git diff --quiet test/fixtures/fit/` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/fit/error-paths.test.ts && npm test && [ "$(grep -cE 'InvalidFitHeaderError|FitCrcError|FitTruncatedError|NoRecordMessagesError' test/fit/error-paths.test.ts)" -ge "4" ] && grep -E 'instanceof\s+FitLoadError|toBeInstanceOf\(FitLoadError\)' test/fit/error-paths.test.ts && [ "$(grep -cF 'DeveloperFieldShadowError' test/fit/error-paths.test.ts)" = "0" ] && git diff --quiet test/fixtures/fit/</automated>
  </verify>
  <done>
    `test/fit/error-paths.test.ts` ships and passes. Each of the four FitLoadError subclasses (InvalidFitHeaderError, FitCrcError, FitTruncatedError, NoRecordMessagesError) is exercised on appropriate corrupt input. All thrown errors are instanceof FitLoadError. No DeveloperFieldShadowError reference. No committed fixture file mutated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write test/fit/dev-field-shadow.test.ts (FIT-05 amended / D-FIT-10 — non-fatal + debuglog)</name>
  <files>test/fit/dev-field-shadow.test.ts</files>
  <read_first>
    - .planning/phases/02-fit-loader-normalization/02-CONTEXT.md (D-FIT-10 — shadow non-fatal, debuglog only)
    - .planning/REQUIREMENTS.md (FIT-05 amended 2026-05-16 — "the loader does **not** throw on shadow conflicts")
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Critical Finding — shadow.fit yields record.power = 999 because dev wins; §Pattern 4 — util.debuglog channel)
    - test/fixtures/fit/shadow.fit (committed in plan 02-02; hand-rolled with developer-defined `power` field colliding with standard)
    - test/fixtures/fit/README.md (shadow.fit provenance)
  </read_first>
  <action>
    Create `test/fit/dev-field-shadow.test.ts`. This test verifies D-FIT-10's
    locked behavior — the OPPOSITE of what RESEARCH §Pattern 3 originally
    recommended. Cite this in the file header so future readers don't assume
    the test is wrong.

    Required tests:

    **Test 1 — Loads without throwing (D-FIT-10).**
    - `const buf = readFileSync(resolve(__dirname, '../fixtures/fit/shadow.fit'));`
    - `expect(() => loadFitFromBuffer(buf)).not.toThrow();`
    - `const records = loadFitFromBuffer(buf);`
    - `expect(records.length).toBeGreaterThan(0);`
    - `expect(records[0]!.power).toBeDefined();` — power is populated (whatever value the parser produced; we don't assert 999 vs 200 here because the contract per D-FIT-10 is "returns whatever the parser produced", and that's an implementation detail of fit-file-parser 3.0 that may shift in 3.0.x).

    **Test 2 — Emits util.debuglog on shadow detection.**
    Subprocess approach: spawn a Node child process with `NODE_DEBUG=trainer-sim:fit` env var, call `loadFitFromBuffer(readFileSync('shadow.fit'))` inside the child, capture stderr, assert it contains a message naming the shadowed field (`power`).

    Implementation:
    ```ts
    import { spawnSync } from 'node:child_process';
    // ... inside test ...
    const result = spawnSync(process.execPath, [
      '--input-type=module',
      '--experimental-strip-types',
      '-e',
      `import {loadFitFromBuffer} from './src/fit/loader.ts';
       import {readFileSync} from 'node:fs';
       loadFitFromBuffer(readFileSync('test/fixtures/fit/shadow.fit'));`,
    ], {
      env: { ...process.env, NODE_DEBUG: 'trainer-sim:fit' },
      encoding: 'utf8',
    });
    expect(result.status).toBe(0);
    expect(result.stderr).toMatch(/developer field shadow/i);
    expect(result.stderr).toMatch(/power/i);
    ```
    Note: `node:util.debuglog` writes to stderr by default. Vitest's mock
    facilities for util.debuglog are awkward; subprocess capture is the
    deterministic, behaviorally-faithful approach.

    Alternative (simpler) approach if subprocess proves flaky: stub
    `node:util.debuglog` via vitest's `vi.mock`. But that mocks the import in the
    test module, not in `src/fit/loader.ts` (which has its own `import`
    binding). The cleanest path is the subprocess; per D-FIT-09 the debuglog IS
    the observable contract, and we should test it the way real consumers see it.

    **Test 3 — D-FIT-06 negative: shadow does NOT cause a FitLoadError.**
    - `try { loadFitFromBuffer(readFileSync(resolve(__dirname, '../fixtures/fit/shadow.fit'))); } catch (e) { expect.fail('shadow.fit should not throw per D-FIT-10; got error: ' + (e as Error).message); }` — explicitly fails if any throw fires.

    File header JSDoc: cite "D-FIT-10 (CONTEXT.md, REQUIREMENTS.md FIT-05 amendment 2026-05-16) — shadow non-fatal. RESEARCH §Critical Finding §Pattern 3 originally recommended throwing DeveloperFieldShadowError; that recommendation was SUPERSEDED by user decision in /gsd-discuss-phase. This test asserts the locked behavior (debuglog + continue), NOT the rejected one (throw)."

    Forbidden:
    - Asserting any throw on shadow.fit. The whole point of D-FIT-10 is non-fatal.
    - Asserting `record.power === 200` (the "standard" value). Per D-FIT-10 we accept whatever fit-file-parser produces; in 3.0.0 that's the dev value (999). Asserting 200 would fail today and be wrong tomorrow if the parser is fixed; asserting 999 would pin us to a parser implementation detail. Just assert `power !== undefined`.
    - Importing DeveloperFieldShadowError: it does not exist (D-FIT-10 lock).
    - `console.warn` capture: util.debuglog writes to stderr via Node, NOT console.warn. The subprocess approach captures stderr correctly.

    Commit:
    `test(02-04): shadow.fit loads without throwing + emits util.debuglog (D-FIT-10)`
  </action>
  <acceptance_criteria>
    - `test/fit/dev-field-shadow.test.ts` exists: `test -f test/fit/dev-field-shadow.test.ts`.
    - File asserts shadow.fit does NOT throw: `grep -E 'not\.toThrow|expect\.fail.*shadow' test/fit/dev-field-shadow.test.ts` exits 0.
    - File asserts records.length > 0 after loading shadow.fit: `grep -E 'records\.length.*Greater|length.*toBeGreaterThan' test/fit/dev-field-shadow.test.ts` exits 0.
    - File spawns a subprocess with NODE_DEBUG to capture debuglog (or uses an equivalent mechanism): `grep -F 'NODE_DEBUG' test/fit/dev-field-shadow.test.ts` exits 0 OR `grep -F 'spawnSync' test/fit/dev-field-shadow.test.ts` exits 0.
    - File asserts the captured stderr contains the shadow message naming `power`: `grep -E "stderr.*toMatch|toMatch.*power|toMatch.*shadow" test/fit/dev-field-shadow.test.ts` exits 0.
    - File does NOT reference DeveloperFieldShadowError (D-FIT-10): `grep -cF 'DeveloperFieldShadowError' test/fit/dev-field-shadow.test.ts` reports 0.
    - File header cites D-FIT-10: `grep -F 'D-FIT-10' test/fit/dev-field-shadow.test.ts` exits 0 OR `grep -F 'amendment' test/fit/dev-field-shadow.test.ts` exits 0 OR `grep -F '2026-05-16' test/fit/dev-field-shadow.test.ts` exits 0.
    - vitest runs and passes: `npx vitest run test/fit/dev-field-shadow.test.ts`.
    - Whole suite still passes: `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/fit/dev-field-shadow.test.ts && npm test && grep -E 'not\.toThrow|expect\.fail' test/fit/dev-field-shadow.test.ts && grep -F 'NODE_DEBUG' test/fit/dev-field-shadow.test.ts && grep -E 'toMatch.*(power|shadow)' test/fit/dev-field-shadow.test.ts && [ "$(grep -cF 'DeveloperFieldShadowError' test/fit/dev-field-shadow.test.ts)" = "0" ]</automated>
  </verify>
  <done>
    `test/fit/dev-field-shadow.test.ts` ships and passes. Asserts shadow.fit loads without throwing AND that NODE_DEBUG=trainer-sim:fit captures a stderr message naming the shadowed `power` field. Cites D-FIT-10 lock in file header. No DeveloperFieldShadowError reference.
  </done>
</task>

<task type="auto">
  <name>Task 4: Write test/fit/perf.test.ts (ROADMAP <100 ms perf gate, asserts <50 ms median for headroom)</name>
  <files>test/fit/perf.test.ts</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 perf gate: "<100 ms parse for typical 1-hour file")
    - .planning/phases/02-fit-loader-normalization/02-RESEARCH.md (§Code Examples Example 7 — full perf-test skeleton with warm-up + 11-run median; verified perf headline: 1.85 ms median on synthetic 3600-record file)
    - .planning/phases/02-fit-loader-normalization/02-CONTEXT.md (perf-1hr.fit fixture mapping — Zwift FTP Test, 4562 records over 76 minutes — denser than the synthetic 3600 RESEARCH measured against, so allow more headroom)
    - test/fixtures/fit/perf-1hr.fit (committed in plan 02-02)
  </read_first>
  <action>
    Create `test/fit/perf.test.ts` per RESEARCH §Code Examples Example 7. The
    fixture is `test/fixtures/fit/perf-1hr.fit` (4562 records, 76 minutes).
    Threshold: 50 ms median (gives ~2× margin under the ROADMAP <100 ms gate).
    RESEARCH measured 1.85 ms median on a smaller synthetic file; real Garmin
    files have more message types so a real 76-min file may take more — but 50 ms
    still leaves order-of-magnitude headroom.

    Required structure:

    1. Imports: `describe, it, expect` from 'vitest'; `readFileSync` from 'node:fs'; `resolve` from 'node:path'; `performance` from 'node:perf_hooks'; `loadFitFromBuffer` from '../../src/index.js'.
    2. One `describe('Phase 2 perf gate (<100 ms parse for ~1-hour file)', ...)` block.
    3. One `it('parses perf-1hr.fit in <50 ms median over 11 runs after 3 warm-up runs', ...)` test:
       - Read `perf-1hr.fit` once (outside the timing loop).
       - Run 3 warm-up iterations of `loadFitFromBuffer(buf)` (V8 JIT warm-up).
       - Run 11 timed iterations: capture `performance.now()` deltas in a number[].
       - Sort the array, take `times[Math.floor(11/2)]` (the 6th element = median).
       - `expect(median).toBeLessThan(50);`
       - Console.log the min/median/max so future regressions are visible in test output.

    Tolerance considerations:
    - CI runners have variable performance. 50 ms should be safe even on slower CI hosts (RESEARCH measured 1.85 ms on a developer machine; even a 25× slowdown leaves margin).
    - If CI flakes (test occasionally fails), the threshold can be widened to 75 ms in a follow-up. Don't preemptively widen — the ROADMAP gate is 100 ms and we want to detect drift early.

    Forbidden:
    - Asserting against the synthetic perf headline (1.85 ms) directly — that's a developer-machine number, not a CI-machine number.
    - Running fewer than 3 warm-up iterations (V8 JIT cold path can dominate the first run).
    - Using a single timed run (high variance; median of 11 is the standard pattern).
    - Disabling or `.skip`-ing this test on CI — the perf gate is part of the ROADMAP success criteria and must be verifiable in CI. If it flakes, we widen the threshold or improve the fixture, we don't skip the test.

    Commit:
    `test(02-04): ROADMAP perf gate — perf-1hr.fit parses in <50 ms median`
  </action>
  <acceptance_criteria>
    - `test/fit/perf.test.ts` exists: `test -f test/fit/perf.test.ts`.
    - File imports loadFitFromBuffer from public surface: `grep -E "from\s+['\"]\\.\\./\\.\\./src/index\\.js['\"]" test/fit/perf.test.ts` exits 0 AND `grep -F 'loadFitFromBuffer' test/fit/perf.test.ts` exits 0.
    - File imports performance from node:perf_hooks: `grep -E "from\s+['\"]node:perf_hooks['\"]" test/fit/perf.test.ts` exits 0.
    - File references the perf fixture: `grep -F 'perf-1hr.fit' test/fit/perf.test.ts` exits 0.
    - Includes warm-up runs (>= 3): `grep -E '(warm.?up|warmup)' test/fit/perf.test.ts` exits 0 AND `grep -E '<\s*3' test/fit/perf.test.ts` exits 0 OR (loop with iteration count >= 3).
    - Computes median over multiple runs (>= 11): `grep -E '11|median|sort' test/fit/perf.test.ts` exits 0.
    - Asserts median < 50 ms: `grep -E 'toBeLessThan\s*\(\s*50' test/fit/perf.test.ts` exits 0.
    - Does NOT skip the test on CI: `grep -E '\.skip|skipIf' test/fit/perf.test.ts | wc -l` reports 0.
    - vitest runs and passes (perf test passes on the dev machine — CI threshold has 25× headroom over RESEARCH-measured 1.85 ms): `npx vitest run test/fit/perf.test.ts`.
    - Whole suite still passes: `npm test`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/fit/perf.test.ts && npm test && grep -F 'perf-1hr.fit' test/fit/perf.test.ts && grep -E "from\s+['\"]node:perf_hooks['\"]" test/fit/perf.test.ts && grep -E 'toBeLessThan\s*\(\s*50' test/fit/perf.test.ts && [ "$(grep -E '\.skip|skipIf' test/fit/perf.test.ts | wc -l | tr -d ' ')" = "0" ]</automated>
  </verify>
  <done>
    `test/fit/perf.test.ts` ships and passes. Asserts perf-1hr.fit parses in <50 ms median over 11 timed runs after 3 warm-up runs. No `.skip`. Public-surface import only.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test buffers (in-test) ↔ loader | Tests construct corrupt buffers in memory; loader is the SUT |
| Subprocess stderr ↔ test assertion | dev-field-shadow.test.ts captures stderr of a child Node process; assertion depends on stderr containing the debuglog message |
| Committed fixtures ↔ tests | Tests must NOT mutate committed bytes; in-test corruption only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | A test mutates a committed fixture file in place, drifting it from the scrubber's output | mitigate | Acceptance criterion `git diff --quiet test/fixtures/fit/` after tests run. All buffer mutations happen on in-memory `Buffer.from(readFileSync(...))` copies. |
| T-02-18 | Repudiation | dev-field-shadow.test.ts subprocess captures stderr but the debuglog message is locale-dependent or version-dependent | accept | The message format is owned by `src/fit/loader.ts`. Test asserts a regex (`/developer field shadow/i` and `/power/i`), not exact string. Loader cannot freely change the message without breaking this test, which is the desired coupling. |
| T-02-19 | Denial of Service | Perf test runs 11 iterations of a 4562-record parse on a slow CI runner, blowing the timeout | accept | Vitest default timeout is 5000 ms; 11 × 50 ms = 550 ms worst case + warmup; well under the timeout. RESEARCH measured 1.85 ms median; even 100× slower CI is still under timeout. |
| T-02-20 | Tampering | A test imports `src/fit/loader.ts` directly, bypassing the public-API surface and missing index.ts re-export bugs | mitigate | Acceptance criterion in task 1 grep-bans `src/fit/(loader|normalize)` imports in `test/fit/loader.test.ts`. (Plan 02-05's normalize.test.ts is allowed to import normalize directly because it unit-tests the pure function — different concern.) |
</threat_model>

<verification>
- All four test files run under `npm test` (CI tier; no env vars required).
- `test/fit/loader.test.ts` asserts FIT-01 path/buffer parity, FIT-04 real-world quirks, D-FIT-01 wire-honest 0, D-FIT-02 gap preservation, D-FIT-07 sync-return invariant — against the public surface only.
- `test/fit/error-paths.test.ts` asserts each of InvalidFitHeaderError, FitCrcError, FitTruncatedError, NoRecordMessagesError fires on appropriate corrupt input AND each is `instanceof FitLoadError` (D-FIT-06). NO DeveloperFieldShadowError reference.
- `test/fit/dev-field-shadow.test.ts` asserts shadow.fit does NOT throw, returns records, AND emits util.debuglog (subprocess + NODE_DEBUG capture). Cites D-FIT-10 amendment.
- `test/fit/perf.test.ts` asserts perf-1hr.fit median parse time < 50 ms over 11 runs after 3 warm-ups. Not skipped.
- `git diff --quiet test/fixtures/fit/` after the suite passes (no committed fixture file mutated by tests).
</verification>

<success_criteria>
- The Phase 2 loader and error surface have CI-tier test coverage against six of the seven committed fixtures (basic.fit, autopause.fit, zero-power.fit, dev-fields-non-shadow.fit, shadow.fit, perf-1hr.fit; duplicates.fit's coverage lives in plan 02-05's normalize.test.ts).
- All locked decisions are reflected in test assertions: D-FIT-01 (zero-power test), D-FIT-02 (autopause gap test), D-FIT-06 (instanceof FitLoadError), D-FIT-07 (sync-return), D-FIT-10 (shadow non-fatal + debuglog).
- ROADMAP perf gate (<100 ms) is enforced with 2× margin (<50 ms).
- Four atomic conventional-prefix commits land cleanly: `test(02-04)` × 4.
- `npm test` exits 0 with all four new files contributing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fit-loader-normalization/02-04-SUMMARY.md` documenting:
- Each test file's line count and tests-added count.
- The actual measured median for the perf test on the dev machine (so plan 02-05 / future phases have a reference point).
- Confirmation that no committed fixture file was mutated (`git diff --quiet test/fixtures/fit/` exit code).
- Whether the dev-field-shadow.test.ts subprocess approach worked or if a fallback was needed.
- Note that test/fit/normalize.test.ts and the TEST_FIT_DIR opt-in suite ship in plan 02-05.
</output>
</content>
