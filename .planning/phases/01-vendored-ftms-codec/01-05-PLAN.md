---
phase: 01-vendored-ftms-codec
plan: 05
type: execute
wave: 3
depends_on: [01, 03]
files_modified:
  - .github/workflows/ci.yml
  - scripts/nrf-connect-demo.ts
  - .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png
  - .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md
autonomous: false
requirements:
  - FTMS-05c
tags:
  - ci
  - github-actions
  - nrf-connect
  - manual-verification
  - phase-gate

must_haves:
  truths:
    - "GitHub Actions CI workflow runs on macOS + Ubuntu, Node 24 only (D-13)"
    - "CI executes npm ci, npm run build, npm test, npm run validate:publint, npm run validate:attw — fails on any non-zero exit"
    - "fail-fast: false so a macOS-only failure does not mask a Linux-only failure (or vice versa)"
    - "scripts/nrf-connect-demo.ts is a tsx-runnable dev script that prints the encoded hex bytes for {power: 200, cadence: 90} (and the speed-present payload), with explicit instructions to read the same values back via nRF Connect on a phone"
    - "A screenshot of nRF Connect reading the encoded payload is attached at .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png (D-03)"
    - "nrf-connect-verification.md captures the procedure, the device used, observed values, and the human-eyeball comparison"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "macOS + Ubuntu CI on Node 24 — build, test, validate"
      contains: "matrix"
    - path: "scripts/nrf-connect-demo.ts"
      provides: "Reproducible dev script that prints the encoder bytes for canonical payloads, ready to feed into nRF Connect"
    - path: ".planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png"
      provides: "Screenshot proving a real third-party FTMS decoder (nRF Connect) reads back the encoder's output correctly — the only genuinely third-party gate (D-03)"
    - path: ".planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md"
      provides: "Procedure, device used, observed values, screenshot link, sign-off note"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts (build, test, validate:publint, validate:attw)"
      via: "run: lines"
      pattern: "npm\\s+(run\\s+)?(build|test|validate)"
    - from: "scripts/nrf-connect-demo.ts"
      to: "src/ftms/indoor-bike-data.ts"
      via: "relative import of encodeIndoorBikeData"
      pattern: "encodeIndoorBikeData"
    - from: "nrf-connect-verification.md"
      to: "scripts/nrf-connect-demo.ts"
      via: "procedure step 'run npx tsx scripts/nrf-connect-demo.ts'"
      pattern: "nrf-connect-demo"
---

<objective>
Close Phase 1 with the third verification gate and CI: GitHub Actions workflow on
macOS + Ubuntu (D-13), and the one-shot manual nRF Connect verification (D-03 / FTMS-05c)
with a screenshot pinned next to the phase artifacts.

Implements CONTEXT.md decisions D-03 (one-shot nRF Connect verification — non-optional;
the only genuinely third-party check), D-13 (GitHub Actions, Node 24, both OSes).

Claims requirement FTMS-05c.

Purpose: D-03 is the only check in the three-gate strategy that goes through software
not authored or maintained by us. Byte fixtures (gate A, plan 04) and the spec-cited
decoder (gate B, plan 04) both depend on us reading the spec correctly; nRF Connect
reads the spec the way the BLE ecosystem does. If our encoder roundtrips through
both gate A and gate B but nRF Connect shows wrong values, the spec-mis-read is in
OUR code (and the fix is here, not in nRF Connect).

CI matters because Phase 2 onward inherits this skeleton — every subsequent phase
needs a green build/test/validate path on both supported OSes.

Output: CI workflow, dev script, screenshot, signoff note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/phases/01-vendored-ftms-codec/01-CONTEXT.md
@.planning/phases/01-vendored-ftms-codec/01-RESEARCH.md
@.planning/phases/01-vendored-ftms-codec/01-01-SUMMARY.md
@.planning/phases/01-vendored-ftms-codec/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GitHub Actions CI workflow for macOS + Ubuntu, Node 24</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/01-vendored-ftms-codec/01-RESEARCH.md (§Stack-Config Concrete Templates → .github/workflows/ci.yml paste-and-tweak template)
    - .planning/phases/01-vendored-ftms-codec/01-CONTEXT.md (D-13 — macOS + Linux, Node 24 only; D-12 — publint + attw in CI)
    - CLAUDE.md (§Constraints — macOS / Linux only for v1)
    - package.json (just-built — confirms script names: build, test, validate:publint, validate:attw)
  </read_first>
  <action>
    Create `.github/workflows/ci.yml` per RESEARCH.md template. Required structure:

    1. Workflow name: `CI`.
    2. `on` triggers: `push` to `main`, `pull_request` to `main`.
    3. One `test` job with:
       - `name: test (${{ matrix.os }})`
       - `strategy: { fail-fast: false, matrix: { os: [ubuntu-latest, macos-latest] } }` — D-13 mandates both OSes; `fail-fast: false` per RESEARCH.md note.
       - `runs-on: ${{ matrix.os }}`.
    4. Steps:
       - `uses: actions/checkout@v4`
       - `uses: actions/setup-node@v4` with `node-version: '24'` and `cache: 'npm'` — D-13/D-16 lock Node 24.
       - `run: npm ci`
       - `run: npm run build`
       - `run: npm test`
       - `run: npm run validate:publint`
       - `run: npm run validate:attw`

    publint and attw are SEPARATE steps (not bundled into one `npm run validate` invocation) so the CI UI shows distinct red/green per check.

    Forbidden in this file:
    - Any matrix dimension on `node-version` (D-13 — Node 24 only; the matrix idea is in CONTEXT.md Deferred Ideas).
    - Any `windows-latest` runner (CLAUDE.md — macOS/Linux only for v1).
    - Skipping the build step before publint/attw (publint/attw need dist/ to exist).
    - Use of any non-pinned-major actions (e.g., `actions/checkout@main`) — pin to a major.

    After creating, validate the YAML structurally if a parser is available:
    `node -e "const fs=require('fs'); const y=fs.readFileSync('.github/workflows/ci.yml','utf8'); if(!y.includes('macos-latest')||!y.includes('ubuntu-latest')) process.exit(1)"`. (A real YAML parser is preferable; if `js-yaml` is installed transitively, use it. Otherwise the smoke check + the grep acceptance criteria below catch the structural errors.)

    Do NOT push to GitHub from this task. The user owns the push/merge.
  </action>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists.
    - Workflow runs on both macOS and Ubuntu: `grep -E 'ubuntu-latest' .github/workflows/ci.yml` exits 0 AND `grep -E 'macos-latest' .github/workflows/ci.yml` exits 0.
    - Workflow uses Node 24: `grep -E "node-version:.*['\"]?24['\"]?" .github/workflows/ci.yml` exits 0.
    - Workflow runs the validate steps separately: `grep -E 'npm\s+run\s+validate:publint' .github/workflows/ci.yml` exits 0 AND `grep -E 'npm\s+run\s+validate:attw' .github/workflows/ci.yml` exits 0.
    - Workflow runs build before validate: `awk '/npm run build/{b=NR} /validate:publint/{p=NR} END{exit !(b<p)}' .github/workflows/ci.yml` exits 0.
    - Workflow runs tests: `grep -E 'npm\s+test' .github/workflows/ci.yml` exits 0.
    - No Windows runner: `grep -c 'windows' .github/workflows/ci.yml` reports 0.
    - No node-version matrix: `grep -cE 'node-version:\s*\[' .github/workflows/ci.yml` reports 0 (Node 24 is a scalar, not an array).
    - fail-fast disabled: `grep -E 'fail-fast:\s*false' .github/workflows/ci.yml` exits 0.
    - Action versions pinned to a major: `grep -E 'uses:\s*actions/checkout@v4' .github/workflows/ci.yml` exits 0 AND `grep -E 'uses:\s*actions/setup-node@v4' .github/workflows/ci.yml` exits 0 (or whichever current major is appropriate at execution time — adjust if newer GA exists, but pin a major).
  </acceptance_criteria>
  <verify>
    <automated>grep -E 'ubuntu-latest' .github/workflows/ci.yml && grep -E 'macos-latest' .github/workflows/ci.yml && grep -E "node-version:.*['\"]?24['\"]?" .github/workflows/ci.yml && grep -E 'fail-fast:\s*false' .github/workflows/ci.yml && grep -E 'npm\s+run\s+validate:publint' .github/workflows/ci.yml && grep -E 'npm\s+run\s+validate:attw' .github/workflows/ci.yml && [ "$(grep -c 'windows' .github/workflows/ci.yml)" = "0" ] && awk '/npm run build/{b=NR} /validate:publint/{p=NR} END{exit !(b<p)}' .github/workflows/ci.yml</automated>
  </verify>
  <done>
    `.github/workflows/ci.yml` declares a CI job on macOS + Ubuntu Node 24, runs `npm ci → build → test → validate:publint → validate:attw` on push and pull-request to main, with `fail-fast: false` and no Windows.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/nrf-connect-demo.ts dev script</name>
  <files>scripts/nrf-connect-demo.ts</files>
  <read_first>
    - .planning/phases/01-vendored-ftms-codec/01-CONTEXT.md (D-03 — what the script must demonstrate; payload `{power, cadence}` per the decision)
    - .planning/phases/01-vendored-ftms-codec/01-RESEARCH.md (§Reference Payloads — Payload 1 `{power: 200, cadence: 90}` is the canonical demo case with hex `45 00 B4 00 C8 00`; Payload 5 covers the speed-present branch `44 00 B8 0B 78 00 64 00`)
    - src/ftms/indoor-bike-data.ts (encoder under test — confirms how to call encodeIndoorBikeData)
    - package.json (confirms `tsx` is installed for running TS scripts)
  </read_first>
  <action>
    Create `scripts/nrf-connect-demo.ts`. The script's job:
    1. Encode `{power: 200, cadence: 90}` (RESEARCH.md Reference Payload 1) using `encodeIndoorBikeData` from `src/ftms/indoor-bike-data`.
    2. Print the resulting bytes as a space-separated hex string AND as a comma-separated decimal Uint8Array literal.
    3. Encode `{power: 100, cadence: 60, speed: 30}` (Payload 5 — bit-0 inversion branch) and print its bytes, calling out the bit-0 difference (`45 → 44`).
    4. Print explicit instructions for the operator on using nRF Connect:
       - Open nRF Connect on a phone.
       - Use any FTMS-aware decoder OR the bytes-as-hex view to compare against the values printed.
       - Note that v1 has no real BLE transport (BlenoTransport is v2 / out of scope), so this verification is byte-level: confirm the decoded values match the source values.
       - Save the screenshot to `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png`.
       - Update `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md`.

    Required output format (the script's stdout when run):
    - Line containing "trainer-sim — nRF Connect verification helper" (or similar header).
    - Line containing the literal hex string `45 00 B4 00 C8 00`.
    - Line containing the literal hex string `44 00 B8 0B 78 00 64 00`.
    - Line containing `200 W` (or similar showing Payload 1 expected power).
    - Line referencing `FTMS-05c` or `manual verification`.
    - Procedure block with 4–6 numbered steps.

    Implementation rules:
    - Run via `npx tsx scripts/nrf-connect-demo.ts`.
    - Import the encoder via relative path: `import { encodeIndoorBikeData } from '../src/ftms/indoor-bike-data';` (extension convention: match what plan 03 produced for `src/index.ts` — likely no extension under bundler resolution, OR `.js` extension if that is what the rest of the codebase uses; stay consistent).
    - The hex strings appearing in stdout MUST be computed from the live encoder output (`encodeIndoorBikeData(...)`) — NOT hard-coded literals. Format with `Buffer.from(view.buffer, view.byteOffset, view.byteLength).toString('hex').match(/.{2}/g)?.join(' ').toUpperCase()` or equivalent. The whole point of this script is to demonstrate that the live encoder actually produces these bytes.
    - Add a top-of-file JSDoc explaining the script's role in FTMS-05c verification.
    - Add `'use strict'` is unnecessary in TS modules — skip.

    Forbidden:
    - Hard-coding the hex output strings instead of computing from the encoder (the entire point is demonstrating the live encoder output).
    - Importing from `dist/` (use the source `src/ftms/indoor-bike-data` — tsx handles TS at runtime).
    - Adding the script to `package.json` `scripts.*` (it is a one-off; not part of the test suite).
    - Any reference to BLE peripheral advertising (D-14 — no native deps).
  </action>
  <acceptance_criteria>
    - `scripts/nrf-connect-demo.ts` exists.
    - Script imports the encoder from src: `grep -E "from.*['\"]\.\./src/ftms/indoor-bike-data" scripts/nrf-connect-demo.ts` exits 0.
    - Script does NOT hard-code Payload 1 hex output as a string literal (must compute): `grep -cE "['\"]\s*45\s*00\s*B4\s*00\s*C8\s*00\s*['\"]" scripts/nrf-connect-demo.ts` reports 0.
    - Script calls encoder for both payloads: `grep -c 'encodeIndoorBikeData' scripts/nrf-connect-demo.ts` reports >= 2.
    - Script runs and prints Payload 1 hex bytes: `npx tsx scripts/nrf-connect-demo.ts | grep -E '45 00 B4 00 C8 00'` exits 0.
    - Script prints Payload 5 hex bytes: `npx tsx scripts/nrf-connect-demo.ts | grep -E '44 00 B8 0B 78 00 64 00'` exits 0.
    - Script output mentions 200 W: `npx tsx scripts/nrf-connect-demo.ts | grep -E '200\s*W'` exits 0.
    - Script output mentions FTMS-05c or "manual verification": `npx tsx scripts/nrf-connect-demo.ts | grep -iE 'FTMS-05c|manual verification'` exits 0.
    - Script output is non-empty: `[ "$(npx tsx scripts/nrf-connect-demo.ts | wc -l | tr -d ' ')" -ge 10 ]` exits 0.
    - No reference to bleno: `grep -ic bleno scripts/nrf-connect-demo.ts` reports 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsx scripts/nrf-connect-demo.ts | grep -E '45 00 B4 00 C8 00' && npx tsx scripts/nrf-connect-demo.ts | grep -E '44 00 B8 0B 78 00 64 00' && [ "$(grep -c 'encodeIndoorBikeData' scripts/nrf-connect-demo.ts)" -ge "2" ] && [ "$(grep -ic bleno scripts/nrf-connect-demo.ts)" = "0" ]</automated>
  </verify>
  <done>
    `scripts/nrf-connect-demo.ts` exists; running `npx tsx scripts/nrf-connect-demo.ts` prints both canonical payload hex sequences (computed from the live encoder), the expected decoded values, and the manual verification procedure.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: One-shot manual nRF Connect verification — operator runs script, captures screenshot, signs off</name>
  <files>.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png, .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md</files>
  <read_first>
    - .planning/phases/01-vendored-ftms-codec/01-CONTEXT.md (D-03 — manual verification mandatory and non-optional; FTMS-05c)
    - scripts/nrf-connect-demo.ts (just created — the script the operator will run)
  </read_first>
  <what-built>
    Tasks 1 and 2 created the CI workflow (`.github/workflows/ci.yml`) and the
    nRF Connect demo script (`scripts/nrf-connect-demo.ts`). The encoder (plan 03)
    and the test gates (plan 04) are already passing. This checkpoint executes the
    third gate of the FTMS-05 verification strategy: the only genuinely third-party
    check (D-03), where the operator confirms a real-world FTMS decoder reads back
    the encoder's output correctly.
  </what-built>
  <how-to-verify>
    Operator (developer with phone + nRF Connect installed) does the following:

    1. From the repo root, run: `npx tsx scripts/nrf-connect-demo.ts`. Note the printed
       hex bytes for Reference Payload 1 (`45 00 B4 00 C8 00`) and Payload 5
       (`44 00 B8 0B 78 00 64 00`).

    2. Open nRF Connect on a phone (Android: nordicsemi.com → Play Store; iOS: App Store).

    3. Use one of these methods to feed the bytes to nRF Connect (operator picks the
       most convenient — v1 has no real BLE transport, so byte-level verification is
       sufficient):

       - **Option A (Advertiser mode, if device supports peripheral mode):** In
         nRF Connect → Advertiser → create a new advertisement set. Add a
         `Service Data` entry with UUID `0x1826` (Fitness Machine Service) and the
         data field set to the hex bytes from step 1. Start advertising. Use a second
         BLE-aware app (or a second phone running nRF Connect in Scanner mode) to
         observe the advertised payload and decode it as FTMS IndoorBikeData.

       - **Option B (Hex viewer):** In nRF Connect → Utilities → Bytes ↔ Hex, paste the
         hex bytes from step 1 with FTMS IndoorBikeData decoding selected (if the
         build supports it). Otherwise, the operator manually reads:
         - bytes 0-1 (flags, LE uint16): expect `0x0045` for Payload 1 (or `0x0044` for
           Payload 5).
         - bytes 2-3 (cadence wire, LE uint16): expect `0x00B4` = 180 for Payload 1
           (= 90 rpm at 0.5 resolution).
         - bytes 4-5 (power, LE sint16): expect `0x00C8` = 200 W for Payload 1.

       - **Option C (BLE peripheral via macOS bleutil, deferred):** v2 BlenoTransport
         will support this. Out of scope for v1 (D-14).

    4. Capture a screenshot showing nRF Connect (or the chosen verification tool)
       displaying the decoded values for at least Payload 1: cadence=90 rpm, power=200 W.
       Save it as `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png`.

    5. Create `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` with:
       - Date of verification.
       - Phone make/model + nRF Connect app version.
       - Verification method used (A, B, or C from above).
       - Observed decoded values (cadence + power; speed if Payload 5 was tested).
       - Whether values matched the source values (yes/no, with any anomaly notes).
       - Sign-off line: "Verified by [name]" or "Verified by Claude under operator instruction."

    Resume by typing "approved" if values matched, or describe any mismatches.
    A mismatch on this gate indicates a spec mis-read in the encoder; rewind to
    plan 03 to investigate.
  </how-to-verify>
  <acceptance_criteria>
    - `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png` exists: `test -f .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png` exits 0.
    - The PNG is non-trivial (not a 1-byte placeholder): `[ "$(stat -f%z .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png 2>/dev/null || stat -c%s .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png)" -ge 1024 ]` exits 0 (>= 1 KB — a real screenshot is much bigger; this floor catches the "empty file" failure).
    - `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` exists: `test -f .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` exits 0.
    - The markdown file contains a date in 2026 format: `grep -E '2026-[0-9]{2}-[0-9]{2}' .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` exits 0.
    - The markdown file mentions cadence and power: `grep -i cadence .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md && grep -i power .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` both exit 0.
    - The markdown file mentions either "matched" or "verified": `grep -iE 'matched|verified' .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` exits 0.
    - The markdown file references the screenshot filename: `grep nrf-connect-verification.png .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` exits 0.
  </acceptance_criteria>
  <action>
    This is a `checkpoint:human-verify` task. The operator follows the procedure in `<how-to-verify>` above: runs `npx tsx scripts/nrf-connect-demo.ts` from the repo root, opens nRF Connect on a phone, feeds the printed bytes through it (via Advertiser mode, hex viewer, or manual byte-level read per Options A/B in `<how-to-verify>`), captures a screenshot showing the decoded values, saves it as `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png`, and writes `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` documenting the date, device, method, observed values, and pass/fail outcome. Claude then resumes after the operator types "approved" (or describes any mismatch, which routes back to plan 03 for spec re-read).
  </action>
  <verify>
    <automated>test -f .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png && [ "$(stat -f%z .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png 2>/dev/null || stat -c%s .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png)" -ge 1024 ] && test -f .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md && grep -E '2026-[0-9]{2}-[0-9]{2}' .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md && grep -i cadence .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md && grep -i power .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md && grep -iE 'matched|verified' .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md && grep nrf-connect-verification.png .planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md</automated>
  </verify>
  <done>
    Screenshot exists at `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png` (>= 1 KB). Markdown signoff at `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` records date, device, method, observed cadence + power values, and the operator's confirmation that values matched the encoder output. The third FTMS-05 gate (FTMS-05c) is closed; Phase 1 is done.
  </done>
  <resume-signal>Type "approved" if nRF Connect (or chosen tool) decoded the encoder bytes back to power=200 W and cadence=90 rpm (Payload 1) — and any other payload tested. If values did NOT match, describe the discrepancy; the work returns to plan 03 to investigate the spec mis-read.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ← repo source | Standard CI trust; runs unprivileged, no production secrets in this phase |
| nRF Connect (third-party app) ← encoded bytes | nRF Connect is a Nordic-published, widely-used BLE inspection tool; trusted to decode FTMS per spec |
| Operator phone ← user-supplied screenshot | Screenshot is committed to the repo as a verification artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Spoofing | Forged `nrf-connect-verification.png` | mitigate | Acceptance criteria require the PNG be ≥ 1 KB and the markdown file describe the procedure with date + observed values. Final inspection during checker is a human review of the screenshot before phase signoff. |
| T-01-18 | Tampering | CI workflow modification | accept | Workflow lives in `.github/workflows/`; GitHub enforces branch-protection rules at the org level (out of scope for this plan; future Phase 4/5 may add CODEOWNERS). |
| T-01-19 | Information Disclosure | Screenshot may leak phone metadata or nearby BLE devices | accept | Operator is expected to crop the screenshot to the relevant nRF Connect view. Documented in nrf-connect-verification.md procedure. Risk is low for an open-source dev tool. |
| T-01-20 | Denial of Service | npm registry outage on CI runs | accept | `npm ci` failure is rare and self-resolves; not a security concern. |
| T-01-21 | Repudiation | Unsigned operator sign-off in markdown | accept | "Sign-off" is informal; the screenshot IS the artifact. Markdown captures context. ASVS L1 does not require formal sign-offs for dev-tool artifacts. |

CI workflow has no production secrets in Phase 1 (no npm publish, no deployment). nRF Connect verification is a one-shot human-in-the-loop check with explicit operator-supplied evidence; no autonomous trust placed in the third-party app.
</threat_model>

<verification>
- `.github/workflows/ci.yml` runs on macOS + Ubuntu Node 24, executing build/test/validate:publint/validate:attw.
- `scripts/nrf-connect-demo.ts` runs via tsx and prints the live-computed hex bytes for the canonical Payloads 1 and 5.
- `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.png` exists as a real screenshot (≥ 1 KB).
- `.planning/phases/01-vendored-ftms-codec/nrf-connect-verification.md` documents date, device, observed values, and that values matched the source.
- Phase 1 is complete: all three FTMS-05 gates (byte fixtures, spec-cited round-trip, nRF Connect) green.
</verification>

<success_criteria>
- CI runs green on the first push to main (or pull request opening) on both macOS and Ubuntu.
- `npx tsx scripts/nrf-connect-demo.ts` prints the canonical hex bytes computed from the live encoder.
- nRF Connect verification artifact (PNG + MD) is committed alongside the phase summary, satisfying FTMS-05c per D-03.
- All three gates of CONTEXT.md's three-gate verification strategy are operational; Phase 1 done is reachable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-vendored-ftms-codec/01-05-SUMMARY.md` documenting:
- That the CI workflow exists, has been validated locally (grep checks), and is awaiting first push for live confirmation.
- Confirmation that `npx tsx scripts/nrf-connect-demo.ts` runs and prints the canonical hex bytes.
- That the nRF Connect screenshot was captured, with the operator's name, phone model, and tool method (A/B/C).
- That all three gates of FTMS-05 are now operational (gate A byte fixtures via plan 04, gate B round-trip via plan 04, gate C nRF Connect via this plan).
- The Phase 1 completion checklist: requirements FTMS-01..FTMS-05c all claimed and verified.
</output>
